# 2026-05-26 — Weekend-batch analysis (098c/098d/099b/100/101/095b) + baseline-fairness fix

## Session 1 — Pull, analyze, fix baseline scoring, update memory

### Goal

User asked "is there any update on github?" then "分析这组实验的结果". Pull the H20 weekend
batch, re-score it locally with the canonical scorers (don't trust the commit message), and
deliver a reviewer-2 analysis. User then approved three follow-up actions: fix baseline fairness,
update memory, write this log.

### What was done

#### Pull

- `git pull` (set upstream tracking; was missing) → HEAD `e9bf2ed6`, +2 commits:
  `50561de5` (2560-cfg weekend batch summary) + `e9bf2ed6` (2310 result JSONs). 7187 files,
  almost all `inference_merged.json` bundles.

#### Canonical re-score (scripts/score_summary.py, 11-fact default)

Reference `baseline_v3` = **4.64 ± 1.34 (n=28)**, identical across 099b/100/101 (consistency check).

| Experiment / best cell | mean/11 | n |
|---|---|---|
| 098d `x5_zum_downside` (best of 6) | 5.22 | 27 |
| 100 `bb_p010_s250` (best of 31) | 5.10 | 30 |
| 101 `ha_K06_b016_u08` (best of 25) | 5.10 | 30 |
| 098c `zumdn_s085_lam095` (best of 6) | 5.07 | 30 |
| 099b `memk_s025_lam090` (best of 9) | 5.00 | 28 |

#### Three findings (reviewer-2)

1. **No track beats baseline at p<0.05, even pre-correction.** vs `baseline_v3`: 098d z=1.44
   (p₁=0.075), 100 z=1.22, 101 z=1.30, 098c z=1.10, 099b z=0.97. Every "winner" is a max over
   6–31 cells (winner's curse); after Bonferroni all n.s. (p 0.45→1.00). Effect of all four
   mechanisms consistent with **zero lift over baseline**.
2. **The "first-ever 11/11" (`ha_K04_b016_u08_seed13`) is a seed-lottery artifact.** Cell mean
   4.97 ± 1.71 (n=30); seed 13 is the only seed ≥8 (3.6σ), winning by passing the two
   architectural-floor facts (`hill_tail_index`, `zumbach_asymmetry`) that ~2/30 siblings pass.
   It's not even the best cell by mean (`ha_K06_b016_u08`=5.10 is).
3. **⚠️ Baseline-parity kill-shot.** `score_summary` reported "no eval results" for 095b — root
   cause: baselines (`WGANLPSimulator`/`TrajCastLiteSimulator`, `run_baseline_fit_eval.py:113`)
   are returns-only, emit no volume, so `volume_volatility_corr` is absent → the 11-fact
   instability filter NaN-dropped all 250 baseline seeds. On the **common 10 facts**, two
   baselines beat every weekend ECoMD cell: `trajcast_spx`=5.24 (std 0.78), `wgan_ndx`=5.12;
   best ECoMD `x5_zum_downside`=4.89, `baseline_v3`=4.14. Win is SPX/NDX-specific
   (`trajcast_gold`=4.16, `eurusd`=3.00 lose). Baselines ~2× more seed-stable.

#### Action 1 — fix baseline fairness (tooling, Mac only)

- Added `--exclude FACT...` to `scripts/score_summary.py` and `scripts/score_phase.py`: drops
  named facts from `BANDS` before scoring so the instability filter no longer wholesale-rejects
  returns-only models; denominator made dynamic in score_phase output; SOTA flag suppressed under
  reduced fact set. Did **not** touch `run_baseline_fit_eval.py` (faking a volume channel would
  be fabrication).
- Verified: `score_summary … 095b --exclude volume_volatility_corr` → `trajcast_spx` 5.24 (was
  "no eval results"); 11-fact default path unchanged for EcoMD dirs; unknown-fact warns + ignores.
- Generated the mechanically-produced common-10 head-to-head and saved
  `experiments/095b_baselines_n30/baseline_vs_ecomd_10fact.md` (table + methodology + caveats).
  Global best cell across all six experiments = a baseline (`trajcast_spx` 5.24), not any EcoMD.

#### Actions 2–3 — memory + log

- Updated `memory/project_pareto_ceiling.md` (refuted the n=5 "ECoMD beats every baseline" claim
  with n=30 evidence; marked 099b/095b/098c gates done) and
  `memory/project_paper_a_neurips_2027.md` (Track A failed/demoted; B-β/B-α n.s.; 11/11 lottery;
  baseline-parity risk added to critical reminders; 30-40% accept condition flagged UNMET) +
  MEMORY.md pointers.
- This log.

### Results / findings

- **Diagnose half of Paper A is strong**: mechanism-independent ceiling ~5.1–5.2 across 4
  mechanism families × 30 seeds, reconfirmed.
- **Solve half has no winner**: A/B-β/B-α all within noise of baseline.
- **Baseline parity is the live risk** for a NeurIPS submission.

### Open questions / blockers

- Is the baseline comparison fair given TrajCast/WGAN are return-fitters? Defensible Paper A
  claims are the ceiling + capabilities baselines lack (volume channel, mechanistic knobs), not
  raw fact-count. Needs a framing decision.
- Does any secondary track (B-γ adversarial, B-MACEv2) have a real shot, under a pre-registered
  significance gate (Δ>0 at Bonferroni-corrected p<0.05, not best-of-N)?

### Next steps

- (Deferred, user did not select today) Re-frame Paper A §5-6 around the diagnose/ceiling result,
  demoting memk/B-β/B-α to negative controls if no lift materializes.
- If a ceiling break is ever claimed, pre-register a fresh 30-seed re-run of the candidate config
  and require the *cell mean* to move — not a single seed.
- Analysis plan archived at `~/.claude/plans/modular-imagining-hamster.md`.
