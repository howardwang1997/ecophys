# 2026-08-15

## Session 1 (02:54)

### Goal

Turn the AEMO asynchronous mechanism/observation finding into a frozen machine-readable source contract and
prepare the next zero-row bounded-prefix gate before making any network request.

### What was done

- Reviewed the preserved v4 prefix parser, manifest, collector and tests instead of duplicating ZIP logic.
- Implemented `ecomd/research/aemo_source_contract.py` with strict validation of delivery channel, archive family,
  mechanism/observation intervals, exact versions, required/forbidden fields and partial-provenance constraints.
- Added `data/manifests/aemo_source_contract_v1.yaml`, an exact ten-object collector and a preregistration document.
- Derived samples mechanically: the nearest complete month before the 8 March reporting bridge is 2021-02; the
  nearest complete month after the 24 October WDR/v5.1 boundary is 2021-11. No availability check or prefix request
  informed the selection, and replacement is prohibited.
- Required `DISPATCHMODETIME` and `DISPATCHSUBTYPE` after v5.1 and prohibited them before v5.1. Froze
  `PUBLIC_DVD` bid-period version 1 separately from participant next-day report versions.
- Added eight focused tests. Together with the prior loader/prefix suites, 20 tests passed; Ruff passed. Strict
  mypy passed with `--explicit-package-bases`.

### Results / findings

- The protocol is locally valid and remains network-unexecuted. It caps each object at 262,144 compressed bytes,
  65,536 inflated bytes and the first MMSDM information row; full-download fallback and market-row access remain
  prohibited.
- The temporary recovery clone acquired unrelated missing tracked files between sessions, including
  `ecomd/__init__.py`. They were not restored, staged or otherwise modified. Their absence caused default mypy to
  assign duplicate module names; explicit package bases isolated and passed the new module.
- No ZIP prefix, CSV, full archive, remote worker, GPU, paid data or target outcome was accessed.

### Open questions / blockers

- Do all ten mechanically selected official URLs return bounded partial content and match the frozen contract?
- Is a clean detached worktree available for executing the collector without weakening the clean-repository gate?
- If the metadata gate passes, what is the smallest row-level E1 conformance sample that tests timestamps and joins
  without consuming a confirmatory period?

### Next steps

1. Re-run focused tests, strict types, lint and manifest validation.
2. Commit and push the protocol before any selected URL is requested.
3. Execute it once from a clean detached worktree, with no replacement.
4. Preserve the result, update plans/memory/log and keep row/GPU access locked.

## Session 2 (03:20)

### Goal

Commit and push the frozen source contract before access, execute its exact ten requests once from a clean
detached worktree, and adjudicate the result without replacement or rerun.

### What was done

- Committed and pushed the unexecuted protocol at
  `39c45dddf736e418a3e112a5a4689a782e918466`, then ran its collector once from a clean detached worktree.
- Preserved the immutable raw summary and audited all response byte counts against their `Content-Range` totals.
- Found and repaired a generic safety bug: both prefix auditors hard-coded `full_archive_downloaded=false` instead
  of detecting that a `206` range response can contain the complete small object.
- Added complete-transfer rejection at both object and aggregate levels, regression tests, a separate
  machine-readable adjudication, and synchronized the result across proposal, acquisition, compute and memory
  documents. The frozen preregistration and raw output were not rewritten.

### Results / findings

- Scientific source-contract subgate: `PASS_SOURCE_CONTRACT_10_OF_10`. All ten URLs returned HTTP 206, parsed the
  ZIP information header and matched exact channel/package/table/version plus required/forbidden fields.
- Authoritative total decision: `FAIL_FULL_ARCHIVE_TRANSFER_GUARD`. The 2021-02 and 2021-11
  `DUDETAILSUMMARY` responses were respectively 150,150 bytes (`bytes 0-150149/150150`) and 162,163 bytes
  (`bytes 0-162162/162163`), so every compressed byte was transferred.
- Total transfer was 2,409,465 bytes. No `D` market row was parsed, no response body was persisted, and no GPU,
  paid data or prospective outcome was used.
- The raw summary incorrectly reports an overall pass and no full archive; it is retained unchanged with SHA-256
  `e7abbcf530d8d75a0f69c5885dfcbdc74a84f743ff9a7134754e6e8b28d8a239`. The separate adjudication is
  authoritative.
- The same retrospective check found complete compressed transfers for the earlier 2020-09 and 2022-04 identity
  prefixes. That gate already failed on versions, so its outcome remains failure; its safety record is corrected.
- Final validation passed 23 focused tests, strict mypy with explicit package bases and Ruff. The raw-summary hash,
  total transfer, 10/10 scientific count and both adjudicated `Content-Range` equalities were independently checked.

### Open questions / blockers

- What fixed range is both demonstrably sufficient for the MMSDM information row and safely below known small
  identity archives? The next candidate is 64 KiB, but it must be proven before freezing fresh URLs.
- Which new months follow the next mechanical selection rule without reusing any consumed development object?
- Row-level timestamp/join validation remains unauthorised until that independent metadata gate passes.

### Next steps

1. Run the final focused tests, types, lint, manifest/adjudication integrity checks and diff hygiene.
2. Commit and push the adjudicated failure plus safety repair.
3. Design a separate smaller-range protocol using only synthetic or already consumed material for parse-sufficiency
   proof; freeze new months before making another request.

## Session 3 (14:38)

### Goal

Repair the failed AEMO transfer gate offline, select untouched development months mechanically and freeze a new
protocol before any network access.

### What was done

- Strengthened the generic prefix auditor to reject a mismatch between response length and `Content-Range`, as well
  as a declared total smaller than the final byte.
- Built and ran a deterministic, CPU-only, no-network prefix-budget proof using the metadata of all 20 preserved
  real prefixes and a large synthetic AEMO-style deflated archive.
- Added a separate v2 validator, collector, literal manifest, preregistration and regression suite. The scientific
  source-header subgate is now explicitly separated from exact-range and complete-transfer safety.
- Selected 2021-01 and 2021-12 by moving outward from the two boundaries while skipping the eight consumed months;
  no URL availability or content informed the choice.

### Results / findings

- `PASS_64_KIB_PREFIX_BUDGET`: a 624,432-byte synthetic archive parsed correctly from exactly 65,536 bytes without
  opening a `D` row. Its data offset/header-field count are 95/128 versus preserved real maxima of 89/57.
- Proof artifact SHA-256 is `0be682973ed75bc65c630c8ac650cb839cffc60baa3fa4d256d5a4816ca3c8d5`.
- The proposed range is 83,041 bytes below the smallest known complete compressed object. A future object below
  64 KiB still causes an immutable failure; the protocol makes no availability or minimum-size assumption.
- Final pre-freeze validation passed 31 focused tests, strict mypy, Ruff, manifest validation and proof-hash
  verification. Manifest SHA-256 is `55c1e46e78992719955ca68415724d9dc0a266ccacb2cbddcfaf6fd617c16986`.
  No network, selected prefix, row, remote worker, GPU or paid data was used before protocol freeze.

### Open questions / blockers

- Will every selected object return exactly 65,536 bytes with total size strictly larger than that cap?
- Do the ten independent 2021-01/2021-12 information headers reproduce the already-supported source matrix?
- Row-level timestamp and join semantics remain completely untested and locked.

### Next steps

1. Validate manifest/proof hashes and diff hygiene, then commit and push the network-unexecuted v2 protocol.
2. Execute exactly once from a clean detached worktree with no replacement.
3. Preserve and adjudicate the result before considering any row-level protocol.

## Session 4 (14:55)

### Goal

Execute the pushed 64 KiB repair exactly once from a clean detached worktree, preserve its result and determine the
next admissible work without silently opening rows.

### What was done

- Confirmed detached worktree `23c847de0527ad1e7b367d596d3a4bb82d34c194` was clean, its manifest valid and
  the result path absent.
- Issued the exact ten frozen requests once. No object was retried, replaced or given a larger range.
- Copied only the generated metadata summary back to the active branch and independently checked every count,
  response length, final byte, declared total and collector commit.
- Added the result report and synchronized the canonical plan, acquisition/compute audits and long-term memory.

### Results / findings

- Decision: `PASS_TRANSFER_REPAIR_AND_SOURCE_HEADER_CONFIRMATION`.
- All ten objects passed HTTP 206, exact 65,536-byte partial transfer, ZIP/information-header parsing and the
  scientific channel/package/table/version/field contract.
- Complete compressed objects: 0. Parsed `D` rows: 0. Total transfer: 655,360 bytes. No response body was retained,
  and no GPU, remote worker, paid data or prospective outcome was used.
- Raw summary SHA-256 is `e4bac4ac57b8b25b8d91a3465eba544c9c9e22050c47750c89344e339f6417c4`.
- The smallest selected totals were 150,066 and 162,525 bytes for `DUDETAILSUMMARY`; the 2021-12 bid-period object
  declares 1,610,349,080 compressed bytes. A full-month row strategy is therefore unacceptable for conformance.
- The result independently supports the source matrix across two pre/post month pairs, but contains no evidence on
  timestamp semantics, key uniqueness, effective-dated identity joins, applied-offer joins or causal effects.

### Open questions / blockers

- Are the already consumed AEMO development artifacts still locally/R2 available with enough rows for conformance?
- Which official daily/small-table source can test the same keys without pulling the 1.610 GB monthly object?
- What is the smallest row/date manifest that tests joins without turning development conformance into a causal
  event study?

### Next steps

1. Commit and push the immutable pass result before any further data inspection.
2. Perform a metadata-only inventory of existing local/R2 artifacts and source paths.
3. Design a separate row-level preregistration; do not execute it until committed and pushed.

## Session 5 (15:32)

### Goal

Inventory reusable AEMO row data, identify the smallest official daily alternative and freeze a retention-safe
row-level conformance protocol before any selected market row is accessed.

### What was done

- Confirmed that earlier complete AEMO development ZIPs are absent locally and have no verified R2 raw-object
  manifest. Preserved their summaries but did not claim the missing bytes as reusable data.
- Audited AEMO's official current-report listings, table/file mapping, table triggers and exact
  `DISPATCHOFFERTRK` schema. Established that daily `BID_MOVE_COMPLETE` supplies `BIDDAYOFFER_D` and
  `BIDPEROFFER_D`, while `NEXT_DAY_DISPATCH` supplies `DISPATCHOFFERTRK` and `DISPATCHLOAD`.
- Selected 16 June 2026 mechanically from directory metadata: first Tuesday in the earliest shared complete
  seven-day window. Froze two daily ZIPs and one monthly identity ZIP, totalling 18,085,971 bytes.
- Implemented a literal manifest, validators, one-shot opaque downloader, R2 retention verifier and post-retention
  parser/analyzer. Parsing is impossible through the formal workflow until all local hashes and all three remote
  size/SHA metadata checks pass.
- Implemented schema, CRC, timestamp, primary-key, market-window, bid-parent, applied-offer, physical-dispatch and
  effective-dated-identity gates. Direction-free tracker multiplicity is reported rather than imputed.
- Added six focused synthetic tests and synchronized the preregistration, source inventory, canonical plan,
  acquisition/compute audits and long-term memory.

### Results / findings

- The formal sample is free, CPU-only and small: 18.1 MB compressed, at most about 1.11 GB uncompressed under hard
  caps and zero GPU-hours. Both V100s and the RTX 2060 remain idle.
- `BID_MOVE_COMPLETE` is a public derived summary, not raw participant submission/rejection history.
  `DISPATCHOFFERTRK` identifies the applied offer but omits direction, so bidirectional matches may be ambiguous.
- The protocol is frozen but unexecuted. No selected ZIP or CSV row was accessed, no R2 object was written, no paid
  data or target outcome was used, and no causal/model claim was unlocked.
- Six tests passed; strict mypy passed with explicit package bases; Ruff passed after formatting.

### Open questions / blockers

- Will all three one-shot downloads still match the frozen exact sizes and hashes implied by the selected objects?
- Do all five real headers and timestamp encodings match the modern contracts?
- Is direction-free applied-offer multiplicity below 5%, and do applied-offer/dispatch/identity joins reach the
  frozen 99.5% gates?

### Next steps

1. Run the full focused validation and inspect the exact diff/staging boundary.
2. Commit and push this unexecuted protocol before any selected download.
3. Execute acquisition once from a clean commit, retain all exact bytes in R2, then analyze only if retention
   passes.
4. Preserve the result, update documentation/memory/log and commit/push again without committing raw ZIPs.

## Session 6 (15:47)

### Goal

Execute the pushed AEMO row-conformance protocol once, guarantee raw retention before parsing and determine the
next admissible research step from the immutable result.

### What was done

- Created a clean sparse detached worktree at protocol commit
  `5645990496065b0f812db4c969e152a7b7198c7d` and independently revalidated its manifest.
- Issued exactly one GET for each frozen AEMO object. Verified all exact byte counts and local SHA-256 values while
  keeping ZIPs opaque.
- Uploaded the three exact ZIPs to the frozen R2 prefix and verified remote lengths and SHA-256 metadata. Only then
  ran ZIP CRC and row parsing.
- Copied only credential-free download/retention receipts and the summary into the active branch; independently
  matched all three artifact hashes.
- Preserved the failed gate without changing its threshold, documented the result and kept all model/GPU gates
  closed.

### Results / findings

- Download/R2/CRC: 3/3 passed; 18,085,971 compressed bytes. Summary SHA-256 is
  `fc422654de5c64a91e71b8dcceb22612f406092cdf6a36900859072041a6668b`.
- Five exact headers passed; all five tables were nonempty; 1,415,791 target rows were parsed; all primary keys
  were unique. All 4,535,925 contracted timestamps parsed and every bid/interval date-window check passed.
- Bid period-to-day, tracker-to-period existence, tracker-to-physical-dispatch and dispatch-to-effective-identity
  joins were all 100%; identity overlaps were zero.
- Authoritative decision: `FAIL_MODERN_ROW_CONFORMANCE_SMOKE`. Exactly 43,200 of 592,560 tracker rows had multiple
  period-bid direction candidates (`7.290400972%`), above the frozen 5% maximum. This is the only failed gate.
- The result shows that the modern public data plane is otherwise coherent, but the two-table join cannot identify
  a unique applied action for bidirectional cases. This is not a causal, replay or EcoMD result.
- CPU only; no V100/RTX 2060, paid data, prospective target outcome or model run was used.

### Open questions / blockers

- Does official `DISPATCHLOAD` v6 direction semantics provide a legitimate three-table disambiguation key?
- Are the 43,200 ambiguous cases exactly bidirectional units, or are other multiplicity mechanisms present?
- If a bridge is found post hoc, will it confirm on a fresh mechanically selected day without threshold refitting?

### Next steps

1. Validate the copied artifacts and documentation, then commit and push the immutable result.
2. Run a clearly labelled post-hoc direction diagnostic on the retained development day; do not alter E1a.
3. If supported, freeze a fresh-day E1b bridge protocol before any new source request.
4. Keep multi-day acquisition, Exp156 and GPU work locked until E1b passes.

## Session 7 (15:58)

### Goal

Resolve the conceptual meaning of the failed AEMO direction-multiplicity gate and freeze a reproducible post-hoc
development diagnostic before running additional row summaries.

### What was done

- Checked AEMO's official Data Model v5.3 participant-impact and bidding/dispatch specifications. BDU energy and
  regulation offers have separate `GEN`/`LOAD` legs; contingency FCAS remains one direction-independent offer.
- Checked the retained `DISPATCHLOAD` v6 information header: it has 69 fields and no direction field. Officially,
  `TOTALCLEARED` is negative for BDU import and positive otherwise.
- Rejected the tempting three-table row-selection rule: signed dispatch is a realized outcome, so using it to
  decide which ex-ante offer was applied creates leakage.
- Formulated the alternative set-valued hypothesis: one `DISPATCHOFFERTRK` record may apply an offer bundle with
  both direction-specific period rows.
- Added a post-hoc analysis plan, a provenance-locked diagnostic module/runner and three synthetic tests. The
  diagnostic reproduces the immutable E1a counts before describing cardinality, direction sets, bid types,
  effective identity and realized-sign context. It has no pass/fail output.

### Results / findings

- Official semantics rule out a direct `DISPATCHLOAD.DIRECTION` repair because that field does not exist.
- `TOTALCLEARED` can describe the realized operating side but cannot identify the ex-ante action without outcome
  leakage. This distinction matters for the Lucas-test observation bridge.
- Nine combined bundle/row tests passed; strict mypy and Ruff passed after formatting.
- The real diagnostic remains unexecuted at this point. No new source request, R2 write, GPU, paid data or target
  outcome was used.

### Open questions / blockers

- Are all 43,200 ambiguous records exactly two-row `{GEN, LOAD}` pairs?
- Do they all belong to one effective `BIDIRECTIONAL` identity, and which bid types generate them?
- Will the bundle representation confirm on an untouched day?

### Next steps

1. Commit and push the post-hoc diagnostic code and analysis plan.
2. Execute it once on the already-retained day and preserve the descriptive result.
3. If the bundle pattern is exact, freeze an untouched-day E1b confirmation without using realized dispatch as an
   action selector.

## Session 8 (16:02)

### Goal

Execute the pushed post-hoc bundle diagnostic on the retained development day, preserve the exact result and decide
whether a fresh-day E1b confirmation is scientifically justified.

### What was done

- Ran analyzer commit `b6572efc96b816268bb959e033a9127930970c7f` from a clean detached worktree against
  local inputs whose hashes reproduce the committed download receipt and R2 retention record.
- Reparsed the five frozen tables, reproduced the E1a tracker/ambiguity totals and emitted only aggregate
  cardinality, direction, bid-type, identity and realized-sign counts.
- Kept realized `TOTALCLEARED` explicitly descriptive and prohibited it from selecting a bid leg.
- Copied only the aggregate summary to the active branch and verified its SHA-256.

### Results / findings

- Exact reconciliation: 592,560 tracker rows and 43,200 ambiguous rows, with zero invalid or unmatched references.
- All 43,200 ambiguous cases had exactly two rows, exactly `{GEN, LOAD}`, zero duplicate directions, exactly one
  effective `BIDIRECTIONAL` identity and no other multiplicity pattern. They covered 60 DUIDs.
- Ambiguity was restricted to `ENERGY` (17,280), `LOWERREG` (12,960) and `RAISEREG` (12,960).
- The full candidate relation contains 549,360 one-row links and 43,200 two-row bundles. Single-row directions were
  `GEN` 341,616, `LOAD` 106,368 and `BIDIRECTIONAL` 101,376.
- Many realized targets were zero (12,325/17,280 energy; 8,648/12,960 for each regulation service), and nonzero
  signs mixed. Dispatch sign is neither complete nor temporally admissible as an action selector.
- Development interpretation: the applied offer is set-valued for BDU energy/regulation. Artifact SHA-256 is
  `d8b4bb0ffe6ede3fadf00cdac68c6851568fab132b6e2c94162c8bb4317cfaa2`.
- This is post-hoc schema evidence only. E1a remains failed; no causal/model claim, new source request, GPU or paid
  data was used.

### Open questions / blockers

- Does the exact one-or-`{GEN, LOAD}` relation reproduce on a fresh untouched day?
- Are effective BDU identity and allowed bid-type restrictions sufficient to make the relation contract portable
  across later Data Model versions?
- Passing E1b would validate the modern action representation, not historical regimes or mechanism replay.

### Next steps

1. Validate, commit and push the immutable post-hoc result.
2. Select the earliest fresh complete shared Tuesday mechanically from directory metadata and freeze exact source
   bytes before access.
3. Require 100% relation coverage; every multi-row link must be exactly a unique `{GEN, LOAD}` pair with one
   effective BDU identity and allowed bid type. Do not use realized dispatch to choose a leg.

## Session 9 (16:25)

### Goal

Freeze and validate a genuinely fresh E1b confirmation of the set-valued AEMO applied-offer bridge before any
selected July ZIP or market-row access.

### What was done

- Applied the metadata-only selection rule: first Tuesday of the first complete calendar month after the June
  development month. This selected 7 July 2026 and a new July `DUDETAILSUMMARY` snapshot.
- Froze three literal public objects, exact byte counts, schemas, time window, R2 keys and resource caps. Total
  compressed input is 18,244,129 bytes; metadata listing hashes are recorded in the manifest and preregistration.
- Implemented one-shot opaque download, R2-before-parse retention and analysis runners.
- Implemented the prospective relation gate: one candidate or a unique `{GEN, LOAD}` pair; pairs require an
  effective `BIDIRECTIONAL` identity and bid type `ENERGY`, `LOWERREG` or `RAISEREG`. Realized dispatch is
  explicitly prohibited from selecting a leg.
- Added six synthetic tests covering the frozen manifest, mutation rejection, a mixed singleton/pair pass,
  bid-type failure, identity failure and pre-retention content-access rejection.
- Updated the canonical plan, acquisition/compute audit and durable memory. At this session boundary no selected
  ZIP had been requested or opened and no July market row had been read.

### Results / findings

- Fifteen combined E1a/diagnostic/E1b tests pass. Ruff and strict mypy pass for the new module and three runners.
- The protocol has 25 exact gates and requires zero GPU-hours, no paid data and no remote worker. V100 and RTX 2060
  capacity remains idle.
- A pass can validate only one fresh modern set-valued observation bridge and unlock design of a limited multi-day
  development panel. It cannot validate causality, mechanism replay, historical compatibility or a model claim.

### Open questions / blockers

- Does the one-or-exact-`{GEN, LOAD}` relation pass without exception on the untouched July day?
- Does July contain at least one pair, so the central relation is actually tested?
- If E1b passes, how many mechanically selected days are sufficient for a limited version-stratified development
  panel without turning a schema check into an inflated scientific claim?

### Next steps

1. Commit and push the complete E1b protocol while the selected ZIPs and rows remain untouched.
2. From that exact clean commit, download each object once, retain all exact bytes in R2, then parse and evaluate.
3. Preserve either pass or failure without changing the relation contract; only then decide the multi-day gate.

## Session 10 (16:31)

### Goal

Execute the pushed E1b protocol once, preserve its immutable result and update the next scientific gate.

### What was done

- Created a clean detached worktree at protocol commit `9ee922f91ca1b4be4257e8555b49d13f27272c1a`.
- Requested each of the three frozen AEMO objects exactly once as opaque bytes. Verified HTTP 200, exact byte count
  and SHA-256 without opening a ZIP.
- Uploaded all three objects to the frozen R2 prefix and verified remote content length and SHA-256 metadata before
  any content parse.
- Parsed the retained archives once and evaluated all 25 frozen schema/time/key/join/relation gates.
- Copied only credential-free receipts and the aggregate summary to the active branch and matched their hashes.
- Documented the result in the canonical plan, acquisition/compute audit and durable memory.

### Results / findings

- Authoritative decision: `PASS_FRESH_SET_VALUED_OFFER_BRIDGE_CONFIRMATION`; 25/25 gates passed.
- Exact acquisition/R2/CRC: 3/3, 18,244,129 compressed bytes. Summary SHA-256 is
  `dc36b2fe31cc5c230def8f808a7efef1fa320f80ab30e61f1999d367dd9e466b`.
- All five schemas, 1,422,918 target rows, 4,559,065 timestamps, primary keys, market windows and supporting joins
  passed exactly; identity overlap count was zero.
- All 594,720 tracker records were covered: 549,792 one-row relations and 44,928 exact unique `{GEN, LOAD}` pairs.
  Pairs involved 64 DUIDs and only `ENERGY` (18,432), `LOWERREG` (13,248) and `RAISEREG` (13,248). All frozen
  relation violation counts were zero.
- E1b confirms the modern set-valued applied-offer bridge on one fresh month. It does not validate causality,
  historical versions, replay or any model. E1a remains an immutable failure of the old unique-row contract.
- CPU only; no paid data, remote worker, V100, RTX 2060, target outcome or model run was used.

### Open questions / blockers

- How stable is the relation across ordinary days, seasons and public table-version changes?
- What is the smallest mechanically selected panel that detects relation drift without wasteful bulk acquisition?
- Historical action provenance and prospective event G1 remain unresolved; this schema pass does not solve them.

### Next steps

1. Validate, commit and push the immutable E1b result.
2. Freeze a limited multi-day, version-aware development-panel protocol with exact dates, byte cap and stopping
   rule before any new ZIP request.
3. Keep Experiment 156 and all GPU/model work locked until that panel passes and a separate model protocol exists.

## Session 11 (17:10)

### Goal

Convert the E1b pass into a bounded, outcome-blind multi-day stability protocol without conflating date stability
with historical table-version portability.

### What was done

- Audited the already saved AEMO directory metadata only. The common daily-report window was 16 June through
  14 August 2026; no new ZIP or market row was accessed.
- Defined a weekday-controlled rule: exclude accessed 16 June/7 July dates, retain months with a complete verified
  identity snapshot, then select the earliest and latest remaining Tuesday. This yields 23/30 June and 14/28 July.
- Checked the August monthly identity archive location; it returned HTTP 404 at freeze. Excluded August rather
  than treating July identity data as a complete August snapshot.
- Froze eight literal daily URLs and exact sizes: 70,593,936 new compressed bytes. Froze R2-only reuse of June and
  July identity objects, bringing staged input to 71,350,764 bytes.
- Implemented exact manifest hashing, one-shot acquisition, R2 upload/prior-object materialization, sequential
  per-day analysis and an all-four-days panel decision. Added seven synthetic tests, including mutation, provenance
  and one-failed-day tests.
- Generalized the already tested E1b summarizer to derive date/output identifiers from its supplied manifest while
  leaving the frozen E1b behavior unchanged.
- Updated the canonical plan, data/compute decisions and durable memory. The panel remains unexecuted and row-locked.

### Results / findings

- Twenty-two combined AEMO tests pass. Ruff and strict mypy pass for the panel module/runners and generalized E1b
  summarizer.
- Every panel date must independently pass all 25 relation and data-plane gates. Pooled success cannot hide one
  failed date.
- The job is CPU/data engineering: at most 4,000,000 rows and 1.11 GB uncompressed per sequential day; no paid data,
  remote worker or GPU is required.
- The design tests modern within-version temporal stability only. Historical version portability remains a separate
  scientific gate.

### Open questions / blockers

- Will all four untouched dates reproduce the exact relation with zero violations?
- Are the June/July prior identity objects still byte/SHA-verifiable at their original R2 keys?
- If the panel passes, which smallest historical version strata can be tested without the 1.61 GB monthly object?

### Next steps

1. Commit and push the complete panel protocol before any selected source URL is requested.
2. Execute the eight one-shot downloads, upload/verify them in R2, materialize the two prior identities from R2,
   then analyze four dates sequentially.
3. Preserve the result and keep GPU/model work locked regardless of runtime convenience.

## Session 12 (17:18)

### Goal

Execute stability-panel v1 once and preserve any operational or scientific failure under its frozen rules.

### What was done

- Ran the pushed commit `1f44811e75a455f161532fbfdd3bde2092508bf5` from a clean detached worktree.
- Issued exactly one GET for each of eight new daily objects. All returned HTTP 200 and exact frozen bytes.
- Uploaded/verified all eight new R2 objects and verified/materialized both prior identity objects from their
  original R2 keys. No AEMO identity-source request was made.
- Invoked the sequential analyzer once. It raised `KeyError: 'resource_contract'` while resolving the derived
  first-day manifest, at parser line 693 before the archive loop.
- Verified from control flow that `parse_mmsdm_archive` was never called, no summary exists and no ZIP/CSV content
  was observed. Copied only the credential-free download/retention receipts and recorded a failure artifact.

### Results / findings

- Acquisition: 8/8 exact, 70,593,936 bytes. Retention/materialization: 10/10 exact, 71,350,764 staged bytes.
- Authoritative decision: `FAIL_PANEL_IMPLEMENTATION_PRE_PARSE`.
- Archive open attempts, CSV rows, market content and day summaries are all zero. There is no scientific panel
  result and no evidence for or against temporal stability.
- Root cause is a missing `resource_contract` key in `build_day_manifest`, not data/schema drift.
- No paid data, GPU, remote worker, target outcome or model run was used.

### Open questions / blockers

- Can a separately frozen repair demonstrate the complete parser preflight before touching retained content?
- Will the unchanged four dates then pass all scientific gates?
- Historical portability remains untested regardless of the repair outcome.

### Next steps

1. Validate, commit and push the immutable v1 failure and receipt chain.
2. Freeze a zero-source-request repair protocol referencing the v1 failure and exact R2 hashes.
3. Add the missing resource section and a preflight test using synthetic archives; do not change any scientific
   date, table, relation, threshold or claim boundary.

## Session 13 (17:31)

### Goal

Freeze an independently auditable repair for the content-blind panel v1 failure without changing its scientific
contract or making another AEMO request.

### What was done

- Froze the v1 protocol, download, retention and failure-artifact hashes as repair prerequisites.
- Froze all ten existing R2 keys, byte counts and SHA-256 values; new AEMO request count is exactly zero.
- Added only the missing derived-day `resource_contract.maximum_total_data_rows=4,000,000` field.
- Added a parser-entry integration preflight that monkeypatches archive parsing and verifies all three per-day
  archive calls are reached with the 4,000,000-row cap, preventing recurrence of the pre-loop `KeyError`.
- Implemented an R2-only materializer and repair analyzer that reload the unchanged v1 dates, tables, relation
  gates and all-days decision.
- Added strict repair-manifest, original-provenance, materialization and byte-equivalence tests.
- Updated the canonical plan, data/compute decisions and durable memory. The repair is still unexecuted.

### Results / findings

- Twenty-eight combined AEMO tests pass; strict mypy and Ruff pass for the repair and reused panel modules.
- The repair adds zero new data and zero scientific degrees of freedom. It stages the same 71,350,764 bytes from
  R2 and runs sequentially on local CPU.
- Required paid data, remote workers and GPU-hours remain zero.

### Open questions / blockers

- Will all ten R2 objects re-materialize exactly under the repair commit?
- Will the four unchanged days pass all 25 scientific gates after the plumbing defect is removed?
- A pass would still leave historical table versions and actual causal/model claims unresolved.

### Next steps

1. Commit and push the repair protocol before content access.
2. Materialize all ten objects from R2 with zero AEMO requests, then analyze once.
3. Preserve the repair result and keep historical/model/GPU gates separate.

## Session 14 (17:39)

### Goal

Execute the pushed zero-source-request repair once, obtain the unchanged four-day decision and update the next
research gate without overclaiming.

### What was done

- Created a clean detached worktree at repair commit `59540001a5ce9ee6a340ea346e5755609c4bdd77`.
- Verified and materialized all ten frozen objects from R2. Recorded exactly zero AEMO source requests and verified
  71,350,764 staged bytes before content access.
- Reasserted the repaired 4,000,000-row day cap, then parsed the four dates sequentially.
- Applied the unchanged 25 E1b gates independently to each date and the unchanged all-four-days panel rule.
- Copied only the credential-free materialization receipt and aggregate/day-level summary, then matched hashes.
- Updated the canonical plan, data/compute decision and durable memory.

### Results / findings

- Authoritative decision: `PASS_MODERN_BUNDLE_STABILITY_PANEL`; 4/4 days and all eight panel-level gates passed.
- Day tracker/pair counts were 595,584/43,488; 596,880/44,928; 594,720/44,928; and 595,008/45,216.
  Pair rates ranged from 7.3017% to 7.5992%; every day had zero relation violations.
- Aggregate: 2,382,192 trackers, 2,203,632 singletons, 178,560 exact pairs, 5,697,359 target rows and 18,255,035
  contracted timestamps. All schema, CRC, time, key, parent, dispatch and identity gates passed.
- Summary SHA-256 is `d939fffd8bc7673eb0aac78e06ae2abf38b1ea27e81cba67d03c355bec77ec88`.
- Together with development/E1b, the set-valued relation is supported on six Tuesdays across two identity
  snapshots. This closes modern within-version stability, not historical portability, causal behavior or a model.
- Local CPU and existing R2 only; zero new source bytes in repair, paid data, GPU, remote worker or model run.

### Open questions / blockers

- Can the same logical bridge be established across historical report/table versions without the 1.61 GB monthly
  bid-period object?
- Public derived reports still do not establish raw submitted/rejected actions.
- Prospective event G1 remains 5/10, so no Lucas-test model claim is unlocked.

### Next steps

1. Validate, commit and push the immutable repaired result.
2. Stop acquiring same-version modern days; freeze a minimal historical schema/source bridge audit using metadata
   and small preflights before any bulk object.
3. Continue action/identity provenance work separately; keep Experiment 156 and GPUs locked.

## Session 15 (17:50)

### Goal

Turn the repaired modern-panel pass into the smallest defensible, free historical-portability preflight without
bulk-downloading the 1.61 GB post-v5.1 period-offer archive or consuming fresh confirmation months.

### What was done

- Pushed the immutable repaired panel result at `0fc24416bd6c0a0ab2a2865d3d7d817c25b60a34`.
- Inspected official AEMO archive metadata. The rolling daily-report archive exposes recent months and cannot
  recover the 2021 mechanism boundary; individual historical monthly table archives remain available.
- Reused the already consumed/header-only 2021-01 and 2021-12 period-offer objects for feasibility only. Their
  declared compressed sizes are 95,238,254 and 1,610,349,080 bytes.
- Implemented a fixed-range acquisition, exact download/R2 receipt chain and streaming raw-deflate parser. The
  parser measures first-day completeness, second-day reachability, date order and compressed upper bounds without
  holding decompressed archives in memory.
- Froze one 64 MiB request per object, a 1 GiB per-object inflate cap, R2-before-row access and no retry, extension,
  replacement or full-file fallback.
- Added five focused tests plus Ruff and strict-mypy coverage. Manifest SHA-256 is
  `40bf7a5c76aaf87a3533edb11cc42a41eb59fa2877bbd1c1bcf21f0dc338e9ed`.

### Results / findings

- The next experiment needs 134,217,728 public compressed bytes, existing R2 and local CPU; GPU-hours and paid
  data remain zero.
- The selected months are explicitly ineligible for confirmation. This preserves untouched historical months for
  a later five-role row-bridge gate.
- A pass means only that a complete first-day block is cheaply reachable in both schemas. It does not establish
  joins, action semantics, a 5MS/WDR effect, prediction or EcoMD.
- Five tests pass; Ruff and strict mypy pass for the module and three runners. No selected prefix or market row was
  accessed before the protocol freeze.

### Open questions / blockers

- Are historical period-offer rows actually ordered by market day in both archive generations?
- Does a fixed 64 MiB prefix reach the second market day post-v5.1, or is even one day too costly?
- Raw participant submissions/rejections remain unavailable in these public derived archives.

### Next steps

1. Commit and push the network-unexecuted reachability protocol.
2. Execute its two one-shot range requests from a clean worktree, retain both exact prefixes in R2, then parse.
3. Preserve the decision; design a fresh five-role historical bridge only on pass and keep all GPUs locked.

## Session 16 (18:06)

### Goal

Execute the pushed historical prefix-reachability protocol once, preserve its decision and prevent a failed
date-partition assumption from propagating into bulk acquisition.

### What was done

- Created a clean detached worktree at `d7dd5af5bc3b654a46da37364d118f98e408f067`.
- Issued exactly two frozen 64 MiB range requests. Both returned HTTP 206, exact byte counts and exact declared
  archive totals; no retry or extension occurred.
- Uploaded and verified both exact prefixes in R2 before opening the ZIP member or any CSV row.
- Streamed raw deflate output under the 1 GiB per-object cap and applied the frozen source-header, timestamp, date
  order, second-day and first-day-completeness gates.
- Copied only the credential-free receipts and result summary into the research branch and revalidated their chain.

### Results / findings

- Decision: `FAIL_HISTORICAL_PREFIX_REACHABILITY`.
- January parsed 6,833,888 rows; December parsed 8,671,581. Both had 100% timestamp parsing, zero malformed rows,
  correct source headers and multiple dates.
- Both generations had market-day regressions, so neither first date formed a complete prefix block. January began
  on `2021-01-02` rather than the frozen first day and extended to `2021-02-01`; December exposed all 31 dates but
  included first-appearance/order regressions.
- The parser hit 1 GiB uncompressed after only 23.1 MB and 32.2 MB of compressed payload, confirming extreme
  compression but not date addressability.
- Download, retention and summary SHAs are `13b65238…`, `9a1845c7…` and `4e865928…`.
- No fresh confirmation month, paid data, GPU, remote worker, target outcome or model was used.

### Open questions / blockers

- Does a free queryable or date-partitioned mirror preserve exact AEMO source hashes/versions and a usable licence?
- If no query layer is admissible, is a full sequential 1.7 GB compressed development stream worth its provenance
  and storage cost relative to the paper's main claim?
- Historical public period offers remain derived observations, not raw submitted/rejected actions.

### Next steps

1. Validate, commit and push the immutable failed result; never extend or replace its two prefixes.
2. Audit free queryable/date-partitioned AEMO sources and official APIs before designing any bulk transfer.
3. Keep fresh historical confirmation months, Experiment 156 and all GPU/model work locked.

## Session 17 (18:23)

### Goal

Find a free, date-partitioned official AEMO source after monthly period-offer prefix extraction failed, then freeze
the smallest content-blind feasibility gate before accessing any new archive bytes.

### What was done

- Audited official AEMO NEMDE archive directories, market-solver help, NEMDE Queue guide, current copyright
  permissions and the archive-specific DVD disclaimer.
- Corrected the previous conflation between the restricted NEMDE executable/Queue and public historical
  production format files. The daily audit ZIPs contain one combined input/output/price-setting XML per five-minute
  dispatch interval.
- Documented the scientific boundary: the XML captures the applied production solver state and solution, not raw
  submission/rejection history or participant adaptation rationale.
- Froze a metadata-only protocol on the first day of the already consumed January/December 2021 development
  months. It permits one 1 MiB suffix request per ZIP, R2-before-parse retention and no XML access.
- Implemented strict manifest/receipt/retention validators, a ZIP EOCD/central-directory parser, immutable summary
  gates and five focused tests. Added a source audit and updated the v14 source inventory and plan.

### Results / findings

- The public source is scientifically better aligned than a loose five-table join for production mechanism replay:
  every interval synchronizes applied input, output and price-setting sections in one artifact.
- It still cannot make the exact solver runnable or explain participant decisions. A successful replay benchmark
  will need an audited open reconstruction and separate action/identity provenance.
- Current AEMO general permission and the old archive-specific personal-use notice point in different directions.
  Raw redistribution is locked pending written clarification; the preflight retains private exact bytes and public
  hashes/summaries only.
- The frozen gate downloads 2 MiB total, uses local CPU only and leaves all V100/2060 queues empty. Five tests,
  Ruff and strict mypy pass. Manifest SHA-256 is
  `5e08010ad1cac1b945062ff21921e2de04c1924ec271b035956b43a6309af620`.

### Open questions / blockers

- Do both daily ZIP suffixes contain complete central directories and all interval IDs 001--288?
- What are the exact interval-144 member offsets, sizes, names and compression flags?
- Do two range-retrieved XMLs actually contain all three expected sections and sufficient trader/constraint fields?
- Which AEMO permission governs redistribution of archived NEMDE audit bytes?

### Next steps

1. Run combined tests, commit and push the unexecuted protocol before archive access.
2. Execute exactly two suffix requests from a clean detached worktree and preserve the immutable decision.
3. Only on pass, freeze exact XML member ranges before opening any XML byte; keep GPUs and Experiment 156 locked.

## Session 18 (18:30)

### Goal

Execute the pushed metadata-only NEMDE archive protocol once and determine whether exact dispatch-interval members
can be sampled without full daily downloads.

### What was done

- Created a clean detached worktree at protocol commit `0787be5f04f7d66e80c497f85051ce2b742bcc11`.
- Issued exactly two frozen suffix requests. Both returned HTTP 206 and exactly 1,048,576 bytes with the declared
  total archive sizes; no retry, extension or full-file fallback occurred.
- Uploaded and verified both exact byte strings in R2 before parsing ZIP metadata.
- Parsed EOCD and central-directory records only, checking every member name, interval ID, encryption flag,
  compression method, size, CRC and local-header offset. No XML member was opened.

### Results / findings

- Decision: `PASS_NEMDE_TAIL_INVENTORY`.
- Both archives contain exactly 288 members and complete interval IDs 001--288, with zero encrypted,
  unsupported-compression or wrong-date members.
- The January interval-144 member is `NEMSPDOutputs_2021010114400.loaded`, offset 57,637,442, compressed size
  404,030, uncompressed size 7,851,704 and CRC `11f971a8`.
- The December member is `NEMSPDOutputs_2021120114400.loaded`, offset 65,590,506, compressed size 459,136,
  uncompressed size 8,895,016 and CRC `ebbea93d`.
- Conservative later range bounds are 469,595 and 524,701 bytes. Summary SHA-256 is
  `9fca06c5e627960499f0e045dbbf77de174ac8a99b3f5fe46d975dfdbb1e1bb8`.
- Total source download was 2 MiB. GPU-hours, paid data, target outcomes and raw redistribution were zero.

### Open questions / blockers

- Do these exact members contain valid input, output and price-setting sections in both regimes?
- Which trader, constraint, region and solution attributes are stable enough for a versioned common projection?
- Can `nempy` reconstruct a scoped subset without silently replacing production inputs?
- Written AEMO clarification is still required before redistributing raw XML.

### Next steps

1. Preserve and push the immutable result.
2. Freeze exact local-header range requests and XML conformance gates using only the committed inventory.
3. On pass, design one-day production-output alignment and open-replay error decomposition; keep GPUs locked until
   a CPU baseline and data semantics pass.

## Session 19 (18:42)

### Goal

Convert the passed central-directory inventory into a content-frozen, minimal XML conformance protocol without
opening either selected member.

### What was done

- Copied the exact interval-144 member names, local-header offsets, flags, compression methods, compressed and
  uncompressed sizes and CRCs from the committed inventory into a new immutable manifest.
- Fixed one 1 MiB range per archive beginning at the local header; prohibited retry, extension, replacement, full
  download and parsing bytes belonging to a following member.
- Implemented local-header validation, raw-deflate EOF/size/CRC checks, DTD/entity rejection and namespace-safe XML
  inventory without persisting attribute values.
- Froze the three official sections, six input group families, six output solution families and nonempty price
  analysis as gates. Action-like attribute spelling is reported but cannot fail the protocol.
- Added exact download/R2 receipt validation, runners, five focused tests and plan/memory documentation.

### Results / findings

- At protocol freeze no XML byte had been requested or opened. Total prospective source transfer is 2 MiB.
- Manifest SHA-256 is `25e06b7049f662edc21e1f74c5b0c3d676de9c8a4b3f3b4309a3ab3d1ac40cb5`.
- The protocol can establish that historical production cases carry a synchronized mechanism input/output
  interface in both regimes. It cannot establish raw participant actions, exact counterfactual replay or behavior.
- No GPU, remote worker, paid data, target outcome or raw redistribution is authorized.

### Open questions / blockers

- Do official tag names in both historical regimes match the guide's exact group names?
- Which attribute families changed across 5MS/WDR, and is a stable common projection large enough for replay?
- Does `SolutionAnalysis` carry useful marginal contribution structure in both samples?

### Next steps

1. Run tests, Ruff and strict mypy; commit and push the network-unexecuted protocol.
2. Execute exactly two source requests from a clean detached worktree, retain in R2, then parse once.
3. Preserve the immutable result and either design one-day replay alignment on pass or return to source mapping on
   failure. Keep all GPUs locked.

## Session 20 (18:51)

### Goal

Execute the frozen two-member XML conformance protocol once, preserve its result and decide whether production
case replay is a stronger historical route than monthly table extraction.

### What was done

- Ran exactly the two committed 1 MiB source requests from a clean detached worktree at
  `9f44bb4431b57564608848cb5b577b7b1e58fa9b`.
- Verified both opaque byte ranges in R2 before opening local headers or XML.
- Reproduced member name, flags, deflate method, compressed/uncompressed size and CRC; ignored every byte after the
  selected member.
- Parsed namespace-safe tag/attribute inventories without retaining attribute values and applied all frozen
  section, input-group, output-group and price-analysis gates.

### Results / findings

- Decision: `PASS_NEMDE_XML_CONFORMANCE`; all ten aggregate gate counts are 2/2.
- The cases contain 50,571/58,679 XML elements, 349/383 traders, 815/937 generic constraints and 190/81
  price-setting rows. Both contain all frozen applied-action attributes and full production solution families.
- Participant/unit identifiers, offer date/version, price/availability bands, initial conditions, SCADA, demand,
  constraints, solver version/status/objective, prices, targets, flows and violations coexist in each case.
- Input tag and attribute sets are identical across regimes. Output structure differs only by post-change
  `FSTargetModeTime`, independently consistent with the v5.1 fast-start observation extension.
- Summary SHA-256 is `f48d86b8f7837956fa5813e8719a712d543ade3e795fa1ad691036bd096bb7cd`.
- Source transfer was 2 MiB; GPU-hours, paid data, target outcomes and public raw redistribution were zero.

### Open questions / blockers

- Are schema and solver-version fields stable over all 288 intervals of one day, including rerun/constraint cases?
- Do XML targets/prices/constraints exactly align with public `DISPATCHLOAD`, `DISPATCHPRICE` and constraint rows?
- Which open solver can ingest or faithfully reconstruct these cases, and what mismatch is irreducible without the
  production executable?
- Raw submission/rejection history and redistribution permission remain unresolved.

### Next steps

1. Validate, commit and push the immutable XML result.
2. Audit open NEMDE replay implementations against the actual case format before choosing `nempy` or another
   engine.
3. Freeze a one-day temporal-schema/alignment sample and CPU baseline; use GPUs only after replay semantics pass.

## Session 21 (18:53)

### Goal

Audit open NEMDE replay engines for realized-output dependence before selecting the next free experiment.

### What was done

- Audited Nempy commit `2d3cef0e5545c820067fecddfa2e2fd984ac5583` (version 3.0.3) and akxen/nemde
  commit `23afcdf128352f12d3074194a1321a8f810f4407` at source level.
- Traced each historical input path from case loading through generic-constraint construction and separated
  intervention-mode selection from actual solution leakage.
- Inspected Nempy's input-side RHS evaluator, documented its unsupported-operation boundary and traced the MMSDM
  dependencies of the full replay example.
- Added a claim taxonomy and selected a bounded R2-only input-RHS preflight before any full-day acquisition or
  solver run.

### Results / findings

- Both default engines inject production `ConstraintSolution/@RHS` into their reconstructed optimization. Their
  historical price/target/objective agreement is solution-assisted and cannot establish independent replay.
- akxen/nemde `run_mode="target"` selects the physical intervention case; it does not simply fix every solved
  target. Its material leakage is the production RHS, and the project is otherwise a reverse-engineered
  approximation on a 2021 Pyomo/MySQL stack.
- Nempy is the stronger primary baseline, but its standard `ConstraintData` also reads production RHS. Its separate
  `RHSCalc.compute_constraint_rhs()` offers a testable input-side path and must pass output-sentinel invariance.
- The next gate reuses exactly 2 MiB already retained in R2, needs only local CPU and one small parser dependency,
  and requires zero AEMO requests, paid data or GPU-hours.

### Open questions / blockers

- What fraction of 2021 dynamic RHS equations can the pinned evaluator execute in each regime?
- How close are input-reconstructed RHS values to production after scale normalization, and which operations or
  state variables explain failures?
- Does a full input-only solve remain feasible after RHS reconstruction, or are other production-output flags also
  embedded in the standard replay path?

### Next steps

1. Commit and push the source audit.
2. Freeze the exact two-case R2 materialization, sentinel-invariance and error/coverage thresholds in code and
   tests before opening RHS values.
3. Execute once from the pushed commit, preserve the decision, then choose between a one-day alignment gate and a
   missing-state repair. Keep all GPUs idle.

## Session 22 (19:06)

### Goal

Freeze and implement the two-case input-side RHS reconstruction protocol before reopening production RHS values.

### What was done

- Fixed the two prior R2 range keys, range/XML hashes and ZIP member metadata; prohibited every new AEMO request,
  fallback, replacement and raw Git artifact.
- Pinned clean Nempy commit `2d3cef0e5545c820067fecddfa2e2fd984ac5583`, two audited source hashes and
  `xmltodict==0.12.0`; the protocol does not require `mip` or a mathematical solver.
- Implemented an R2-only materializer, exact member/deflate/CRC/XML verifier, production-reference sealing, dual
  output-RHS sentinels, all-equation execution, Type-7 error summaries and immutable aggregate decisions.
- Froze per-case thresholds before execution: reference coverage 100%, evaluated coverage >=95%, sentinel outcome
  and successful-value invariance 100%, normalized median <=`1e-8` and p95 <=`1e-3`.
- Added eight focused tests, the preregistration, manifest, dependency pin, claim locks and documentation.

### Results / findings

- Manifest SHA-256 is `b56f40999518a7e2df657ea7480a85f0f855a9d8ee8151b6f7cf4f76c99ab7cd`.
- Eighteen combined NEMDE tail/XML/RHS tests pass. Ruff passes; strict mypy passes for the module and both runners.
- At freeze neither R2 object was rematerialized and no RHS value was opened for this experiment.
- Execution will read 2 MiB from existing R2, use local CPU only and consume zero source requests, paid data or
  GPU-hours.

### Open questions / blockers

- Does current 2021 XML exercise operations or input shapes unsupported by the pinned evaluator?
- Will at least 95% of dynamic equations evaluate, and do the reconstructed values meet both frozen error gates?
- If only partial, are failures localized enough for an input-state repair without using production output?

### Next steps

1. Commit and push the unexecuted protocol.
2. Install only the pinned XML parser in the existing Conda environment.
3. Execute once from a clean detached worktree, preserve the result, and keep all three GPUs idle.

## Session 23 (19:14)

### Goal

Execute the pushed RHS protocol once, preserve the immutable decision and enforce the pre-registered tail gate.

### What was done

- Installed only `xmltodict==0.12.0` in the existing `ecophys` Conda environment.
- Created a clean detached worktree at protocol commit
  `8a26320a3b2c1755e873d9c96d06a5196cf45ede` and verified the clean pinned Nempy checkout.
- Materialized both exact 1 MiB ranges from their frozen R2 keys with zero AEMO requests; all remote/local hashes
  passed before content access.
- Reproduced ZIP/deflate/CRC/XML hashes, sealed intervention-zero production RHS, replaced all output RHS with both
  sentinels and attempted every input dynamic equation exactly once per sentinel.
- Applied the frozen per-case coverage, invariance and normalized-error gates without retries or exclusions.

### Results / findings

- Decision: `PARTIAL_INPUT_SIDE_DYNAMIC_RHS_RECONSTRUCTION`; integrity passed but both case decisions failed.
- Pre-5MS: 772/774 scored (99.7416%), reference coverage 100%, median normalized error `4.11e-10`, p95
  `0.00416664 > 0.001`.
- Post-5MS/WDR: 882/884 scored (99.7738%), reference coverage 100%, median `1.34e-10`, p95
  `0.00194488 > 0.001`.
- Sentinel success/error outcomes and all successful float values matched exactly in both cases. Two equations per
  case raised `IndexError` under both sentinels.
- Pre-5MS p99/max normalized errors were `0.988812/0.996800`; post-change p99/max were
  `0.00668113/0.500001`. Median-only reporting would therefore be seriously misleading.
- Summary SHA-256: `704b20a54e8232eda57ebdc4525182c8586b104f400dbdddb0a9aa29c6e57119`;
  materialization receipt SHA-256: `7e475dd334ac5f3005cab2135b4e21206d7d51db8c6e0555ef4bc25a2ebbaba6`.
- New AEMO requests, solver runs, GPU-hours and paid-data spend were all zero.

### Open questions / blockers

- Which constraint/equation families create the sparse catastrophic pre-5MS tail and the smaller post-change tail?
- Are the four `IndexError` cases unsupported RPN operations, malformed stack programs or absent input state?
- Can a principled input-only evaluator repair these tails on fresh intervals without borrowing production RHS?

### Next steps

1. Commit and push the immutable partial result before further target analysis.
2. Run a separately documented post-hoc tail-operation diagnostic on the consumed cases; preserve full IDs and
   never reinterpret the failed threshold.
3. If a concrete repair exists, freeze fresh deterministic intervals and thresholds before executing it. Keep the
   full-day solver and all GPUs locked.
