# 2026-06-16

## Session 1 — ABIDES CPU job complete: agent-based frontier point + SBI cost leg

**Goal:** Resume monitoring the detached ABIDES CPU job on howard-pc (100.105.21.7) through
the Tailscale network outage; when it lands, score the daily leg, fetch results, record.

### What was done
- Network came back (box never rebooted — `boot=2026-05-04`, up continuously the whole outage;
  the connectivity watcher caught the return at ~315 min). Job ran detached throughout.
- Verified final state: **both CPU legs complete**, no workers, done-markers present.
- Investigated a `n_total_rollouts=1` red flag → **by design** for daily mode (`_daily_series`
  in `abides_to_returns.py`: each sim-day → one gross close-to-close return → a single 250-pt
  daily series = one realization). Sims fine; std=0 just reflects one assembled series.
- Hit `score_summary.py` "no eval results found" → root-caused: `is_unstable()` requires the
  dropped `volume_volatility_corr` to be present, so every daily cell is wrongly rejected.
  Worked around by computing `n_pass` over the 10 present facts directly with the same BANDS.
- Fetched the 5 `results_rmsc03_*_daily/inference_merged.json` + `sbi_cost_report.json` to Mac
  (`experiments/105_abides_ceiling/`).
- Retired the `@reboot` resume cron (its completion check wanted 1250 CSVs; we have 1240 —
  10 fewvalue seeds crash deterministically — so it would have looped on reboot).

### Results / findings
**Daily 3-paradigm frontier point (agent-based = JPMC ABIDES RMSC03, 5 cells × 250 seed-days,
full per-second OB logging, 10 daily facts):**

| cell      | n_pass/10 | passes |
|-----------|-----------|--------|
| base      | 2/10 | autocorr, cond_kurtosis |
| morenoise | 2/10 | autocorr, cond_kurtosis |
| fewnoise  | 3/10 | + dfa_hurst |
| **morevalue** | **4/10** | + dfa_hurst, zumbach |
| fewvalue  | 3/10 | + dfa_hurst |

- **Fails EVERY volatility-clustering + fat-tail fact**: acf2≈0, Fano≈0.98 (Poisson, no
  intermittency), agg_gaussianity≈0, leverage≈0, gain_loss≈0.
- **KEY for the figure**: ABIDES daily tails are too THIN (hill 5.0–6.9, band [2,4]) — the
  OPPOSITE sign of EcoMD's too-FAT overshoot (hill≈1.3–1.5). Three distinct failure corners;
  error-sign flips between paradigms (consistent w/ exp 109/111).
- Confirms the ~5.1 ceiling is **paradigm-level**, even for the field-standard agent-based sim.

**§7 calibration-cost leg (`sbi_cost_report.json`):** ABIDES T_sim ≈ 844 s/sim (per-second
logging), 20 timed sims. Projected SBI/search calibration (sim-count-bound):
1k → 235 CPU-h, 5k → 1173 CPU-h, 10k → 2345 CPU-h (~98 CPU-days). Comparand for "ECoMD
gradient calibration ~10² steps, minutes".

### Open questions / blockers
- ABIDES daily fact values are point estimates (one assembled 250-pt series, no CI). If a
  reviewer wants error bars, bootstrap the daily-return series — cheap, no re-sim.
- `score_summary.py` daily-mode harness mismatch (requires dropped fact) — worth a 1-line fix
  if we keep scoring daily cells with it.

### Next steps
- Resolves the long-standing "ABIDES fair re-run owed for the ceiling figure" item.
- Build the 3-paradigm frontier figure: ABIDES 4/10 (thin tails, no dynamics) vs EcoMD ~7/10
  (dynamics, fat-tail overshoot) vs deep-generative (tails, no clustering) — same 10 daily facts.
- Fold the SBI cost number into Paper A §7 (calibration-cost comparison).
- GPU ζ_ED leg (`feature/zeta-ed-gpu`) still to be run by user on the 2-card box.
