# 2026-05-28

## Session 1 — Batch 1 (102/103) review + diagnosis + Path A/ABIDES launch prep

### Goal
Pull H20 Batch 1 results (exp 102 multifact-loss, exp 103 arch×expanded-loss), apply
the pre-registered significance gate, diagnose the result, and — per the user's
"no downgrade" directive — set up the next escalation rather than soften the narrative.
Use today's H20 run-day: Mac pre-flight first, then push.

### What was done
- Pulled `4209c38ed` (102: 420/420 results; 103: 320/390, 70 seeds unfinished).
- **Significance gate** (Welch t + Bonferroni, from scoreboard summary stats): **0/14 (102)
  and 0/12 (103) cells beat `baseline_v3` at p<0.05.** No exp-106 promotion. Best 102 cell
  `d_mse` +0.19 (p=0.60, and it uses no surrogate); best 103 `agentmem_inner4` 5.41 is
  survivorship (13/30 rejected for agg-gaussianity blowup), p=0.077.
- **Diagnosed why** — built `scripts/diagnose_surrogate_coverage.py`. Found exp 102 was an
  **invalid test** of objective-coverage: `mf_fano`/`mf_dfa` are **bit-identical to baseline_v3
  across all 30 seeds** (max-abs-diff 0). Mechanism: training rollout feeds ~7 returns
  (`chunk_steps=24`, `warmup=16`), but surrogate length guards need n≥50 (fano), n≥~200 (dfa),
  n≥400 (agg, else degraded k1-only). Numerically verified grad_fn liveness vs n∈{7..400}.
  Only `gain_loss_skew` was live; where active, surrogates moved the target fact toward band
  with **zero collateral** (weak positive signal). Wiring itself is correct (`losses.py:343`,
  `multi_fact_terms`; kill-test ρ 0.91–1.0).
- **Path A (exp 107) prepared** — `experiments/107_multifact_longroll_n30/generate_configs.py`,
  90 cfg (3 deconfounded cells × 30 seeds): `baseline_v3` (new-N control) → `longroll_moments`
  (rollout-reg on, moments only) → `mf_all_mse_longroll` (reg + 4 surrogates). Fix is
  config-only: enable rollout-reg (`_compute_rollout_reg_loss`, 057) at `rollout_reg_steps=512`
  so all 4 surrogates are live while peak memory stays ~one chunk (respects chunk-OOM
  constraint); N 10K→2000 + bf16.
- **PRE-FLIGHT GATE (M2)** on Mac N=500: **liveness PASS** — on the 474-return rollout all 4
  surrogate terms `grad_fn=LIVE` (fano=71.2≠dead-const 1.0, dfa=0.24≠dead-const 0.0), gradient
  reaches 25/28 params. Stability sub-run (15-iter training) ran 16 min on CPU without
  NaN/crash but was too slow to finish; the agg-gaussianity<1000 + no-OOM conditions are
  deferred to the **H20 first-config check** (GPU+bf16 makes it fast).
- **ABIDES ceiling probe (D7)** — wrote `scripts/h20_run_abides_calibrate.sh` (rmsc04 + 4
  variants × 6 seeds; ABIDES API assumptions flagged with TODOs, H20 not reachable to verify),
  `scripts/abides_to_returns.py` (mid-price→returns→`compute_all`→inference_merged.json, same
  schema as baselines runner), `scripts/h20_abides_baseline.sh` (end-to-end). `experiments/105_abides_ceiling/`.
- **Escalation ladder** — `papers/proposal/plan_v3_addendum_2026-05-28.md`: pre-registered
  A→B→C with binding triggers. A fail (mf_all_mse_longroll ≤ baseline_v3, Bonferroni) → Path B
  (B2 heterogeneous-node MoE first, then B1 mechanism-bound modules, B3 leader-follower dyngraph,
  B-loss adversarial/MMD). ABIDES 3-outcome decision tree for Paper A narrative.
- Memory: new `feedback_no_downgrade`, `feedback_h20_day_budget`, `surrogate-rollout-length-trap`;
  updated `project_paper_a_neurips_2027`, `project_pareto_ceiling`; MEMORY.md synced.

### Results / findings
- **Objective-coverage is UNtested, not falsified.** The 5.1 ceiling stands as the best
  *fairly-tested* number; 102 cannot be cited as "ceiling unbreakable".
- **103 instability is a real finding**: `inner_steps>1`/`agent_memory` blow up
  aggregational_gaussianity on ~40–50% of seeds. ISAB pairwise is dead (2.2–3.1/11).
- Active surrogates show **zero collateral** when moving their target — encouraging for Path A.

### Open questions / blockers
- Does long-rollout (512) make `mf_all_mse_longroll` beat baseline_v3 at the gate? (exp 107)
- Will N=2000 hurt other facts vs N=10K? (107 baseline_v3 control answers this.)
- ABIDES API/version on H20 unverified — calibrate driver has TODO markers to confirm.
- Stability of 104 at chunk-level: deferred to H20 first-config check (Mac CPU too slow to confirm).

### Next steps
1. Commit + push artifacts (this session). Hand back H20 SSH sequence (task 8).
2. H20: run `experiments/107_multifact_longroll_n30` + `scripts/h20_abides_baseline.sh` (parallel),
   confirm first 107 config trains in the first ~5 min (no OOM, loss↓, agg-gauss<1000), then full run.
3. 2026-05-29 AM: pull, score, apply Bonferroni gate vs the new N=2000 baseline_v3, record in
   `logs/2026-05-29.md`, trigger exp-106 promotion or Path B per the addendum.
