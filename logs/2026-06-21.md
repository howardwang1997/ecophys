# 2026-06-21 — exp 124 Phase 1 (NCS order-flow prediction, sim side) COMPLETE

## Session 1 — Phase 1 results (E-S1 dose-response + E-S2 cross-asset OFI)

**Run.** `gpu_exp124_phase1.sh` on .56 (2 cards), ~12h (23:34→11:33), auto-pushed
(`ofi_transient_{spx,ndx,gold,btcusdt,eurusd}.json`). Pure inference, all concave_d050 ckpts present.

**E-S1 — spx OFI-memory dose-response = clean MONOTONIC SIGMOID** (shock-window OFI lag-1 memory by
kick dose): 0.05→**0.05**, 0.1→0.26, 0.2→0.58, 0.3→0.75, 0.5→0.89, 1→0.97, 2→0.99, 3→0.99, 6→1.00,
12→1.00 (control −0.07). Onset ~0.1–0.2, saturates to perfect persistence (~1.0) by dose ~1. |ρ| and
saturation rise monotonically too. **Cleaner/more monotonic than the return-tail dose-response** —
OFI memory is the sharper observable.

**E-S2 — OFI transient GENERALIZES (control vs kick6):** spx/ndx/gold/btcusdt all show the full
burst-and-relax on all 4 measures (mem 0.0→**~1.0**→~0, |ρ|/std/sat burst). eurusd weak at kick6
(mem→0.35, |ρ| flat) — sub-threshold, **consistent with its known higher vol-threshold** (needed mag12
for the tail too; asset-dependent threshold ∝ intrinsic vol). So 4/5 strong + eurusd consistent.

**Interpretation / the sharpened prediction (for Phase 2 real-data).** The sim predicts real
coordinated-liquidation events drive an **order-flow MEMORY burst toward perfect persistence** that
(i) turns on at a threshold, (ii) scales monotonically (sigmoid) with severity, (iii) saturates ~1,
(iv) generalizes across asset classes with a vol-dependent threshold — all where the *return tail* is
stationary. This is the pre-registerable Phase-2 hypothesis.

## Session 2 — E-S3 + E-S4 (Phase 1 COMPLETE)

`scripts/analyze_ofi_entropy.py` (E-S3) + `scripts/fit_ofi_tau.py` (E-S4), run on .56.

**E-S4 — τ_OFI ≈ 22 ± 2 steps** (spx/ndx/gold/btc, R²=1.0) vs return-tail **τ_ED ≈ 236**. The OFI-memory
burst is a **sharp impulse, ~10× faster than the tail** (and at the W=500/stride=100 resolution floor →
"≲ one window"). So order-flow coherence is near-instantaneous at the shock; the tail relaxation is the
slow process. Refines the real-data prediction: expect a **sharp** OFI-memory spike at crash onset.

**E-S3 — entropy-production proxy is FLAT** (kick6 EP Δ+0.004 ≈ control; jump6 flat too). The (sign Δp,
sign OFI) pair-transition irreversibility does NOT burst. Honest reading: the transient is
**persistence/coherence, NOT sign-level irreversibility** (a persistent/AR-like process can be
time-*reversible*). Caveat: the sign discretization is coarse — a finer EP estimator is future work —
but the **robust signal is OFI memory**, not entropy production.

**Implication (changes the Phase-2 plan).** Lead the NCS real-data test with **OFI MEMORY** (clean
sigmoid dose-response, 4/5 cross-asset, sharp τ), NOT entropy production. This *tightens* the
Paper-A/Paper-B split: memory/coherence = Paper A's lane; real entropy production stays Paper B's
(harder) problem. Reports: `ofi_entropy_{5 assets}.json`, `ofi_tau_report.json`.

**Phase 1 COMPLETE.** Frozen prediction for Phase 2 (real L2, gated on Tardis buy): coordinated-
liquidation crashes drive a **sharp OFI-memory spike toward perfect persistence**, sigmoid in severity,
4/5-asset general (vol-dependent threshold), where the return tail is stationary; entropy production is
NOT the (sign-level) discriminator.
