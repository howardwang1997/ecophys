# 2026-06-09

## Session 1 — GitHub sync, 116/120 criticality verdict, Paper A week-sprint build

Branch: `feature/exp113-gabaix-solve`.

### GitHub sync
Pulled `origin` (`1206ed5b0 → e30eac182`): exp116 FSS + exp120 train-at-N result dumps from the
H20 fleet (481 files + `scripts/_h20_1_wait_and_120.sh`). Other branches: paper-a-neurips-2027 /
paper-b-s8-jarzynski in sync; `main` + 10 old experiment branches have **divergent (rewritten)
histories** (ahead=N/behind=N) — left untouched (a routine pull would create garbage merges).

### exp116 FSS + exp120 train-at-N — criticality / Paper B fork verdict
Ran `score_criticality.py` (116) and `score_fss_train.py` (120 vs 116).
- **116 (fixed dynamics, sweep N) — the correct FSS protocol, internally consistent (all T=8000):**
  hill(N) is a smooth, slope-stable crossover; mean never crosses α=2 to N=14000 (κ=0: 4.39→2.58;
  κ=1.2: 4.92→2.07, CI straddles 2); **no finite-size sharpening**; κ shifts the curve. → the
  pre-registered "aggregate-flow crossover" reading, NOT emergent criticality.
- **120 (train-at-N):** hill crosses 2 (spx ~N2000, btc ~N4000), → ~1.3 at N=10000 — BUT eval ran
  **T=4000 vs 116's T=8000**, so the 116-vs-120 "DISAGREE" is rollout-length-confounded
  (`[[project_surrogate_rolloutlen_trap]]`) and **not interpretable as-is**.
- **VERDICT: Paper B criticality / NP-flagship fork = NO-GO on current evidence.** 116 alone is
  decisive (no sharp transition); the artifact question (120) is open but secondary. The ONE
  experiment that could reopen it: FSS with `ed_normalize=True` (√N) — if the N-scaling survives
  normalization → intrinsic transition; if it vanishes → confirmed crossover, criticality closed.

### Paper A — honest state + week plan (user decisions)
Spine collapsed to **diagnose + concave-impact solve + δ\*≈0.5 universality** (SOTA-break G1 and
SV-method DEAD per 115/119). User locked: (1) **n=30→60 power top-up** on ndx/gold/eurusd
(pre-registered); (2) **117 leverage** timeboxed; (3) **btc data is present locally** (the "0/60"
was an H20-clone bug) → complete the 5th asset.

### Built (Mac, all smoke-verified)
1. `papers/proposal/prereg_2026-06-09_power_topup_and_delta_grid.md` — frozen pre-reg (n=60 fixed,
   frozen scorers, dual n=30/n=60 reporting) → kills optional-stopping critique.
2. `experiments/118_delta_grid/generate_configs.py` — added `--deltas` + spx (δ∈{0.35,0.55}×5 + btc).
3. `experiments/114_concave_confirm/generate_configs.py` — added `--assets/--cells/--seed-start/-end`.
4. `experiments/117_leverage/generate_configs.py` — NEW: concave_d050 + {asym_drag, zumbach-downside}
   cells vs concave_only control (mechanisms already in `integrator.py`, no new physics).
5. `scripts/h20_abides_baseline.sh` — fixed: thread `--drop-facts`; `MODE=daily` →
   `results_*_daily/` + auto-drop volume_volatility_corr (the timescale-fair re-run).
6. `scripts/score_surrogate_kill.py`, `scripts/assemble_frontier.py` — new reusable scorers.
7. `papers/proposal/launch_paperA_week_2026-06-09.md` — the fleet launch runbook.

### Key findings (from cached data, Mac)
- **Surrogate-kill: concave solve SURVIVES** (net 7/11 > GARCH-t 5/11; structural 2/3 > 0/3).
  **But the hill+acf² 2-fact gate IS GARCH-fakeable** (GARCH-t: hill 2.38✓, acf² 0.19✓) → the paper
  must lead with the JOINT 11-fact + structural facts (leverage/DFA/zumbach), NOT the 2-fact gate.
- **3-paradigm frontier (tails XOR dynamics):** EcoMD-concave is the ONLY cell passing hill✓ AND
  acf²✓ (7/11); all neural baselines pass hill but fail acf² (~0.1); ABIDES 2/11. The diagnose→solve
  story in one heatmap (`papers/paper_a_methods/figures/fig_frontier_3paradigm.png`, draft).
- **117 reframe:** concave_d050 already passes `leverage_effect` (-0.98, n=30) → 117 is a cheap
  confirm-and-strengthen (per-seed pass-rate) sweep, GREEN-lit. Untrained Mac smoke abandoned
  (pathologically slow, weak signal) — the GPU sweep is the real test.

### Open / next
- LAUNCH on H20 (user): the runbook (δ-grid ext + power top-up + 117 + ABIDES daily).
- Frontier finalization: swap the real 5.96 SOTA cell into `assemble_frontier.py` SOURCES; ABIDES
  row auto-upgrades to `_daily`.
- After H20: re-score δ* (5-asset universality), n=60 strict gate, frontier; draft results section.
- Scratch: `scripts/_smoke_leverage.py` (kept for a future trained smoke if wanted).
