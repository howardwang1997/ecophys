# 2026-09-22

## Session 1 (D0-S4: eval-driver audit fixes, PI constants, RC2 truncation passes)

**Goal.** Close every confirmed defect in the B1 eval-draw driver found by the
69-agent adversarial workflow, obtain the PI decision on all unpinned eval
constants, and — per the PI's explicit "现在扩展驱动" — implement B1's 30,720
RC2 truncation-pass records in the same driver, today.

**What was done.**

- Fixed all 4 confirmed workflow defects in `scripts/reexploration/eval_draws_d0s4.py`
  (blocker: fresh `build_inference_mechanism` per (cell, axis, horizon, draw) record
  — C2(b) replay identity across cells/axes/horizons/resumes; scales-placeholder
  refusal; strict START:STOP seeds + namespace validation; kswap duplicate counts;
  manifest `pinned_constants` block). Selfcheck
  `scripts/reexploration/selfcheck_eval_streams_d0s4.py` 7/7 PASS on the 3080 node;
  full smoke matrix green (G8 payload classes, G9 byte-identity, resume skip,
  horizon-distinguishing hashes, 4-name stream_hashes on 130 records).
  Evidence: `research/discovery/ops/d0s4_eval_driver_audit_20260922/` (workflow
  result + README with confirmed/refuted tables). Commit 9beea5a9d.
- PI decision round 1 (AskUserQuestion): horizon mode **chained**; RC2 **extend
  now** (over the recommended deferral); **blanket approval of D2-D12 defaults +
  production authorization** at B1 60/60 on the 3080 CPU node.
- RC2 grounding in frozen text + engine facts:
  - D_trunc^W mechanically defined in the lemma writeup (kt_g4_g5 §4): out-of-window
    resting orders removed from the deployed book before replay (the replay
    contract's native operation, prestate side).
  - Corpus prestates do NOT serialize arrival clocks (`prestate_from_json` has no
    clock field; KeyError on node) — clocks live only in the generator's raw flow.
    Recovery route: deterministic re-derivation via frozen `generate_episode`,
    verified by the manifest's `episode_canonical_json_sha256`. ID re-execution
    (build_prestate → ReferenceEngine → project_fexec_corpus) reproduces the disk
    corpus **byte-exactly** — proven on the node.
  - DGP clock structure: first-in-LEVEL arrives in [0,40] (latency window),
    followers in [41,80]; ~10-14 resting orders over 5 levels; requests start at
    tick 100 > all clocks, so the request stream and round structure are
    clock-independent by construction.
  - Severity census (seed 11000, 64 episodes): W=0 keeps ~0.1 orders, W=20 mean 2.5
    (levels can empty), W=40 exactly 5 (= the per-level firsts, deterministic, all
    levels straddle), W=60 8.9, W=80 identity. W=20 vs W=40 discriminate on 61/64
    episodes.
- PI decision round 2: W grid **cap=40, lag=20**; RC2 mechanical package
  **approved** (own-truncated-truth evaluation mirroring pop_2x; fixture binding =
  ID-axis stream manifest sha (kswap precedent); canonical-json hash guard; E-4
  conservation+settlement gates re-run on every truncated tape).
- Implemented RC2 in the driver (`trunc_session_episodes` + RC2 emission loop +
  manifest pins; `--rc2-conditions` CLI). Smoke on 3080 node: 385 records; 1
  payload class over 16 draws per (cell, condition, horizon) [G8]; independent
  re-run byte-identical including the re-derivation path [G9]; resume skip; zero
  conservation violations. Exposure structure matches the frozen prediction:
  truncation invisible at h=1/4 (early rounds don't touch removed followers),
  trunc ≠ ID at h=16, lag ≠ cap at h=31.
- Materialized + sealed s_ch (Annex B(e)): `materialize_s_ch_d0s4.py` on the 3080
  node, namespace 20260977, 512 episodes, 6,368 rounds pooled,
  **s_ch = [5.4546989294724275, 5458.5636934342465]**,
  sha256 `debb15a761d5b08cfb3a05428ddd9d5f820abe09d2480b552360ac9800735e0b`.
- Consolidated decision record
  `research/discovery/decisions/pi_d0s4_eval_constants_20260922.yaml` (chained +
  D2-D12 blanket + RC2 W grid/mechanics + production authorization + s_ch seal).
  Commit 93b2da32c (pushed).

**Results / findings.**

- The RC2 arrival-clock problem is fully solved by re-derivation + hash guard —
  no corpus regeneration needed, and the ID re-execution byte-identity check
  doubles as a generator-stability proof on the production node.
- Smoke endpoint_y structure (seed 11000): truncation is invisible at short
  horizons exactly as the exposed-set table predicts (U(τ) ∩ W materializes only
  when the window reaches straddling rounds) — a nice instance-level confirmation
  of T-1's truncation row before any analyzer runs.
- B1 training at session time: 72/120 (raw arms 30/30 on ts; through-M 12/60
  across ts+bts). Seed 11000 complete on ts; its 4 checkpoints were relayed to the
  3080 production tree for a full-grid production-path dry-run.

**Open questions / blockers.**

- B1 through-M completion (~48 jobs left) gates the production eval; watcher
  ba1wcxuju armed for B1-THROUGH-M-COMPLETE-60-OF-60.
- Checkpoint relay ts/bts → 3080 for the remaining seeds must run before/with the
  production invocation (seed 11000 already relayed; its dry-run records, if
  clean, are themselves valid production records — final locked checkpoints,
  deterministic driver).

**Next steps.**

- ~~Verify the seed-11000 full-grid dry-run~~ DONE: exactly 3,076 records
  (2,048 RC1 incl. 256 kswap dupes + 1,024 RC2 split 512/512 + 4 RC4), manifest
  carries the real decision id, 150 s/seed → full production grid ≈ 75 min
  single-thread on the 3080 node. These ARE seed 11000's production records
  (final locked checkpoints); the 60/60 invocation resumes past them.
- All 72 completed checkpoints relayed ts/bts → 3080 production tree (re-relay
  at 60/60 picks up the rest).
- At 60/60 (watcher ba1wcxuju): final relay, production eval 11000:11030 with
  `--production-decision pi_d0s4_eval_constants_20260922`, R2-stage under
  `alpha_cube_d0_20260921/eval/B1/` (task #20).
- B1 cross-host merged block manifest + R2 staging once training closes (task #18).
