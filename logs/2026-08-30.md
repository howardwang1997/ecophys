# 2026-08-30

## Session 1 (04:27)

### Goal of session

Turn the constraint-attribution audit into an ICLR-ready, outcome-blind extension plan and begin
execution without treating the historical five-seed audit as confirmatory evidence.

### What was done

- Reviewed the historical A/B/C/M/E scripts, records, paper skeleton, freeze files, server status,
  seed policy, and ICLR 2027 deadline.
- Froze the extension protocol in
  `papers/proposal/ecomd_constraint_attribution_iclr_extension_freeze_2026-08-30.md`: disjoint
  pilot/confirmation seeds, matched-ID plus matched-compute selection, paired practical-equivalence
  intervals, multi-step rollout horizons, independent contraction holdout, budget, schedule, and
  claim-specific stop rules.
- Kept all historical scripts/results untouched and implemented independent extension code:
  `constraint_iclr_common.py`, PDE/H runner, corrected M2 FIFO runner, pilot/confirmation launchers,
  and paired analyzer.
- Replaced the historical M scientific object with M2: explicit price-time FIFO queues, settlement
  at the resting order, nonzero bilateral fees, and a fee account included in the cash invariant.
- Added Hydra smoke/pilot/confirmation configs and immutable worker manifests. Pilot is partitioned
  across the two V100 workers; confirmation is generated only from the ID-only lock file.
- Added `tests/test_constraint_iclr.py` for spectral invariants, projection geometry, 1D/2D model
  shapes, FIFO priority, fee-account settlement, selector branches, bootstrap determinism, and
  launcher locking.
- Ran local CPU smoke training for A, B including resolution shift, C, H-strong, and M2. Ran Ruff,
  Python compilation, analyzer provenance ingestion, launcher dry runs, and the new test module.
- Read-only checked the authorized V100 nodes. Both are idle V100-SXM2 32 GB machines. Both have a
  reusable Conda environment at `/data/ecophys_workshop/conda_env` with Python 3.11, PyTorch
  2.3.1+cu121, NumPy 2.4.6, and Hydra 1.3.5. Free root-disk space is approximately 19 GB on
  `100.80.236.112` and 37 GB on `100.123.220.57`.

### Results / findings

- `tests/test_constraint_iclr.py`: 18 passed.
- Ruff passes on all new Python files. A/B/C/H/M2 smoke runners each wrote five append-only records
  (four trained arms plus derived projection) with git/head/dirty/config/source-hash provenance.
- Final transfer-ready 27 KB snapshot was built locally at
  `/private/tmp/ecophys_constraint_iclr_20260830_ready.tgz`, SHA-256
  `4db28ce51d866c9792f3b65e7ad39740aa6fe88199d0c0c016b2f3f585295722`; it has not been sent.
- In smoke data only, projection preserved conserving RMSE exactly and hard enforcement reduced
  invariant drift to float precision. These are implementation checks, not paper evidence.
- A 2D sampler bug (`ndarray.square`) was exposed by the C runner smoke, fixed, and covered by a
  regression test before remote execution.
- The original projection gate was mathematically overbroad at autoregressive horizons. The freeze
  now clarifies before remote pilot that exact projection/free equality is a horizon-one gate only;
  later trajectories have different inputs and are reported rather than used as an algebraic gate.
- No remote pilot outcome has been generated or inspected. Confirmation seeds 1000--1019 remain
  untouched.

### Open questions / blockers

- Security review rejected SCP of the uncommitted/dirty-worktree source snapshot to the two remote
  servers despite their compute authorization. Explicit user approval to copy the enumerated new
  experiment files to those hosts is required. No workaround was attempted.
- The RTX 2060S worker at `100.105.21.7` was previously unreachable; the design does not depend on
  it.
- Pilot ID overlap may fail. The frozen response is fixed-compute wording, not widening the 5%
  matching tolerance.

### Next steps

1. Obtain explicit approval to transfer the frozen source/config snapshot to both V100 hosts.
2. Rebuild the snapshot after the final local checks, verify its SHA-256 on both hosts, and launch
   `v100a`/`v100b` pilot partitions under the existing Conda environment with git-head/dirty env
   metadata.
3. Pull append-only pilot JSONL files, run the ID-only selector, freeze the lock, and inspect only
   the gate report.
4. If gates pass and projected total runtime remains below 120 V100-hours, launch the locked
   20-seed confirmation set and generate the paired-equivalence/rollout claim table.

## Session 2 (04:39)

### Goal of session

Deploy the explicitly authorized snapshot, pass remote CUDA gates, and start the frozen pilot
without exceeding its 25 V100-hour cap.

### What was done

- SCP'd the authorized 27 KB archive to both V100 hosts. Both remote SHA-256 values exactly matched
  `4db28ce51d866c9792f3b65e7ad39740aa6fe88199d0c0c016b2f3f585295722` before extraction.
- Ran independent four-arm CUDA preflights: 1D PDE on `100.80.236.112`, 2D U-Net on
  `100.123.220.57`. Both wrote five records including derived projection with correct git head,
  dirty flag, V100 name, source hashes, and finite metrics.
- Launched the frozen workers. V100a PID 983133 continues A/H/M2. V100b PID 163606 began B/C.
- Read runtime/count/finite status only. No pilot ID or OOD metric value was inspected. The first
  B channels-8/epochs-100 arms took approximately 33 seconds each; quadratic channel scaling put
  one full B grid around 31 V100-hours, already above the total pilot cap.
- Confirmed process group 163603 contained only this pilot, then terminated it with SIGTERM under
  the frozen budget rule. GPU returned to 0%/5 MiB. Partial records remain intact and excluded.
- Wrote an outcome-blind B/C timing amendment and a thinned manifest retaining the frozen minimum
  `{8,16}` channels × `{100,200}` epochs × two learning rates. Local Ruff/dry-run passed. Amendment
  bundle SHA-256 is `211c8d53e6a18cc4fee7f3aa5854f18f8c76b7efb1f50b9206f1d85849fd7718`.

### Results / findings

- Preflight projection/free conserving-error difference was exactly zero on both hosts. Peak GPU
  allocation was about 18 MB (1D) and 140 MB (2D); hard drift was about `2.1e-8`.
- At the last check, V100a had 128 A-advection records / 102 trained arms, all finite, and only
  0.0893 accumulated GPU-hours. Its process remains healthy.
- V100b is deliberately paused; no confirmation seed has run.

### Open questions / blockers

- Security review correctly treats the 7.2 KB timing amendment as a new payload not covered by the
  exact original-snapshot authorization. It rejected SCP to V100b. Explicit user authorization for
  this amendment archive is required; no workaround was attempted.

### Next steps

1. After explicit approval, SCP the three-file B/C amendment to `100.123.220.57`, verify SHA-256,
   extract it, move the partial full-grid record intact to `runtime_only_aborted/`, and restart the
   fresh thinned v100b worker.
2. Continue runtime-only monitoring of V100a and both workers against 25 V100-hours.
3. On completion, pull only active pilot JSONL, run the ID-only selector, and freeze the lock before
   any OOD adjudication.

## Session 3 (04:47)

### Goal of session

Deploy the explicitly approved B/C runtime amendment, resume the second V100, and incorporate the
PI's instruction that scientifically necessary evidence must not be stopped by fixed hour caps.

### What was done

- SCP'd the exact 7.2 KB amendment to V100b. Remote archive SHA-256 matched
  `211c8d53e6a18cc4fee7f3aa5854f18f8c76b7efb1f50b9206f1d85849fd7718`.
- Created `runtime_only_aborted/`, verified the destination was unused, and moved the partial
  full-grid B-advection JSONL there intact. No record was deleted or pooled with active pilot data.
- Extracted the amendment and verified all three individual file SHA-256 values against local
  sources. Restarted the thinned B/C worker as PID 164840 with the alternate manifest.
- Added `ecomd_constraint_attribution_iclr_compute_authority_amendment_2026-08-30.md` before any
  pilot ID/OOD analysis or confirmation run. The 25/120-hour values are now monitoring estimates,
  not scientific stop conditions.
- Predeclared an ID-only expansion: if thinned B/C lacks ID+compute overlap, add all omitted cells
  from the original grid before locking; never inspect OOD or widen tolerance.
- Expanded confirmation prospectively from 20 to 30 paired seeds, 1000--1029. Updated both
  confirmation configs and the analyzer; all 30 are one inferential set with no 20-seed interim.
- Added a regression assertion for the 30-seed partition. New targeted suite: 19 passed.

### Results / findings

- V100b resumed successfully at 96--97% utilization with the exact
  `{8,16}` × `{100,200}` × two-learning-rate B/C grid.
- The first 12 fresh B records explicitly contain SHA-256 entries for both the timing amendment and
  thinned manifest, plus V100 identity and seed 100; provenance continuity is confirmed.
- Latest runtime-only status: V100a completed all 270 A-advection records and entered A-diffusion;
  234 trained arms total, 0.2076 accumulated GPU-hours, all finite. V100b had four fresh
  B-advection records / three trained arms, 0.0272 GPU-hours, all finite.
- Confirmation seeds 1000--1029 remain untouched.

### Open questions / blockers

- None for the running pilot. B/C grid expansion is conditional only on the frozen ID-only overlap
  result. Confirmation deployment will use a new hash-verified snapshot after the cell lock.

### Next steps

1. Let both pilot workers finish while monitoring process health, finite status, disk, and
   accumulated runtime; do not inspect OOD values.
2. Pull active pilot files only, validate provenance/completeness, and run the ID-only selector.
3. If necessary, execute the predeclared B/C overlap expansion; otherwise freeze the selected-cell
   lock immediately.
4. Deploy and run all 30 confirmation seeds, then adjudicate equivalence, attribution, rollout, M2,
   and contraction claims.

## Session 4 (live expansion checkpoint)

### Goal of session

Complete the frozen pilot, perform the outcome-blind ID-only cell-selection audit, and execute the
predeclared full-grid expansion wherever the simultaneous ID-and-compute overlap condition failed.

### What was done

- Both original pilot workers completed normally. V100a produced six 270-record files (1,620
  records; 1,296 trained arms; 1.208 GPU-hours). V100b produced four 120-record files (480
  records; 384 trained arms; 5.452 GPU-hours). All records were finite and all seeds were 100--102.
- Pulled the active JSONL files into `experiments/constraint_attribution_iclr/pilot/raw/`; retained
  the aborted B full-grid partial separately under `runtime_only_aborted/`. Local and remote file
  hashes matched.
- Ran only the frozen ID-error/compute selector. The provisional lock candidate was
  `pilot_lock_thin_20260830.json`, SHA-256
  `9ad88f746bc0fd8bb03d81720747cd7beafd588201ff394af54600e654054cb2`.
  `free_res`--`hard` matched in all ten systems. `free`--`free_res` matched only C-ad2d and M2, so
  the predeclared expansion trigger fired for A-advection, A-diffusion, A-burgers, B-advection,
  B-diffusion, B-burgers, H-near, and H-strong. A later runtime-only configuration audit found the
  B/C learning-rate inheritance error described below; this provisional lock is discarded and
  cannot launch confirmation.
- Froze the expansion decision before OOD inspection in
  `ecomd_constraint_attribution_iclr_pilot_expansion_decision_2026-08-30.md`, then queued the eight
  expansion jobs behind per-server `flock` locks. The PI explicitly authorized the final H-near
  command on `root@100.80.236.112`; it was launched as PID 1361582 with the exact frozen grid.

### Live status / findings

- V100a is actively running A-burgers; A-advection, A-diffusion, H-strong, and H-near are alive and
  waiting on the same GPU lock. Its latest A-burgers count was 464/720; all other queued V100a
  files remain at their initial 270/720 while waiting.
- V100b is actively running B-advection; B-burgers and B-diffusion are alive and waiting on its GPU
  lock. Its latest B-advection count was 149/330 raw records; the other B files remain at 120/330
  while waiting. The 330-record raw target includes preserved accidental `lr=0.003` records that
  are excluded from final selection.
- Latest GPU snapshots were 6%/420 MiB on V100a between short 1D training steps and 97%/5,616 MiB
  on V100b. Log tails contain completed run IDs and no observed traceback.
- OOD metrics remain unopened after the ID-only selection. Confirmation seeds 1000--1029 remain
  untouched.

### B/C learning-rate correction

- The ETA audit found that `pilot_manifest_bc_thin.yaml` omitted its `lr_grid` override. The
  executed B/C thin pilot therefore inherited `{0.001,0.003}` instead of the timing amendment's
  `{0.0003,0.001}`. This was detected from resolved configs and metadata, without reading OOD.
- Froze `ecomd_constraint_attribution_iclr_bc_lr_correction_2026-08-30.md`. Existing JSONL remains
  append-only. B's live expansion already supplies the correct pair. The PI explicitly authorized
  the missing C-ad2d cells; PID 171506 is queued on V100b with `{8,16}` × `{100,200}` ×
  `{0.0003,0.001}` and will skip completed `lr=0.001` IDs.
- Both B/C manifests now explicitly encode the frozen learning rates. The selector excludes
  B/C `lr=0.003` pilot records and reports exclusion counts in the final lock. Ruff, launcher
  dry-run, and the targeted suite pass; 20 tests passed.

### Open blocker

- None. All eight outcome-blind expansion jobs and the C correction are active or queued.

### Next steps

1. Monitor the expansion queues for process health, completeness, finiteness, and provenance; do
   not use the former hour estimates as stop conditions.
2. Pull all completed expansion/correction records and rerun the corrected ID-only selector. If C
   then lacks overlap, apply its already frozen omitted-cell expansion before the final lock.
3. Freeze the final cells before opening any OOD metric, then deploy and run the untouched 30-seed
   confirmation set and perform the frozen adjudication.

## Session 5 (2026-08-31 provenance gate)

### Completed compute and retrieval

- All A/H/B expansion jobs and the C learning-rate correction completed normally; both V100s
  returned to 0% utilization / 5 MiB. Final raw counts are A/H 720 each, M2 270, B 330 each, and C
  180: 5,040 append-only records / 4,032 trained arms. Recorded training runtime totals 30.534
  V100-hours before provenance replacement runs.
- Pulled a new immutable snapshot under `pilot/raw_expanded_20260831/`. All ten final local file
  SHA-256 values match their remote sources. An apparent empty-file mismatch was an SCP/hash race;
  the hashes matched after transfer completion, without retransmission or source mutation.
- Expansion and correction logs contain final completed run IDs and no traceback, Hydra error, or
  floating-point failure.

### Provenance gate and clean rerun

- The corrected selector stopped in `validate_provenance` before reading any pilot ID or OOD
  metric. Exactly 510 records lack `git_head`: 450 H-near expansion records and 60 C correction
  records. The other 4,530 records contain Git HEAD
  `86dd76ee0127c5eb7945a5806bdad74548c62459` and `git_dirty=true`.
- Source SHA-256 maps are complete and consistent, but the frozen rule says missing provenance
  must be rerun rather than patched. Froze
  `ecomd_constraint_attribution_iclr_provenance_rerun_2026-08-31.md`; no exception or attestation
  bypass was used.
- With explicit PI authorization, launched full clean H-near replacement PID 2281697 on V100a and
  corrected thin C-ad2d replacement PID 182376 on V100b. Outputs are separate
  `*_provenance_rerun.jsonl` files. Both commands set the exact Git HEAD and `ECOPHYS_DIRTY=1`.
  First-record checks passed before continuing: H 10 records and C 3 records at the check, with
  correct provenance.
- Made provenance capture fail before training if neither Git nor explicit environment variables
  provide HEAD or dirty state. Ruff passes and the targeted suite now has 23 passing tests.

### Next steps

1. Let both replacement files complete; validate 720 H-near and 120 C records, exact grids,
   finiteness, provenance, and local/remote hashes.
2. Build a clean selector input using the original snapshot with only H-near and C replaced. Keep
   the invalid originals immutable and excluded.
3. Run the corrected ID-only selector once. Expand C only if the corrected eligible grid triggers
   the frozen rule; otherwise freeze the final lock immediately.
4. Package a confirmation snapshot with fail-fast provenance, preflight one record, and launch the
   untouched 30-seed set without opening pilot OOD.

## Session 6 (2026-08-31 final lock and confirmation deployment gate)

### Completed replacements and selection

- Clean H-near and corrected C-ad2d provenance reruns completed at 720 and 120 records. Their
  local/remote SHA-256 values match exactly:
  `934a74662a480a8197775b2399e89f9a0dce7d9531c06ba6d6b8c51b8974215d` and
  `d0bf4646a85e5c41cda8b170df5f21481b82e5705ecb88abc34b1e7ea6db0f11`.
- Froze `final_selector_input_20260831.json`, SHA-256
  `d6e8fd774cfa37f5b24478dbcc583ced30af37938a916414b261d7e5be5b94b4`: ten explicit inputs,
  4,980 unique records, 4,800 eligible records, zero provenance failures. The old H/C files,
  provisional lock, and 180 B `lr=0.003` records are excluded without mutation.
- Ran the corrected ID-only selector exactly once. Final lock
  `pilot_lock_final_idonly_20260831.json` has SHA-256
  `5100ae5ca6f757a5166fba3692fd912e19e7a0c7a4da65980488e92a402c604e`, 30 untouched paired
  seeds, and 58 unique mechanism/cell jobs. C matched both primary comparisons and did not expand.

### Confirmation readiness

- Added cryptographic lock binding to the launcher, PDE/market runners, provenance, run identity,
  and confirmation analyzer. Missing or mismatched lock files fail before training; records from a
  different lock fail before analysis. Ruff passes and all 25 focused tests pass.
- Both worker dry-runs bind the exact final hash and produce 36 jobs on V100a plus 22 on V100b.
  Expected append-only output is 1,440 and 870 records, including free-arm projections.
- Read-only server check: both Tesla V100-SXM2-32GB GPUs are idle at 0% / 5 MiB with no experiment
  process. Pilot-derived training-only estimates are 3.3 hours on V100a and 24.6 hours on V100b;
  evaluation adds time and neither estimate is a stop condition.
- Built and clean-room verified the 29-entry, 41,426-byte confirmation archive at
  `experiments/constraint_attribution_iclr/deployment/constraint_iclr_confirmation_snapshot_20260831.tar.gz`.
  Archive SHA-256:
  `b70c000cdd1c70d676ce1846f74e66d147bcd8a03a0fc0a84564e8e0cbbb1305`; all 28 per-file checks
  pass after extraction.
- The archive has not been copied and confirmation has not been launched. Pilot OOD and all
  confirmation outcomes remain unopened.

### Next gate

Obtain explicit approval for this exact archive and the two destinations. Then verify archive and
per-file hashes remotely, run the focused tests, execute isolated seed-999 lock/provenance CUDA
preflights, and launch the frozen workers only if both gates pass.

### Approved deployment and current-machine-decision pause

- The PI approved the exact archive, both destinations, remote verification, seed-999 preflights,
  and conditional production launch. Both remote archives match
  `b70c000cdd1c70d676ce1846f74e66d147bcd8a03a0fc0a84564e8e0cbbb1305`; all 28 per-file hashes
  pass after extraction.
- The production Conda environment lacked pytest. Installed pytest 9.0.3 and its pure-Python
  dependencies only under each deployment's isolated `.testdeps`, leaving the training
  environment's installed packages unchanged. Both hosts then passed 25/25 tests.
- Both isolated seed-999 confirmation-mode CUDA preflights passed exact-lock, Git HEAD/dirty,
  V100 identity, nonzero peak-memory, source-hash, and recursive finite-number checks. These are
  outside seeds 1000--1029 and outside production outputs.
- The final production process creation encountered the repository `AGENTS.md` rule requiring a
  current machine decision. V100a was rejected before process creation. Because the two launches
  were submitted concurrently, V100b briefly started the first B-advection command; it was
  terminated by exact process group immediately after the asymmetry was detected. It wrote no
  JSONL record and created no production output directory. Its 579-byte command-only log is
  preserved. Both GPUs are idle again.
- Production remains paused until the PI makes an informed current machine decision covering the
  exact snapshot, lock, hosts, seeds, authorized outcome access, and forbidden adaptations.

### Current decision registered and production running

- The PI explicitly authorized an ICLR-specific current machine decision for only snapshot
  `b70c000cdd1c70d676ce1846f74e66d147bcd8a03a0fc0a84564e8e0cbbb1305`, final lock
  `5100ae5ca6f757a5166fba3692fd912e19e7a0c7a4da65980488e92a402c604e`, the two V100s, and seeds
  1000--1029. Grid/seed changes, early analysis, and outcome-driven stopping are forbidden.
- Registered the scoped, non-Nature-promotion decision at
  `research/discovery/decisions/constraint_attribution_iclr_confirmation_20260831.yaml`, SHA-256
  `9d6e1e67cc0055b719c1b904f266123e29b9b31f1f24008a58bafbc8ea49dace`, with current pointer SHA
  `fcb56fddbf6966ce92b918e6bd564b53acd3197f49994d2e00753f3651fbae37`. Local and both remote
  field/hash gates passed; the formal discovery validator remains green.
- Relaunched sequentially with decision and snapshot hashes in the process environments. V100a
  launcher PID 2568827 started at 10:46:00 +08; V100b PID 190373 at 10:46:43 +08. Both use the
  exact final lock and separate authorized logs.
- First production-record gates passed without displaying metric values. V100a passed on ten
  records from seeds 1000--1004; V100b passed on the first two seed-1000 free/projection records.
  Both passed unique run IDs, seed range, stage, lock binding, Git HEAD/dirty, V100 identity,
  source-lock hash, and recursive finite-number checks.
- At the 02:51:41 UTC checkpoint, launchers were alive, logs had no matching traceback/runtime/
  OOM/nonfinite error, V100a had 18 A-advection records and V100b had two B-advection records.
  GPU snapshots were 5% / 438 MiB and 98% / 4,624 MiB, respectively. No early analysis or metric
  inspection was performed. Launch receipt:
  `experiments/constraint_attribution_iclr/deployment/confirmation_launch_receipt_20260831.yaml`.
