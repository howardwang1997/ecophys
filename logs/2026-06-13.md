# 2026-06-13

## Session 1 — Compile all experiment results + Paper A writing plan + gap analysis

**Goal.** User: "整理实验所有结果，然后看看 paper A 现在应该怎么写，缺什么。" Settled positioning:
Paper A = NeurIPS/ICML (Paper B is the Nature Physics one); SOTA 5.96 is *our own* zumdn cell, not
a literature method; all prior experiments are fair game to support Paper A.

**What was done.**
- Surveyed the full corpus: ~120 experiment dirs. Found the three existing Paper-A docs
  (`outline.md`, `draft_sections.md`, `proposal/paper_a_neurips_2027_state.md`) are **all stale** —
  ICAIF/v2.1/"8-11 calibrated tool" era + the 2026-05-21 Track-A/B pivot; **none reflects the
  current diagnose→solve→δ≈0.5 spine.**
- Re-scored the major pillars on Mac to ground the compilation:
  - 092 5-asset ceiling **[v]**: best `xa_gold_zumdn` 5.96 (n=26); no cell >5.5 except gold zumdn.
  - 089 attribution matrix regenerated (Fig 1 specialist-mechanism source).
  - 109 tail-attack **[v]**: `normal_j00` (Gaussian, 0 jumps) still Hill≈1.30, mean 5.20 → tail
    overshoot is **dynamical not distributional** (the §4.2 key fact).
  - 110 MoE **[v]**: best 4.83, no ceiling break.
  - (frontier / concave / δ-grid already scored 2026-06-12.)
- Wrote **`papers/paper_a_methods/results_compilation_2026-06-13.md`** — the new source of truth:
  6-cluster experiment map (every claim → experiments + n + status), §-by-§ writing plan mapped to
  evidence, prioritized gap analysis, dead-claims list, doc-hygiene note.

**Findings.**
- The spine is real and well-supported by a large corpus (not a thin 3-experiment paper): §4
  diagnose (089/092/096/097 + frontier), §4.2 why (109), §5 negative controls (memk/B-β/B-α/MMD/
  MoE/SV all n.s.), §6 solve (113/114/118). Honest accept estimate ~25–35% executed cleanly.
- Two `score_summary` flags: 111 diffusion has no net-eval via the 11-fact path (scored only via
  the distributional scorer); 104 MMD shows 0 results on disk.

**Gaps (prioritized in the compilation).** P0: ABIDES daily fair re-run (105, n=1 intraday); render
hill(δ) + concave before/after figures; fix `score_concave_confirm.py:139` hardcoded "UNIVERSAL".
P1: frontier on >1 asset; non-linear hill(δ) fit for ndx (decides 4/5 vs 5/5); ABIDES+SBI leg (091).

**Next steps.**
- [ ] Decide P0 #1: run ABIDES daily re-run (handback) vs demote ABIDES to supporting point.
- [ ] Render the two solve figures from 114/118.
- [ ] Fix the hardcoded scorer line.
- [ ] Then start the §4 + §6 prose (reuse draft_sections §2/§3).

## Session 2 — tail-transfer derivation added + verified; paper-split decision

**Goal.** User: add the δ≈0.5 derivation to the paper; then list what experiments are still missing.

**What was done.**
- Wrote the **tail-transfer lemma** paper-ready: `papers/paper_a_methods/theory_tail_transfer.md`.
  `α = ζ_ED/δ` (change-of-variables on regularly-varying tails) ⇒ `δ* = ζ_ED/α = ζ_ED/3`; Gabaix
  ζ_ED≈3/2 ⇒ **δ*=1/2 (TLB √-law). δ≈0.5 is derived, not fitted.**
- **Verified the law on existing δ-grid** (`scripts/score_transfer_law.py`, no retrain): the
  parameter-free prediction `Hill·δ = ζ_ED = const` holds — Hill·δ ≈ 1.48/1.60/1.51/1.55/1.49
  (spx/ndx/gold/eurusd/btc), CV 1–9%, all ≈ Gabaix 1.5. Resolves the old low-r² worry (per-seed
  scatter; means are a clean 1/δ curve). **ndx δ*=0.53 now EXPLAINED by ζ_ED=1.60** → headline flips
  to "δ*=ζ_ED/3 for all 5; δ*=0.5 ⟺ ζ_ED=3/2." Unifies with exp 109 (dynamics make the ED tail;
  impact transfers it).
- Folded into `results_compilation_2026-06-13.md` (spine P3, §4.5 theory, derivation-aware gap list)
  and memory.

**Paper-structure decision (user).** User chose to **split** into a framework/systems paper +
findings paper. I gave the reviewer-2 case against (value is in the method↔finding loop; each half
weaker alone; salami risk; splitting promotes the weakest-standalone piece to its own headline) and
the case for (independent tool adoption / guaranteed pub / land-grab / disjoint audience). If
splitting: framework paper must be **repositioned off the refuted "first differentiable market sim"**
(prior art: Bouchaud-Cont 98, Dyer 23-25, GradABM, JAX-LOB) onto mechanism-decomposability +
attribution + gradient calibration, and target a prior-art-tolerant applied venue (ICAIF/JOSS/
workshop), NOT NeurIPS. Decision still being weighed by user (asked "why not recommend", "won't
combined be unfocused").

**Findings.** The derivation is the biggest single upgrade to Paper A's theory standing this week,
and it was nearly free (re-analysis of existing data). It converts the headline from empirical
("we found δ≈0.5") to derived+verified ("δ*=ζ_ED/3, and the model's ED tail is the Gabaix 3/2").

**Open / next.** P0: measure ζ_ED directly (ED-logging rollout) to close end-to-end; ABIDES daily
re-run vs demote; frontier on >1 asset. Then resolve the split decision and draft §4 prose.

## Session 3 — launch ABIDES CPU job on howard-pc (100.105.21.7) + resume hardening

**Goal.** User: run the CPU (ABIDES) leg directly on 100.105.21.7; later "run as-is" (~66h) with
robust breakpoint-resume.

**What happened.**
- 100.105.21.7 = **howard-pc**, the user's personal desktop (16 cores), NOT a configured node — no
  repo, no abides/ecophys envs. SSH from Mac works as `howardwang@` (key auth). Provisioned it.
- **Repo clone bloat**: a full clone hit 2.3G+ and 81k objects (committed result-JSON history) and
  stalled. Fixed with a **partial+sparse clone** (scripts/ + ecomd/ only) → 14M. Killed the runaway
  clone (reparented git children needed hard kill; pkill self-matched my own ssh shell — used a
  `[c]` bracket trick to avoid).
- **abides env**: runbook's "only numpy/pandas" was wrong — needed scipy + tqdm/matplotlib/seaborn/
  jsons/joblib/psutil/pprofile, and a **pandas-2.x patch** (`pandas.io.json.json_normalize` →
  `pandas.json_normalize`, 3 files). Baked all into `~/abides_bootstrap.sh` (idempotent).
- **Launched** `cpu_paperA_abides.sh all` (PARALLEL=14, N_SEEDS=250), detached.
- **Validated output** + measured: CSV = `timestamp,mid` × 23,401 rows/sim (per-second OB log).
  **~41 min/sim concurrent → daily ETA ~58h, +sbi ~8h ≈ ~66h.** Flagged the 1.5× overrun vs my
  20–40h quote + the free `book_freq` coarsen (→1–3h); user chose **run as-is**.
- **Resume hardening**: `~/abides_resume.sh` (skips done CSVs) + **@reboot cron** + pgrep/flock
  guards + self-clean when done. Verified it no-ops while the live run holds the workers.

**Findings / lessons.** (1) The repo's committed result JSONs make any fresh clone multi-GB —
always sparse/shallow. (2) Per-second OB logging dominates ABIDES cost; daily mode doesn't need it.
(3) `pkill -f` self-matches the issuing ssh command — bracket-trick the pattern. (Self-note: stop
putting parens in `bash -lc "echo ...(...)"` headers — broke several polls.)

**Open / next.** ABIDES daily+sbi running ~66h on howard-pc (auto-resume armed). Monitor:
`ssh howardwang@100.105.21.7 'ls ~/ecophys/experiments/105_abides_ceiling/abides_raw/*.csv | wc -l'`
(/1250). GPU ζ_ED job (feature/zeta-ed-gpu) still to launch on the 2-card box. Memory updated in
[[project_h20_fleet_scheduling]].
