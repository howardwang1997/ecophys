# 2026-06-18 — Session 2 (Mac): Phase 0 blast-radius + driven-transient design

> Session 1 (H20: env config + ζ_ED burn-in discovery) is on branch
> `diagnostics/zeta-ed-burnin-artifact` (`logs/2026-06-18.md`). This Mac session continues it
> from `feature/exp113-gabaix-solve`.

**Goal.** Open Phase 0 (decide whether the ζ_ED burn-in contamination is confined to the
tail-transfer derivation or reaches the standard 11-fact scoring) and write the driven-transient
experiment design that earns-or-kills the positive frontier story.

## What was done
- Fetched origin → one new branch `diagnostics/zeta-ed-burnin-artifact` (commit `99e9c0521`): the
  GPU ζ_ED leg refuted the ζ_ED≈1.5 validation (burn-in transient artifact). Read the finding doc,
  memory, log.
- **Phase 0 (Mac, no GPU) — code-path trace of the standard scoring:**
  - `ecomd/inference/run_large.py:107` `returns = traj.log_returns_np()[1:]` — full series, no
    warmup discard (`[1:]` only drops the t=0 diff).
  - `ecomd/physics/observables.py:61` `log_returns_np` returns the series verbatim.
  - → `compute_all` → `hill_tail_index` ⇒ **standard Hill is burn-in-inclusive**, same mechanism
    as the ζ_ED probe.
  - Aggregated the standard Hill across `experiments/114_concave_confirm` (n=30, 5 assets):
    baseline 1.23–1.38 (TOO-FAT) → concave_d045/d050 2.9–3.4 (IN-BAND). The 5-asset concave
    "solve" is exactly the contaminated cell; finding D1 shows in-band concave Hill (3.42) → 11.5
    (too-thin, FAIL) at drop=50.
  - Raw per-step series is NOT in `inference_merged.json` and 0 trajectory npz on Mac ⇒ exact
    per-cell magnitude can't be re-scored on Mac (= R1, GPU).

## Results / findings
- **Phase 0 verdict: blast radius = PROJECT-WIDE on `hill_tail_index`** (not confined to
  tail-transfer). Direction certain (drop warmup → Hill up / tails thinner); magnitude predicted
  (concave in-band → steady-state too-thin → FAIL). Doc:
  `papers/paper_a_methods/phase0_burnin_blastradius_2026-06-18.md`.
- Survives unchanged: lemma α=ζ/δ; Pareto ceiling *structure* (only strengthened); ABIDES daily
  (close-to-close, burn-in washed out). Contaminated: hill≈1.3 overshoot, concave solve, δ-grid
  Hill·δ, δ*≈0.5 (if Hill-criterion-based).
- Wrote `experiments/123_driven_transient/DESIGN.md` — pre-registered driven-transient experiment.
  Discriminates (P) non-equilibrium-physics vs (A) t=0-startup-artifact by shocking a steady-state
  system and testing whether the cube-law tail revives + relaxes with the SAME ζ_ED signature as
  the burn-in template. 5 pre-registered hypotheses (H1 control-light, H2 shock-revives, H3
  transient, H4 same-as-burn-in, H5 transfer-law-in-regime) + binding decision gate. Stage-1
  earns-or-kills run ≈ 31 h on 8 cards (spx, S1-news, 3 doses + no-shock control, n=30).
- `EcoMDSimulator.run(..., s_init=...)` accepts an init state but re-inits hidden states each call
  ⇒ design uses single-rollout-with-mid-shock (needs a `step()` shock hook), not run-to-run carry.

## Open questions / blockers
- Per-cell steady-state Hill magnitude (R1, GPU): does concave actually flip to too-thin at n=30?
- Non-tail facts (autocorr/acf2/leverage/dfa) are also computed burn-in-inclusive — expected small
  shift, unverified; cheap R1 add-on (all 11 facts at drop ∈ {0,50,200}).
- Plumbing before any GPU: raw pre-impact ED logging (P1), `step()` shock hook, `--windows`
  estimator mode, Mac smoke.

## Next steps
- Stage 0 plumbing (Mac): P1 raw-ED + shock hook + `--windows` mode + N≤500 smoke.
- Stage 1 (GPU, decision run): driven-transient spx S1-news ≈31 h → H1–H4 verdict.
- R1 piggyback: re-run 114 cells with `--save-trajectory` + warmup discard for Phase-0 magnitude.
- Branch hygiene: consolidate Phase-0 doc + exp-123 design onto `diagnostics/zeta-ed-burnin-artifact`
  (or merge it) so the burn-in investigation lives on one branch. Nothing committed this session.
