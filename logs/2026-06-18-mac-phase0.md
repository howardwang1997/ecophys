# 2026-06-18 — Session 2 (Mac): Phase 0 blast-radius + driven-transient design

> Session 1 (H20: env config + ζ_ED burn-in discovery) is on branch
> `diagnostics/zeta-ed-burnin-artifact` (`logs/2026-06-18.md`). This Mac session continues it
> from `feature/exp113-gabaix-solve`.

**Goal.** Open Phase 0 (decide whether the ζ_ED burn-in contamination is confined to the
tail-transfer derivation or reaches the standard 11-fact scoring) and write the driven-transient
experiment design that earns-or-kills the positive frontier story.

## What was done
- Fetched origin → one new branch `diagnostics/zeta-ed-burnin-artifact` (commit `99e9c0521`): the
  GPU ζ_ED leg refuted the ζ_ED≈1.5 validation (burn-in transient artifact). Read the finding doc,
  memory, log.
- **Phase 0 (Mac, no GPU) — code-path trace of the standard scoring:**
  - `ecomd/inference/run_large.py:107` `returns = traj.log_returns_np()[1:]` — full series, no
    warmup discard (`[1:]` only drops the t=0 diff).
  - `ecomd/physics/observables.py:61` `log_returns_np` returns the series verbatim.
  - → `compute_all` → `hill_tail_index` ⇒ **standard Hill is burn-in-inclusive**, same mechanism
    as the ζ_ED probe.
  - Aggregated the standard Hill across `experiments/114_concave_confirm` (n=30, 5 assets):
    baseline 1.23–1.38 (TOO-FAT) → concave_d045/d050 2.9–3.4 (IN-BAND). The 5-asset concave
    "solve" is exactly the contaminated cell; finding D1 shows in-band concave Hill (3.42) → 11.5
    (too-thin, FAIL) at drop=50.
  - Raw per-step series is NOT in `inference_merged.json` and 0 trajectory npz on Mac ⇒ exact
    per-cell magnitude can't be re-scored on Mac (= R1, GPU).

## Results / findings
- **Phase 0 verdict: blast radius = PROJECT-WIDE on `hill_tail_index`** (not confined to
  tail-transfer). Direction certain (drop warmup → Hill up / tails thinner); magnitude predicted
  (concave in-band → steady-state too-thin → FAIL). Doc:
  `papers/paper_a_methods/phase0_burnin_blastradius_2026-06-18.md`.
- Survives unchanged: lemma α=ζ/δ; Pareto ceiling *structure* (only strengthened); ABIDES daily
  (close-to-close, burn-in washed out). Contaminated: hill≈1.3 overshoot, concave solve, δ-grid
  Hill·δ, δ*≈0.5 (if Hill-criterion-based).
- Wrote `experiments/123_driven_transient/DESIGN.md` — pre-registered driven-transient experiment.
  Discriminates (P) non-equilibrium-physics vs (A) t=0-startup-artifact by shocking a steady-state
  system and testing whether the cube-law tail revives + relaxes with the SAME ζ_ED signature as
  the burn-in template. 5 pre-registered hypotheses (H1 control-light, H2 shock-revives, H3
  transient, H4 same-as-burn-in, H5 transfer-law-in-regime) + binding decision gate. Stage-1
  earns-or-kills run ≈ 31 h on 8 cards (spx, S1-news, 3 doses + no-shock control, n=30).
- `EcoMDSimulator.run(..., s_init=...)` accepts an init state but re-inits hidden states each call
  ⇒ design uses single-rollout-with-mid-shock (needs a `step()` shock hook), not run-to-run carry.

## Open questions / blockers
- Per-cell steady-state Hill magnitude (R1, GPU): does concave actually flip to too-thin at n=30?
- Non-tail facts (autocorr/acf2/leverage/dfa) are also computed burn-in-inclusive — expected small
  shift, unverified; cheap R1 add-on (all 11 facts at drop ∈ {0,50,200}).
- Plumbing before any GPU: raw pre-impact ED logging (P1), `step()` shock hook, `--windows`
  estimator mode, Mac smoke.

## Next steps
- Stage 0 plumbing (Mac): P1 raw-ED + shock hook + `--windows` mode + N≤500 smoke.
- Stage 1 (GPU, decision run): driven-transient spx S1-news ≈31 h → H1–H4 verdict.
- R1 piggyback: re-run 114 cells with `--save-trajectory` + warmup discard for Phase-0 magnitude.
- Branch hygiene: consolidate Phase-0 doc + exp-123 design onto `diagnostics/zeta-ed-burnin-artifact`
  (or merge it) so the burn-in investigation lives on one branch. Nothing committed this session.

## Session 3 — branch consolidation + exp 123 Stage-0 plumbing

**Consolidation:** merged `origin/diagnostics/zeta-ed-burnin-artifact` into `feature/exp113-gabaix-solve`
(clean, no overlapping edits) so one tip carries ABIDES + burn-in finding + Phase-0 + coherent memory;
pushed. Memory updated across 5 files (project_burnin_artifact +Phase-0/pivot/frontmatter,
project_paper_a_neurips_2027 new 06-18 section, project_pareto_ceiling caveat,
project_neural_sde_tournament caveat, MEMORY.md pointer+hooks). diagnostics branch now redundant
(fully contained).

**Stage-0 plumbing (DONE, validated, committed `ee9b1fd5a`):**
- P1 raw pre-impact ED logging: `ExcessDemandParams.log_raw_excess_demand` (default False = bit-exact);
  `price_formation.py` captures `excess_demand_raw` before `_concave_impact`; meta `ed_is_raw` stamped
  in `run()` + run_large npz.
- Shock hook: `EcoMDSimulator._shock_schedule` (default None = no-op) dispatched at top of `step()`;
  `_apply_shock` = `state_kick` (universal, primary H4 probe) + `news` (`_fundamental`).
- `score_transfer_law.py --windows`: sliding-window Hill α(t); reads `ed_is_raw`, marks shock step.
- Smoke (`scripts/smoke_exp123_plumbing.py`, all pass): P1 logging-only (returns bit-identical), shock
  no-op-by-default + no backward leak (first diff @ exactly K=300), estimator renders the burn-in dip
  **live** (α_ED 1.06 in the t≈0 window → 4.8 by t≈100) on an untrained sim — the Phase-0 finding
  reproduced by the new estimator. 66 price/sim/observable tests pass.

**Next:** config-family generator + `run_large --shock` wiring → pre-register → Stage-1 GPU
(spx, news/state_kick shock, 3 doses + control, n=30, ≈31h/8-card) = the earns-or-kills run.

## Session 4 (2026-06-19) — exp 123 Stage-1 EVAL landed: (P) PHYSICS

Ran the eval over SSH on h20-2 (100.91.139.56, shared NFS checkout /AI4S/Users/howardwang/h204/ecophys;
188 trajectories present). Fixed two bugs live: conda not on non-interactive PATH (prepend
/root/miniconda3/bin); verdict crashed on centers(76) vs alpha_ED(75) — ED series T vs ret T-1 window
count (aligned in load() + windows_mode).

**Result — the driven-transient earns-or-kills verdict:**
- spx windowed α_ED(t): control flat-light (burn-in 0.66 → steady [500,3000) **4.75** → stays ~4.8);
  kick3/6/12 **crater to ≈0.5 at the shock**, recover to **≈4.7 within ~1k steps**.
- **H1 PASS (control light 4.75) · revival YES (4.75→0.5) · H3 PASS (transient, recovers) · H4 PASS
  (post-shock 0.5 ≈ burn-in 0.66)**. ndx replicates (4.4→0.58→4.65) = **P**.
- spx = **P\*** — the only miss is dose-MONOTONICITY: the dip saturates (0.51/0.49/0.58 flat across
  mag 3/6/12). Owed: (1) sub-threshold dose sweep mag<3; (2) market-realistic shock (news/liquidity)
  vs the "state_kick is mechanical" skeptic.
- **(A) t=0-only startup-artifact reading is RULED OUT.** The heavy tail genuinely recurs under driving.
- R1 (warmup-discard, n=8000): baseline 3.87→6.55 (Δ+2.68, IN-BAND→TOO-THIN), concave 5.05→9.26
  (Δ+4.21). **Concave "solve" confirmed burn-in-held** (too-thin in steady state).

Verdicts/reports committed (033a36d87): verdict_{spx,ndx}.json + 6 windowed_hill_report.json +
r1_warmup_report.json. See [[burnin-artifact-zeta-ed-2026-06-18]].

**Next:** Stage-1.5 sub-threshold dose sweep (state_kick mag {0.3,0.5,1,2}) + a news/liquidity shock arm
→ clean dose-response + skeptic rebuttal → then Stage-2 multi-asset + the positive frontier paper.

## Session 5 (2026-06-19) — exp 123 Stage-1.5 + 2a + follow-up: (P) PHYSICS, 5/5 assets

Ran overnight on the 8-card box .14 (100.91.194.14, idle): Stage-1.5 spx sub-threshold dose sweep
+ Stage-2a multi-asset, then a follow-up (spx onset micro-sweep mag 0.05/0.1/0.2 + eurusd mag 6/12).
All detached, reuse checkpoints, auto eval+verdict+push. (One push rejected on divergence → fixed by
rebase on the box.)

**Result — the driven-transient is now fully characterized:**
- **spx full dose-response = clean SIGMOID** (mag 0.05→12): post-shock α_ED =
  4.24/3.01/1.88/1.45/1.08/0.77/0.58/0.51/0.49/0.58. Light below mag≈0.1 (no revival), onset 0.1–0.2,
  graded ramp, saturated floor ~0.5 above mag 3. Monotone 9/10 (floor-noise wiggle only).
- **5/5 assets revive (all P):** spx, ndx(0.58), gold(0.54), btcusdt(0.57), eurusd. **eurusd resolved**:
  A at mag3 (4.16) → P at mag12 (2.91→1.68). **Asset-dependent threshold ∝ intrinsic volatility**
  (equity/gold/crypto onset ~0.1–0.2; low-vol FX eurusd ~6–12; eurusd burn-in template also lighter, 1.04).
- H1/H3/H4 hold throughout; (A) startup-artifact ruled out across the full dose range + all assets.

Verdicts `verdict_{spx,ndx,gold,eurusd,btcusdt}.json`; claim updated in
`papers/paper_a_methods/claim_and_roadmap_2026-06-19.md`. Auto-loop STOPPED (inference legs done).

**Next (deliberate, code/data — not overnight inference):** Stage 2b market-realistic shock channel
(price-jump/liquidity, vs the "state_kick is mechanical" skeptic) → Stage 2d relaxation-τ (free from
existing data) → **Stage 3 REAL-DATA crash validation (decides NeurIPS vs Nature Physics).**
