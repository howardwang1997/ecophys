# 2026-08-21

## Session 1 (00:02)

### Goal

Complete the cross-midnight archival and verification work for the closed Morpho Public Allocator D0 v2 route.

### What was done

- Preserved the formal merged result byte-for-byte and archived exact copies of both aggregate remote chain
  artifacts in the repository.
- Added an independent immutable-result test covering canonical and file hashes, same-batch provenance, source
  and config binding, hidden CUDA, transport-failure status, absent support and the outcome-blind data contract.
- Changed only future merge behavior so missing support on either required chain produces pooled `null` rather
  than a misleading numeric zero; added regression coverage for both one-chain and two-chain missing support.
- Wrote the formal result report and updated Plan v4 plus short- and long-term project memory.

### Results / findings

- Final verification passed: 66 affected pytest cases, Ruff and strict mypy on the v2 runner are green;
  `git diff --check` is clean. The merged/Base/Ethereum file hashes independently match
  `6b261b78…a5c4`, `b183ef85…e973` and `8f90f2f…5313`.
- The formal result remains a transport failure with unknown empirical support. Its legacy pooled zeros are
  explicitly treated as unavailable-data sentinels and cannot be cited as observations.
- The route remains closed before D1, protocol values, market outcomes, EcoMD, GPU use, paid data, endpoint
  substitution or rerun.

### Open questions / blockers

- No implementation blocker remains. A new scientific route still requires problem selection and a fresh
  pre-registration; transport repair cannot retroactively reopen this frozen D0.

### Next steps

1. Commit and push the complete immutable evidence and documentation bundle.
2. Begin a fresh problem-selection audit rather than D1 on the closed route.

## Session 2 (01:25)

### Goal

Select a fresh, non-trivial real mechanism after the Morpho route closed, freeze a zero-cost feasibility test and
preserve the preregistration before formal data access.

### What was done

- Audited six candidate families against current literature, public-data transport and the NMI/NCS contribution
  boundary: CoW solver competition, ERCOT RTC+B, Ethereum fee controllers, Dependabot/agent-authored pull
  requests, shared oracle liquidations and dynamic AMM fees.
- Parked ERCOT RTC+B because its two live production tests are bundled, announced system-wide interventions with
  only two independent event clusters; no outcome rows were acquired.
- Verified outside the formal sample that CoW exposes official leave-one-winner reference scores and that public
  settlement events can enumerate competition transaction hashes without a Dune or vendor key.
- Created branch `problem-first-computational-liquidity-2026-08-21` from the immutable Morpho archive head.
- Wrote and froze the computational-liquidity T0 plan and config before querying the formal block interval.

### Results / findings

- The selected question is whether nominally numerous autonomous solvers remain functionally replaceable as
  task coupling increases. This is distinct from ordinary winner concentration only if the formal sample has
  sufficient multi-order overlap and usable official removal counterfactuals.
- Exploratory checks touched only auctions `13600000`, `13628466`, `13628554` and blocks near `25796557`; they
  are explicitly excluded. Formal T0 is frozen at Ethereum blocks `25780000--25780499`.
- No formal-window event, competition score, hypothesis association or GPU computation has been accessed.

### Open questions / blockers

- Multi-order submitted solutions may be too rare or may be removed by the platform's fairness filter. Either
  result can stop the original complexity route under the frozen decision tree.
- Blockscout is a public indexer rather than consensus; a later paper-level dataset needs independent receipt
  reconciliation.

### Next steps

1. Commit and push the preregistration before formal acquisition.
2. Implement tested acquisition, validation and criticality/coupling summaries.
3. Run T0 on the frozen interval and obey the GREEN/AMBER/RED decision without expanding the window.

## Session 3 (01:44)

### Goal

Implement, run and honestly close the preregistered CoW T0; archive data provenance and any software defect
without rewriting the frozen result.

### What was done

- Added typed settlement/competition validation, exact winner-removal arithmetic, submitted/eligible coupling
  support, immutable gzip source shards, a provenance manifest and GREEN/AMBER/RED evaluation.
- Added eight pre-run unit tests and passed them with Ruff and strict mypy before committing/pushing implementation
  SHA `3b1ce6f3f`.
- Ran the formal 500-block interval once from the clean pushed SHA. No endpoint, chain, window, definition or gate
  was changed.
- Audited the five reported duplicate conflicts post-run and traced all of them to reversed ordering of the
  semantically unordered `transactionHashes` array. Fixed only future semantic hashing and added regression
  coverage; the formal result remains byte-for-byte unchanged.
- Wrote the result report and post-CoW research/process topology.

### Results / findings

- Formal decision: **RED stop**. The interval has 132 unique settlements (required 200), 131 valid mappings,
  119 competitions (required 180), 12 low-criticality removals (required 20), 12 submitted-coupled competitions
  (required 30) and two eligible-coupled competitions (required 10).
- Passed support: mapping 99.24%, complete references 119/119, exact transaction membership and 132/132 valid
  official removal counterfactuals; 97 removals have criticality at least 1%.
- No association/hypothesis test, window expansion, GPU, EcoMD or paid data was used. Raw CoW responses moved
  127,045,560 bytes and compress to 54,013,690 bytes; their manifest is tracked while raw data remain local.
- Post-run semantic normalization makes all five duplicate groups agree, but removing that false gate does not
  change RED because four independent frozen support requirements still fail.
- Uploaded both gitignored raw gzip shards to
  `r2://ecophys/raw/cow_computational_liquidity_t0_v1/`; R2 HEAD verification matches local sizes and the tracked
  receipt retains keys, ETags and local SHA-256 values.

### Open questions / blockers

- No CoW T1 is authorized. The sample cannot establish whether coupling predicts winner criticality.
- ERCOT remains parked because two bundled system-wide tests are not enough independent causal clusters.
- The next problem is not yet selected. It must have dense repeated interventions or built-in counterfactuals
  and outcome-blind support calibration before another formal holdout.

### Next steps

1. Verify immutable result/config/manifest hashes and all affected tests.
2. Commit and push the formal negative result plus the future semantic-hash correction.
3. Resume literature/data search under the new D-1 support-calibration protocol; do not reopen CoW.

## Session 4 (02:13)

### Goal

Select the next problem under the post-CoW protocol without consuming another hypothesis holdout on event-rate or
schema uncertainty.

### What was done

- Audited the Dependabot/agent-PR literature and separated occupied bot-PR, alert-fatigue and aggregate CI-waste
  claims from a residual cross-work externality claim.
- Verified the new field intervention: GitHub changed the default version-update cooldown from zero to three days
  on 2026-07-14; security updates remain immediate and repositories can override the setting.
- Probed only identities, timestamps, counts, configuration presence, workflow schema and runner labels in an
  exploratory top-100 active/high-star frame. Timing outcomes, conclusions, review responses and merge outcomes
  remained unopened.
- Forked branch `dependabot-cooldown-verification-liquidity-2026-08-21` from the immutable CoW RED archive head.
- Wrote the problem/venue plan and froze the formal outcome-blind D-1 contract before querying its top-1,000 frame.
- Implemented typed repository/config/PR/run/job schemas, historical cooldown classification, arrival clustering,
  frozen support gates, a fail-closed outcome-key guard, authenticated GitHub transport, deterministic checkpoints
  and immutable allowed-record/result manifests.
- Ran three non-formal ten-repository smoke iterations. The final runtime uses the full frozen pre-period for the
  schema probe so that endpoint accessibility is not confused with absence of an event in one arbitrary week.
- Added an immutable first-acquisition frame checkpoint and append-only transport-hash journal after observing that
  live star counts can legitimately change between smoke snapshots; a resumed formal run now reuses the exact
  repository IDs, ranks and metadata from its first frame response.

### Results / findings

- Of 100 exploratory repositories, 34 had both current Dependabot configuration and Actions. Their calibration
  window contained 1,493 Dependabot PRs; 16 repositories had at least 20.
- Timestamp-only sampling produced conservative lower bounds of 326 one-hour, 285 six-hour and 195 24-hour
  within-repository arrival clusters. Public run/job schemas expose PR/head-SHA links and runner labels; a small
  probe included GitHub-hosted, custom and explicit self-hosted pools.
- The candidate is **verification liquidity**: whether an agent-side safety throttle changes scarce validation
  service for unrelated human work. A GitHub-only result is not NCS; NMI/NCS require transfer, prospective policy
  value, and for NCS a new guaranteed method plus a second independent domain.
- The final ten-repository smoke completed 49 public API requests in 21.25 seconds. It recovered ten deterministic
  records, 51 bot PRs and 20 24-hour clusters; outcome seal, config recovery, workflow access, queue-field presence
  and deterministic-hash checks passed. Scale gates correctly failed because smoke uses 10 rather than 1,000
  repositories. Allowed-record canonical SHA is `7d9af87a…c62b`; no sealed value was persisted.

### Open questions / blockers

- GitHub's announcement date may differ from a gradual backend rollout, and public metadata do not cleanly label
  security versus version-update PRs.
- Earliest job start is a queue proxy, not a direct scheduler timestamp; hidden orchestration and global hosted
  capacity require mechanism controls.
- Current star-ranked sampling is transparent but not a historical population sample; GH Archive replication is
  needed before broad generalization.

### Next steps

1. Commit and push the plan/config before formal-frame access.
2. Commit and push the tested collector/runtime implementation.
3. Run the frozen D-1 from a clean pushed SHA; stop before outcomes if any gate fails.

## Session 5 (03:18)

### Goal

Complete the frozen formal Dependabot cooldown D-1, independently verify the outcome seal and decide whether D0
is scientifically authorized.

### What was done

- Resumed the clean formal job without changing its frozen frame or any contract field; it completed all 1,000
  repository checkpoints and the deterministic 50-treated/50-control workflow/job schema probe.
- Recomputed the allowed gzip file hash, decoded-record canonical hash, result hash and manifest hash independently
  of the writer.
- Recursively scanned all 1,003 local raw/checkpoint artifacts and 4,573 transport observations for forbidden
  timing, conclusion, review, merge, duration, release-age and vulnerability keys.
- Added immutable formal-result regression coverage and wrote the formal result report; updated the candidate plan,
  Plan v4 and short-/long-term memory.

### Results / findings

- Formal decision: **GREEN to freeze D0 before opening queue outcomes**. All 11 gates passed: 1,000 frame repos,
  226 default-treated+Actions, 72 high-support treated, 447 eligible controls, 9,280 bot PRs and 2,499 24h clusters.
- Historical-config recovery is 97.44%; workflow-run access and required job-start field presence are both 100% in
  the frozen probe. Independent outcome scan found zero forbidden keys.
- Formal result canonical SHA is `e697028e…c37e`; allowed-record canonical/file SHAs are `ae7c9f36…b423` and
  `74dc95a2…2448`.
- The frame has no explicit zero-day opt-out. D0 cannot use a post-hoc opt-out design; it must match against the
  frozen no-Dependabot pool and use the 63 already-cooled repositories only as a negative control.
- This remains feasibility evidence only. No queue/review/merge/security outcome, GPU, EcoMD, paid data or H20 was
  used.

### Open questions / blockers

- D0 still must freeze the rollout blackout, exact retrospective/prospective windows, deterministic matching,
  root-job linkage and minimum effective clusters before any queue value can be opened.
- Earliest job start is a proxy for queueing, hosted-runner capacity is partly global, and the announcement may not
  equal an instantaneous backend rollout.

### Next steps

1. Verify the new regression suite and commit/push the immutable D-1 result bundle.
2. Derive and freeze an outcome-blind D0 candidate-ID ledger and analysis contract.
3. Only after the D0 freeze is pushed, implement the sealed D0 support collector; do not run D1 yet.

## Session 6 (03:33)

### Goal

Translate the D-1 GREEN decision into an exact, outcome-blind D0 candidate ledger and scientific contract before
any new sample acquisition.

### What was done

- Mechanically generated a tracked ledger from the immutable allowed-record artifact, with source/file/canonical
  hashes and exact IDs/covariates for treated, primary-control and already-cooled negative-control cohorts.
- Froze equal 28-day primary pre/post windows, a three-day rollout blackout, an untouched 56-day prospective
  interval, primary/secondary run definitions and deterministic per-repository-day sampling.
- Froze pre-period-only 1:3 optimal matching, balance criteria, workflow concurrency/environment flags, runner-label
  overlap, official incident sensitivity and GREEN/AMBER/RED support gates.
- Added contract tests binding candidate IDs, cohort hashes, symmetric windows, prospective embargo, matching and
  the D0 outcome seal.
- Updated the main candidate plan and project memory before any D0 GitHub sample acquisition.

### Results / findings

- Frozen candidates: 72 high-support default-treated, 447 no-Dependabot controls and 33 high-support
  already-cooled negative controls. The ledger canonical/file SHAs are `b6c21ed8…5c62` and `b5dc728c…cd82`.
- The primary human population is first-attempt user `push` workflows; PR/manual runs remain secondary. This avoids
  making the main result depend on GitHub's separate July-28 fork-workflow hold policy.
- Primary time windows are 2026-06-16--07-13 and 2026-07-17--08-13; July 14--16 is excluded for uncertain rollout
  timing. The prospective 2026-08-22--10-16 interval is sealed until 2026-10-17 UTC.
- Runner overlap is not treated as proof of internal GitHub scheduling. A failure of only that gate produces AMBER
  and requires a separately frozen reduced-form claim; other D0 failures are RED.
- No queue start value, first-stage contrast, review/merge/security outcome, GPU, paid data, EcoMD or H20 was used.

### Open questions / blockers

- D0 must show that at least 60 treated repositories can be balanced against 180 distinct controls and that enough
  pre/post primary run identities survive deterministic sampling without outcome-based selection.
- Workflow YAML at each run SHA may be unavailable for deleted histories; at least 90% structure recovery is
  required.
- The public API is the likely bottleneck; CPU/GPU capacity does not improve the shared authenticated rate limit.

### Next steps

1. Run contract, Ruff and strict-mypy checks, then commit/push the D0 freeze.
2. Implement fail-closed identity pagination, workflow parsing, schema probes, matching and immutable manifests.
3. Smoke-test on a deterministic subset, then run formal D0 from its clean pushed implementation SHA.

### Pre-acquisition amendment

Code-design review found that v1 did not specify how to select a matched subset if all 72 treated repositories
could not receive three controls. No D0 API call had occurred. V2 therefore supersedes v1 while keeping every
candidate ID, time window, estimand, forbidden field and numerical gate unchanged. It adds prequalification based
only on pre-period identity/schema data, deterministic feasible-control ordering, the largest complete matched
prefix rule and explicit schema denominators. V1 remains immutable as process history; v2 is authoritative.

### Outcome-seal smoke amendment

The first deterministic 11-repository D0 smoke completed all repository identity checkpoints in 1,250.8 seconds
but produced no final result or manifest. The fail-closed outcome guard rejected the external GitHub Status key
`started_at` because that generic name is also forbidden for sampled workflow/job outcomes. No queue value,
duration, conclusion, first-stage contrast, review, merge or security outcome was persisted or analysed. A direct
count-only transport check found 21,024 workflow-run identities for RSSHub in the fixed acquisition window, which
means formal D0 will require checkpointed progress across multiple API-rate windows rather than a smaller post-hoc
repository set. Contract v3 supersedes v2 solely by renaming external incident fields to
`reported_start_utc`/`reported_resolution_utc`; all candidate IDs, windows, estimand, matching rules, forbidden
sample outcomes, thresholds and gates are unchanged. Next: commit/push v3, make the collector resume the existing
smoke directory, and require the full sealed smoke to pass before formal D0.

## Session 7 (16:36)

### Goal

Complete and independently audit the outcome-blind D0 implementation, recover the failed smoke without reacquiring
its large identity frame, and make the collector safe to commit before formal execution.

### What was done

- Committed and pushed authoritative contract v3 as `da7b7eb0b`; its only scientific-contract change from v2 is
  the external incident field pair `reported_start_utc`/`reported_resolution_utc`.
- Added a fail-closed `--smoke-directory` resume mode restricted to an existing unfinished child of the configured
  `/tmp` smoke root. Reused all 11 immutable repository checkpoints and the append-only response-hash journal.
- Completed the v3 engineering smoke, then added strict-JSON enforcement after the empty-match path exposed a
  Python `Infinity` value.
- Audited implementation against the frozen D0 text and repaired the missing per-selected-run workflow step:
  provisionally selected primary runs are deduplicated by repository/head SHA/workflow path, historical structures
  are checkpointed, and missing or intentional-wait structures are excluded before exact D1 IDs are written.
- Added an outcome-blind necessary-gate stop before schema probes. Its prequalification upper bound counts every
  inaccessible or truncated repository as a possible pass, so it can stop only when a frozen gate is already
  impossible.
- Added regression coverage for renamed incident fields, resume path safety, strict JSON on empty matching,
  selected-run structure exclusion, checkpoint resumption and conservative identity-support upper bounds.

### Results / findings

- The resumed engineering smoke covered 46,862 workflow-run identities, 28 schema/workflow probes, 22 official
  Actions incidents and 692 journaled responses. The outcome seal passed and no queue, duration, conclusion,
  first-stage, review, merge or security value was persisted or analysed.
- The deterministic 3/6/2 smoke subset has two prequalified treated repositories and zero prequalified controls.
  This is not a formal D0 gate because the smoke is not the frozen 72/447/33 cohort.
- The smoke's empty-match result is a superseded engineering trace because Python emitted non-standard `Infinity`.
  Current writers fail on non-finite values and encode unavailable balance metrics as JSON `null`.
- All 34 verification-liquidity tests pass. Ruff, strict mypy, bytecode compilation and `git diff --check` pass.

### Open questions / blockers

- Formal D0 still has to enumerate all 552 candidates. The smoke's six inactive controls raise a real feasibility
  concern, but only the frozen full-frame necessary-support upper bound may decide it.
- If identity prerequisites pass, schema probes and per-selected-run historical workflow acquisition will require
  additional GitHub API windows. GPU capacity does not shorten this API-bound stage.

### Next steps

1. Commit and push the verified collector/runtime/tests/docs.
2. Start formal D0 from that clean pushed SHA and resume only through its immutable checkpoints after rate resets.
3. If a frozen necessary gate fails, write the RED artifact and stop before D1 outcomes, GPU, EcoMD or paid data.

## Session 8 (19:09)

### Goal

Start formal D0 from the clean implementation SHA, preserve progress through transport failure, and make resumption
robust without altering the frozen scientific design.

### What was done

- Started formal outcome-blind D0 from clean pushed `c98b4ed86` at 04:41 UTC.
- Monitored whole-repository checkpoints, append-only response hashes and rate metadata while keeping the worktree
  clean; no result field or D1 outcome endpoint was accessed.
- Diagnosed the failed attempt: a `rust-lang/rust` time shard encountered three consecutive SSL EOFs. The eager
  executor had submitted all 552 tasks, so shutdown continued requests after the main result loop stopped.
- Froze runtime v2 as a transport-only supersession of runtime v1. It records the exact predecessor hash and failed
  execution history, limits all acquisition stages to four in-flight tasks, signals peer cancellation after an
  error, persists peer successes, and adds immutable ≤900-run time-shard checkpoints.
- Added shard round-trip/source/hash tests and a concurrency regression proving that no task beyond the bounded
  initial set starts after a synthetic peer failure.
- Ran two checkpoint-reuse engineering smokes. The first failed on a zero-sample JSONL encoded as one blank line;
  it generated no result/manifest. Fixed the writer to produce a true zero-line gzip file and added immutability and
  round-trip coverage. The second completed end to end and passed an independent recursive outcome-seal scan.

### Results / findings

- Formal progress safely preserved: 9 complete repository checkpoints and 3,095 journaled HTTP-200 response hashes.
  No formal result or manifest exists, and all queue/first-stage/review/merge/security outcomes remain sealed.
- Runtime v2 smoke: 29.0 seconds, 43 status responses, 22 incidents, strict `null` balance metrics and a zero-row
  selected-sample gzip. Result canonical/file SHAs are `b097af1f…55ef`/`ce81d11d…8e67`; this is engineering only.
- Runtime v2 SHA before its implementation commit is `e377aa43…1f16`.
- The full related suite is 38/38 passing; Ruff, strict mypy, bytecode compilation, strict JSON parsing and seal scan
  pass.

### Open questions / blockers

- The 543 incomplete repositories must be reacquired. The first nine whole-repository checkpoints are reusable;
  the earlier 3,095 responses lack shard payload checkpoints and therefore remain journal evidence, not identities.
- Very high-activity repositories can still span multiple rate windows, but v2 loses at most one incomplete leaf
  (≤900 identities) per worker on a terminal transport failure rather than an entire repository or executor queue.

### Next steps

1. Commit/push runtime v2, shard/bounded-executor implementation, tests and documentation.
2. Resume formal D0 from the new clean SHA with the same raw directory and immutable journal.
3. Continue checkpointed resumes until the conservative identity gate yields a formal GREEN/AMBER/RED decision.
