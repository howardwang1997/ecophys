# 2026-08-16

## Session 1 (00:22)

### Goal

Freeze an outcome-blind replacement-source decision for V14 M3/M4 after Uniswap U1R failed, then prepare the best
free zero-row feasibility audit.

### What was done

- Compared Compound III, Aave V3, GC0166, Uniswap Proposal 94, CoW and AEMO FTA under twelve common criteria and
  five mandatory kill switches.
- Inspected only official Compound/Aave documentation and official repository metadata. Pinned Comet source commit
  `f766f51583c23acc33b2a7824654ef2029a96804`; disclosed that repository tree, licence/README, the mainnet USDC
  configuration/roots pair and required source markers were seen before protocol freeze.
- Added strict machine-readable selection and Compound metadata-preflight manifests, validators, deterministic
  collector, focused tests, preregistration, runbook, proposal plan and compute audit updates.
- Kept chain RPC, governance payloads, accounts, participant actions, realized responses, paid data, remote workers
  and all GPUs closed.

### Results / findings

- Compound is the first metadata-audit route, not an admitted event: five criteria pass, five are partial and two
  unresolved. Exposure denominator, outcome-blind controls and licence/retention remain non-pass kill switches.
- Its useful statistical object is an on-chain account position. The observable null is no successful adjustment,
  not no intent; manager, Bulker, sender, account and beneficial person cannot be conflated.
- Aave is the predetermined fallback because its public payload/address ecosystem is strong but state
  reconstruction is more complex. GC0166 remains conditional on official provenance/identity clarification.
- Nine focused tests pass; Ruff and strict mypy pass for both new research modules.

### Open questions / blockers

- Do all six pinned mainnet markets expose a complete internally consistent minimum source surface?
- Can later chain metadata prove authoritative deployments and archive completeness without opening account rows?
- Can a future single-market parameter change supply outcome-blind controls with overlap and adequate lead time?
- What exact raw/derived chain-data retention terms are needed beyond the source-code licence record?

### Next steps

1. Commit and push the scorecard and frozen source-metadata preflight before executing it.
2. Run once from the pushed commit against the clean exact official checkout and preserve pass/failure.
3. If it passes, freeze a separate chain-metadata-only conformance protocol; keep account/action/response and GPU
   access locked.

## Session 2 (00:28)

### Goal

Execute the pushed Compound source-metadata preflight once from a clean detached worktree.

### What was done

- Pushed protocol commit `aa501def3` and created a clean detached worktree at that exact commit.
- Verified the external official Comet checkout at the frozen commit and remote with empty Git status.
- Ran nine focused tests successfully.
- Recorded the sparse-materialization setup miss and then the frozen wrapper's immediate package-import failure.
- Added an immutable v1 result and a v2 invocation-only repair; did not alter the collector, manifest, source,
  markets, gates or access boundary.

### Results / findings

- V1 decision is `INFRASTRUCTURE_FAILURE_NO_SOURCE_AUDIT`.
- The direct script failed at its first import with `ModuleNotFoundError` before argument parsing, manifest loading,
  Git inspection or reading a Compound source file. No artifact was created.
- No scientific gate was evaluated. Network/RPC, governance, account, action, response, paid-data, remote-worker
  and GPU use all remained zero.
- The minimal repair is package-module execution from the repository root.

### Open questions / blockers

- Will the byte-identical collector and manifest execute cleanly through the package-module entry point?
- If it runs, do all nine source-conformance gates pass without market or marker repair?

### Next steps

1. Commit and push v1 failure plus the sealed v2 invocation repair.
2. Execute v2 exactly once from a new clean detached worktree.
3. Preserve its result and keep chain/account/action/response/GPU access locked regardless of source result.

## Session 3 (00:32)

### Goal

Execute and independently verify the sealed Compound source-metadata v2 repair.

### What was done

- Committed and pushed v2 protocol commit `0245e6ebcd11e263e13bdeb98ff2d66cb1498b6c`.
- Created a new clean detached worktree, materialized the committed experiment path without a Git diff and
  independently rechecked the clean official source remote/commit.
- Ran the package-module collector exactly once, copied only its summary artifact to the active branch and
  independently verified all 16 retained source-file hashes.
- Inspected all gate decisions, six market records, unique roots, source provenance and access flags.

### Results / findings

- Decision: `PASS_SOURCE_METADATA_AUTHORIZE_CHAIN_METADATA_ONLY`; 9/9 gates pass.
- Six markets have six unique Comet roots, 22 collateral configurations and 56 migration filenames. Every frozen
  action/state/Configurator/licence marker is present.
- The six root files share one Configurator address. This supports a common mechanism surface but raises bundled-
  governance and spillover risk for any cross-market control design.
- Artifact size is 11,318 bytes; SHA-256 is
  `643edb6750ab5567528a9754bdf92b84143deaa82a9225a9c8ea9730e0d81e11`.
- Repository metadata are not proof of deployed/current state or execution. G1 remains unpassed; no account,
  action, price, response, paid data, remote worker or GPU was used.

### Open questions / blockers

- Do deployed proxy/implementation/configuration states conform to the pinned source for all six markets?
- Which free archive route can prove code, storage and governance clocks with explicit finality and retention?
- Can a governance change affect one market without bundling plausible controls through the shared Configurator?
- Can raw and derived chain metadata be retained and released under acceptable provider terms?

### Next steps

1. Commit and push the immutable v2 result.
2. Freeze a chain-deployment/archive metadata-only audit before the first RPC call.
3. Keep account mappings, participant actions, prices, liquidations, responses and all model/GPU work locked.

## Session 4 (00:47)

### Goal

Freeze the first chain-deployment/archive metadata gate after the official Compound source audit passed.

### What was done

- Reduced the permitted chain surface to six source-pinned Comet proxies, one shared Configurator, exact ERC-1967
  slots, four non-account getters and two block headers.
- Calculated and froze getter selectors locally with OpenSSL Keccak-256 before any RPC request.
- Selected fixed block 17,000,000 and the two oldest source-listed mainnet markets for a bounded archive test.
- Added a strict manifest, capped hash-only collector, pure gate evaluator, five focused tests, preregistration,
  runbook and proposal/compute updates.
- Explicitly prohibited accounts, arbitrary storage, logs, transactions, receipts, totals, prices, liquidations,
  responses, paid endpoints, remote workers and GPUs.

### Results / findings

- The exact request count is 61: 24 `eth_call`, one chain ID, two block headers, 18 code and 16 storage queries.
- All twelve gates are conjunctive. A pass can authorize exposure/control protocol design only; it cannot authorize
  account or action data.
- Fifteen combined source/chain-selection tests pass; Ruff and strict mypy pass for all three new modules.
- Expected work is about 31 seconds, under 8 MiB and zero GPU-hours.

### Open questions / blockers

- Does the public endpoint support `finalized` and ERC-1967 historical storage at block 17,000,000?
- Do source-pinned immutable getter values match every current implementation?
- Are historical proxy code and implementation state coherent for both oldest markets?

### Next steps

1. Commit and push this RPC protocol before the first chain request.
2. Run once from a clean detached worktree and preserve transport/scientific failure without endpoint repair.
3. Keep G1, accounts, actions, responses and GPUs locked after either decision.

## Session 5 (01:03)

### Goal

Execute the sealed Compound chain-metadata v1 once, preserve its decision correctly, and freeze a narrowly scoped
transport repair before any further RPC access.

### What was done

- Ran v1 from the clean detached pushed protocol commit after its combined test suite passed.
- Archived the exact endpoint failure in an immutable `RESULTS_V1.md`; confirmed neither output artifact exists.
- Added a v2 manifest and preregistration that retain the endpoint, six markets, four getters, Configurator,
  historical block/pair, request/byte caps and every account/action/response/GPU prohibition.
- Extended the collector and gate evaluator with explicit versioned contracts so v1 remains reproducible while v2
  reads latest head, derives `head - 64`, reads that numeric header and pins subsequent current-state calls to it.
- Added regression coverage for both schema versions, exact call vectors and rejection of an observed 63-block
  head/snapshot gap.

### Results / findings

- V1 made one successful `eth_chainId` call. Its next logical call returned `Invalid block number` for the
  `finalized` tag on all three allowed attempts, so no contract, storage, getter or historical query ran.
- This is an endpoint-capability failure, not a pass/fail on the twelve metadata gates. No artifact, paid data,
  remote-worker work or GPU work was produced.
- V2 has exactly 62 successful calls: 24 `eth_call`, one chain ID, three block headers, 18 code and 16 storage.
- A 64-block confirmation-depth snapshot is explicitly not consensus-finalized; actual event work still needs
  separate finality and provider-replication protocols.
- Sixteen combined tests and Ruff pass; strict mypy covers the versioned collector before protocol freeze.

### Open questions / blockers

- Does the endpoint return coherent `latest`, explicit `head - 64` and historical block-17,000,000 headers?
- Do all current source-pinned getters, proxy slots and implementation codes pass at that explicit snapshot?
- Does the endpoint expose coherent historical code/storage for mainnet USDC and WETH?

### Next steps

1. Commit and push the immutable v1 result and sealed v2 protocol.
2. Run v2 exactly once from a clean detached worktree and independently verify any artifacts.
3. Keep G1, account/action/response access and all GPU work locked after either decision.

## Session 6 (01:07)

### Goal

Execute and independently verify the pushed Compound chain-metadata v2 protocol.

### What was done

- Created a new clean detached worktree at the exact pushed v2 protocol commit and materialized only required
  sparse paths.
- Re-ran all 16 related tests, confirmed the source artifact hash and both v2 output paths were absent, then made
  the single sealed network execution.
- Independently checked manifest/source hashes, collection commit, all request indexes/labels/method counts,
  one-attempt status, block gap/times, six getter tuples, archive/current proxy relations, gate set and artifact
  hashes without re-querying the endpoint.
- Added the immutable result and updated proposal, compute audit and long-term memory.

### Results / findings

- Decision: `PASS_CHAIN_METADATA_AUTHORIZE_EXPOSURE_PROTOCOL_DESIGN_ONLY`; all 12/12 conjunctive gates passed.
- Exactly 62/62 calls succeeded once in 36.18 seconds, transferring 418,232 bytes. Head 25,760,636 minus snapshot
  25,760,572 equals the frozen depth 64; this remains provider-confirmed, not consensus-finalized.
- All six proxies match source-frozen getters. Asset counts are 13, 10, 16, 4, 17 and 7.
- Proxy code is shared, but six current implementations/hashes are distinct. All six market admins and the
  Configurator share one admin address, so cross-market controls remain unproved.
- Fixed block 17,000,000 returned coherent USDC/WETH proxy and historical implementation state across upgrades.
- Summary SHA is `14f5b044d8c51e4323419b901a7f037c84c4af45083464e0daaefc561256c944`; response-hash ledger SHA is
  `68f7b21b10e770f0b4695231e0c142a3843d095351ac8a43068726f1a56a54c5`.
- No paid data, remote worker or GPU was used. `G1` and all participant/model access remain locked.

### Open questions / blockers

- Can a pre-event account denominator be enumerated without selecting on post-event activity?
- Can governance payload/event timing isolate a market-specific change despite shared admin/Configurator authority?
- What external control family survives spillover, support and parallel-trend diagnostics outcome-blindly?
- Which provider/licence terms permit retained derived account panels, and how will consensus finality be replicated?

### Next steps

1. Commit and push the verified v2 artifacts and result record.
2. Write—but do not execute—a zero-row exposure/control protocol with hard kill switches.
3. Keep accounts, actions, responses and all GPU queues locked until that protocol is committed and reviewed.

## Session 7 (01:25)

### Goal

Turn the passing chain audit into a rigorous zero-row exposure/control protocol without opening participant data.

### What was done

- Inspected the pinned current Comet storage/accounting, action interfaces, extended implementation, manager logic,
  Configurator and ProxyAdmin source. A broad source search incidentally exposed migration filenames and
  `ProposalCreated` marker lines only; no governance action/payload values were decoded.
- Derived and implemented the exact aggregate-minus-enumerated exposure residual identity.
- Defined the account unit and seven separate identity layers, exact state-owner event roles, successful-action
  null, liquidation competing risk and four response horizons.
- Froze clean event classes and the proxy `Upgraded` operative clock, plus atomic Configurator/CometDeployed/
  Upgraded linkage and multi-market/bundling exclusions.
- Froze four control families, shared-authority spillover rules, overlap/ESS/concentration thresholds, consensus
  finality replication, keyed address pseudonymization and provider-terms gates.
- Added a strict machine manifest, validator/evidence checker, five tests and a staged D0--D5 design/topology.
- Reviewed current official Ethereum, Compound and Blockscout documentation. Per-instance Blockscout access is a
  bounded feasibility source, not a durable sole dependency; terms/redistribution remain legally unresolved.

### Results / findings

- The denominator has an exact falsification certificate: positive principal, negative-principal magnitude and
  every collateral balance must reproduce aggregate totals at the same block with zero tolerance.
- The certificate can prove position-set completeness conditional on same-block reads and implementation/accounting
  conformance; it does not prove identity, intent or action completeness.
- Source inspection exposes a harder M3 blocker: some successful base transfers can change debt without a complete
  state-owner event. Full action semantics need successful call traces and state reconciliation.
- The operative parameter-change clock is proxy `Upgraded`, not proposal creation or Configurator setter time.
- Twenty-one relevant tests, Ruff and strict mypy pass. The design uses no chain data, paid source, worker or GPU.

### Open questions / blockers

- Does the historical governance inventory contain any atomic single-market/single-existing-asset clean event?
- Can a free, terms-compatible source provide complete historical successful internal call traces?
- Will the exact residual certificate pass at pre-event snapshots for target and candidate control markets?
- Can any control family pass spillover, SMD, ESS and concentration gates under shared administration?

### Next steps

1. Commit and push the zero-row design before any new RPC query.
2. Freeze D0 governance/configuration metadata inventory with exact calls, topics, dates, caps and provider terms.
3. Run D0 only if its pushed protocol preserves all account/action/response locks; keep GPUs idle.

## Session 8 (04:19)

### Goal

Freeze the first executable Compound D0 governance/configuration inventory without opening participant data.

### What was done

- Fixed an inclusive block window, end hash, seven addresses, five event ABIs/topics and 48 root intervals per
  address, with exact recursive saturation semantics.
- Implemented a capped two-provider JSON-RPC collector with strict ABI decoding, raw-body disposal, response
  hashes, duplicate/conflict detection and an immutable clean-commit CLI.
- Defined the conservative same-transaction Configurator/CometDeployed/Upgraded provisional-candidate rule.
- Added manifest validation, parent-hash checks, unit tests, preregistration/runbook and updated the NCS plan,
  source scorecard, compute audit and long-term memory.

### Results / findings

- Expected unsaturated work is 339 successful calls: three chain/header calls plus 336 root log queries.
- D0 is metadata screening only. It cannot validate governance payload isolation, call-path spillovers, existing-
  collateral conformance, consensus finality, controls, exposure completeness or any response.
- Hard caps are 2,000 attempts, 256 MiB and 100,000 normalized rows at two requests per second.
- Thirty relevant tests, Ruff, formatter check and strict mypy pass; all five topics independently reproduce from
  the source-declared event signatures with legacy Keccak-256.
- No paid data, remote worker or GPU is needed; both V100s and the RTX 2060 remain idle.

### Open questions / blockers

- Does the fixed window contain any event satisfying the exact atomic log triple without bundled market changes?
- Will the public endpoints return every root partition below the documented 1,000-log limit or cleanly bisect?
- If a candidate exists, do receipt, calldata and the complete governance call path preserve its isolation?

### Next steps

1. Commit and push the tested frozen D0 protocol.
2. Run it once from a clean detached worktree at that exact commit and independently verify all artifacts.
3. Preserve either result; do not open account/action/response rows or queue a GPU job.

## Session 9 (04:45)

### Goal

Execute and independently verify the immutable Compound D0 governance-log inventory.

### What was done

- Committed and pushed the sealed protocol at `db26bbe91111b6a71f6a4083231927574dd2b188`, then created a clean
  detached worktree at that exact remote-confirmed commit.
- The inherited sparse-checkout initially omitted two committed parent-artifact directories, producing three
  file-missing test failures. Materialized only those fixed paths; the worktree remained clean and 30/30 tests
  passed before any network access.
- Executed the collector once, copied all three immutable artifacts and ran an independent offline verifier that
  did not import the collector.
- Recomputed request hashes/order, seven exact interval grids, byte/method/provider counts, unique normalized log
  identities, event counts and all 184 candidate check vectors.
- Mapped the two top-priority asset addresses only through the pinned official deployment source; an `rg` also
  displayed already-authorized migration filenames identifying wstETH/rsETH, with no payload or response access.

### Results / findings

- Decision: `PASS_GOVERNANCE_LOG_INVENTORY_AUTHORIZE_RECEIPT_PAYLOAD_PREFLIGHT_ONLY`; all 10/10 gates passed.
- Exactly 339/339 requests succeeded on the first attempt in about 188.9 seconds, transferring 605,863 bytes.
  There were 886 normalized logs, zero saturation, zero duplicates and zero conflicts.
- Of 184 eligible updates, 14 at distinct blocks pass the provisional log rule: 12 supply-cap and two borrow-
  collateral-factor changes. There are no qualifying liquidation-factor changes.
- Bundling is material: 170 eligible rows have another Configurator log, 167 another eligible update and 126
  another frozen-market upgrade; these overlapping failures validate the shared-authority warning.
- Frozen priority puts mainnet-USDS wstETH `0.82 -> 0.80` at block 22,273,296 and mainnet-WETH rsETH
  `0 -> 0.80` at block 25,571,051 first. Neither is selected or causally valid yet.
- Summary/inventory/response-ledger SHA values are respectively
  `e03161118ac4cd7aea56f92f40aabdd49348d3170ce256ff42896291c1470b7f`,
  `0435a55b652f5b6a6419f22b296b72184aed25c0073a132c232de55f741fd9e2` and
  `85e41d06b3db59b8684ff33c113146dd722c45b7bea7439958ad59f085f5ea5f`.
- No paid data, remote worker or GPU was used. `G1` and participant/model access remain locked.

### Open questions / blockers

- Which of the 14 candidates survive full receipt/calldata/call-path spillover and exact pre/post getters?
- Does each candidate have a clean 24-hour governance neighborhood and reproducible consensus-finality evidence?
- Can any surviving candidate later pass exact exposure residuals and outcome-blind control support?

### Next steps

1. Commit and push the immutable D0 result and updated documentation.
2. Freeze one receipt/payload/getter/finality preflight over all 14 candidates before any new chain query.
3. Keep D1 account census, action/response rows and GPU jobs locked after either decision.

## Session 10 (05:26)

### Goal

Freeze the all-candidate receipt/payload/trace/getter/finality preflight before any further chain access.

### What was done

- Re-derived all 14 D0 candidates in the pre-existing event priority, then block/transaction order; no outcome or
  participant data entered ordering.
- Derived exact 24-hour neighbor blocks from the complete D0 inventory using the post-Merge 7,200-block bound.
- Reviewed pinned Configurator, CometProxyAdmin and proxy source signatures and independently recomputed nine
  function selectors with legacy Keccak-256.
- Specified a stateful call-cone isolation rule around one exact setter, Configurator deploy, proxy-admin deploy/
  upgrade and target-proxy upgrade; successful stateful siblings fail the candidate.
- Implemented strict transaction/receipt/log, callTracer, ERC-1967 slot/code, eight-word AssetInfo and finality
  normalization with global request/byte/node/input/log caps.
- Added the machine manifest, collector, ten synthetic/parent tests, preregistration/runbook and updated plan,
  scorecard, compute audit, exposure design and long-term memory.
- Tightened the pre-freeze implementation after reviewer-style reconciliation: receipt logs now re-decode and
  exactly compare all retained indexed/value fields; every log must match its receipt identity; required mechanism
  calls must be real `CALL`s with deploy/upgrade under the same proxy-admin call; and the collector checks the full
  ordered 188-operation parameter plan rather than totals alone.
- Consulted the official Execution and Beacon API specifications and current PublicNode/Blockscout documentation;
  no candidate transaction, receipt, getter or trace was queried.

### Results / findings

- Exact no-retry plan: 187 JSON-RPC calls plus one Beacon request. Provider counts are 156 Blockscout, 31
  PublicNode execution and one PublicNode Beacon.
- All 14 candidates must have complete transport/trace coverage; candidate-specific mechanics, getter and
  contamination failures are retained, with at least one full survivor required.
- The finality gate is stronger than 64-block confirmation but remains provider-reported: the code does not verify
  Beacon BLS signatures or Merkle branches locally.
- Thirty-five related tests pass; Ruff, formatter check, strict mypy and `git diff --check` all pass.
- A repository-wide quiet pytest run produced no buffered result within roughly four minutes and was interrupted;
  it supplies neither a pass nor a failure. The bounded Compound test stack above is the protocol freeze gate.
- This remains CPU/network-only. Both V100s and the RTX 2060 are idle; participant/model access stays locked.

### Open questions / blockers

- Does the free PublicNode endpoint support complete historical `debug_traceTransaction` callTracer responses?
- Do all historical Blockscout T-1/T asset getters and ERC-1967 reads remain archive-complete?
- How many candidates survive sibling payload calls, exact getter diffs and 24-hour contamination together?

### Next steps

1. Finish formatting/static checks, commit and push D0b before any capability probe.
2. Run once from a clean exact worktree; preserve endpoint failure or scientific candidate filters unchanged.
3. Do not design or run D1 acquisition until D0b passes and its immutable result is pushed.

## Session 11 (05:56)

### Goal

Freeze, push and execute the all-candidate Compound D0b v1 mechanics preflight exactly once.

### What was done

- Strengthened the pre-freeze protocol so receipt logs re-decode all retained indexed/value fields, required calls
  are real `CALL`s with proxy-admin ancestry/caller checks, and all 188 operation labels/providers/methods/params
  are checked in order.
- Reproduced all parent hashes and the exact 14-row derivation; 35 related tests, Ruff, formatter, strict mypy and
  diff checks passed. A repository-wide quiet pytest run did not return a buffered result within roughly four
  minutes and was interrupted, so it supplies neither a pass nor a failure.
- Committed and pushed protocol commit `2ee8a87442b6e5a912354362bddcef572876eb59`, confirmed the remote ref, and
  prepared a clean detached worktree at that exact commit.
- Repeated the bounded tests and parent validation, confirmed all three output paths absent, and ran the sealed
  command once without an endpoint substitution or capability probe.

### Results / findings

- D0b v1 stopped after about 15.2 seconds at the first candidate's `debug_traceTransaction`. PublicNode returned
  JSON-RPC `-32601`, `the method debug_traceTransaction does not exist/is not available`, on all three attempts.
- The deterministic program path completed ten preceding logical operations: four initial chain/finality calls,
  two neighbor headers, and the first transaction, receipt and two block headers.
- This is an infrastructure capability failure, not a scientific gate result. No trace result, slot, code, getter,
  later candidate, account, participant, price or response row was opened.
- No output artifact exists. V1's completion-only writer also means prior response hashes/bytes/retry counts were
  not durably emitted; this is an explicit provenance weakness to repair in a new version.
- No paid data, remote worker or GPU was used. Both V100s and the RTX 2060 remain idle; D1 and `G1` remain locked.

### Open questions / blockers

- Which documented, reproducible free or locally operated trace source can return complete historical calls for
  all 14 fixed transactions?
- Can v2 preserve partial response-hash evidence on a bounded failure without retaining raw payloads?
- After transport repair, will any candidate survive the unchanged mechanics/getter/contamination filters?

### Next steps

1. Commit and push the immutable v1 infrastructure-failure result.
2. Use documentation—not an ad hoc candidate probe—to select and freeze one v2 trace transport.
3. Add a bounded failure artifact, preserve every scientific/access rule, and run v2 only after a new pushed
   protocol commit.

## Session 12 (06:27)

### Goal

Design and implement the narrow D0b v2 trace-transport repair without probing a frozen candidate or changing any
scientific selection rule.

### What was done

- Reviewed the official Blockscout raw-trace endpoint, request-limit and tracing documentation and pinned the
  relevant backend/OpenAPI commits and file hashes. The backend establishes that the endpoint obtains raw traces
  through its configured execution node and may trigger on-demand fetching; it is provider evidence, not local EVM
  replay.
- Rejected a value-transfer-only internal-transaction substitute as insufficient for complete selector/path
  isolation. Froze `GET /api/v2/transactions/{transaction_hash}/raw-trace` at one global request per second without
  issuing a candidate capability probe.
- Added a hash-pinned v2 overlay over the immutable v1 manifest/result. The overlay permits only raw-trace transport,
  bounded failure-evidence and conservative SELFDESTRUCT-classification changes; all 14 rows, values, order, gates,
  access locks and resource caps inherit unchanged from v1.
- Implemented strict flat-trace normalization: a unique empty root, every parent, exact direct-child indexes and
  `subtraces`, strict call/create/selfdestruct action decoding, ancestor-propagated success and root-envelope match.
  CREATE2 is not distinguished by the source schema and is conservatively stateful CREATE; SELFDESTRUCT is stateful.
- Replaced the 14 unavailable debug RPC operations in the ordered plan with 14 exact raw-trace REST paths. The new
  fixed composition is 173 JSON-RPC + 14 raw trace + one Beacon = 188 successful no-retry operations.
- Added an attempt-level transport ledger and mutually exclusive outputs. Full completion emits three success
  artifacts; any caught collection exception emits only a bounded failure artifact with every prior logical
  response hash and every HTTP attempt outcome/hash, never raw bodies.
- Added v2 preregistration/runbook, unit tests and updates to the NCS plan, exposure design, source scorecard,
  compute audit and long-term project memory.

### Results / findings

- Thirteen targeted candidate-mechanics tests pass, including manifest/parent validation, exact operation order,
  incomplete/duplicate flat-trace rejection, exact proxy-admin topology, out-of-cone stateful calls,
  SELFDESTRUCT and failure-ledger bounds.
- The full bounded Compound stack passes 38/38. Independent manifest/plan inspection reports zero validation or
  parent errors, 14 candidates, 188 operations, the exact 156/14/17/1 provider split, 14 raw-trace operations and
  zero debug methods. The v2 overlay SHA-256 before protocol commit is
  `47ef0fab151a532e0fdc4fce2689d67179ccf8b5d1ba22276f7c3d5e8ffe3738`.
- Ruff, formatter check and strict mypy pass for the changed module/tests.
- No frozen-candidate raw trace, account, participant, liquidation, price/oracle or response row was opened. No
  paid data, remote worker or GPU was used; both V100s and the RTX 2060 remain idle.

### Open questions / blockers

- Will the documented public instance return complete raw traces for all 14 fixed historical transactions?
- Does the first real response conform exactly to the pinned Parity-style schema and child-count invariants?
- If transport completes, does any candidate survive the unchanged mechanics/getter/contamination gates?

### Next steps

1. Run the bounded related test stack and independent manifest/plan checks; freeze file hashes.
2. Commit and push v2 before any raw-trace request, then create a clean detached worktree at that exact commit.
3. Execute once and preserve either the complete result or the bounded infrastructure-failure artifact; do not
   substitute an endpoint or unlock D1/G1/GPU work without a full pass.

## Session 13 (06:51)

### Goal

Freeze, push, execute and independently verify D0b v2 exactly once, then determine the scientifically correct next
gate rather than automatically opening account data.

### What was done

- Committed and pushed the v2 protocol at `c7f770a938abe9f0e8452c9bda84bb8ed69f2e5e`, confirmed the remote ref,
  and created a clean detached sparse worktree at that exact commit.
- Materialized only the fixed Compound parent directories omitted by sparse checkout. The first clean-worktree test
  pass exposed two missing-parent-file failures; after materialization, the worktree remained clean and the same
  38/38 tests, Ruff, formatter and strict mypy all passed.
- Reconfirmed the v2 manifest SHA, parent evidence, 14-row order, 188-operation vector and four absent output paths,
  then ran the sealed collector once with no prior candidate probe or provider substitution.
- Copied the three success artifacts back byte-for-byte. A standalone offline verifier that did not import the
  collector rejected duplicate JSON keys and rebuilt parent hashes, all canonical request hashes, the exact
  operation plan, attempt reconciliation, D0 receipt matches, flat-trace topology, exact mechanism calls/cones,
  getters, proxy state, contamination, finality, every candidate check and all global gates.
- Added the immutable result and updated the NCS plan, exposure design, compute audit, source scorecard and long-term
  memory. Reviewer-style interpretation identified an event-class-specific estimand blocker before D1 acquisition.

### Results / findings

- Decision: `PASS_CANDIDATE_MECHANICS_AUTHORIZE_D1_EXPOSURE_PROTOCOL_DESIGN_ONLY`; all 12/12 gates passed and 4/14
  candidates fully conform.
- All 188 logical operations completed in 191 HTTP attempts. Three first-attempt HTTP 500s succeeded on the next
  attempt. The ledger spans 240.472 seconds and records 3,711,152 response bytes, 599 trace nodes, 532,327 trace-
  input bytes and 129 receipt logs.
- The four survivors are blocks 16,133,171; 16,520,572; 16,549,206; and 16,668,519. All are supply-cap increases
  from December 2022 through February 2023; the last two repeat the same WETH-market asset.
- Eight candidates fail the conservative stateful-sibling call-cone rule, three fail the 24-hour contamination
  rule, and one fails both. Every transaction/receipt/D0, header, trace root, required-call topology, proxy/getter
  and finality check passes for all 14.
- Artifact SHAs are summary `83e32d0e07ac395d02a9d8a8e385633545a605f62d778f60071e424e32923474`,
  candidates `553055dae591daab0ef662b02d7e32a98d726fd154a5f43d34575ca3f31160d5` and HTTP evidence
  `814e66afe43f06cc19b99426f9b7408ccbc423f65e051fcd63de7cbaf5844b87`.
- The mechanics pass does not solve exposure. A supply-cap increase does not perturb existing accounts' balances,
  collateral factors or liquidation thresholds; generic nonzero positions are not a treated cohort, and would-be
  constrained suppliers lack a pre-event on-chain denominator.
- No account/action/liquidation/price/response row, paid data, external worker or GPU was used. Both V100s and the
  RTX 2060 remained idle.

### Open questions / blockers

- Was each old supply cap actually binding at T-1, and for how long before the expansion?
- Can a market-level entrant/collateral-flow estimand be identified without pretending pre-event would-be suppliers
  are observable or selecting controls from outcomes?
- Do the two repeated cbETH-market events leave nonoverlapping horizons and independent enough support for any
  development analysis?

### Next steps

1. Commit and push the immutable v2 result before any new chain request.
2. Freeze a zero-account-row D1a cap-activation/estimand protocol over all four survivors, including exact aggregate
   getter semantics, historical source conformance, fixed lookback clocks, binding thresholds and kill decisions.
3. Run D1a only after its own pushed protocol. Do not enumerate accounts or open actions/responses unless D1a
   establishes an active mechanism and a defensible estimand; keep G1 and all GPUs locked.

## Session 14 (07:30)

### Goal

Turn the four D0b v2 supply-cap survivors into a falsifiable zero-account activation/estimand protocol before any
new historical source or aggregate-state request.

### What was done

- Derived all four survivors and seven unique old/new implementation addresses and code hashes exclusively from
  the hash-pinned D0b v2 evidence. No candidate source/getter endpoint was probed.
- Reviewed the official Blockscout smart-contract response schema and the fixed Compound source semantics. The
  protocol requires fully verified unchanged Solidity source, deployed-bytecode hash identity, exact aggregate and
  configuration ABI shapes and all storage/enforcement markers for every historical implementation.
- Implemented a bounded D1a collector with strict `TotalsCollateral(uint128,uint128)` decoding, duplicate-key/path
  rejection, source/file/ABI hash reduction, two-provider historical state replication, exact request-order
  checks, attempt-level failure evidence and mutually exclusive success/failure outputs.
- Froze six pre-event block offsets per candidate: 1, 300, 1,800, 7,200, 21,600 and 50,400. Each snapshot reads the
  contemporaneous cap and aggregate collateral total from both providers and records an exact timestamp. No
  event/post-event aggregate total, account, action, trace, price, liquidation or response is opened.
- Defined exact T−1 saturation as the only confirmatory activation. Utilization at 90%, 95% and 99% and other
  snapshots are diagnostics only. Participant-level M3 is structurally retired for every outcome; a pass can
  authorize only separately freezing a market-level collateral-flow D1b design.
- Added the manifest, preregistration, runbook and seven targeted tests, and updated the NCS plan, exposure design,
  source scorecard, resource audit and long-term memory.

### Results / findings

- The exact no-retry plan is 141 read-only operations: 134 RPC plus seven Blockscout source REST calls. Method
  counts are two chain IDs, eight code reads, 24 headers and 100 historical calls; provider counts are 49
  Blockscout RPC, seven Blockscout source and 85 PublicNode execution.
- The seven targeted tests and full 45-test bounded Compound stack pass. Ruff lint and format checks pass, and
  strict mypy passes with explicit package bases required by the sparse recovery worktree. Independent derivation
  reproduces four candidates, 141 operations and zero manifest/parent errors; the pre-commit manifest SHA-256 is
  `aac34fd0b8bb1d3e1f63b12fb0230f10039a0d2d7019bf8ea461d433cb5b3037`.
- A slack cap can still constrain a sufficiently large hypothetical deposit. D1a therefore does not claim
  non-saturation proves zero physical effect; it treats the route as unidentified because depositor intent and
  counterfactual order size are not observed. This distinction is recorded in the protocol.
- Expected wall time is a few minutes with 15 minutes budget. Paid data, remote-worker and GPU requirements are
  zero. Both V100s and the RTX 2060 remain idle.

### Open questions / blockers

- Do all seven Blockscout historical implementation records satisfy the frozen full-source/bytecode/ABI markers?
- Is any one of the four events exactly saturated at T−1, and do earlier diagnostic snapshots show repeated rather
  than isolated saturation?
- If exact saturation exists, can a market-level entrant/collateral-flow estimand and non-outcome-selected controls
  survive a separate D1b design review?

### Next steps

1. Run the broader bounded Compound test stack and independent parent/plan checks; freeze hashes.
2. Commit and push the D1a protocol before any candidate source/state access.
3. Create a clean detached worktree at the exact remote commit, execute once and independently verify either the
   three success artifacts or the bounded failure artifact without changing endpoints or thresholds.

## Session 15 (07:43)

### Goal

Freeze, push and execute Compound D1a v1 exactly once, preserve its first outcome and determine whether it reaches
a scientific cap-activation decision.

### What was done

- Committed and pushed the complete D1a protocol at
  `1dfdecf47d60b3f78a1076d2a09ed60f7ad5fc7f`, verified the remote ref, and created a clean detached sparse
  worktree at that exact commit.
- Materialized the fixed Compound experiment parents. The detached worktree remained clean; the same 45/45
  Compound tests, Ruff, formatter, strict mypy, manifest hash, parent validation and four-candidate/141-operation
  reconstruction passed. All four output paths were absent.
- Ran the sealed collector once. It stopped on the first historical PublicNode code read after both chain IDs and
  all seven Blockscout source-metadata requests had completed. No endpoint was changed and v1 was not rerun.
- Copied the sole failure artifact byte-for-byte and wrote the immutable result. A standalone verifier that did not
  import the collector rejected duplicate JSON keys and rebuilt manifest/failure hashes, the exact nine-operation
  prefix, RPC IDs/request hashes, seven REST paths/hashes, failed request, attempt/byte accounting, exclusive
  outputs and access locks.
- Updated the NCS plan, exposure design, resource audit, source scorecard and long-term memory.

### Results / findings

- Formal decision: `INFRASTRUCTURE_FAILURE_NO_SUPPLY_CAP_ACTIVATION_RESULT`. This is not a cap-saturation,
  source-conformance or causal-estimand result.
- Nine logical operations succeeded on first attempts: two chain IDs and seven Blockscout source HTTP 200s. The
  first candidate's PublicNode `eth_getCode` at block 16,133,170 returned HTTP 403 on all three permitted attempts.
- The ledger contains 12 attempts, 1,370,203 response bytes and a 14.878-second span. Failure artifact SHA-256 is
  `225266d3fb1e394f0714ceed60000ca42c84476ac8bd1641b4eacb8da30c40c8`; the independent verifier passed.
- Source records were normalized in memory before the code request, but v1 failure evidence retains only response
  hashes. HTTP success cannot be called source conformance. No historical code result, configuration, aggregate
  total, account, action, price, liquidation or response row was opened.
- Paid data, remote-worker and GPU use remained zero. Both V100s and the RTX 2060 stayed idle.

### Open questions / blockers

- Which officially documented free endpoint can provide the fixed historical `eth_call` state, or should v2 use
  Blockscout alone with parent/source hash triangulation and explicitly drop cross-provider historical-state
  replication?
- Can v2 durably retain already-normalized source evidence on later failure without treating it as a complete
  activation result?
- After a documented transport repair, is any event exactly saturated at T−1?

### Next steps

1. Commit and push the immutable v1 failure result before another candidate request.
2. Review official provider/archive documentation only; do not probe a frozen address.
3. If a defensible free transport exists, freeze a narrow v2 overlay that changes only historical-state transport
   and partial normalized-source evidence, then push before one new run. Otherwise record the external archive
   dependency and keep Compound M3 retired/locked.

## Session 16 (08:04)

### Goal

Use official capability documentation—not candidate probing—to design a narrow D1a v2 repair after v1's archive-
state HTTP 403.

### What was done

- Reviewed the official PublicNode Ethereum page, which labels archive data as available through a separate “Get
  Archive Access” action. The unauthenticated endpoint is no longer treated as a historical-state source.
- Reviewed Blockscout's official per-instance ETH RPC reference, which documents `eth_call` with an explicit block
  parameter and no required API key. D0b's successful historical Blockscout code/getter calls supply additional
  non-candidate-probe transport evidence.
- Implemented a hash-pinned v2 overlay over the immutable v1 manifest/result/failure. V2 changes only historical-
  state transport, removes redundant code/post-config requests and makes completed normalized source/candidate
  evidence durable on later transport failure.
- Retained PublicNode for chain identity and 24 historical block headers. Every lookback now requires exact
  Blockscout/PublicNode number/hash/timestamp identity, while Blockscout alone returns contemporaneous asset info
  and aggregate collateral total. The protocol explicitly rejects calling this independent state replication.
- Preserved the exact four candidates, seven implementations, six lookbacks, source/ABI/enforcement semantics,
  exact T−1 saturation rule, diagnostic-only near-cap thresholds, decisions and all participant/response locks.
- Added the v2 overlay manifest, collector, preregistration, runbook and five tests including a full synthetic
  105-operation pass path. Updated plans, budgets, scorecard and long-term memory.

### Results / findings

- The exact no-retry v2 vector is 105 operations: 98 RPC plus seven source REST. Method counts are two chain IDs,
  48 headers and 48 historical calls; provider counts are 73 Blockscout RPC, seven Blockscout source and 25
  PublicNode execution.
- Five v2 tests and the full 50-test bounded Compound stack pass. They reproduce all v1/D0b/source parents and unchanged scientific fields, exclude every
  PublicNode state/code request, verify exact ordering/counts, enforce archive/access locks, preserve partial
  evidence on failure and exercise a synthetic 10/10-gate exact-saturation pass.
- Ruff and formatting pass, and strict mypy passes. Independent reconstruction reports zero manifest/parent errors,
  four candidates and 105 operations; pre-commit manifest SHA-256 is
  `aca9d2af723a9644066a85f8ce0c98893a97463402574cdb04feb771944163f9`. No candidate source/state request was
  issued during v2 design.
- Single-provider historical state remains weaker than an independent archive replica or Ethereum state proof. It
  is sufficient only as a free feasibility kill gate; it cannot by itself support a final NCS causal claim.
- Expected runtime is roughly two minutes plus latency, with zero paid data, worker or GPU use. Both V100s and RTX
  2060 remain idle.

### Open questions / blockers

- Will all seven normalized verified-source records actually conform, or will source/ABI markers fail despite HTTP
  success?
- Will Blockscout return every fixed historical asset/aggregate call, and is any T−1 total exactly saturated?
- If v2 passes activation, what independent archive/state-proof replication is required before a paper claim?

### Next steps

1. Run the full bounded Compound stack and independent v2 parent/plan checks; freeze hashes.
2. Commit and push v2 before any further candidate request.
3. Execute v2 once from a clean exact worktree, preserve the first outcome and independently verify all evidence.

## Session 17 (08:30)

### Goal

Freeze and execute the documented D1a v2 transport repair once, verify the first outcome independently, and update
the NCS route without moving the preregistered activation threshold.

### What was done

- Verified that the v2 staging set contained only the intended 11 protocol/code/test/document files, committed it
  as `a9af33e9c1e519a1b670f5700bf627655ff053fa`, pushed it and reproduced the exact remote ref.
- Created a clean detached sparse worktree at that commit. Re-ran the 50-test bounded Compound suite, Ruff format
  and lint, strict mypy, all v1/D0b/source parent checks and an independent four-candidate/105-operation plan
  reconstruction. Confirmed all four v2 output paths were absent before launch.
- Ran v2 exactly once. Copied the three first-run success artifacts byte-for-byte; no `failure.json` exists.
- Wrote a standalone verifier that does not import the collector. It rejects duplicate YAML/JSON keys; pins all
  parent and result hashes; rebuilds candidates, source-inventory hashes, the exact request plan and every request
  hash; reconciles attempt/byte accounting; checks all headers/state arithmetic; and reproduces gates and decision.
  It explicitly reports that raw-response parsing cannot be replayed because raw bodies were never retained.
- Wrote `RESULTS_V2.md` and updated the authoritative NCS plan, Compound design, data/compute audit, M3/M4 source
  scorecard and long-term memory. The scorecard now hands the next zero-row audit design to Aave rather than
  continuing a Compound participant census.

### Results / findings

- Formal decision: `FAIL_NO_EXACT_T_MINUS_ONE_SATURATION_RETIRE_COMPOUND_M3_CAUSAL_ROUTE`.
- All 105 operations succeeded on their first attempt in a 107.202-second response span, totaling 1,961,938 bytes.
  Seven source implementations, four candidate state records and all ten integrity gates conformed.
- T−1 utilization was approximately 99.860059%, 73.530680%, 99.999622% and 99.999949%, but no raw integer total
  equalled its cap. None of the 24 frozen lookback snapshots was exactly saturated.
- Artifact SHA-256 values are summary `e5f4ef20a3561d75cd8b13007fcd2b0e23e518c13aa20f7d0cd1fd4b1f5caa66`,
  candidates `e452a2fcd00a238f5d44fb9b9c04eab294979fd93b3f6be2490f5845f26e5707` and HTTP evidence
  `cf62cf400b243e9883e4d97d69d47b12339f2da2ee312a1437351a6cb03069ad`; the offline verifier passed.
- Near-boundary values do not alter the confirmatory decision. They show only that larger attempted deposits may
  have been constrained; those attempts and suppliers are unobserved. A threshold change now would be post-hoc.
- No account/action/response, price, liquidation or post-event aggregate row was opened. Paid data, external
  workers and GPU use were zero; both V100s and the RTX 2060 remained idle.

### Open questions / blockers

- Can Aave provide an outcome-blind, pre-event enumerable treated denominator for an unbundled governance effect
  class without first opening participant responses?
- Can a new cross-protocol hard-constraint boundary-layer study obtain attempted/reverted-action evidence and an
  untouched confirmation set, or is that only a descriptive mechanics result?
- Final-paper use of historical state still needs an independent archive replica or verified Ethereum state proof;
  v2's two-provider header agreement does not replicate Blockscout state.

### Next steps

1. Verify copied artifacts and the standalone verifier in the branch, then commit and push this immutable result.
2. Freeze a zero-network Aave source/effect-class design document before opening proposal or participant rows.
3. Keep Compound D1b, all participant/response acquisition, G1 and GPU training locked.

## Session 18 (16:25)

### Goal

Turn the post-Compound Aave handoff into a frozen, zero-chain-row source/effect protocol without mistaking a
source identity for an event, a health-factor crossing for executable liquidation, or design feasibility for G1.

### What was done

- Inspected pinned official Aave V3.7 source around `PoolConfigurator`, `GenericLogic`, `WadRayMath`,
  `ValidationLogic`, pool views, user/reserve configuration and the V3 config-engine route.
- Implemented `aave_liquidation_threshold_source.py`: exact uint256/half-up arithmetic, strict LT-only validation,
  eMode and disabled-collateral zero routes, source-manifest validation, fixed-file marker audit, deterministic
  self-checks, fail-closed decisions and mutually exclusive success/infrastructure-failure artifacts.
- Corrected two initially overbroad descriptions before freeze. `configureReserveAsCollateral` can also write
  pending LTV for a frozen reserve, so the gate is “no account write/iteration,” not “reserve-only.” `HF < 1` is a
  health-factor boundary, not sufficient evidence of executable liquidation.
- Added the v1 manifest over one exact official commit and fourteen source/licence files. It excludes frozen
  reserves and same-execution account-relevant configuration bundles from any later strict candidate class.
- Added eight tests covering the manifest/access locks, direct identity and boundary case, two zero routes,
  bundle/overflow failures, a deterministic monotonicity grid, a synthetic twelve-gate source pass, fail-closed
  source mutations and manifest drift.
- Wrote preregistration, one-run runbook and a larger Aave-to-NCS design with A0--A3/G1 data and compute gates.
  Updated the NCS plan, source scorecard and resource audit.

### Results / findings

- The exact eligible direct operator is
  `W_new = W_old - C_changed * (LT_old - LT_new)` followed by Aave's half-up `wadDiv` and integer division by
  10,000. It is nonlearned; learning begins only at post-shock account adaptation.
- Base-LT effect is exactly zero where eMode supplies the reserve's effective threshold or the reserve is not
  enabled as collateral for the account.
- All eight tests pass. Ruff formatting/lint and strict mypy pass. The manifest validator returns zero errors.
- Frozen-marker calibration opened all fourteen allowed official blobs through GitHub's API: 197,858 bytes and
  zero missing markers. This makes A0 a reproducible source certification, not a blind empirical discovery test.
- A0 requires one local CPU core, no chain/API/account row, no paid data, no worker and zero GPU. Both V100s and the
  RTX 2060 remain idle; H20 is not assumed.
- A source pass can authorize only deployment/version/LT-event inventory design. It cannot establish event
  existence, account-denominator completeness, causal identification, G1 or an NCS claim.

### Open questions / blockers

- Will a clean pinned checkout reproduce remote/commit/cleanliness, file hashes, transition checks and all twelve
  gates in the sealed artifact?
- Does any strict, unbundled LT decrease exist after version, frozen-reserve, eMode/config-bundle and untouched-
  confirmation exclusions?
- Can a later account census reconcile all reconstructed positions exactly to authoritative reserve totals without
  outcome-informed selection or paid archive dependence?

### Next steps

1. Run all bounded static/test checks, inspect the exact staging set, then commit and push the A0 protocol.
2. Only after remote SHA verification, stage the pinned official source and execute A0 once from a clean detached
   protocol worktree.
3. Independently verify the first artifact, update results/log/memory and commit/push it. Keep chain events,
   participant rows, G1 and GPUs locked regardless of an A0 pass.
