# 2026-08-10

## Session 1 (01:44) — event-gradient, mixing-diagnostic and distributed-resume preflight

### Goal

Continue the zero-cost NCS feasibility phase on `ncs-invariant-calibration-v4`: run the remaining E1/E2
synthetic audits, implement state-complete distributed exact resume, use a V100 only if idle, and synchronize
all artifacts, plans and long-term memory without purchasing data or expanding compute.

### What was done

- Preregistered and ran exp131 on a two-state chain and compound-Poisson AR/OU system: 64 batches × 512
  replicates, short horizon 24, long horizon/burn 2,048, with known LR, stationary-oracle and coupled-FD controls.
- Preregistered and ran exp132 on 64 × 64 bistable Langevin chains across easy/medium/hard temperatures with a
  frozen four-part fail-visible mixing diagnostic.
- Added `SimulatorState.to(device)` and atomic distributed checkpoint format v2. It captures each rank's complete
  simulator state, primary/auxiliary generators, PyTorch CPU/local CUDA RNG, NumPy/Python RNG and history.
- Added validation for format/world-size/state-complete/rank-list mismatch and optimizer-state device migration.
- Found and fixed missing initial parameter/buffer broadcast in the manual all-reduce distributed trainer.
- Added a two-rank local Gloo harness comparing uninterrupted six-step training with a three-step checkpoint and
  continuation in new processes. A first harness smoke exposed that setting the split run's planned total to
  three changed the cosine LR schedule; `stop_after_iter` now separates planned horizon from process stop.
- Local `torch.distributed.run --standalone` hung before worker launch on this Mac, so the reproducible test
  launcher uses explicit subprocess ranks and a file-store rendezvous. This was infrastructure debugging, not
  a scientific failure.
- Ran the formal two-rank CPU experiment after implementation commit `a54b4406f`; ran 21 focused regression
  tests plus Ruff import/undefined-name checks and `git diff --check`.
- Checked both V100 nodes read-only. Each was running an unrelated graphene formal calculation for about
  53 minutes, so no EcoPhys CUDA job was launched and no process was touched.
- Wrote result reports for exp131--133 and updated Plan v4, the G0 audit, WP1 technical note and long-term memory.

### Results / findings

- Exp131 passed all seven frozen harness gates and exact CPU forward/RNG parity. In the slow two-state cell,
  persistent-detached LR bias was `-38.01%`; stationary oracle, full-long and FD-CRN biases were `+0.30%`,
  `+0.47%` and `-3.06%`. In the slow jump cell, coupled FD bias was `+5.64%` with `92.19%` coverage, whereas
  persistent-detached LR bias was `-31.63%`. This is baseline validation, not a new estimator.
- Exp132 failed overall exactly as frozen: hard false-safe rate was `0%`, but easy resolved rate was
  `78.125% < 80%`. The threshold remains unchanged. Hard chains were correctly rejected while full-long and
  persistent-detached gradient biases were `-96.53%` and `-99.88%`.
- Exp133 CPU/Gloo passed every gate. Uninterrupted versus resumed model, optimizer and both rank runtime/RNG
  difference counts were all zero; both ranks had the same final model digest; world mismatch hard failed; no
  checkpoint temporary file remained. Formal wall time was about 18.2 seconds.
- Focused regression suite: 21 passed. Ruff `I,F` and whitespace checks passed before the implementation commit.
- G0 remains AMBER. No experiment in this session introduced a candidate method or improved the known
  accuracy--cost frontier.

### Open questions / blockers

- The single-V100 exact-resume gate is waiting for either existing graphene production job to finish.
- Production resume still needs the chosen loader's sampler/data cursor, W&B contract, and any future
  scheduler/AMP-scaler state; multi-node NCCL and real asynchronous preemption remain untested.
- Exp132 v1 cannot be repaired by changing its threshold. A v2 requires independent diagnostic tuning and
  validation data.
- There is still no mathematically specified candidate estimator or five-nearest-method non-equivalence table;
  E3 candidate scaling is therefore premature.
- Synthetic queue/event emission and parameter recovery remain the next free observation-bridge experiment.

### Next steps

1. Freeze the candidate estimator mathematics, residual diagnostic and non-equivalence table before more G0
   candidate experiments; otherwise stop the NCS method claim.
2. Preregister and implement the synthetic queue/event emission plus recovery benchmark using generated data
   and the exp130 reconstruction conventions.
3. Run the single-V100 exp133 gate and canonical V100-equivalent anchor as soon as a card is genuinely idle.
4. Freeze the formal WP2 five-arm preregistration, but do not spend its production budget before the G0/G1
   preflight review permits it.

## Session 2 (02:01) — synthetic aggregate-L2 emission and recovery

### Goal

Execute the next no-purchase, CPU-only observation-bridge feasibility experiment while keeping a hard boundary
between correctly specified synthetic recovery and any EcoMD or real-market claim.

### What was done

- Audited exp124/130 and the Plan v4 WP5 requirements, then preregistered exp134 before implementation at
  commit `dd30be020`.
- Added a typed `ecomd.observation` module implementing a five-level aggregate book, LOBSTER-shaped add/cancel/
  delete/visible-execution/hidden-execution messages, independent message reconstruction, and maximum-likelihood
  fits for latent flow slope, level decay and latent-conditioned size.
- Added four unit tests covering exact reconstruction/hidden invariance, parameter recovery, exact sign gauge,
  non-positive queue failure and unknown event-type failure. New core and experiment files pass strict mypy and
  full Ruff checks.
- Committed the implementation at `79b7126cd`, then ran the frozen formal design: 3 mixing regimes × 3 slopes ×
  8 independent streams × 30,000 events, with a 60/40 time split.

### Results / findings

- All ten gates passed on 2,160,000 events. Reconstruction was bit-exact, hidden display changes were zero,
  minimum queue was 95,418 and all fits converged. Wall time was about 2.4 seconds on Mac CPU.
- Nonzero flow-slope cell-median relative errors were 0.97--2.12%, with 8/8 correct signs. Held-out gains were
  about 0.092--0.096 nats/event for `beta=0.5` and 0.223--0.232 for `beta=1.0`.
- Permuting latent values inside train/test blocks reduced median absolute slope to at most 0.0076 and held-out
  gain to numerical noise. Median size-slope relative error was 1.08%.
- Flipping the latent sign produced exactly the opposite slope and identical likelihood in all streams. A
  buy/sell sign anchor is necessary; L2 cannot orient an arbitrary latent coordinate.
- This is an intentionally easy correctly specified recovery test. It does not pass G3 or justify data purchase.

### Open questions / blockers

- EcoMD still exposes the latent statistic under the misleading `ofi` name and lacks a versioned adapter with a
  fixed sign convention.
- The operator does not model individual orders, price-time priority, price moves, depletion, latency, censoring
  or model misspecification.
- Observation-only, Hawkes/queue-reactive and resampling baselines remain absent. Real L2 remains gated.

### Next steps

1. Migrate `ofi` to `latent_flow_alignment` with backward-compatible artifact and API aliases.
2. Preregister an EcoMD-adapter and misspecification/observation-only stress test before inspecting outcomes.
3. Keep G3 open and do not buy data until those synthetic gates identify the fields and model class actually
   needed.

## Session 3 (02:44) — safe V100 queue, semantic migration and observation-bridge falsification

### Goal

Execute every currently unblocked zero-cost experiment, safely queue CUDA work behind existing production jobs,
remove the false OFI terminology, and keep G0/G3 decisions synchronized with frozen evidence.

### What was done

- Inspected both V100 nodes read-only. Both were running graphene production jobs, so no process was stopped and
  no opportunistic sharing was attempted.
- Added `scripts/v100_queue_exp133.sh` and installed a transient `ecophys-exp133-queued.service` on V100-A.
  The queue requires the blocker service to be inactive, no compute process, ten consecutive one-minute
  low-utilization polls and no graphene priority release marker. Node B was left free of duplicate EcoPhys work.
- Migrated the simulator quantity from `ofi` to `latent_flow_alignment` throughout the core recorder, price
  path and trajectory export. Legacy API constructor/property/method and NPZ key aliases remain readable; new
  artifacts mark the old key as a legacy alias.
- Wrote `ncs_candidate_estimator_spec_v0.md` with the stationary target, hybrid event gradient, randomized
  horizon, staleness bound, error ledger and six-nearest-method audit. The direct-composition non-equivalence
  test failed before candidate coding.
- Preregistered exp135 at commit `795308eb5`, added general fixed-feature logistic evaluators, committed the
  implementation at `eca713fac`, and ran the clean-tree formal misspecification stress.
- Preregistered exp136 at commit `ff724fe1b`, implemented a typed checkpointable absolute-clock EcoMD-to-L2
  adapter and tests, committed at `bcb025bd9`, and ran its clean-tree formal CPU audit.
- Ran focused simulator/observation suites and Ruff on every new module; all checks passed.
- Updated Plan v4, the G0 audit and long-term memory. The existing exp127 V100 canonical anchors were not
  redundantly queued because no new state-complete production configuration is frozen yet.

### Results / findings

- The exp133 queue is active but held correctly. At the last check graphene and a compute process were still
  active, GPU utilization was 20%, and the queue state was `waiting`, stable idle count `0/10`.
- The estimator v0 is not novel: it is fully described by persistent chains + hybrid pathwise/LR +
  Rhee--Glynn telescoping + diagnostics. G0 stays AMBER and E0--E3 candidate runs remain blocked.
- Exp135 passed every frozen gate on 144 streams and 4,320,000 events in 8.25 seconds. It selected latent-only,
  observation-only, combined, lagged and even-nonlinear truths correctly and kept null gains near zero. At
  `rho=0.98`, lagged versus current latent differed by only `0.00844` nats/event, preserving the preregistered
  temporal-identifiability warning.
- Exp136 passed every frozen gate in 32.75 seconds. Eight `N=64,T=3000` paths and 20,000 emitted messages were
  bit-exact under simulator chunks, emitter chunks and serialized resume. The structural pressure/buy sign was
  exact; median recovered slope was `2.010` for truth 2.0; latent beat observation-only by `0.0543`
  nats/visible event; permutation gain was `1.15e-4`; anchored sign beat its flip by `0.232`.
- Exp135/136 are deliberately generated-data tests. They close evaluator and plumbing gaps but do not validate
  EcoMD against markets, clear G3 or unlock paid L2.

### Open questions / blockers

- Exp133 CUDA exact resume remains queued behind graphene; production data cursor, scheduler/scaler, W&B,
  multi-node NCCL and asynchronous interruption remain outside that gate.
- G0 has no irreducibly novel estimator. A v1 requires a new theorem/identity for cross-parameter coupling,
  finite-budget residual control or variance/cost before more method experiments are justified.
- G3 still lacks dynamic price/queue semantics, external market validation of the sign convention,
  latency/censoring stress and Hawkes/queue-reactive/empirical baselines.
- Formal WP2 and paid-data work remain locked behind their preceding gates.

### Next steps

1. Let the exp133 queue launch automatically only after its safety conditions pass; record its CUDA artifact.
2. Separately preregister dynamic price/queue semantics and strong point-process baselines before extending the
   easy correctly specified adapter fixture.
3. Seek a steady-state-sensitivity specialist review of the v0 non-equivalence result; stop the NCS method route
   if no new mathematical primitive survives the citation audit.
4. Freeze a new production state-complete configuration before scheduling another canonical V100 benchmark.

## Session 4 (03:20) — provenance-safe recovery and queue handoff

### Goal

Preserve the completed preregistration/implementation/result chain after a Mac filesystem incident, verify the
remote queue without disturbing production work, and leave the branch in a pushable audited state.

### What was done

- Rechecked both V100 nodes read-only. V100-A retained the supervised exp133 queue behind graphene; V100-B had
  no duplicate EcoPhys service.
- A broad pytest attempt had been killed by macOS after 66 tests; a six-file batch then passed 47 tests, and
  isolated `test_b3_discrete_regime.py` and `test_b4_power_law.py` passed 8 and 7 tests respectively. The
  resource-heavy `test_bptt_checkpoint.py` was killed after four tests, so no full-suite PASS is claimed.
- After those runs, macOS File Provider exposed roughly 15,000 worktree and `.git` files as zero-readable
  `dataless` placeholders, and normal Git commands in the original clone exited with `SIGBUS`. No placeholder
  was overwritten.
- Built a fresh sparse recovery clone from the GitHub branch head and reconstructed all eight local commits
  from the original loose objects plus the resident preregistration. Every reconstructed tree and commit hash
  exactly matched `455ed73a6` through `bcb025bd9` before adding the final exp136 artifacts and documentation.
- Closed type gaps in the four new observation/experiment modules without changing the estimator or emission
  equations. New-module Ruff and strict mypy with imported legacy modules skipped both passed; 45 focused
  simulator/observation tests passed in the healthy sparse clone.
- Re-ran exp135 and exp136 after the type-only changes to temporary outputs. Both retained every hard-gate PASS,
  and each scientific payload was exactly equal to the frozen artifact after excluding environment and elapsed
  time metadata.
- Recorded the workspace durability rule in long-term workflow memory: keep the repository downloaded or move
  it outside a cloud-backed Desktop before broad local tests; use a sparse canonical-Git recovery path if it
  is ever evicted again.

### Results / findings

- V100-A queue status remained `waiting`: graphene blocker active, one compute process active, current
  utilization 20%, stable-idle count `0/10`. V100-B also had one compute process and 22% utilization. No new
  experiment should be placed on either card now.
- The scientific artifacts are intact and their preregistration chronology is unchanged. Focused tests and
  all formal exp135/136 gates passed; the only unverified claim is repository-wide pytest completion.
- Repository-wide lint is also not claimed: a broad Ruff invocation exposed 87 existing warnings in historical
  core files, while strict mypy across the full legacy import graph still has pre-existing debt. The four new
  modules themselves pass the bounded strict check.
- The filesystem failure is operational, not a scientific result. The original clone should not be trusted for
  edits or Git operations until Finder has rehydrated it or it has been replaced by a normal local clone.

### Open questions / blockers

- Exp133 still depends on graphene releasing V100-A and then satisfying ten one-minute idle polls.
- The original Mac worktree remains partially evicted; broad local verification is deferred to a healthy clone.
- Scientific gates are unchanged: G0 has no novel candidate estimator and G3 has no real-market observation
  validation or strong dynamic point-process baseline.

### Next steps

1. Let the supervised exp133 queue run automatically; do not duplicate it on V100-B.
2. Rehydrate or replace the Mac clone before the next coding session, then rerun the focused suite and divide
   broad tests into resource-bounded groups.
3. Preregister dynamic price/queue semantics plus Hawkes and queue-reactive baselines; keep paid L2 locked.
4. Obtain specialist feedback on whether any v1 invariant-sensitivity theorem is genuinely non-equivalent;
   otherwise close the NCS-method route and execute the simulator-audit/TMLR fallback.

## Session 5 (13:55) — CUDA resume closure and dynamic queue/price falsification

### Goal

Run every currently feasible zero-cost experiment, including CPU work on the two V100 hosts, without using
paid data or spending GPU time on scientifically blocked work; then preserve the preregistration, code,
artifacts and interpretation boundary on the NCS branch.

### What was done

- Confirmed that the supervised exp133 queue eventually launched only after V100-A became safe. The formal
  single-card CUDA uninterrupted/resume comparison completed rather than being duplicated on V100-B. Archived
  its remote JSON as `V100_RESULTS_455ed73a6f41.json`; local and remote SHA-256 are both
  `3b3ab8be3bc77e0a88bd619a527c3b8d85093d96b3ae4d68f4703421e4bdf943`.
- Preregistered exp137 at `69266f26` before implementation or fitted output. The frozen design contains 2 truth
  families × 2 AR persistence values × 2 censoring rates × 8 streams × 40,000 events, with a dynamic aggregate
  L5 book, queue depletion, price moves, exact reconstruction and nine observation/latent baselines.
- The first unit test exposed an underspecified no-op boundary for a non-best queue already at one unit. Before
  any pilot or formal statistic existed, commit `fc9aff87` froze the minimal clarification: displayed removal
  levels are conditioned on being executable, while the best level remains eligible because it triggers the
  declared reset. No coefficient, seed, count, threshold or truth family changed.
- Implemented the state-complete dynamic generator and reconstructor, queue-reactive and discrete marked-
  Hawkes-logit comparisons, 20% prospective censoring, exact chunk/serialized-resume checks, formal shard
  provenance checks and hard-gate merger at `ba763c7d`.
- Ran Ruff, bounded strict mypy and 13 focused observation tests; all passed. Two 3,000-event local smoke shards
  passed structural/fitting checks, remained explicitly non-formal and dirty, and were rejected by the formal
  merger as intended.
- Created independent sparse clones at exact clean commit `ba763c7d` on both 16-core V100 hosts. Launched even
  and odd formal shards with one CPU process/BLAS thread each and `CUDA_VISIBLE_DEVICES=-1`. Both services
  exited zero in 194.36 and 193.45 seconds; neither GPU was used.
- Downloaded both shards, verified local/remote hashes, merged them once, and wrote the result report. The
  merged artifact hash is `c41278c104cdaf36a1a5523c823bb92befa50453e0e71770ccedea229f6f9f17`;
  shard hashes are `dba20846...f4227ff` and `1273b2fb...d83640`.
- Re-ran the formal merger to a temporary path; its complete JSON was byte-identical to the archived result.
  The final focused test invocation initially resolved the stale installed package from the damaged original
  clone; with the recovery clone explicitly first on `PYTHONPATH`, all 13 tests passed again. Ruff and bounded
  strict mypy also passed.

### Results / findings

- Exp133 single-V100 CUDA exact resume passed 12/12 frozen comparisons with every difference count equal to
  zero in 16.81 seconds. This closes the mechanics probe, not the production data-cursor/W&B/multi-node or
  asynchronous-durability contract.
- A final read-only check found both exp137 services inactive and both V100s idle at 0% utilization with no
  compute processes; the completed CPU experiments left no GPU workload running.
- Exp137 passed all 9 hard gates across 64 streams, 2,560,000 generated events, 2,187,971 retained events and
  576 converged fits. Every stream was bit-exact under chunk/resume and independent reconstruction and had both
  price-move signs; cell-median move rates were 3.205%--4.356%.
- Under latent-incremental truth, the joint current-latent/observation model beat the strongest observation
  baseline by `0.0566--0.1136` held-out nats/event and beat latent-only by `0.0507--0.0564`; at zero censoring,
  fitted latent coefficients were `0.6547--0.6569` for truth 0.65.
- Under observation-only truth, adding latent changed held-out likelihood by only `[-4.30e-5, -3.41e-7]`
  nats/event and median absolute latent coefficients were `0.00415--0.00704`. Permutation gains were at most
  `1.00e-4`. All substantive and negative-control gates survived 20% censoring.
- This is synthetic identification evidence, not market physics or model-to-real validation. The DGP is
  representable by the evaluator, depletion reset is deterministic, queues are aggregate and the declared
  `marked_hawkes` model is a discrete logistic history baseline rather than a continuous-time Hawkes likelihood.
  G3 remains open and paid L2 remains locked.

### Open questions / blockers

- G0 remains AMBER because there is still no estimator identity/theorem that survives the non-equivalence audit.
  More baseline or GPU runs cannot repair this mathematical blocker.
- G3 now has dynamic synthetic price/queue semantics and stronger controls, but still lacks external message
  timestamps/sign conventions, a continuous-time marked point-process likelihood, out-of-family real holdout
  evidence and individual-order semantics.
- The original cloud-backed Mac worktree is still partially `dataless`; the healthy sparse recovery clone and
  pushed GitHub branch remain canonical until the original is rehydrated or replaced.

### Next steps

1. Freeze a free-data, out-of-family observation protocol with external timestamp/sign audit and a genuine
   continuous-time marked point-process baseline; do not inspect a held-out period before its metrics are fixed.
2. Complete the five-nearest-method citation/theorem audit for one genuinely non-equivalent G0 candidate; if no
   candidate survives, formally stop the NCS method route and execute the TMLR/simulator-audit fallback.
3. Add production sampler/data cursor, scheduler/scaler, W&B and interruption semantics before treating exp133
   as a production checkpoint contract.
4. Keep paid L2 and large GPU campaigns locked until their preceding scientific gates pass.

## Session 6 (15:47) — external continuous-time baseline and frozen optimizer failure

### Goal

Execute the next zero-purchase G3 preflight on external messages: audit vendor timestamp/sign semantics, add a
genuine continuous-time queue/Hawkes likelihood and strong controls, run the frozen formal shards on the two
V100 hosts' CPUs, and preserve a fail-visible decision without spending GPU time or inspecting targets before
preregistration.

### What was done

- Audited all eight tracked free LOBSTER archives and exp130. Selected five nonduplicated symbol streams:
  AAPL/AMZN/GOOG/MSFT L10 full days and one SPY L50 hour, totaling 2,641,557 messages. Raw timestamps were
  nondecreasing but 3.145%--10.673% of increments were zero, so two deterministic tie policies were required.
- Verified the embedded vendor ReadMe hash and its external side convention: `+1` buy limit, `-1` sell limit,
  with the execution aggressor opposite the executed limit side. Recorded exact archive hashes, tracked-source
  history and the limit that the original exact download time is unavailable in `data/sample/LOBSTER/PROVENANCE.md`.
- Rechecked the primary methodological anchors: Hawkes (1971), Ogata (1988), Wu et al. queue-reactive Hawkes,
  and Martins--Hendricks on LOB Hawkes fit significance.
- Preregistered exp138 at `e4fa739b` before implementation, synthetic pilot, real queue-feature construction or
  target likelihood inspection. The protocol froze six marks, `nextafter` and `capped_uniform` tie policies,
  a 10/50/40 causal row split, row-`i-1` queue state, fixed three-scale Hawkes traces, six baseline models, a
  deterministic shifted-queue control, eight generated controls, optimizer settings and nine hard gates.
- Implemented analytic exponential-Hawkes traces/compensators/gradients, queue-reactive and combined
  likelihoods, exact time-rescaling diagnostics, stationary Poisson-cluster simulation, external ZIP/schema
  verification, formal sharding and the hard-gate merger at clean commit `09ded226`.
- Added eight focused continuous-time tests. Before formal execution, 21 focused observation tests, Ruff and a
  bounded strict-mypy check passed. Two explicitly non-formal 20,000-row smokes exposed that combined fits can
  hit the frozen 300-iteration cap; no model, threshold or formal setting changed afterward.
- Created fresh sparse clones at exact clean commit `09ded226` on both V100 hosts. Each formal service used one
  CPU process and one BLAS thread with CUDA hidden. Shard 0 owned GOOG/SPY plus synthetic replicates 0/2/4/6;
  shard 1 owned AAPL/AMZN/MSFT plus replicates 1/3/5/7. Both exited zero.
- Downloaded and hash-verified both raw shards, ran the formal merger, archived all data and wrote `RESULTS.md`.
  A second merge to `/private/tmp` was byte-identical to the canonical merged artifact.
- Final verification passed 29 focused observation tests, Ruff, bounded strict mypy, JSON gate assertions,
  `git diff --check` and local artifact hashes. Both services were inactive and both V100s reported 0% compute
  utilization with no compute processes after completion.

### Results / findings

- Exp138 formally **FAILED: 8/9 hard gates passed**. Exact provenance, timestamp/marks, causal split/no-lookahead,
  generated recovery, training nesting, aligned queue control, tie robustness and complete unfavorable reporting
  passed. The real-fit convergence gate failed.
- All eight two-mark generated Hawkes fits converged. Median immigrant-rate errors were 2.66%/1.66%, branching-
  entry errors were 2.06%--7.35%, Hawkes-minus-Poisson held-out gain was 0.04492 nats/event, and median residual
  mean/KS/absolute lag-1 were 1.0084/0.01074/0.00723.
- All real values were finite. However, aligned combined queue--Hawkes fits converged in 0/10 symbol-policy
  cells and shifted fits in 1/10. Failed-cell infinity-norm gradients were 0.245--0.644, far above the frozen
  `1e-5` fallback; multiple per-mark fits reached the 300-iteration limit.
- Aligned-minus-shifted held-out likelihood was positive for 5/5 symbols under both tie policies. Cross-symbol
  medians were 0.003731 (`nextafter`) and 0.003913 (`capped_uniform`) nats/event. Combined-minus-full-Hawkes
  medians were 0.001098 and 0.001161, but SPY was unfavorable at -0.0271/-0.0294. Because combined fits did not
  converge, these are diagnostic patterns, not a passed real-data result.
- Full Hawkes beat Poisson on every stream by 2.318--4.107 nats/event, while queue-reactive alone lost on AMZN
  and SPY. Real time-rescaling diagnostics also remained imperfect. This is useful evidence that event history
  cannot be omitted and that the current queue feature/model/optimizer package is not yet a calibrated market
  generator.
- Shard 0 consumed about 9.0 CPU minutes and shard 1 about 7.2; merged worker time was 964.02 seconds. Both GPUs
  stayed unused. Shard hashes are `3c28a680...172f5e` and `624ffd91...43c6d`; merged hash is
  `0e25df8c...2fecf2`.
- Exp138 contains no EcoMD latent, covers only one sample day per equity and one SPY hour, and cannot establish
  market physics or model-to-real validity. G3 remains open and paid L2 remains locked.

### Open questions / blockers

- The combined queue--Hawkes numerical fit needs a new, preregistered optimizer/KKT benchmark. Exp138 cannot be
  repaired post hoc, and its inspected test partition cannot become an independent confirmation set.
- No additional independent free days are currently tracked. Development can use generated controls and
  training-only/nonindependent schema fixtures, but real confirmation eventually needs unseen days or markets.
- G3 also still lacks a frozen EcoMD-to-external-message map and individual-order or explicitly bounded
  aggregate semantics. G0 remains AMBER with no non-equivalent invariant estimator.
- The original cloud-backed Mac clone remains `dataless`; the healthy sparse recovery clone and pushed branch
  are canonical.

### Next steps

1. Freeze exp139 as a numerical-method experiment before trying optimizer variants: generated and training-only
   development data, projected-gradient/KKT diagnostics, objective/gradient agreement, matched starts and a
   held-back synthetic validation suite. Do not reinterpret exp138's real test likelihoods.
2. If the numerical gate passes, add the repaired baseline to a future observation-bridge protocol evaluated
   on genuinely unseen dates/markets; until then keep paid data and positive model-to-real language locked.
3. In parallel, finish the five-nearest-method/theorem audit for a genuinely non-equivalent G0 estimator;
   additional observation baselines cannot resolve the NCS method novelty blocker.
4. Preserve the production resume backlog: real data cursor/sampler, scheduler/scaler, W&B, multi-node NCCL and
   interruption durability before claiming a production checkpoint contract.

## Session 7 (20:11) — projected-KKT repair launched; remote connectivity blocker

### Goal

Continue immediately from exp138's frozen optimizer failure: distinguish raw-gradient boundary artifacts from
genuine nonstationarity, preregister a numerical repair without reusing the exposed real test split, implement
and verify it, then run the generated and real-training-only formal shards on the two V100 hosts' CPUs.

### What was done

- Reconstructed the exp138 combined training objectives at the archived parameters and computed mathematically
  correct lower-bound projected gradients. This was a read-only audit: no candidate optimization and no new
  real held-out likelihood were evaluated.
- Froze exp139 at `338e0165`, then froze an explicit `1e-6` central-difference step, `5e-7` gradient tolerance
  and `1e-15` hand-bound tolerance at `732e1e7b`, all before candidate implementation or generated seeds.
- Kept the exp138 likelihood, six marks, features, fixed betas, timestamp policies, 10/50 split, aligned/shifted
  designs and queue/Hawkes starts unchanged. The repair refines both legacy endpoints through at most six
  deterministic 500-iteration L-BFGS-B stages and selects only projected-KKT-qualified endpoints.
- Added reusable bound-constrained KKT and optimizer-stage diagnostics, a real training-prefix-only loader,
  timestamp-only tie sentinel, generated intercept-only combined/full-Hawkes equivalence controls, a shared
  cross-node anchor, two-shard formal runner/merger and two new unit tests. Clean implementation commit:
  `e6a7eaafb61f272c8e6711947e1a7da9f427cc03`.
- Passed 31 focused observation tests, Ruff and bounded strict mypy. Two parallel dirty/non-formal 20k-row
  smokes exited zero in 13.14/13.90 seconds and produced identical anchor hashes.
- Verified both remote hosts were initially idle with about 29 GiB available RAM and GPUs at 0%/5 MiB. Created
  isolated sparse clones at exact clean `e6a7eaaf`, verified NumPy 2.4.6/pandas 3.0.5/SciPy 1.17.1 and launched
  one CPU process/BLAS thread per host with `CUDA_VISIBLE_DEVICES=-1`, CPUQuota 100% and MemoryMax 24 GiB.

### Results / findings

- The KKT audit reduced exp138's 20 cell-level raw gradient maxima from `0.245--0.644` to projected residuals
  `1.318e-4--1.092e-2`. The raw diagnostic did overstate active-bound violations, but every projected residual
  remained above `1e-5`; exp138's frozen FAIL is unchanged.
- Smoke results were numerically strong but non-formal: 120/120 selected targets reached KKT with maximum
  `4.21e-7`; 120/120 had both starts qualified; maximum dual-start objective gap was `3.69e-8`; minimum objective
  improvement over legacy was positive (`6.11e-8`); generated equivalence error was `1.11e-15`. Some targets
  consumed all six refinement stages, so the formal budget was not shortened.
- Formal shard1 (AAPL/AMZN/MSFT plus generated 1/3/5/7) completed cleanly in 819.84 seconds. Its environment,
  protocol hash `870a3360...4475f`, self-checks and exact implementation SHA were correct. Artifact size is
  1,703,829 bytes; SHA-256 is `4e4879d823c5672dd5ca0428efee3058996c14c751d7e679e6362844dd9aed35`.
- Formal shard0 (GOOG/SPY plus generated 0/2/4/6) was last observed active after about 16.8 CPU minutes, using
  about 0.60 GiB RAM, with no error, no partial artifact and GPU still at 0%. Both Tailscale IPs then became
  unreachable simultaneously. The local Tailscale process, route and `100.100.100.100` service remained up,
  indicating a remote/tailnet reachability failure rather than an experiment exception.
- Exp139 has **no formal PASS/FAIL yet**. Shard1 alone is not inspected as a scientific decision, shard0 is not
  duplicated, and no formal merger has run.

### Open questions / blockers

- Both V100 hosts must become reachable again. Shard0 may have completed after disconnection, may still be
  running if the host stayed online, or may have been interrupted if the host rebooted; current evidence cannot
  distinguish these cases.
- Restarting the local VPN or blindly rerunning shard0 could affect other connections or create duplicate formal
  execution and is not authorized. The remote service/artifact must be inspected first.
- G0 and G3 remain open, and paid L2 stays locked irrespective of the eventual numerical exp139 result.

### Next steps

1. Restore or confirm Tailscale reachability to both hosts; first inspect shard0 service, exit code and artifact.
2. If shard0 completed, download and hash-check both shards, merge once at exact `e6a7eaaf`, and archive the
   frozen decision. If interrupted, establish that no process/artifact remains before rerunning the same shard.
3. Never use exp139 to reinterpret exp138's exposed real test pattern as confirmation; a positive repaired
   baseline requires genuinely unseen dates/markets under a new protocol.
4. Update result report, Plan v4, memory and artifact hashes only after the formal merge has an actual decision.

## Session 8 (20:31) — exp139 recovered and frozen 8/9 failure

### Goal

Recover the two formal exp139 jobs without duplicating work, verify their execution/provenance, perform the
single frozen merge, and archive the decision with its claim boundary.

### What was done

- Reconnected to both V100 hosts and first checked service/artifact/process state. Both transient services had
  deactivated successfully, both artifacts were complete, no `run_optimizer_gate.py` process remained, and
  both GPUs were idle. No shard was rerun.
- Retrieved the artifacts and verified their local hashes against the remote files. Both are formal, clean,
  exact `e6a7eaaf` runs with protocol hash `870a3360...4475f`, identical cross-node anchor hash
  `8d692d1f...fb72`, and identical finite-difference/bound self-checks.
- Ran the frozen merger once, preserving the failing decision. Repeated the merge only to a temporary path as
  a deterministic serialization check; it was byte-identical to the archived result.
- Archived both raw shards, the complete merged JSON and `RESULTS.md`; updated Plan v4 and durable memory.

### Results / findings

- Exp139 formally **FAILED 8/9 hard gates**. The sole failure was generated equivalence: 14/16 direct-Hawkes
  references met the strict `1e-8` projected-KKT selection threshold, while replicate/target `(1,0)` and
  `(5,0)` stopped at `1.6248e-8` and `2.0216e-8` after all six refinement stages.
- The combined equivalence side was numerically successful: 32/32 starts met `1e-7`, maximum KKT was
  `9.8646e-8`, and maximum absolute objective difference from the direct reference was `1.7875e-14` nats/event.
  This localizes the failure to strict reference-solver convergence but cannot change the frozen FAIL.
- The real training-prefix repair succeeded on every numerical criterion: 120/120 selected targets and all
  240 starts met `1e-5`; maximum selected KKT was `8.9118e-7`, maximum complementarity `7.6208e-9`, maximum
  dual-start objective gap `6.5250e-9`, and every selected objective improved on the exact legacy rerun.
- Seventy-two starts consumed all refinement stages and 169/240 reached the stricter `1e-7` stop. The maximum
  raw selected gradient was `0.6485`, versus maximum projected KKT `8.91e-7`, confirming the active-bound
  diagnostic error in exp138 without retroactively changing its result.
- Shard0 used 1,412.49 worker seconds; shard1 used 819.84. Artifact SHA-256 values are
  `2fe252db...8276`, `4e4879d8...d35`, and merged `21818f9d...984`; both V100s were hidden and unused.

### Open questions / blockers

- A formal numerical PASS would require a new preregistered solver family and fresh generated seeds. The two
  failed references and all current generated streams are now development evidence and cannot validate tuning.
- Exp139 evaluated no real held-out metric. Exp138's exposed test split remains ineligible for confirmation;
  no independent unseen free days/markets are currently tracked.
- G0 still lacks a non-equivalent invariant estimator, and G3 still lacks a frozen EcoMD-to-message map,
  independent real confirmation and defensible order-level or explicitly limited aggregate semantics.

### Next steps

1. Preregister a narrow independent numerical gate only if its solver family and fresh generated seeds are
   fixed before execution; do not tune a tolerance around `2.02e-8`.
2. In parallel, prioritize the load-bearing G0 novelty/theorem audit and specify the frozen EcoMD-to-external-
   message map; optimizer work is infrastructure, not the NCS contribution.
3. Search only license-safe, genuinely unseen free dates/markets for a future observation confirmation protocol.
   Keep paid L2 locked until G0/G3 prerequisites justify it.

## Session 9 (20:55) — exp140 frozen active-set Newton implementation

### Goal

Resolve exp139's sole failed direct-reference component without relaxing its threshold, reusing formal seeds or
touching real held-out data; freeze a genuinely independent numerical gate before implementation.

### What was done

- Used a temporary untracked prototype only on exp139's already exposed generated endpoints. Added the exact
  convex Hawkes Hessian and a deterministic lower-bound active-set Newton direction with Armijo line search.
  This development run generated no new formal stream and opened no real archive.
- Preregistered exp140 at `6d3940b9` and pushed it before implementation. It freezes root `140202608`, 16 new
  streams, two fixed starts, a warm L-BFGS-B stage, at most eight Newton iterations, `1e-10` KKT/
  complementarity gates, derivative checks, exact parameterization identity, two shards and nine hard gates.
- Implemented reusable analytic-Hessian and active-set Newton results, full per-iteration diagnostics, two unit
  tests, a generated-only shard runner, fresh-seed isolation checks, shared anchor and hard-gate merger.
- Ran two dirty/non-formal smoke shards only from root `140202610`; formal root `140202608` remains ungenerated.
- Passed 25 focused observation tests, Ruff and strict mypy on the modified core/runner.

### Results / findings

- On the disclosed exp139 development endpoints, one Newton step reduced all 16 KKT residuals to at most
  `4.3877e-15`; maximum absolute objective movement was `1.3323e-15`, and Hessian condition numbers were
  `6.82--7.68`.
- Smoke produced 8/8 qualified endpoints. Maximum KKT was `6.8662e-12`, maximum complementarity `4.7236e-13`,
  maximum dual-start objective gap `2.2204e-16`, and no polished objective increased from its warm endpoint.
- Direct versus intercept-only combined evaluation agreed to `2.2204e-16` in objective, `5.2297e-17` in the
  gradient chain rule and `5.5511e-17` under the inverse parameter transform. Both smoke anchor hashes matched.
- The self-check errors were `1.72e-11` for gradients, `4.56e-11` for Hessians, `2.78e-17` for symmetry and
  exactly zero for the lower-bound case, all far inside their frozen thresholds.

### Open questions / blockers

- Smoke is only execution evidence. Exp140 has no formal PASS/FAIL until clean exact-commit shards generate all
  16 untouched formal streams and the frozen merger evaluates all nine gates.
- A PASS will close only the direct-reference numerical sub-blocker. There is still no unseen real confirmation,
  frozen EcoMD-to-external-message map or non-equivalent G0 estimator.

### Next steps

1. Commit and push the verified implementation as a clean immutable SHA.
2. Launch even/odd CPU-only shards on the two V100 hosts with CUDA hidden, merge once, and archive every outcome.
3. Do not revisit exp138's real test partition or present exp140 as a physics/result contribution.

## Session 10 (21:07) — exp140 formal 7/9 failure and solver-chase stop

### Goal

Run the untouched exp140 formal seeds from the clean implementation, merge exactly once, diagnose every failed
gate without changing thresholds, and decide whether further optimizer work has scientific value.

### What was done

- Verified both V100 hosts were idle and the exp140 path absent. Created isolated sparse clones at exact clean
  `1312b5bef6748a8363cfb0326b3a91a2c51d27f7`, with matching Python 3.11.15, NumPy 2.4.6 and SciPy 1.17.1.
- Launched even/odd formal services with one CPU/BLAS thread, `MemoryMax=4G` and CUDA hidden. Both deactivated
  successfully; no prior directory, seed or process was reused and both GPUs remained at 0%/5 MiB.
- Downloaded and hash-checked both raw shards, verified formal ownership/SHA/protocol/anchor/self-checks, ran the
  frozen merger once, and reproduced its serialized output byte-for-byte in a temporary second merge.
- Archived the raw shards, merged result and complete result report; updated Plan v4 and durable memory.

### Results / findings

- Exp140 formally **FAILED 7/9 gates**. Chronology, fresh-seed isolation, derivative/KKT checks, monotone repair,
  algebraic equivalence, cross-node determinism and complete reporting passed. Newton convergence and start
  robustness failed.
- 62/64 endpoints met `1e-10`; median KKT was `3.17e-15`. Replicate 5/target 1/rate-zero stopped at
  `2.9868e-10`, and replicate 8/target 1/positive stopped at `2.4487e-10`. Maximum complementarity remained
  inside threshold at `5.1366e-11`.
- Only 30/32 pairs were dual-qualified because of those endpoints, although maximum/median two-start objective
  gaps were `2.22e-16`/`0`. Algebraic objective, gradient-chain and inverse-transform errors were at most
  `3.33e-16`, `4.37e-16` and `5.55e-17`.
- Both failed Hessians were well conditioned (about 6.7--7.0). Directional decrease was only `1e-20`; Armijo
  backtracked 23--25 times until candidates became bit-identical, then recorded eight accepted zero-displacement
  iterations. This is a concrete float64 stagnation/reporting defect, not evidence against objective identity.
- Shards used 3.853/3.765 worker seconds and hashes `c1d7f0ed...a69f5`/`598b7510...14719`; merged hash is
  `7216af08...32871`. Formal anchor hash `108bc914...748c` matched exactly.

### Open questions / blockers

- The frozen formal numerical reference gate remains failed. It cannot be repaired by relabeling `3e-10` as
  close enough, even though this discrepancy is scientifically negligible relative to the real `1e-5` gate.
- G0 novelty and G3's EcoMD-to-message semantics/unseen real confirmation dominate the publication risk.

### Next steps

1. Add a regression-tested bit-identical-candidate guard so future solvers terminate honestly as stagnated;
   do not rerun exp140 or preregister exp141 merely to cross `1e-10`.
2. Return to the G0 nearest-method/theorem audit and freeze the EcoMD-to-external-message map before any new
   empirical claim.
3. Keep paid L2 locked and preserve exp138's test partition as already exposed.

## Session 11 (21:10) — post-result float-stagnation guard

### Goal

Fix the software defect diagnosed by exp140 without modifying or rerunning its frozen formal result.

### What was done

- Added an exact `np.array_equal(candidate, current)` check inside the active-set Newton line search. A
  bit-identical candidate now terminates immediately with an explicit float-precision stagnation message rather
  than being recorded repeatedly as an accepted Armijo step.
- Added a regression fixture with a sub-ULP Newton direction and verified one honest failed trace instead of
  eight zero-displacement iterations.
- Updated the formal report, Plan v4 and durable memory to distinguish the post-result software behavior from
  immutable implementation `1312b5be`; exp140 was not rerun and remains 7/9 FAIL.

### Results / findings

- Twenty-six focused observation tests pass after the guard; Ruff and strict mypy pass.
- The guard improves termination/reporting only. It does not lower either archived residual or supply new
  evidence, so no scientific gate changes.

### Open questions / blockers

- G0 still has no non-equivalent estimator/theorem, and G3 still has no frozen external observation semantics
  or independent unseen real panel. These now take priority over numerical polishing.

### Next steps

1. Commit and push the post-result guard separately from the formal artifact commit.
2. Resume the G0 prior-art/non-equivalence audit and G3 EcoMD-to-message specification; do not launch exp141.

## Session 12 (21:38) — G0 forward audit closes the NCS method route

### Goal

Complete the 2024--2026 nearest-method audit, decide whether any v1 mathematical primitive survives, and stop
or authorize candidate experiments according to the frozen G0 rule.

### What was done

- Audited 33 primary papers across steady-state sensitivity, stochastic/discrete differentiation, coupled
  debiasing, persistent chains, controlled Markov stochastic approximation, evolving-target particle methods,
  mixing diagnostics and invariant-measure learning. The closest 2024--2026 algorithms/theorem scopes were
  checked directly rather than relying on secondary summaries.
- Compared all three permitted v1 exits against SOUL, Jarzynski EBM/JALA, persistent-CD uniform-in-time theory,
  ICML 2026 SOSMC, Rhee--Glynn/Jacob, StochasticAD/GGE and Markov-data lower bounds.
- Wrote the frozen forward-audit decision, updated the v0 spec and Plan v4, and created Plan v4a for a
  falsifiable simulator-audit/benchmark plus EcoMD's first formal release.

### Results / findings

- G0 is formally **FAIL**. The v0 estimator is direct composition, and the forward audit found no new
  cross-parameter coupling, weaker-assumption finite-budget residual theorem or variance--cost theorem.
- Warm-started changing kernels are already explicit in SOUL. Weighted reuse across evolving `pi_theta` is
  covered by Jarzynski/JALA/SOSMC, including Feynman--Kac weights and ESS-triggered resampling. Persistent-CD
  now has explicit uniform-in-time theory under stated assumptions.
- Empirical ESS/IACT/split-chain/coupling diagnostics can support fail-visible reporting but cannot certify an
  unobserved metastable mode without contraction, minorization, meeting-time or other model-specific bounds.
- Candidate E0--E3 and NCS WP3/WP4 are stopped. The old 7--12% NCS probability and scale-up budget are void;
  no purchase or expansion is unlocked.

### Open questions / blockers

- The fallback's F0 novelty still needs a separate simulator-verification claim audit.
- The current EcoMD L2 adapter has not yet passed a field-level external observation-semantic gate. Synthetic
  recovery alone does not establish real mapping or individual-order semantics.

### Next steps

1. Freeze the EcoMD state-to-external-observation field map and identify every unsupported/aliased quantity.
2. Preregister a zero-purchase synthetic observation-semantic falsification gate before implementation.
3. Only if F0/F1 survive, launch the low-cost state/law/observation defect screening; otherwise prepare the
   narrower EcoMD v1 software release.

## Session 13 (21:49) — EcoMD observation support boundary

### Goal

Audit the actual EcoMD-to-external-L2 mapping before launching any further likelihood or real-data experiment.

### What was done

- Traced the source semantics from `EcoMD.step` and the price-formation observables through
  `EcoMDL2Adapter`, `DynamicL2Generator`, and the exp138 LOBSTER projection.
- Froze `papers/proposal/ecomd_observation_map_spec_v0.md` with four explicit support levels, three permitted
  price choices, causal latent-inference requirements and an identifiability ledger.
- Updated Plan v4a so the failed message/order-level route cannot be silently reused; retained only the weaker
  `aggregate_bin` + P3 route as an AMBER hypothesis for a preregistered semantic-contract test.

### Results / findings

- The current adapter emits exactly one event per EcoMD step and has no fitted conditional event clock.
- Its sizes are in latent model units, while external sizes are shares/contracts. Its consecutive IDs identify
  events, not persistent orders referenced by cancellations or executions.
- The adapter book price evolves independently of EcoMD's internal `log_price`/`log_return`, producing a
  dual-price contradiction if both are treated as one observed market path.
- `latent_flow_alignment` is a dimensionless latent predictor, not empirical CKS OFI. The current repository
  also lacks a causal filter connecting an unobserved EcoMD path to a real held-out message path.
- Therefore the current map is valid only as a `synthetic_fixture`; real `aggregate_event` and
  `individual_order` claims FAIL. Aggregate-bin validation remains possible but unproven.

### Open questions / blockers

- A machine-readable contract must still show that the narrower declaration is mechanically enforceable and
  that all known semantic mutations fail before fitting.
- F0 simulator-audit novelty remains unaudited, so even a passing semantic contract does not yet support a
  TMLR general-method claim.

### Next steps

1. Commit and push the frozen observation-map decision.
2. Pre-register a deterministic CPU-only semantic-contract experiment in a separate commit before coding.
3. Implement the validator and exact negative-control suite; do not fit real data or launch a GPU run.

## Session 14 (21:50) — preregistration of the observation-contract firewall

### Goal

Freeze the machine-enforcement question, cases and stopping rule before implementing or running the validator.

### What was done

- Reserved exp142 to avoid reusing exp141, which the prior numerical-work log explicitly stopped.
- Pre-registered three positive support declarations, twelve isolated semantic mutations, a strict JSON
  round-trip, a callback firewall and a fixed-seed runtime probe of the current adapter.
- Froze stable errors for dual prices, event clocks, physical-bin width, fake order IDs, missing order
  lifecycle, market-size units, latent/OFI relabeling, sign reversal, test-conditioned latent, and invalid
  latent-versus-observation-only claims/controls.

### Results / findings

- No result has been generated and no implementation exists at preregistration time.
- The formal run is deterministic, CPU-only, uses no external archive and is expected to finish within two CPU
  core-hours. It is an enforcement preflight, not evidence of real-data fit.

### Open questions / blockers

- A passing self-declaration validator cannot attest that a matching engine or causal filter exists; those
  capabilities need separate code-backed experiments. For this reason no positive individual-order fixture is
  included.
- F0 novelty and empirical aggregate-bin validation remain later independent gates.

### Next steps

1. Commit and push the preregistration before creating implementation files.
2. Implement the immutable contract, strict serializer, validator, fit firewall and adapter probe.
3. Run smoke/tests, freeze a clean implementation commit, then execute the formal runner exactly once.

## Session 15 (22:02) — exp142 implementation and non-formal smoke

### Goal

Implement the preregistered observation-semantic contract without inspecting the formal seed or changing any
frozen case, code or pass criterion.

### What was done

- Added immutable typed declarations for support, data scope, clock/count law, price linkage, size units,
  identity semantics, latent access/sign/input claims and required negative controls.
- Added strict canonical JSON parsing/round-trip, deterministic validation reports and a public callback
  firewall that raises before invoking work on an invalid declaration.
- Added the three frozen positive fixtures, twelve single-defect cases, explicit support-escalation checks and
  the current-adapter structural probe to the exp142 runner.
- Added focused tests and an implementation-embedded quality gate for pytest, Ruff and strict mypy.

### Results / findings

- The first smoke attempt stopped before evaluation because direct subdirectory execution did not put the repo
  root on `sys.path`; no formal seed/result was touched. The runner now resolves and inserts its own repo root.
- The non-formal smoke root `142202609` passed all nine gates. Eighteen focused contract tests and 31 related
  observation tests pass; Ruff and strict mypy pass.
- The smoke confirms only implementation plumbing. The formal root `142202608` remains unrun and uninspected.

### Open questions / blockers

- Formal chronology requires the implementation and tests to be committed in a clean tree before the single
  formal execution.
- Even a formal PASS will leave aggregate-bin measurement/reconstruction and F0 novelty untested.

### Next steps

1. Commit and push the implementation without any formal output.
2. Run exp142 formally once from that clean SHA and archive every result.
3. Update F1 and the active queue according to the immutable formal decision.
