# 2026-06-29

## Session 1 — root-cause analysis of the negatives + positive reframe + exp 125 design

**Goal.** User directive: (1) treat *building EcoMD* as the positive result; (2) diagnose the root
cause of each negative result — fix if fixable, de-emphasize if not; (3) decide whether any negative is
*fundamental* (principle/data). Also: design the experiments to run on the 2+2+4+8 H20 fleet, and keep
GenAI-workshop framing positive (don't headline negatives), within the no-downgrade rule.

**What was done.**
- Reviewed the two workshop drafts + NCS draft + strategy memo (earlier this session) → claims,
  gaps, and a fix-list.
- **Root-cause analysis** (`papers/proposal/paper_a_rootcause_and_reframe_2026-06-29.md`): the light
  steady-state tail (N1) is **fundamental**, from three first-principles reasons — (P1) Boltzmann
  light tail of a smooth confining potential; (P2) **CLT self-averaging** of ED=κΣΔs over N≈10⁴
  finite-variance, short-ξ agents (the dominant effect); (P3) sub-critical coupling (exp 116/120
  smooth crossover, no N_c). Heavy tail = driven transient because the shock imposes coordination
  transiently; CLT reasserts on relaxation. Grounded in code (`price_formation.py:344`,
  `integrator.py`, `ecomd.py`).
- **Solvability** (3 routes): A heavy micro-noise (Lévy/Student-t — *in code & runnable*) CAN install
  a stationary tail but the index is hand-set and trades off clustering (the Pareto cost); B SV /
  multiplicative — explored, hit the ceiling (exp 119/108-110); C criticality — explored, no sharp
  N_c. ⇒ N1 "fixable only by changing the model class, at the cost of the temporal facts" = the
  Pareto-frontier finding. N2 (real return tails stationary) is a **data fact**, the predicted
  boundary, not a model defect → demote to a one-paragraph scope note. N3 → one sentence (Paper-B
  carve-out). N4 → present as a positive structural result.
- **Code (smoke-tested, no regression):** two new *non-mechanical* shock channels
  `temperature_spike` (T×m) and `liquidity_drop` (γ×1/d), transient multiplier active for `--shock-dur`
  steps then auto-clearing (endogenous relaxation), reset per rollout. `run_large.py` inference
  overrides `--noise-dist/--noise-df/--noise-levy-alpha/--n-agents` so the root-cause ablations are
  pure rollouts on existing checkpoints. `scripts/smoke_exp125_channels.py` (T7–T10) passes;
  `smoke_exp123_plumbing.py` still passes; Lévy/Student-t rollout paths run end-to-end.
- **exp 125** (`experiments/125_rootcause_controllability/{DESIGN,PREREG}.md`): G-A rigor (n=30 CIs +
  warm-up table), G-B controllability atlas (temp/liq mechanism-robustness, GenAI lead), G-C τ(dose)
  relaxation law (ML4PS lead), **G-D root-cause ablations** (D1 heavy-noise→stationary tail?;
  **D2 steady α_ED vs N at fixed dynamics = the CLT self-averaging proof** — the clean test exp 120's
  train-at-N confounded), G-E 2nd-generator pitfall. **Binding PREREG**: every arm pre-committed to a
  positive-content statement for *either* outcome (the honest-framing guardrail for "more positive
  results"). Driver `scripts/gpu_exp125_atlas.sh` (worker/ablate/eval/fanout), dry-run validated.
- Committed + pushed `01dfbecf3` to `feature/paper-a-figures-voice`.

**Findings.**
- The central negative is reframed into the paper's main *positive mechanistic claim*: we explain WHY
  endogenous fat tails are hard (CLT) and can switch the heavy tail on/off by intervention — which
  observation of real markets cannot do. The light steady state is the *evidence*.
- The decisive, cheap experiment is **G-D2** (α_ED vs N at fixed trained dynamics): a rising α_ED(N)
  is the direct interventional signature of self-averaging.

**Open questions / next steps.**
- Eval-time analysis scripts still to write: `aggregate_atlas_cis.py`, `fit_tau_dose_law.py`,
  `score_noise_ablation.py` (11-fact + steady-α_ED across the noise/N arms; note run_large already
  emits the 11 facts per rollout in `inference_merged.json`, and `score_transfer_law.py` gives
  warm-up-discarded α). Write before final eval.
- One-step T/γ shock is weak on the untrained smoke sim → use `DUR=20` primary on trained ckpts; the
  dose/dur threshold is what G-B/G-C map.
- Then fold G-D into the workshops: GenAI leads with EcoMD-as-generator + atlas + warm-up protocol +
  "know your generator's tail" mechanism; ML4PS leads with the driven transient + CLT mechanism + τ law.
- Run on H20 (launch commands handed to user); pull ckpts from R2 first.
