# 2026-06-19 — Paper A → NCS push: Stage 2b (market-realistic shock) + Stage 2d (τ)

## Session 1 (10:30) — decision: push Paper A to Nature Computational Science

**Goal.** Decide the next step to *close* Paper A given `papers/proposal/paper_a_b_ncs_strategy_2026-06-19.md`.

**Decision.** User chose **push to NCS** (over "close-strong at NeurIPS" / "close-fast"). That commits
both NCS gates: **Gate 1** market-realistic shock channel (Stage 2b) + **Gate 2** real-data pilot
(Stage 3). Shared immediate critical path (2b + τ-fit + verdict hygiene); diverges at the Stage 3 fork.

## Session 2 (10:40) — Stage 2b: the `price_jump` market-realistic shock channel (NCS Gate 1)

**Goal.** Rebut "state_kick is a mechanical latent perturbation" with an exogenous, observable shock.

**Done (code, all on Mac, committed `ff6b248d2`).**
- `ecomd/models/ecomd.py`: new **`price_jump`** channel. First attempt (perturb `log_price` +
  `last_log_return`) was caught by the smoke as a *no-op on the return series* — returns are recorded
  from the per-step `log_ret`, NOT `diff(log_prices)`, and price formation overwrites `last_log_return`.
  Reworked: `_apply_shock` stashes an exogenous return `r=−mag·σ`; `step()` folds it into the
  **realized return** right after price formation (enters the return series, shifts the price level,
  re-drives the vol EWMA → clustering). The heavy tail that follows is **endogenous**.
- `run_large.py`: `--shock-type price_jump` (+ `--shock-sign`). `score_transfer_law.py`: `--verdict
  --channel {kick,jump}` → `verdict_<asset>_jump.json`. `gpu_exp123_stage1.sh`: `jump*` arms.
  `gpu_exp123_stage2b.sh`: 2-card driver. Smoke T5 added (validates the gap, no backward leak, no
  state_kick regression). `PREREG_stage2b_2026-06-19.md` froze the H1–H4 reuse + the event-window
  caveat + the binding decision.

**Validated.** Mac smoke T1–T5 all pass. GPU smoke on `.56` (trained spx ckpt, mag6): gap injected
at step 3000 (|r|≈0.059 ≈ 3.5× pre-shock median, clear window max) + elevated post-shock vol.

**Launched.** Stage 2b detached on **100.91.139.56** (2× H20), PID 1075544: spx `jump{1,2,3,6,12}`,
n≈30/arm, reuse spx ckpt + reuse committed spx control. Auto eval → `--channel jump` verdict → push.
ETA ~2.5–3 h. Log `experiments/123_driven_transient/_stage2b_20260619_105208.log`. (4 rollouts in.)

## Session 3 (11:00) — Stage 2d: relaxation-timescale τ (free analysis)

**Goal.** Quantify "relaxes on a finite timescale" from the committed windowed α_ED(t) reports.

**Done.** `scripts/fit_relaxation_tau.py` — fits `α(t)=α_∞−A·exp(−(t−t_min)/τ)` on the post-shock
recovery limb. Ran on spx kick arms → `tau_report_spx.json`.

**Results (spx, kick channel).**
- **Burn-in (t=0) template: τ≈20 steps** (R²=0.98) — matches the known ~20-step burn-in exactly.
- Light shocks peg τ at the windowing-resolution floor (~20 = stride); **deep/saturated shocks
  (kick3/6/12) give τ≈240 steps** (238/236/243, tight, R²≈0.8). So the **driven transient relaxes
  ~10× slower than the cold-start burn-in**, and **τ grows with dose** (deeper from equilibrium →
  slower return). Resolved relaxations (kick0.5/2/3/6/12) cluster ~200±45 (CV~22%).
- **Quantifies** the prior "~1k steps": deep-shock τ≈240, full recovery ~3–5τ ≈ 0.7–1.2k. Consistent.

**Open questions / blockers.**
- τ≈20 for light shocks is at the W=500/stride=100 resolution floor → can't resolve fast relaxations
  without re-windowing the (gitignored, on-box) trajectories. Deep-shock τ≈240 is the robust number.
- Verdict hygiene (the `verdict_spx.json` P*/H2 strict-monotonicity vs the resolved sigmoid) still owed.
- Stage 2b result pending (collect from `.56`); Stage 3 real-data pilot (NCS Gate 2) not started.

**Next steps.** Collect Stage 2b verdict (jump channel) → if H1∧H2∧H3∧H4 past the event window, NCS
Gate 1 met. Then verdict hygiene + Stage 3 real-data pilot (free crash data: 2020 COVID equity,
Luna/FTX crypto via Binance, SVB) + rewrite the Paper A spine to the NCS 7-section structure.

## Session 4 (11:30) — Stage 3 real-data pilot (NCS Gate 2) → driven transient does NOT replicate

**Goal.** Test whether real crashes show the EcoMD α-dip-and-recover (Gate 2, the venue-decider).

**Done.** `scripts/fetch_crash_windows.py` pulled free 1m BTC/ETH from Binance for Luna (2022-05) +
FTX (2022-11) + a calm control (2023-07). Windowed (reused `--windows`) + a robust regime-level Hill
+ a vol-standardized confound check (`scripts/analyze_crash_tails.py`). Findings doc
`papers/paper_a_methods/stage3_realdata_pilot_2026-06-19.md`.

**Result (ROBUST NEGATIVE).** Real crashes do NOT drive the α-dip. Regime-level: Luna BTC pre α=2.54
→ crash α=**3.97** (tail *lighter*, Δ+1.43); ETH +1.76. FTX ≈flat (Δ−0.16/−0.23), inside the calm
null 2.63±0.17. Vol-standardized (removes the high-vol→lighter-Hill confound): Luna still lightens
(3.42→4.47), FTX flat — so NOT a vol artifact. Real fat tails are ~**stationary cube-law** (calm AND
crash) = Gabaix–Plerou–Stanley universality. The EcoMD transient is a property of the *simulator*
(het_mass off → no stationary heavy-tail source), NOT of real markets.

**Implication (reviewer-2 call).** **NCS Gate 2 FAILS** — Paper A cannot be sold as a real-market
discovery. The honest, defensible paper is **simulator-physics + a model–reality gap** (NeurIPS/ICML):
"in EcoMD heavy tails are a driven transient and the steady state is light-tailed, *unlike* real
markets whose cube-law tail is stationary — localizing a missing stationary mechanism in this sim
class." This is the original "close-strong NeurIPS" path. Stage 2b (Gate 1, market-realistic channel)
still valuable — it strengthens the SIMULATOR claim regardless of venue.

**Open / next.** Surface the venue pivot to the user (NCS-stretch → NeurIPS-close). Collect Stage 2b
(still running on .56). Caveats on the negative: 2 episodes, crypto only; transient could live in a
non-return observable (order-flow/L2 — Tardis, not yet bought) — a future direction, not this pilot.

## Session 5 (11:35) — broaden the negative (user: "broaden before deciding") → CONFIRMED

**Goal.** Firm up the Stage 3 negative before abandoning NCS: more episodes + a formal null test.

**Done.** Added 3 crypto crashes (COVID-2020 −56%, China-ban-2021 −47%, Celsius/3AC-2022 −41%) +
a 3-month calm null source. `scripts/null_test_crash_tails.py`: Δα = α(crash 2d) − α(pre 5d),
vol-standardized; null = same window-pair slid across the calm stretch (n=170, Δα=+0.02±0.43).

**Result — NEGATIVE CONFIRMED (formal).** Across 5 crashes: pooled Δα z=**+1.03** (slightly LIGHTER,
opposite the prediction). Per-episode: China-ban the only hit (Δα−0.91, p=0.01); Luna +1.05 and
Celsius +0.83 significantly LIGHTER; COVID/FTX flat. 1/5 = within the 5-test false-positive rate.
Real return tails are stationary cube-law across calm AND crash. Underpowered-2-episode caveat removed.

**Note.** The crashes that *lighten* (Luna, Celsius) were multi-day grind-downs; the one that heavied
(China-ban) was a sharp single-day drop — a hint crash *shape* matters, but no clean signal; pooled
effect null/opposite.

**Infra mishap + fix.** `git pull --rebase origin HEAD` (partial-clone remote) triggered a full-branch
rebase onto an old commit → broke the local tree; `git rebase --abort` fully restored it. Fixed the
same trap in `gpu_exp123_stage2b.sh` (explicit branch, never `HEAD`); killed + relaunched Stage 2b on
.56 with the fixed driver (was 16/150 rollouts in).

**Recommendation (reviewer-2).** Negative now robust + formally tested → **pivot to NeurIPS
simulator-physics + model–reality gap.** Awaiting the user's now-informed venue call.

## Session 6 (15:00) — Stage 2b finished → market-realistic channel does NOT reproduce the transient

**Result (NEGATIVE, validated).** The `price_jump` (market-realistic price-gap) run finished on .56
(5 doses, 300 rollouts; driver's fixed auto-push worked). Verdict `verdict_spx_jump.json`:
**H2=False, H4=False → "A — ARTIFACT: no revival."** post-shock min α_ED flat at ~4.5–4.6 (vs
state_kick's 4.7→**0.5**). **Checked it's not an invalid test** (price_jump perturbs the return, not
positions → could show in α_ret not α_ED): α_ret pre≈7.7 → min 7.3 (mag1) … 6.0 (mag12) — a weak
dose-dependent nudge that **stays LIGHT** (α≈6, nowhere near cube ~1.5). Holds on **both** observables.

**Interpretation.** The driven transient is **specific to displacing agent latent states** (state_kick
= the burn-in mechanism); an exogenous price/return shock barely moves the tail (~100× weaker, stays
light). So state_kick and burn-in are the *same* latent-equilibration mechanism — the transient is a
**model equilibration phenomenon, not a response to market shocks.**

**Compounding picture (honest).** Two-channel robustness (Stage 2b) FAILS + real markets show nothing
(Stage 3) ⇒ the EcoMD driven-transient is a fairly specific latent-equilibration artifact, **not** a
robust market-physics discovery. **What survives solidly: the burn-in MEASUREMENT CORRECTION** (the
methods contribution) + the mechanistic localization (latent displacement drives it; price shocks
don't). E2 news/info channel (Stage 2c) is now low-value — same indirect mechanism as price_jump,
likely also negative.

**Venue implication.** This further weakens the NCS *positive-discovery* framing (NCS odds → ~8–15%);
the paper leans more toward a **measurement-correction + bounded-characterization** identity → TMLR /
PRE / measurement-methods better calibrated than NCS. Surfaced to the user; their ladder still holds
(TMLR floor) but NCS is now a longer shot.

## Session 7 (2026-06-20 09:30) — OFI logging added + sim-side OFI transient analysis

**Done.** Added per-step **order-flow imbalance** ρ=Σdpos/(Σ|dpos|+ε)∈[-1,1] to the recorder
(`observables.py`/`price_formation.py`/`ecomd.py`/`run_large.py`, smoke T6, commit e19d868b5) — the
no-buy Route-A prerequisite. Then ran fresh spx control/kick6/jump6 rollouts (OFI logged) on .56 +
`analyze_sim_ofi_transient.py` → `ofi_transient_spx.json` (commit ccb49b744).

**Result.** Windowed OFI measures (|ρ|, std, lag-1 memory, saturation) around shock@3000, n=30/arm:
- **kick6 → strong OFI transient on ALL 4 measures.** |ρ| 0.009→0.042→0.006; std 0.018→0.129→0.008;
  **memory 0.018→0.995→−0.03** (order flow becomes near-perfectly persistent at the shock, then
  relaxes); sat 0.003→0.035→0. Burst partly by construction (coherent displacement), but the
  **relaxation is emergent.**
- **jump6 → NO OFI transient — bit-for-bit identical to control** on every measure. The market-realistic
  price gap is **inert**: zero coherent order-flow response.

**Interpretation.** (1) **OFI does NOT rescue price_jump** — the channel fails on tail AND order flow;
price shocks just don't propagate into agent flow in this model. Two-channel robustness definitively
dead. (2) **Reframe:** a real crash is *coordinated agent liquidation* (= state_kick), NOT an exogenous
price gap (= price_jump) → state_kick is the better crash analogue, and its OFI signature (coherence +
memory burst that relaxes) is a **concrete falsifiable prediction for real crash order-flow** — Route A's
L2 test, distinct from the stationary return tail. OFI logging works + is sensitive (picks up the
transient dramatically), so the Route-A measurement substrate is validated.

**Next.** Route A real-data (needs Tardis L2 buy, justify via Paper B); meanwhile the sim-side OFI
prediction is in hand. Recorder-half of work-list E5 DONE.
