# Session 1 (12:00)

## Goal

Move checkpoint binaries out of the Mac working copy and Git history, store them in Cloudflare R2, manage metadata through Supabase, and document the H20 checkpoint offload workflow.

## What Was Done

- Added `ecomd.data.checkpoint_sync` for scanning, uploading, Supabase upserts, verified cleanup, Git index cleanup, and Supabase migration.
- Added the Supabase `public.checkpoints` migration and `.env.supabase.example`.
- Uploaded all local checkpoint binaries to Cloudflare R2 and upserted the checkpoint catalog into Supabase.
- Removed local checkpoint binaries after verifying the corresponding R2 objects.
- Committed the migration/offload change locally.
- Rewrote Git history with `git-filter-repo` to remove historical checkpoint binaries.
- Restored the `origin` Git remote after `git-filter-repo` removed it.
- Documented the new R2 + Supabase checkpoint workflow in `README.md`, `CLAUDE.md`, `scripts/README.md`, and `ecomd/data/storage_setup.md`.
- Updated project memory so future H20 runs use the checkpoint offload workflow by default.

## Results / Findings

- Supabase `checkpoints` contains 3986 checkpoint records.
- Local `experiments` has 0 remaining `checkpoint*.pt` files.
- Git tracks 0 checkpoint `.pt` files.
- `.git` size dropped from about 3.1G to 265M after history rewrite and cleanup.
- `pytest tests/test_checkpoint_sync.py` passed with 4 tests.

## Open Questions / Blockers

- Remote GitHub history still needs a force push after review because local history was rewritten.
- Other clones of this repo will need to re-clone or hard reset after the rewritten branch is pushed.

## Next Steps

- Review the rewritten branch.
- Push with `--force-with-lease` when ready.
- On every H20 training run, archive checkpoints with `python -m ecomd.data.checkpoint_sync sync --delete-local`.
