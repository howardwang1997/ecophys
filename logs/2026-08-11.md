# 2026-08-11

## Session 1 (00:04) — strict typing closure and clean S0 rebuild

### Goal

Close the source-preview R2 typing blocker, verify the committed package rather than a dirty worktree, and leave
the two V100s idle until a unique M0 configuration is frozen.

### What was done

- Re-ran the configured full-package strict check and corrected the true baseline to 121 errors in 26 files;
  the earlier 83/17 count covered only `train_distributed.py` and its import graph.
- Added precise types across simulator/integrator, recurrent state, potential, price, BPTT, training, baseline,
  inference, data-ingest and risk paths; added `types-PyYAML` and `types-requests` to the dev dependencies.
- Preserved runtime semantics while adding explicit impossible-state guards and a non-empty-target guard.
- Committed and pushed `4b741d6692e934540f9849c13d3d89b3878c5426`.
- Re-ran strict mypy and all tests from the clean commit. The first `pytest` executable invocation resolved an
  old editable install in the damaged workspace; `conda run -n ecophys python -m pytest` correctly exercised
  the current checkout. This invocation rule is now part of the verification record.
- Built the allow-listed source archive twice, extracted both independently, built a wheel in each tree,
  installed one wheel into an isolated target and re-ran the deterministic CPU smoke, artifact tests and mypy.
- Compared Ruff diagnostics against the exact added-line ranges in the commit. No added line has a finding;
  the separate full-package historical Ruff baseline is 342 findings, mostly scientific Unicode and math-style
  names, and was not hidden by weakening the lint policy.

### Results / findings

- Strict mypy: **PASS**, zero errors across 66 source modules.
- Repository tests: **76 passed**. Extracted source-preview tests: **27 passed**.
- Source archive: 89 files, 246,840 bytes, two builds bit-identical, SHA-256
  `7bec028d8a3468f546792b868b4991d1eb900b1e2b6af825294183faaf1afba6`; forbidden path/suffix matches: zero.
- Wheel: 248,107 bytes, two independent source trees bit-identical, SHA-256
  `5535add9760a4627698cfbe0d76148f83baaabc32622490a32a9dd29e77a3caa`.
- Isolated wheel smoke remains exact for arbitrary chunking and checkpoint resume, with 35 finite nonzero
  gradient-bearing parameters and returns hash
  `38398a13e5ce56cf6bd3dd789884c517488c8d39b3da8aa035968845b8d1db71`.
- S0 R0--R3 are operationally **PASS** and S0 is ready but not published. No checkpoint or positive fidelity
  claim is authorized. S1 remains blocked.

### Open questions / blockers

- EcoMD v1 still lacks a unique canonical M0 architecture/configuration and formula-config-code traceability.
- No eligible state-complete checkpoint exists, and historical stationary fidelity remains failed.
- The full Ruff baseline remains maintenance debt but is not a newly introduced defect or a hidden release gate.

### Next steps

1. Freeze one M0 architecture, config schema, seed/dtype/state/checkpoint policy and resource envelope.
2. Run only a tiny CPU M1 path/stationarity-harness smoke from that frozen config.
3. If the smoke passes, queue the first short fp32 V100 pilot; reserve the second V100 for independent repeat.

## Session 2 (00:20) — M0 contract implementation and pre-commit CPU gate

### Goal

Turn the pre-implementation EcoMD v1 freeze into one executable configuration and close the free mechanical
gates before using either V100.

### What was done

- Added `configs/ecomd_v1/m0_reference.yaml` and a strict loader that rejects scientific changes, unknown
  fields, unsafe release semantics, a too-short loss horizon, or deviations from the frozen data/compute split.
- Added Kac normalization to dense and stochastic pair potentials. Corrected the stochastic estimator to use
  the actual partner count when `N-1 < k_random`; the old scale would otherwise bias small-N runs downward.
- Added analytic full-neighbour energy and force equivalence tests in both legacy and Kac modes, a frozen config
  hash/parameter-count/module audit, and mutation-rejection tests.
- Corrected the long-rollout regularizer documentation: detaching state at boundaries truncates cross-chunk
  gradients but does not release chunk activation graphs retained by the final concatenated loss.
- Added a data-free CPU feasibility runner using the production `train_distributed` entry point. It permits only
  the pre-registered `N=64` and `n_iters=2` overrides and compares uninterrupted training with exact
  interrupt/resume across model, optimizer, dynamic state, RNG streams, history, and an eight-step continuation.
- Extended the source-preview allow-list and run instructions with the M0 config, contract, runner and tests.

### Results / findings

- Frozen reference model: `N=256`, `d=32`, hidden 96, stochastic `k=50` pair MLP with Kac normalization,
  global GRU, four fixed agent types, Gaussian bath, zero jumps and fixed square-root impact; 36,541 parameters.
- Config SHA-256: `8e161bee28a4712e9a152539894ad6a9193d2311498fa066d24e16427e9ffa33`.
- Pre-commit/debug CPU gate: **PASS**. Four total training iterations took about 5.8 seconds; 18 parameter tensors
  had finite nonzero gradients; model, optimizer, complete state/RNG, history and post-resume trajectory were
  bit-exact. This dirty-worktree run is diagnostic only and is not the archived formal result.
- Repository regression: 86 tests pass; strict mypy remains zero errors across 67 modules; new-file and
  commit-added-line Ruff checks pass.

### Open questions / blockers

- A formal CPU report must still be produced from a clean implementation commit before V100 authorization.
- The CPU gate establishes implementation feasibility only. It supplies no market-fidelity or physics evidence.
- M1 stationary fidelity and all positive checkpoint/model-paper claims remain blocked.

### Next steps

1. Commit and push the M0 implementation.
2. Re-run the formal CPU gate with `--require-clean` on that exact SHA and archive its machine-readable report.
3. If the clean gate passes, launch the fixed 10-iteration FP32 pilot on the first V100; use the second only for
   cross-host reproduction after the first host meets memory/runtime/resume gates.

## Session 3 (00:34) — formal clean M0 CPU evidence

### Goal

Commit the M0 implementation, execute its pre-registered CPU feasibility check from a clean SHA, and decide
whether the first V100 pilot is authorized.

### What was done

- Committed and pushed M0 implementation commit `8951208ffd2f83e044a1094b5b98b206c9e839bb`.
- Ran `scripts/run_m0_cpu_feasibility.py --require-clean` with output outside the repository so the clean-tree
  assertion referred to the exact committed implementation.
- Archived the complete machine-readable result as
  `release/verification/8951208ffd2f-m0-cpu.json` and updated the release/model-paper plans and durable memory.

### Results / findings

- Formal M0 CPU gate: **PASS** in 6.85 seconds total; uninterrupted two-iteration training took 3.92 seconds.
- Model, optimizer, complete dynamic state/RNG, two-record history and post-resume eight-step trajectory are all
  bit-exact. The continuation return SHA-256 is
  `8f8fde594a47e529832894f6aeeaadb19306ce627354125cfcfcbbd9b97ddf7b`.
- All existing gradient tensors are finite; 18 are nonzero. Loss/history and continuation are finite.
- Worktree was clean at start; config SHA remains `8e161bee28a4712e9a152539894ad6a9193d2311498fa066d24e16427e9ffa33`.

### Open questions / blockers

- This is a synthetic-target mechanical gate. It does not change the failed historical stationary-fidelity
  evidence and authorizes no positive market/physics claim.
- The first V100 still needs a dedicated fixed 10-iteration runner/report before execution; the second host must
  remain idle until the first host passes HBM, time projection and exact-resume gates.

### Next steps

1. Add and test the V100 pilot runner without changing the frozen architecture or loss.
2. Commit/push the runner, then execute it on `100.80.236.112`.
3. If peak reserved memory is at most 26 GiB, projected 600-iteration time is at most 12 hours and exact resume
   passes, execute the same runner on `100.123.220.57` and compare host reports.

## Session 4 (00:40) — fixed V100 pilot runner

### Goal

Make the authorized V100 gate executable without permitting silent architecture, scale, precision or iteration
changes.

### What was done

- Added `scripts/run_m0_v100_pilot.py`, which uses the full N=256 frozen configuration and the same synthetic
  target as the CPU mechanical gate.
- Fixed the protocol to ten uninterrupted iterations versus five iterations plus exact resume for five more.
- Added hard checks for V100 identity, at least 30 GiB physical memory, FP32, clean worktree, N=256, peak reserved
  HBM <=26 GiB and a linearly projected 600-iteration duration <=12 hours.
- Added GPU UUID/driver/runtime, hashed host ID, memory, timing, state/RNG/optimizer/history/continuation equality
  and gradient evidence to the JSON report.
- Added config-only tests that freeze the effective pilot SHA and reject iteration, precision and N mutations.

### Results / findings

- Local config validation passes. Effective pilot config SHA-256 is
  `0adb0ef6a5a3e9693e9b91fe301ab1ada19360a2b58de3bc0ec2173580937a88`.
- Repository regression is now 90 tests passing; strict mypy remains 0/67 and new-file Ruff passes.
- Read-only inspection identified the first card as PCI `GV100GL [Tesla PG503-216] [10de:1df2]`, 32,768 MiB,
  idle at 0% with no compute process. The runner now accepts both normal V100 product strings and this deployed
  V100 board string, while still rejecting 16 GB cards and H20; seven runner tests pass after the correction.
- The Graphene monitor reports `idle_for_new_primary_task=true`; its 375 K queue is validation-locked, the
  release marker does not exist, and automatic start before release is false. No GPU experiment has run yet.

### Open questions / blockers

- The historical `/data/ecophys_workshop/repo` checkout no longer exists; create a new isolated sparse clone for
  the exact pilot commit rather than mutate old exp137--140 provenance clones.
- The second host remains intentionally idle until the first-host report passes every gate.

### Next steps

1. Commit and push the archived CPU report, status updates and V100 runner.
2. Inspect `100.80.236.112` read-only, update its existing checkout to the committed branch, then run the pilot.
3. Archive the first-host JSON before deciding whether to start `100.123.220.57`.

## Session 5 (00:48) — first-host V100 pilot

### Goal

Run the frozen ten-iteration M0 capacity/exact-resume pilot on V100-A and decide whether a second-host repeat is
scientifically and operationally authorized.

### What was done

- Created an isolated sparse clone under `/data/ecophys_m0_v100a` and checked out exact clean commit
  `f1e3cd4fb312cdf484aa65564953e17e8125f00b`.
- The first exact-checkout command used an incorrectly transcribed full SHA and was rejected before execution;
  the true local/remote SHA was then verified and used. No GPU work ran under the wrong identifier.
- Confirmed the Graphene release marker was absent and no compute process existed immediately before launch.
- Ran the pilot as transient service `ecomd-m0-v100a-f1e3cd4fb` with a 40-minute maximum runtime.
- Downloaded the completed JSON, matched local and remote SHA-256, asserted every gate, and confirmed the GPU
  returned to 8 MiB/0% with no compute process.

### Results / findings

- V100-A pilot: **PASS**, service exit 0. Ten uninterrupted iterations took 17.0196 seconds; the total
  continuous-plus-resume comparison took 32.0485 seconds.
- Peak allocated/reserved HBM: 11.4751/11.9941 GiB versus the 26 GiB cap.
- Projected 600-iteration duration: 0.28366 hours (about 17.0 minutes) versus the 12-hour cap.
- Model, optimizer, complete dynamic state/RNG, history and eight-step continuation are bit-exact; all existing
  gradients are finite and 18 parameter tensors are nonzero.
- Report SHA-256: `c99df8708de58119769a24069ff53da3049b6ea6da0c56d6048d15990b1410d7`; archived as
  `release/verification/f1e3cd4fb312-v100a.json`.

### Open questions / blockers

- Cross-host determinism is not yet known. The second V100 must run the exact same SHA/config/environment gate.
- This remains synthetic-target capacity/reproducibility evidence, not stationary market fidelity.

### Next steps

1. Commit/push the V100-A report before starting the second host.
2. Inspect V100-B's hardware, environment and priority queue read-only; launch only if safely idle.
3. Download/hash the V100-B report and compare config, software, history and continuation hashes to V100-A.

## Session 6 (00:54) — second-host V100 reproduction

### Goal

Repeat the exact frozen pilot on V100-B and determine cross-host rather than merely same-process reproducibility.

### What was done

- Verified V100-B had the same 32 GB GV100 board, driver, Python, PyTorch, CUDA and NumPy stack as V100-A; the
  525 K Graphene queue was validation-locked with no release marker and no GPU process.
- Created `/data/ecophys_m0_v100b`, checked out exact clean `f1e3cd4fb312cdf484aa65564953e17e8125f00b`,
  validated both config hashes, and ran service `ecomd-m0-v100b-f1e3cd4fb`.
- Downloaded and hash-verified the report, asserted every local gate, then compared selected immutable and
  numerical fields against V100-A. Both GPUs returned to 8 MiB/0% afterward.
- Corrected future host IDs to hash `/etc/machine-id` with hostname after observing both nodes share `ubuntu22`;
  current independence is established by distinct GPU UUIDs.

### Results / findings

- V100-B: **PASS**, service exit 0. Ten iterations took 16.5446 seconds; total comparison took 30.9468 seconds.
- Peak allocated/reserved HBM exactly matches A at 11.4751/11.9941 GiB. Projection is 0.27574 hours.
- Across distinct GPU UUIDs, environment, config, target, parameter count, complete ten-record history, memory
  profile and continuation SHA are exact. Shared continuation SHA is
  `eae72431c96618b423c7e5b4d90aefc78bc4c07b5a5aa454f50c462d00640692`.
- B report SHA is `1830a8527bd03e181a487b766aeea9a81c33403163fe55029c0611a1a9caf911`;
  A/B ten-iteration timing differs by only 2.83% of their mean.

### Open questions / blockers

- M0/R4 mechanical feasibility is complete, but no eligible real-data checkpoint or stationary evidence exists.
- Before a 600-iteration run, freeze the exact SPX input shards, provenance, preprocessing hash, stationarity
  test, burn-in rule, scoring windows and stop/report policy.

### Next steps

1. Archive V100-B and a machine-readable cross-host comparison, run final repository checks, commit and push.
2. Audit the currently available SPX 2015--2024 shards and provenance without reading sealed 2020 outcomes for
   model selection.
3. Pre-register and test the stationary-fidelity evaluator on controls; only then queue the 600-step reference.

## Session 7 (01:12) — physical-Parquet loader repair

### Goal

Remove a data-loading blocker before freezing SPX provenance or launching any real-data M1 training.

### What was done

- Audited the ten local Yahoo `^GSPC` daily shards for 2015--2024 without inspecting return outcomes: fixed
  schema, UTC-monotone timestamps, no duplicate timestamps, no nulls, 250--253 rows per year, and recorded
  per-file byte counts and SHA-256 values for the forthcoming provenance manifest.
- Reproduced an Arrow failure caused by reading a physical Parquet file below Hive-style folders such as
  `symbol=^GSPC`: directory-inferred dictionary partition fields conflicted with the same string field stored in
  the file.
- Added a single-physical-file reader based on `pyarrow.parquet.ParquetFile`, and routed both distributed
  training and baseline Yahoo/Binance loaders through it.
- Added regression coverage that recreates the conflicting path/schema and verifies the training loader uses
  `adjusted_close` and the requested year range. Added the test to the source-preview allow-list.

### Results / findings

- Full regression passes: 95 tests. Strict mypy passes on all 68 package modules; Ruff passes on both new files.
- Whole-repository Ruff still reports 345 historical findings, so this change is judged against the clean
  changed-file surface rather than silently rewriting unrelated legacy code.
- The available Yahoo bytes do not carry an acquisition timestamp or a complete request/version manifest. Git
  history supplies only a first-commit time, not data provenance, so those bytes cannot be the canonical M1
  input without a documented provenance exception.

### Open questions / blockers

- A canonical free-data acquisition must record exact UTC acquisition times, provider/library version, request
  parameters, normalized-file hashes, schema/coverage checks, preprocessing hash and temporal split roles.
- The stationary-fidelity protocol and rollout seeds remain unfrozen; therefore the 600-step V100 training is
  still intentionally not authorized.

### Next steps

1. Commit and push the loader repair independently.
2. Add a deterministic Yahoo acquisition/provenance manifest and reacquire 2015--2024 SPX without committing
   restricted vendor bytes.
3. Freeze M1 stationarity windows, seeds and kill/continue rules before launching the real-data checkpoint.

## Session 8 (01:37) — M1 data and scientific protocol freeze

### Goal

Freeze the data acquisition, training, calibration/held-out ordering and kill/continue rules before producing
the new state-complete SPX checkpoint or any of its trajectories.

### What was done

- Added a dedicated free-data acquisition/audit path for `^GSPC`, fixed to 2015-01-01 inclusive through
  2025-01-01 exclusive, daily, unadjusted Yahoo request plus an explicit `adjusted_close` preprocessing path.
- The audit requires exactly the 2015--2024 physical shards, validates schema/symbol/year/UTC ordering/nulls/
  duplicates/positive adjusted prices, and records file hashes plus platform-independent hashes of each split's
  derived float64 log-return vector without disclosing values.
- Froze M1 in a human-readable document and executable YAML contract. Training is exactly seed 0, FP32,
  iteration 0--300 plus exact resume to 600 on V100-A; no early stopping or alternate checkpoint.
- Assigned new, previously unused rollout seeds: calibration 811000--811015 and held-out 811100--811115, split
  evenly across the two V100s. Held-out launch is forbidden until calibration gate fitting is committed.
- Reused the independently validated exp127 energy/ADF-KPSS definitions but froze a new energy bootstrap seed,
  the complete early/post/late scoring grid and source-file hashes.
- Froze the screen decision: no W-star/failed held-out transfer is failure; either post or late <=2/11 stops the
  positive model-paper route, 3--4/11 is diagnostic-only, and both >=5/11 plus no >10% late distance degradation
  only authorizes—not establishes—the multiseed/baseline/cross-market stage.

### Results / findings

- New acquisition/audit and contract tests pass; full repository regression is 105 tests. Strict mypy passes on
  70 package modules and changed-file Ruff passes.
- No new model checkpoint, rollout, target estimate, 2019 validation outcome or sealed-2020 outcome was
  generated or inspected while choosing these rules.

### Open questions / blockers

- The formal acquisition must be run from the clean freeze commit and its manifest committed before training.
- Training still needs a fail-closed manifest verifier and CLI-controlled 300-iteration segment boundary before
  the V100 service can be launched.

### Next steps

1. Commit/push this pre-output freeze, then run the canonical free-data acquisition from that clean SHA.
2. Bind the acquired manifest hash into a training execution record and add the exact 300+300 runner controls.
3. Deploy the exact committed SHA/data to V100-A and start the single reference training.

### Execution note (01:44)

- The first acquisition command failed before imports completed and before any network request or output because
  direct `python scripts/...py` resolved the stale editable installation instead of this sparse checkout.
- The formal invocation is corrected to repository-root module mode,
  `python -m scripts.acquire_m1_spx_data`; the launcher is recommitted before retry so its recorded code hash
  remains truthful.

## Session 9 (01:50) — formal free SPX acquisition

### Goal

Create the canonical M1 input version from the clean committed acquisition code, without disclosing or using
validation/crash outcomes for model or protocol selection.

### What was done

- Ran the fixed Yahoo request from clean commit `4a2332d62616e3cf51ec2b11047528ce458b19ca` into an
  internal temporary directory; vendor Parquet bytes remain untracked and excluded from public artifacts.
- Generated `data/manifests/ecomd_v1_m1_spx_4a2332d62.json` with UTC acquisition times, exact request,
  environment/code versions, physical-file hashes/schema/coverage, split roles and derived-return hashes.
- Compared the new physical-file hash/row/coverage records with the pre-audit historical cache without reading
  or printing price/return values.

### Results / findings

- Acquisition and audit passed: ten expected annual shards, 2,516 price rows from 2015-01-02 through
  2024-12-31, no nulls or duplicate timestamps. The 2015--2018 training split has 1,005 returns.
- Every newly acquired annual file is byte-identical to its historical cached counterpart. There is no detected
  provider revision or normalization drift in this range.
- Manifest file SHA-256 is `0836ddd279a16db5010907120e65da87db58a2f2634c25f0e732334a98ffff7c`;
  its canonical pre-self-hash payload SHA is
  `c8733a5f5e9442e3ae5e83209deea992ce9c4ec3656f0e0db8d50a15a8f41fb8`.

### Open questions / blockers

- The training path must verify both manifest integrity and the physical bytes/derived train-return hash before
  allocating a GPU; merely copying the manifest is insufficient.
- The 300-iteration segment boundary is frozen but not yet exposed by the production CLI.

### Next steps

1. Commit/push the non-value manifest.
2. Implement and test fail-closed data binding plus the exact 300+resume-to-600 execution wrapper.
3. Transfer only the required train shards and manifest to V100-A, verify idle/queue state, and launch.

## Session 10 (02:07) — fail-closed M1 training entry

### Goal

Make it impossible for the canonical reference run to train on altered data, silently reuse a checkpoint or
resume under a different code/config/protocol binding.

### What was done

- Bound the acquired manifest path and file SHA into the pre-output M1 YAML and added a dated data-binding
  amendment to the human protocol; no checkpoint or rollout existed at amendment time.
- Added a verifier that checks manifest file SHA, canonical payload self-hash, policy, exact shard years, every
  physical file hash/schema/coverage and every reconstructed split-return hash.
- Added a pre-CUDA M1 preflight that requires a clean worktree, validates the strict M0/M1 contracts, confirms
  the exact manifest path and builds checkpoint metadata binding Git SHA, config/protocol/manifest hashes,
  dataset ID and train-return hash/count.
- Extended state-complete checkpoints with optional execution metadata and made resume reject a metadata
  mismatch before loading model/optimizer state.
- Exposed a global `--stop-after-iter` without changing the 600-iteration schedule. The canonical first segment
  now requires no existing checkpoint and exactly stop=300; the second requires an existing checkpoint,
  explicit `--resume`, expected iter=300 and an unshortened run to 600.
- Added per-segment records with checkpoint hash, time and HBM, plus regression tests for physical-data mutation,
  manifest mutation, phase misuse and checkpoint-binding mismatch.

### Results / findings

- Full repository regression passes: 110 tests. Strict mypy passes on all 71 package modules; Ruff passes on all
  new files and the changed production trainer.
- This closes the last known software/data blocker before remote launch. Formal remote preflight still needs to
  be exercised on the exact clean execution commit and copied internal data.

### Open questions / blockers

- None for starting the seed-0 reference after commit/deploy. Calibration/held-out tooling is deliberately next;
  held-out remains prohibited until the calibration fit is committed.

### Next steps

1. Commit/push the fail-closed runner.
2. Clone the exact commit on V100-A, copy and verify the ten small internal shards, then run local preflight.
3. Launch segment 0--300 and, only after its checkpoint/record passes, resume 300--600.

## Session 11 (02:28) — M1 reference checkpoint and calibration lock

### Goal

Produce the single frozen seed-0 state-complete checkpoint, audit it, then prepare calibration-only rollout
inputs without unlocking held-out evaluation.

### What was done

- Verified V100-A idle (8 MiB, 0%, no compute process), its Graphene 375 K queue validation-locked and release
  marker absent. V100-B was independently idle with its 525 K queue likewise locked.
- The first new clone accidentally omitted `--no-checkout` and began a full 60,604-file checkout; it was
  terminated before CUDA use and its residual directory was not used. A first shared sparse clone then exposed
  missing promised blobs and was rejected due deleted paths. The actual execution used a third, clean, exact
  shared sparse checkout at `7b2cd40fe0f387893d8de7511a38217e5bd16b34`.
- Copied the ten small internal Yahoo shards, then passed the remote pre-CUDA verifier on Git/config/protocol/
  manifest/physical-shard/derived-return hashes.
- Ran the frozen 0--300 service, audited format/iter/runtime/history/finite gradients and its checkpoint hash,
  then explicitly resumed from iter 300 with metadata equality to iter 600.
- The first launch command locally expanded its final `nvidia-smi` substitution and printed a command-not-found
  warning. Independent checks 11 seconds before and immediately after launch established no competing PID; the
  resume launch used the corrected remote-only guard. This is recorded as an operational deviation, not hidden.
- Archived a no-value training verification report and froze four node/split seed files plus a calibration-only
  binding. Held-out remains hard-locked. Added a calibration launcher that pins checkpoint/model/physics/scoring/
  seed hashes and explicitly loads full checkpoint state under newer PyTorch.

### Results / findings

- Training completed normally. Segment 0--300: 294.85 s; segment 300--600: 294.61 s; total 589.46 s. Peak
  allocated/reserved HBM was 11.429/11.951 GiB.
- Final checkpoint SHA-256 is `7822c030b44aa8e18110d807d7a4bfc15dea5ebd0175c5bd0fa2d2a10f96f34a`.
  It is format 2, state-complete, iter 600, one rank runtime, history 0--599, all checked values finite and all
  600 gradient norms positive. The exact execution metadata matches the preflight binding.
- Both services exited 0; execution checkout stayed clean; V100-A returned to 8 MiB/0% with no process and the
  Graphene release marker still absent.
- Repository regression after calibration tooling is 119 tests; strict mypy passes 72 modules and changed-file
  Ruff passes. No calibration or held-out trajectory has run yet, so stationarity/fidelity remains unknown.

### Open questions / blockers

- Calibration code/binding must be committed and independently preflighted on both nodes before the first
  trajectory. V100-B needs the exact checkpoint copied and hash-verified.
- Held-out cannot run until all 16 calibration trajectories are returned, gate fitting is frozen and the gate
  artifact is committed.

### Next steps

1. Commit/push the checkpoint verification, seed files and calibration-only launcher.
2. Deploy the evaluation commit to both clean V100 checkouts; copy/hash the checkpoint to V100-B.
3. Run eight calibration seeds per node, retrieve and hash every trajectory, then fit and commit W-star before
   authorizing held-out.

## Session 12 (02:18) — M1 calibration rollout and result-blind fitter

### Goal

Run only the 16 frozen calibration trajectories on the two idle V100s and prepare a fail-closed gate fitter
without opening or generating any held-out trajectory.

### What was done

- Rechecked both Graphene queue-status records. Each GPU was explicitly `idle_for_new_primary_task=true`; the
  held Graphene tasks remained validation-locked and unable to auto-start before their absent release markers.
- Copied the final reference checkpoint to V100-B and independently matched SHA-256
  `7822c030b44aa8e18110d807d7a4bfc15dea5ebd0175c5bd0fa2d2a10f96f34a` on both hosts.
- Passed the calibration preflight in clean exact-commit `a18d70c1c78e7072b2780c776b12de71570aacf1` checkouts:
  A loaded the eight frozen even seeds and B the eight frozen odd seeds.
- Launched one bounded systemd service per host after same-shell marker/GPU-process guards. Each produced its
  eight 8,000-return trajectories and a no-value shard manifest, then exited successfully; held-out was not run.
- Added a calibration-only fitter that rejects dirty source trees, pre-existing held-out files, manifest/
  checkpoint/binding/hash/schema/seed mismatches and cross-node Git mismatches. It reconstructs the matrix in
  the protocol's global seed order before bootstrap fitting, rather than depending on host concatenation order.

### Results / findings

- V100-A finished at 22:17:51 UTC+8; manifest SHA is
  `959c40473d6cf9855096809f68e51d1640b2f4e9921ef959188964ea8c2151d9`.
- V100-B finished at 22:17:38 UTC+8; manifest SHA is
  `44c2e9443ff74a42bce656354d0a712f8651cf1f7edf9ab59d14704842aad6e8`.
- Per-rollout time was about 43--51 seconds and reported peak reserved CUDA memory was about 0.12 GiB. Both GPUs
  returned to 8 MiB and 0% utilization with no compute process.
- The fitter and new tests pass Ruff, strict mypy; the full sparse regression is 122 tests and strict mypy now
  covers 73 package modules.
- Calibration values have not yet been retrieved or fitted in this commit. W-star and stationarity remain
  unknown; no fidelity claim is authorized.

### Open questions / blockers

- The result-blind fitter must be committed before calibration files are copied locally and used to fit the
  threshold. Its output must then be committed before any held-out launch.

### Next steps

1. Commit/push the fitter and tests.
2. Retrieve and independently verify both calibration shards, run the exact clean fitter and commit the frozen
   gate artifact.
3. Only if W-star exists, create a gate-bound held-out launcher and run the 16 untouched held-out seeds.

## Session 13 (02:22) — M1 calibration gate freeze

### Goal

Retrieve the completed calibration-only shards, verify their complete provenance and freeze W-star before any
held-out generation.

### What was done

- Committed and pushed the result-blind fitter as `179da420a65b69d6a677aff850f2fb37a827c4d7` before
  downloading or loading any trajectory values.
- Rechecked both remote trees for any `8111xx` trajectory; none existed. Copied the two complete calibration
  shards into a fresh internal `/private/tmp` root.
- Ran the fitter from the clean exact commit. It verified both shard manifests, all 16 trajectory hashes/sizes/
  schemas/seeds, checkpoint and binding hashes, execution Git equality, inference summaries and the absence of
  held-out artifacts. It reordered the interleaved host shards to the frozen global seed order.
- Fit the frozen energy-distance gate with bootstrap seed 811900 and the frozen ADF/KPSS comparator. Wrote the
  no-trajectory-values artifact `configs/ecomd_v1/m1_frozen_gate.json`.

### Results / findings

- Gate artifact SHA-256: `17ad4f26c5f5ce5040aba028e70bda711de0306620070be96d22c63bbb14ae55`.
  Calibration matrix hash: `18149f878817932152cd1d0faa32de1148647dd22bdd223c25ecf16d4094a90c`.
- Energy tolerance is `0.019507027186009607`. Calibration median distance at W=0 is
  `0.020421648813179982` and fails; W=500, 1000 and 1500 are all below tolerance, so frozen energy W-star is
  **500**. All later listed energy blocks also lie below tolerance.
- The ADF/KPSS comparator passes every listed aggregate block and selects W-star 0. This disagreement is useful:
  the multivariate gate detects a small initial joint-distribution transient that the classical marginal tests
  do not.
- This is only a calibration outcome. It authorizes held-out transfer testing but establishes neither held-out
  stationarity nor any stylized-fact fidelity, market physics, baseline superiority or paper claim.

### Open questions / blockers

- The gate artifact must be committed/pushed, then a held-out launcher must pin its exact SHA and refuse any
  alternate W-star or output reuse before the 16 held-out seeds may run.

### Next steps

1. Commit/push the frozen gate artifact and this audit record.
2. Implement/test a gate-bound held-out launcher and scoring path, commit it separately, then deploy it to both
   V100s.
3. Evaluate transfer at frozen W-star 500 and report all fixed scoring windows without best-window selection.

## Session 14 (02:30) — held-out execution and analysis freeze

### Goal

Freeze a gate-bound held-out launcher and complete scoring/decision implementation before generating any of the
16 held-out trajectories.

### What was done

- Added `m1_heldout_binding.yaml`, which pins the committed gate SHA/commit/W-star 500, calibration input and
  binding hashes, reference checkpoint/config/protocol, model/physics tree objects, scoring implementations,
  untouched node seed files and the complete fixed scoring grid.
- Added a held-out preflight/launcher that requires a clean repository; proves the gate commit is an ancestor
  and its blob unchanged; revalidates checkpoint history/metadata; rejects altered source/seed/config hashes;
  forbids shocks/overrides and output reuse; and writes a hash-only shard manifest.
- Added an additional first-add freeze check: the held-out binding must have exactly one Git add commit and its
  current blob must equal that first committed blob. The freeze commit is recorded in each remote manifest.
- Added the pre-result analysis implementation. It verifies both held-out manifests and every trajectory hash/
  size/schema, reconstructs the global 811100--811115 order, applies the frozen energy gate unchanged, scores
  all ten W values at exactly L=4000, aggregates per-fact medians and applies the frozen decision tiers.
- Added tests for gate mutation rejection, exact analysis-source binding, trajectory volume alignment and every
  frozen decision branch (stationarity stop, <=2 hard stop, 3--4 diagnostic, >=5/ratio authorization, and late
  degradation rejection).

### Results / findings

- Full sparse regression passes: 132 tests. Strict mypy passes all 75 package modules; changed-file Ruff and
  whitespace checks pass.
- The analysis implementation frozen into the binding has SHA-256
  `a93849df795d5d481a140a3f9373aa4336ca4d84fc83a25e4d5cbc64ed1956de`.
- No held-out trajectory was generated or inspected while implementing these rules. The current gate result
  remains only a calibration authorization.

### Open questions / blockers

- The first binding commit must be pushed and deployed; exact local and remote preflight on that clean commit
  must pass before GPU launch.

### Next steps

1. Commit/push the held-out freeze.
2. Fetch the exact commit on both V100s, independently preflight checkpoint/gate/source/seeds, and recheck both
   queue markers and compute-process lists.
3. Run eight held-out seeds per host, retrieve/hash the shards and execute the already-frozen analysis once.

## Session 15 (02:55) — M1 held-out result, hard stop and evaluator audit

### Goal

Run the untouched held-out seeds under the committed W-star 500 gate, apply the frozen decision once, and
separate the protocol outcome from the scientific validity of the generic canonical bands.

### What was done

- Committed/pushed the first and immutable held-out binding at
  `80a3d61a547f91d893f25376b9bbb98e612f467d`; local checkpoint preflight passed for both node seed shards.
- V100-B deployed cleanly. V100-A's shared partial clone again lacked five new blobs and initially left those
  paths deleted after checkout. No Python/CUDA ran. The exact six known blobs (including the sparse memory blob)
  were fetched through the original promisor; only the five deleted paths were restored. HEAD, hashes and a
  completely clean worktree were then independently verified.
- Both remote preflights proved the gate blob unchanged from commit `3f1303b04`, the held-out binding unchanged
  from its first-add commit, checkpoint/source/config/protocol hashes exact, and A/B seeds equal to the frozen
  even/odd held-out shards. Both queue records said `idle_for_new_primary_task=true`; release markers were absent.
- Ran eight held-out trajectories per V100 as bounded systemd services, retrieved both complete shards and ran
  the frozen analysis. A synthetic Student-t smoke independently exercised all 10 windows x 11 facts before
  formal scoring and found every estimate finite.
- The first analysis invocation requested four process workers and failed while constructing Python's process
  pool because the macOS sandbox denied `SC_SEM_NSEMS_MAX`. It failed before window scoring or output creation.
  Worker count is execution-only and not part of the frozen estimand, so the unchanged code was rerun serially;
  it completed and wrote the sole formal result. Matplotlib used a writable temporary cache and emitted a
  non-scientific warning.
- After the formal result, audited the canonical-band add commit and ran a clearly labeled diagnostic sanity
  check on exact SPX train, 2019 report-only and 2021--2024 temporal data. Sealed 2020 stayed unopened.

### Results / findings

- A/B held-out services exited 0 after 7min09s/7min32s CPU time and returned the GPUs to 8 MiB/0%. Their manifest
  hashes are `ebda817f03c961c86a2fc4b8e3a0d81eeb3740a6d905b7ead4ec41661d8b8f3c` and
  `9641cd8dbaf08529c45690a8b17c7d61c3603a0d2eaa5f39751548661331fe07`.
- Frozen W-star 500 transfers: all nine held-out gate-block medians are below calibration tolerance. Thus the
  small initialization transient is controlled; this is not real-market equivalence.
- Every one of the ten fixed scoring windows passes exactly **2/11** generic bands. W=0/500/4000 mean normalized
  distances are 0.244691/0.243187/0.252227; late/post ratio is 1.03717. Only raw-return autocorrelation and
  conditional kurtosis pass. Frozen tier is unequivocally `stop_positive_model_paper`; next-stage authorization
  is false.
- Target-matched diagnostics reinforce a real structural miss: at W=500 EcoMD versus 2015--2018 SPX gives Hill
  6.738 vs 3.518, squared-return ACF 0.0108 vs 0.1151, absolute-return DFA 0.586 vs 0.853, leverage +0.017 vs
  -1.066 and volume--volatility correlation -0.0003 vs 0.409.
- The exact formal result SHA is `2d046ce38f5a78f35c3be952e53d81110d01d9c74f1eb0f809becf6635973846`.
- Reviewer audit found the generic bands were added in `2734ee9351` without cited dataset/frequency/length/
  coverage provenance. The gain/loss estimator has always returned skewness since `e7c282c2d2`, but its band is
  `[-30,-3]`; all three inspected real SPX splits fail it. All three real splits pass only 5/11 generic bands and
  are shorter than model L=4000. Therefore 2/11 remains the binding operational stop but is not a publishable
  validated realism score. Diagnostic artifact SHA is `e79e1ee28a396b0654b87835279255e9eadc2932ae93b8bf712eba448cb6c5da`.

### Open questions / blockers

- EcoMD v1 lacks the empirical dynamics required for a positive model paper even without relying on the suspect
  gain/loss band. S1 model release and R6 production are stopped.
- A publishable evaluator needs metric definitions, units, matched length/frequency, real-data bootstrap bands,
  self-coverage and independent time/market validation. Current held-out values cannot tune that evaluator.
- A new model needs mechanistic slow volatility, asymmetric return--volatility and observable flow/volume
  coupling; adding external heavy-tail noise alone is not an acceptable repair.

### Next steps

1. Commit/push the formal artifact, diagnostic audit and result document.
2. Keep both V100s idle; do not run v1 multiseed/baseline/ablation/cross-market production or buy data.
3. On CPU/free data, specify an estimator-v2 self-coverage protocol using training-only blocks and independent
   temporal/market validation, then decide whether a mechanistically redesigned EcoMD v2 is worth a new freeze.
4. Keep the main scientific route empirical-physics-first; use a simulator only after a real-data law survives
   multi-market/multi-timescale falsification.

## Session 16 (03:39) — Evaluator-v2 result-blind feasibility freeze

### Goal

Replace the invalid generic-band realism score with a finite-sample evaluator qualification study whose rules
are frozen before any new data are acquired or metric outputs are inspected.

### What was done

- Added `configs/evaluator_v2/feasibility_v1.yaml` as the machine-readable contract. It fixes four free daily
  markets, disjoint reference/conformal/confirmation/report-only/sealed/temporal periods, matched non-overlapping
  block lengths, split-conformal coverage, eligibility rules and metric-specific surrogate falsification tests.
- Added a result-blind protocol note at
  `papers/proposal/evaluator_v2_feasibility_freeze_2026-08-11.md`. It forbids using the EcoMD-v1 held-out result
  to select thresholds, keeps calendar 2020 sealed, and defines explicit stop rules before any simulator score.
- Added `scripts/acquire_evaluator_v2_free_data.py` to acquire the exact 2005--2024 annual Yahoo shards for SPX,
  NDX, GLD and EURUSD, audit them, and create a content-addressed, no-values manifest. Raw provider bytes remain
  temporary and are not redistributed.
- Added acquisition-contract tests, including canonical manifest self-hashing. Corrected YAML date parsing and
  passed Ruff, strict mypy, the full 134-test suite and `git diff --check`.

### Results / findings

- The new feasibility phase is CPU-only, free-data-only and explicitly GPU-forbidden. Both V100s remain idle.
- The primary question is estimator validity on real data: independent real-market self-coverage and loss of
  expected structure under targeted surrogates. It is not an attempt to rescue EcoMD v1.
- Length 120/240 are primary because they test the finite-sample regime relevant to later comparisons; length
  500 is diagnostic. Metrics that cannot meet their predeclared sample/observable requirements must be marked
  ineligible rather than silently redefined.
- `gain_loss_asymmetry` is frozen under its actual implementation semantics (sample skewness) and only for equity
  indices. Leverage is equity-only; volume coupling applies only when volume is finite and non-constant; daily
  Zumbach is diagnostic-only.

### Open questions / blockers

- Formal acquisition must start from the exact clean, pushed protocol commit and a fresh output directory.
- Acquisition fails closed if a symbol does not provide exactly 20 auditable annual shards; protocol rules will
  not be relaxed after seeing provider outputs.
- The manifest must be bound into the protocol before any evaluator metric is computed.

### Next steps

1. Commit and push the frozen protocol and acquisition tooling.
2. Acquire/audit the four free series from that exact commit, bind the manifest hash, then commit/push the binding.
3. Implement and test matched-length conformal self-coverage and targeted surrogate diagnostics.
4. Run the evaluator qualification on CPU and decide whether its metric family is usable before designing EcoMD v2.

## Session 17 (03:44) — Formal free-data acquisition and binding

### Goal

Acquire the evaluator-v2 input from the exact pushed freeze commit, audit only provenance/schema metadata, and
bind a no-values manifest before any stylized-fact estimator is run.

### What was done

- Verified local HEAD and upstream were both clean at `0de183067568d9201377e91bfb41992990e43bfd`.
- Ran the frozen acquisition once into fresh internal root
  `/private/tmp/ecophys-evaluator-v2-data-viDtFu` using the `ecophys` Conda environment.
- Acquired and audited exactly 20 annual parquet shards for each of `^GSPC`, `^NDX`, `GLD` and `EURUSD=X`.
- Wrote `data/manifests/evaluator_v2_free_daily_2005_2024.json`, then bound its canonical payload hash, file hash,
  acquisition commit and pre-acquisition protocol hash into the YAML without changing scientific rules.
- Added a test that recomputes both manifest hashes and verifies the clean acquisition commit, protocol hash,
  no-values policy and sealed-2020 policy.

### Results / findings

- Acquisition passed closed-form schema/provenance checks: 80/80 expected files, zero duplicate timestamps, and
  full requested 2005--2024 coverage. SPX/NDX/GLD each have 5,033 price rows; EURUSD has 5,189.
- Canonical manifest payload SHA is `28560c599e9c63418732d34cee0ade29665e24eb75b5b5fc123435d0bc005666`;
  file SHA is `a6d46c595041cb37d587296dac841b20b8257edd36c0c5ae65bec112bf243eba`.
- The internal raw dataset is approximately 1.3 MB. No prices, returns, metric values or sealed-period statistics
  were inspected during binding.

### Open questions / blockers

- The bound manifest and binding tests must be committed/pushed before evaluator implementation begins.
- EURUSD volume eligibility is deliberately unresolved until the evaluator performs the predeclared finite and
  non-constant observable check; it cannot be assumed from provider naming.

### Next steps

1. Commit/push the manifest binding as its own provenance layer.
2. Implement block construction, eligibility, split-conformal coverage and deterministic surrogates with tests.
3. Commit/push the implementation and source hashes before producing the first metric output.

## Session 18 (03:56) — Evaluator-v2 implementation freeze

### Goal

Implement every predeclared finite-sample, conformal and surrogate rule, validate it on synthetic tests, and
commit the source before producing any real-data metric output.

### What was done

- Added `ecomd/eval/evaluator_v2.py` with aligned non-overlapping blocks, finite-sample split conformal,
  deterministic SHA-derived surrogate seeds, the four frozen surrogate generators, per-metric scalar dispatch,
  paired relation tests and metric/symbol/length eligibility aggregation.
- Added `scripts/run_evaluator_v2_feasibility.py`. It requires a clean commit equal to upstream, verifies the bound
  manifest and all 80 raw shard hashes, parses no sealed-2020 values, verifies each non-sealed derived-return hash,
  records all code/environment hashes and atomically refuses to overwrite a formal result.
- Added synthetic tests for block/remainder behavior, conformal order-statistic insufficiency, all surrogate
  invariants and determinism, paired-IQR relation semantics, and fail-closed L=120 estimator behavior.
- Before seeing any real metric output, resolved implementation-only ambiguities in YAML: paired effect uses the
  within-block median of eight controls; pooled IQR includes real and every control replicate; ties fail; nonpositive
  IQR fails; 2019 is excluded from selection; the volume metric needs three eligible markets.

### Results / findings

- Full test suite passes: 140 tests in 3.96 s. Ruff passes the changed code and strict mypy passes all 76 package
  source files.
- Synthetic L=120 checks confirm the unchanged legacy aggregational-Gaussianity estimator is non-finite because
  only one aggregation scale has enough samples, and DFA fails because max scale 12 is below min scale 16. These
  will be formal estimator-ineligibility results if they occur on real blocks; the runner will not adapt them.
- The runner contains no torch/CUDA path. It verifies sealed files only as raw bytes against the manifest but never
  parses the sealed 2020 parquet values for metrics.

### Open questions / blockers

- The implementation commit must be pushed and the tree reverified clean before the formal CPU run.
- GARCH-based conditional-kurtosis controls may dominate runtime; this is bounded CPU work and must not be moved
  to V100 or parallelized by changing the estimand.

### Next steps

1. Commit/push the implementation freeze.
2. Run the formal evaluator once from that exact clean upstream commit into a fresh result path.
3. Audit the machine-readable result without retuning; document the metric-by-metric decision and next model step.

## Session 19 (04:00) — No-output diagnostic-relation runner fix

### Goal

Launch the exact committed evaluator once and, if an execution-only defect appears, preserve the frozen estimand
while failing before artifact creation.

### What was done

- Verified clean local/upstream SHA `71ed5a9b6416708812750adc2ab773dc956f720b` and a fresh result path, then
  invoked the CPU runner against the bound internal data root.
- The invocation verified inputs and computed cells in memory, but stopped before JSON serialization because the
  diagnostic-only Zumbach relation string `report_no_eligibility_decision` was incorrectly passed to the three-way
  eligibility relation parser.
- Confirmed neither the formal output nor its temporary file exists. No metric values were printed or inspected.
- Added a dedicated declared-relation dispatcher: diagnostic cells retain their real/control estimates but return
  `passed: null` and never enter eligibility algebra. Added a regression test for this exact contract.

### Results / findings

- This is an execution-control bug, not a scientific failure or license to change the protocol. Data, splits,
  estimators, surrogate values, thresholds and all primary gates remain byte-for-byte unchanged.
- The focused suite now passes 9 tests; Ruff and strict mypy pass the runner/evaluator sources.

### Open questions / blockers

- The two-line semantic fix and regression test must be committed/pushed before retrying the formal run.

### Next steps

1. Commit/push the diagnostic relation fix.
2. Retry the unchanged formal CPU command from the new clean upstream commit.

## Session 20 (04:05) — Formal evaluator-v2 result and hard decision

### Goal

Execute the unchanged formal CPU study from the pushed diagnostic fix, independently verify its artifact, and
apply the frozen metric-by-metric rules without retuning.

### What was done

- Verified clean HEAD/upstream at `c6c606485c2c4eec63528f3dc376dd6e7816ba0a` and reran the exact formal command.
- The runner reverified all 80 file hashes, every non-sealed split return hash, and that sealed 2020 was not parsed.
- Created the sole formal result `results/evaluator_v2/feasibility_v1.json`; independently recomputed its canonical
  self-hash and audited all primary cell finite, coverage and surrogate gates.
- Wrote the full decision and failure taxonomy in
  `papers/proposal/evaluator_v2_feasibility_result_2026-08-11.md`.

### Results / findings

- Only `autocorr_returns` qualifies, with SPX/GLD/EURUSD passing both L=120/240; NDX fails one coverage and one
  equivalence cell. This is a sanity check, not a sufficient realism evaluator.
- Strong-but-incomplete candidates are acf-squared (6/8 primary cells; SPX/EURUSD eligible), volume--volatility
  correlation (5/6; NDX/GLD eligible), leverage (both indices pass L120 but fail L240 coverage), and aggregation
  Gaussianity (all four markets pass L240, but L120 is structurally non-finite).
- Current Hill has only 2/8 passing cells; sample-skew gain/loss and DFA have zero; Fano is non-finite in 7/8.
  Conditional kurtosis responds to its surrogate in 8/8 cells but self-covers in only 3/8, confirming regime
  dependence rather than a universal static band.
- L=500 makes the estimators finite but cannot support conformal coverage: n_cal=2 while k=3. Gain/loss skewness
  and Fano still fail every L=500 surrogate cell.
- Result canonical SHA is `057946d365686cc4e63c13e8db3d547e83601560d701e3ac52e39f572438d25f`;
  file SHA is `52814472321ae9dbc89b74b4eb73a0b5eac170b3d9fdb157d70699c506b1df02`.
  Runtime was 8.82 s on CPU; no GPU was used.

### Open questions / blockers

- The 11-fact evaluator cannot authorize EcoMD v2 scoring or V100 production. Only one mostly trivial metric passed.
- Current four-market confirmation/temporal periods are spent for estimator design; a changed estimator needs new
  confirmatory markets/history under a new freeze.
- Metric-specific sample requirements and regime conditioning need a fresh exploratory protocol; thresholds cannot
  be repaired within feasibility-v1.

### Next steps

1. Commit/push the immutable formal artifact, result note, log and durable decision.
2. Keep V100s idle and do not train EcoMD v2 against the failed 11-fact suite.
3. Freeze a CPU/free-data estimator-v2.1 plan: analytic sample-complexity first, fresh-market confirmation second.
4. Reorient the scientific spine to real-market regime-dependent volatility/leverage/volume physics; treat a
   simulator as downstream only after those observables have valid measurement contracts.

## Session 21 (04:13) — Synthetic sample-complexity result-blind freeze

### Goal

Freeze a zero-cost instrument-calibration study that determines estimator-specific sample requirements before
using any redesigned metric on fresh real markets.

### What was done

- Added `configs/evaluator_v2/sample_complexity_v1.yaml` with seven sample lengths, 64 independent paths per
  length, eight controls per path, root seed, burn-in, DGP parameters, signal/null mapping and exact gates.
- Added known-structure fixtures for Gaussian/Student iid, centered negative jumps, Gaussian/t GARCH,
  multiscale log-volatility, GJR-GARCH and coupled/independent volume. These are measurement unit tests, not
  candidate market simulators.
- Froze metric-specific analytic floors before synthetic outputs: aggregation/Zumbach 180, DFA/conditional/skew
  240, Hill 500, Fano 2000, and 120 for the remaining estimators.
- Required 64/64 complete path/control sets, signal power and null specificity. The admissible minimum must pass
  at that grid length and every longer one; any later failure invalidates an earlier pass.
- Documented purpose, non-claims and stopping rules in
  `papers/proposal/evaluator_v2_sample_complexity_freeze_2026-08-11.md` and added a contract-completeness test.

### Results / findings

- No synthetic metric has been computed. The contract is explicitly post-feasibility-v1 but result-blind with
  respect to this experiment.
- Current four-market 2015--2024 data remain forbidden for redesigned-estimator confirmation. A pass only permits
  design of a separately frozen fresh-market protocol.
- This phase is sequential CPU-only; both V100s remain idle.

### Open questions / blockers

- The freeze must be committed/pushed before DGP/harness implementation begins.

### Next steps

1. Validate and commit/push the result-blind contract.
2. Implement DGP invariants and the Monte Carlo runner with synthetic-only tests.
3. Commit/push source before the sole formal CPU run.

## Session 22 (04:22) — Synthetic DGP and Monte Carlo implementation freeze

### Goal

Implement the frozen sample-complexity experiment, prove the synthetic fixtures contain their declared structures,
and commit all source before the 64-path formal run.

### What was done

- Added `ecomd/eval/synthetic_dgps.py` with deterministic Gaussian/Student iid, centered negative jumps,
  Gaussian/t GARCH, multiscale log-volatility, GJR-GARCH and coupled/independent-volume processes.
- Added `scripts/run_evaluator_v2_sample_complexity.py`. It derives every path/control seed from the root seed and
  cell identity, runs signal and null conditions, applies complete-case/power/specificity gates, and requires
  suffix-stability for the reported minimum length.
- The formal runner verifies clean HEAD=upstream, source feasibility artifact hashes, protocol/code hashes and a
  fresh output path; it records CPU/no-GPU provenance and writes atomically.
- Added DGP determinism/alignment tests, structural tests for skew, squared-return memory, leverage and volume
  coupling, suffix-decision tests, and a reduced 11-metric × 7-length × signal/null execution smoke.

### Results / findings

- All 146 repository tests pass in 4.82 s; strict mypy passes 77 package modules and Ruff passes all changed files.
- The reduced full-matrix smoke completed without a dispatch, diagnostic, below-floor or empty-suffix error.
- No formal 64-path aggregate has been generated or inspected. The smoke uses one path/control solely to exercise
  implementation branches and cannot determine admissibility.

### Open questions / blockers

- Source must be committed and pushed; the formal runner will reject a dirty or unpushed tree.
- Full CPU runtime is expected to be dominated by GARCH fits and DFA, but changing parallelism or estimators is
  forbidden. V100s remain unused.

### Next steps

1. Commit/push the implementation freeze.
2. Run the sole formal CPU result from that exact commit and poll without changing settings.
3. Apply suffix-stability once and freeze the fresh-market candidate list.

## Session 23 (04:28) — Formal synthetic sample-complexity result

### Goal

Run the sole frozen 64-path CPU calibration, audit the unexpectedly uniform pass pattern, and derive honest data
requirements without treating synthetic detectability as market evidence.

### What was done

- Ran the formal study from clean/pushed `3fe5a5eed4de7d118912f428b2836b45def6185e`; it completed all signal/null,
  seven-length and eight-control cells sequentially in 129.90 s.
- Independently verified the result canonical self-hash, file hash, source-result binding and no-GPU provenance.
- Audited seed construction, completeness, signal direction/effect, null equivalence, below-floor failures and
  suffix decisions for every metric.
- Documented the result and the strong-DGP limitation in
  `papers/proposal/evaluator_v2_sample_complexity_result_2026-08-11.md`.

### Results / findings

- Detectability floors under the frozen fixtures are: autocorr/acf-squared/volume N=120; aggregation N=180;
  conditional kurtosis/gain-loss skew N=240; Hill/DFA/leverage N=500; Fano N=2000. Zumbach remains diagnostic.
- DFA fails at N240 (direction 0.563/effect 0.143 IQR); leverage fails N120--240 (direction 0.641--0.719); Fano's
  short cells are incomplete/low-power and only stabilize at its predeclared N2000 floor.
- Every minimum and longer cell has 64/64 complete signal and null path/control sets. Null effects at the minima
  are all within ±0.157 IQR; signal directions/effects clear the frozen gates.
- Uniform eventual success is expected because the DGPs deliberately contain strong targeted signals. It proves
  estimator sensitivity to these fixtures, not market validity or realistic effect-size power. The real-data FAIL
  remains binding.
- Per-symbol four-role non-overlap needs roughly 16N observations: ~7.6y at N120, 15.2y at N240, 31.7y at N500
  and 127y at N2000. Fano is therefore infeasible for near-term per-market confirmation.
- Canonical result SHA is `376eeca24355e0d5ce1eebc0cb4b67ebcf3962019da651aa5de7c8a48881a183`;
  file SHA is `d1f920f300711bf5118c34de0c4f787f0cd79c2700dd0ab1c2a74e899f9ca73a`.

### Open questions / blockers

- Fresh-market confirmation needs a universe frozen before acquisition metrics, with automatic coverage/volume
  eligibility. Current four markets cannot be reused as confirmation.
- N500 metrics require multi-decade histories; N2000 Fano is out unless a separately justified hierarchical
  estimand replaces per-market non-overlap.
- Synthetic fixture strength is not calibrated to real effects; no minimum N may be marketed as universal.

### Next steps

1. Commit/push the immutable formal result and reviewer-2 interpretation.
2. Freeze a fresh free-market universe based only on symbol class and coverage/schema rules.
3. Prioritize acf-squared, volume coupling and N≥180 aggregation; keep conditional/skew as falsification targets,
   long-N metrics as data-availability dependent, and Fano out of the near-term confirmatory set.

## Session 24 (04:32) — Fresh-instrument confirmation freeze

### Goal

Freeze a genuinely unseen instrument universe and short-block confirmation contract before acquiring any of its
data or computing any metric.

### What was done

- Added `configs/evaluator_v2/fresh_market_confirmation_v1.yaml` with six unseen equity indices (`^DJI`, `^RUT`,
  `^N225`, `^FTSE`, `^GDAXI`, `^HSI`) and four volume-bearing ETFs (`SPY`, `EEM`, `TLT`, `EWJ`). The spent
  SPX/NDX/GLD/EURUSD symbols are explicitly excluded.
- Froze metric-specific lengths: autocorr/acf-squared/volume 120/240, aggregation 180/240, conditional/skew 240,
  and diagnostic Zumbach 180/240. Long-floor Hill/DFA/leverage/Fano are excluded from this experiment.
- Kept the original calendar splits and conformal/surrogate thresholds. Marked the experiment as instrument
  holdout but not independent macro-time holdout; a pass cannot establish time-independent universality.
- Defined automatic pre-metric block eligibility, minimum market counts, a four-metric core-suite decision and a
  hard prohibition on model scoring/training even after a pass.
- Added result-blind acquisition/audit tooling for exactly 200 annual shards and a protocol-constant test. Human
  freeze: `papers/proposal/evaluator_v2_fresh_market_confirmation_freeze_2026-08-11.md`.

### Results / findings

- No new market data or metric has been accessed. Universe selection used instrument class and expected public
  history only; missing coverage later causes ineligibility rather than symbol replacement.
- The core suite requires autocorr sanity, acf-squared, aggregation and ETF volume coupling all to qualify.
  Conditional/skew are secondary and cannot rescue a core failure.
- CPU/free-data only; both V100s remain idle.

### Open questions / blockers

- Freeze/acquisition source must be committed and pushed before formal download.
- Exact 2005--2024 Yahoo coverage is unknown by design; acquisition fails closed on any missing annual shard.

### Next steps

1. Validate and commit/push the fresh-market freeze.
2. Acquire/audit all ten symbols from that exact commit into a fresh internal root.
3. Bind the no-values manifest before implementing or running metric-specific confirmation.

## Session 25 (04:35) — Fresh-market acquisition and manifest binding

### Goal

Acquire the ten frozen instruments from the exact pushed universe commit, inspect only coverage/schema provenance,
and bind the no-values manifest before metric implementation.

### What was done

- Verified clean/pushed acquisition commit `a2fe0101ed4fdc1d9e73b1ef31398453d79dee30` and acquired into fresh root
  `/private/tmp/ecophys-evaluator-v2-fresh-data-RYarjf`.
- Audited exactly 20 annual shards for each of ten symbols and wrote
  `data/manifests/evaluator_v2_fresh_markets_daily_2005_2024.json`.
- Bound manifest canonical/file hashes, acquisition commit and pre-acquisition protocol hash into the YAML.
- Added a test that recomputes both manifest hashes and verifies the universe-freeze/no-values policies.

### Results / findings

- All 200/200 expected shards passed schema/provenance audit. Every series spans its requested 2005--2024 period.
- N225 and HSI have the shortest confirmation/temporal returns (981/979 and 983/981); both still yield four
  non-overlapping N240 blocks in every gating split. No symbol fails the frozen block-count precondition.
- Manifest canonical SHA is `e738e53e7ae74205f5d85fe16c070188bfa7d809df8420d2bf95391b8cd0ce0d`;
  file SHA is `1e0393f8f803c104e0326225ebdee7c9443bcdf38132a2a0ff23ba1d82314cf0`.
- Internal raw data are approximately 3.1 MB. No price, return, volume statistic or stylized-fact value was
  inspected before binding.

### Open questions / blockers

- The bound manifest commit must be pushed before the metric-specific runner is implemented.
- ETF volume finiteness/non-constancy remains a runner gate; it has not been inferred from symbol names.

### Next steps

1. Commit/push the manifest binding.
2. Implement block-count-first metric-specific confirmation using the immutable data hashes.
3. Commit/push source before formal CPU output.

## Session 26 (04:41) — Fresh-market metric-specific runner freeze

### Goal

Implement the bound fresh-instrument protocol with block-count-first execution and commit source before any new
market metric output.

### What was done

- Added `ecomd/eval/fresh_market_confirmation.py` to build the union N grid once, enforce four-block eligibility
  before estimator calls, run only each metric's declared lengths/scope, and apply conformal/surrogate/market-count
  and core-suite decisions.
- Added `scripts/run_evaluator_v2_fresh_market_confirmation.py` with clean HEAD=upstream enforcement, complete
  200-shard verification, source sample-complexity binding, non-sealed derived-return hash checks and atomic output.
- Added a regression test that makes every metric call fatal and proves short data are rejected by block count
  first, plus a reduced full protocol smoke covering all seven metrics/scopes/lengths.
- Ensured report-only 2019 non-finiteness can suppress only its displayed coverage and never change or abort a gate.

### Results / findings

- Full suite passes 150 tests in 5.22 s; strict mypy passes 78 package modules and Ruff passes the changed files.
- No fresh-market metric output has been generated. Reduced smoke uses synthetic arrays only.
- Formal runner has no torch/CUDA path and hardcodes `model_scoring_or_training_authorized=false` independent of
  the core result.

### Open questions / blockers

- Implementation source must be committed/pushed before formal execution.
- The formal core outcome is fully determined by the frozen metric decisions; no secondary result can rescue it.

### Next steps

1. Commit/push the runner freeze.
2. Execute the sole formal CPU confirmation from that exact commit.
3. Audit instrument dependence and shared-calendar limitations before any scientific interpretation.

## Session 27 (04:45) — Formal fresh-market confirmation and stop decision

### Goal

Execute the sole frozen fresh-instrument confirmation, audit every failure class, and decide whether evaluator v2
can authorize EcoMD-v2 scoring or training.

### What was done

- Ran the formal CPU confirmation from clean/pushed commit `31cddbfbeb9abe3c82805c1363137fad4deb6e3a` against
  the hash-bound ten-market data root.
- Independently recomputed the artifact canonical self-hash and file hash, verified the manifest/protocol/source
  bindings, and confirmed `gpu_used=false` and that sealed 2020 was never loaded.
- Audited block eligibility, finite estimates, all-N instrument decisions, and mutually exclusive coverage-only,
  surrogate-only and joint-failure counts for every primary/secondary metric.
- Documented the formal result and reviewer-2 interpretation in
  `papers/proposal/evaluator_v2_fresh_market_confirmation_result_2026-08-11.md`.

### Results / findings

- Core suite FAILS and model scoring/training authorization is false. Only acf-squared qualifies, exactly 6/10
  instruments; autocorr and aggregation qualify 1/10 each, while ETF volume coupling qualifies 2/4 (minimum 3).
- Primary cell counts are acf-squared 15/20, aggregation 6/20, autocorr 8/20 and volume 6/8. The all-N rule makes
  SPY/TLT ineligible for volume because their N120 coverage fails even though N240 passes.
- Secondary conditional kurtosis qualifies 2/10 and index gain/loss skew 1/6. Zumbach remains diagnostic-only.
- All symbol/N block gates pass and all original/control gating estimates are finite. Failures are conformal temporal
  coverage and/or surrogate specificity, so missing observations and numerical instability cannot explain them.
- Artifact canonical SHA is `8a946a32e6c77d784063e1f044f3bd2253b4bbf2cac9bf811cfd09cbda2654ac`;
  file SHA is `20871116e69dcce9249891658a0d4c70561fa9fdfeff9df3a8b99b328390d7be`; internal runtime is
  8.18 s, CPU-only.
- The common 2005--2024 calendar means correlated macro exposure across instruments. Even the acf-squared pass is
  instrument-holdout evidence only and cannot establish time-independent universality.

### Open questions / blockers

- Static per-symbol universal bands are not a defensible model-selection target; changing gates after this result
  would be post-hoc fitting.
- A physical claim now needs a regime-conditioned estimand plus new macro-time or independent-data confirmation.
  All fourteen markets used in feasibility/fresh confirmation are spent for designing that estimand.

### Next steps

1. Commit/push the immutable result, documentation, log and memory update.
2. Freeze an exploratory, no-cost regime-dependence protocol using spent data only for design, with explicit
   volatility/liquidity labels, time ordering and surrogate controls.
3. Use the exploratory result to define one falsifiable physical law; reserve a new time/data holdout before any
   confirmatory run or EcoMD training. Keep both V100s idle.
