# 2026-05-29

## Session 1 — exp 107 (Path A) + 105 (ABIDES) review; Path A falsified; build exp 104 (Path B0 MMD)

### Goal
Pull the H20 results (exp 107 long-rollout surrogates = Path A; exp 105 ABIDES
ceiling probe), apply the pre-registered gate, and — per the no-downgrade
ladder — execute the next escalation rather than soften. User picked B0
(MMD long-rollout = exp 104) as the next H20 move.

### What was done
- **Pulled** `1612b350a` (107: 90/90 results; 105: 5 ABIDES variant dirs).
- **Path A gate (exp 107)** — Welch t + Bonferroni (α=0.05/2), vs the new N=2000
  baseline_v3. **FALSIFIED**: `mf_all_mse_longroll` 4.27±2.11 (n=26) vs
  baseline 4.14±1.70 (n=22), Δ=+0.13, **p=0.81**; `longroll_moments` Δ=−0.09,
  p=0.86. The surrogates were LIVE this time (pre-flight verified), so this is a
  fair, clean null. **Hand-built per-fact surrogates do not break the ceiling.**
- **New confound found (constrains Path B, does NOT rescue Path A):** the
  N=2000+bf16 budget regime is unstable. Old baseline_v3 (N=10K, fp32): 2/30
  rejected, mean 4.64. New (N=2000, bf16): **8/30 rejected** (agg-gaussianity
  blowup), mean 4.14. The N cut quadrupled the instability + inflated the noise
  floor. `_compute_rollout_reg_loss` is **truncated BPTT** (peak mem = one
  chunk), so N=2000 was a throughput choice, NOT a memory requirement — N=10K
  costs the same peak memory. ⇒ Path B / exp 104 runs at **N=10K, fp32**.
- **ABIDES (exp 105) = 2–3/11 on all 5 RMSC03 variants.** Misses fat tails
  (hill 4.2–5.6), vol clustering (acf_sq≈0), agg-gaussianity, leverage, zumbach
  — consistent with the literature (vanilla ABIDES needs heavy calibration for
  fat tails). **Two validity gaps before this drives the Paper A narrative:**
  (a) `volume_volatility_corr=1.0` exactly on all 5 → extraction artifact
  (likely |r|-proxy), mark N/A not "fail" (doesn't change ≤4/11); (b) run
  **intraday** (60s bars, 390/sim=1 day) but scored on **daily bands** —
  timescale mismatch (hill/agg-gauss are length+scale sensitive, same trap that
  bit the surrogates). Needs an apples-to-apples re-run (multi-sim-day daily OR
  score EcoMD intraday) before "paradigm-SOTA" is paper-grade. The scoreboard
  showed "0 runs" — scorer glob misses the ABIDES dir layout (cosmetic).
- **Built exp 104 (Path B0 — MMD long-rollout):**
  - **Wiring (was unwired):** threaded the real return series as
    `target_returns` into the long rollout-reg path ONLY. losses.py: the
    distribution-distance block (mmd/wasserstein/sinkhorn) now **skips** (not
    raises) when `target_returns is None`, so the ~7-return main loop trains
    structure only and MMD fires only on the 512-return rollout (enough
    samples). train_distributed.py: build a label→returns map, pass to
    `_compute_rollout_reg_loss`.
  - **Mac liveness probe (deleted after use):** MMD term grad_fn=LIVE, skipped
    when None (no raise), grad reaches sim_returns; rollout-reg path on a tiny
    sim → finite loss, grad reaches 25/28 params.
  - **w_mmd CALIBRATION (the key catch):** raw MMD term ~0.012 vs structural
    (acf_sq+lev) ~1.16 → w_mmd=1 makes MMD ~1% of the loss = negligible = an
    invalid test (same failure shape as the dead surrogates, new cause). w_mmd
    ~50 → MMD ~50% of structural. So the MMD cell is a **sweep {20,50,100}**
    (negligible→dominant) — a bad single guess can't invalidate the test.
  - `experiments/104_mmd_longroll_n30/generate_configs.py`: 5 cells × 30 = 150
    cfg (baseline_v3 / hybrid_nomm_longroll / mmd_w{20,50,100}_longroll), N=10K
    fp32, rollout_reg_steps=512 every=2.
  - Launcher `scripts/h20_104_mmd_2026-05-29.sh` with a `--probe` first-config
    timing+stability gate (N=10K/fp32/every=2 wall-clock is unverified; don't
    burn 6h on an unproven regime).
  - Updated `tests/test_loss_redesign.py` for the new skip-not-raise contract.
    Full suite green (360 passed); only failure is the PRE-EXISTING, unrelated
    `test_dynamic_graph.py::test_default_off_equivalence` golden-value drift
    (fails identically on clean HEAD — flag for a later fix).

### Results / findings
- **Objective-coverage (hand-built per-fact surrogates) is FALSIFIED** at the
  pre-registered gate — fair test, p=0.81. Ladder escalates to Path B (no
  downgrade). The 5.1 best-fairly-tested ceiling stands.
- N=2000+bf16 is an unstable regime — a reusable lesson; rollout-reg permits
  N=10K at one-chunk memory.
- ABIDES weak (2–3/11) ⇒ paradigm-SOTA framing is ON THE TABLE, pending the
  timescale-fair re-run.

### Open questions / blockers
- Does MMD on a 512-return rollout (any w_mmd in 20–100) beat the no-MMD hybrid
  control at the stable N=10K? (exp 104, next H20 run.)
- Exp 104 wall-clock at N=10K/fp32/every=2 unverified → `--probe` gate first.
- ABIDES timescale-fair re-run still owed before paper narrative.
- Pre-existing broken test: `test_dynamic_graph.py::test_default_off_equivalence`.

### Next steps
1. Commit + push exp 104 (code + configs + launcher) + this log + memory.
2. H20: probe one 104 config (timing+stability), then full 150-cfg sweep + score.
3. 2026-05-30 AM: pull, Welch+Bonferroni (best mmd_w* vs hybrid_nomm_longroll
   isolated; vs baseline_v3 headline). Pass → exp 106 5-asset confirm of that
   w_mmd. Fail → distribution-matching falsified → Path B2 (heterogeneous-node
   MoE). Plus: schedule the ABIDES timescale-fair re-run.

## Session 2 — re-plan beyond the framework; build the Neural-SDE stochastic-vol head (tournament hero)

### Goal
User: "重新规划实验，追求最好的结果，不要局限于现在的架构和框架里" with a 5-6h slot now + a
60h weekend. Re-plan as a TOURNAMENT of beyond-framework entrants (user choice),
seeded by a 5-6h SCOUT (user choice). Build the hero entrant — a learned
stochastic-volatility / Neural-SDE diffusion — and pass its pre-registered build gate.

### What was done
- **Re-plan** (plan file pull-adaptive-thacker.md): tournament + scout. Motivation:
  Path A falsified, ABIDES 2-3/11 → ceiling is paradigm-level, and the failing facts
  (#2 tails, #4 agg-gauss, #5 Fano, #8 DFA) are ALL volatility-structure facts. The
  current vol process is fixed/hand-designed/detached — so make the volatility process
  itself learned, multi-timescale, return-coupled, while staying physics-readable
  (generalized Langevin → also serves Paper B).
- **3 parallel Explore agents + 1 Plan agent** confirmed feasibility: the eval/scoring/
  inference pipeline is model-agnostic; the integrator already exposes a gradient-carrying
  `noise_scale_mult` hook; the rollout-reg/MMD path threads only `h_regime`, so the vol
  latent must ride in `PriceState` (and be detached in `_detach_price`).
- **Built A1 — Neural-SDE stochastic-vol head:**
  - `ecomd/models/stoch_vol.py` (NEW): `StochVolProcess` — K-component mean-reverting
    log-OU latent. Multi-timescale (distinct kappa_k → DFA #8 / agg-gauss #4), stochastic
    innovation (fat tails #2 / Fano #5), asymmetric `relu(-r)` leverage coupling (#9/#11).
    Positivity (exp) + hard bound (tanh clamp) by construction → no agg-gauss blow-up.
    `sigma_eff = sigma_price * exp(g * softmax(a)·v)`.
  - `price_formation.py`: `sv_*` fields on `ExcessDemandParams` (flow via free-form
    price_formation_kwargs → zero EcoMDConfig change); `PriceState.vol_latent` field
    (rides the vanilla path automatically); `ExcessDemandPrice.step` applies sigma_eff.
  - `ecomd.py`: `sv_integrator_enabled/sv_integrator_gain` (the same latent scales the
    Langevin noise → multiplicative-noise overdamped Langevin, Paper-B physics); guard
    raising on the grouped-checkpoint path (drops vol_latent).
  - `bptt_step_function.py`: threaded `vol_latent` through the custom-autograd Function
    (`has_vol` flag, mirroring `h_global`) + `step_via_function`. (bptt_custom_function=true
    is set in our configs, so this path is live.)
  - `train.py`: `_detach_price` detaches `vol_latent` (truncated-BPTT → one-chunk memory).
- **Tests + smoke gates (all PASS):** `tests/test_stoch_vol.py` (11 tests: positivity,
  hard bound, leverage asymmetry, OFF-path determinism + no latent, grad liveness through
  BOTH vanilla and custom-fn paths, integrator placement, grouped-checkpoint guard). Full
  suite 370 passed (only the pre-existing unrelated test_dynamic_graph golden-value drift
  deselected). Inference-scale smoke (N=500, sv_d3+integrator, custom-fn): liveness (5/5 sv
  params get grad), loss 1.34→0.95, inference finite, **agg_gauss=411 (<1000), hill=4.20,
  DFA=0.800 (in-band!)** — early signal the SV head produces the missing vol-structure.
- **Scout (exp 108) ready:** `experiments/108_neural_sde_scout/generate_configs.py` (60 cfg
  = 5 cells × 12 seeds, SPX, N=10K fp32, reg_every=8 ~5h): baseline_mmd / sv_d1 / sv_d3 /
  sv_d3_nolev / sv_d3_both. Launcher `scripts/h20_108_neural_sde_scout.sh --probe`.
  Pre-registered scout gate: a cell advances to the weekend tournament iff it lifts ≥1 hard
  floor (#2/#4/#5/#8) w/o ≥20pp collateral AND mean n/11 ≥ baseline_mmd.

### Results / findings
- The SV head is a genuine beyond-framework change (learned multi-timescale stochastic
  volatility), additive behind flags (OFF = bit-exact, verified). Build gate fully passed.
- N=500 smoke already shows non-trivial tail (hill 4.20) + long-memory (DFA 0.800) — the
  exact facts the paradigm ceiling fails. Encouraging but NOT a result (n=1, N=500).

### Open questions / blockers
- Does the SV head lift the hard floors at N=10K n=12 (exp 108 scout)? — Slot 1.
- A2 (heterogeneous-node MoE) and A3 (diffusion/score baseline) for the weekend tournament
  are NOT yet built (Phase-0 stretch items; build before the 60h weekend).

### Next steps
1. Commit + push (this session): stoch_vol head + tests + exp 108 scout + launcher.
2. Slot 1 (5-6h H20): `h20_108_neural_sde_scout.sh --probe` then full; apply the scout gate.
3. Before the weekend: build A2 (MoE) + A3 (diffusion baseline) + the ABIDES timescale-fair
   re-run; assemble the 60h tournament (survivors at n=30 SPX+NDX) + the ceiling map.
