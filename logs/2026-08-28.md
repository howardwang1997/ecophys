# 2026-08-28

## Session 1 (00:50)

### Goal

- PI required the attribution audit be applied to financial markets; explain the connection
  and execute a market family.

### What was done

- Collision checks: the practice of embedding financial invariants in learned market models is
  established and citation-dense (Cohen--Reisinger Neural-SDE 2023, 78 cites; ARBITER 2025;
  martingale-constrained pricing) but **no matched-parameterization attribution audit exists**
  in that literature — same structural gap as the physics side.
- Wrote the Family-M child freeze
  (`papers/proposal/ecomd_constraint_attribution_family_m_freeze_2026-08-28.md`): market-native
  invariants = accounting identities (Σ inventory constant; Σ cash constant with zero fees) in
  a synthetic Gode--Sunder-class CDA with exact settlement; six arms; OOD = high-imbalance and
  volatility-shock regimes (imbalance = the data-flat direction); decision rules inherited
  verbatim; anti-relabel statement distinguishing it from Cycle-7 (bookkeeping-as-topic) and
  Cycle-16 (field validity) closures; no EcoMD codebase use, no participants, no field claims.
- Implemented `scripts/audit_family_m.py` (engine + learner + decomposition). Mini-validation
  caught two bugs: (1) fee pipe made the true dynamics non-conserving — set fee=0 so both
  invariants are exact (verified: |ΔΣ| ≤ 5e-15); (2) **conservation slices misaligned with the
  feature layout** (book features occupy the first 4 dims) — fixed with a BOOK_DIMS offset;
  after the fix, projection and hard drift are exactly 0.0000 and the decoupling control holds.
- Launched the full frozen grid on howard-pc (aux node, `--device cuda`).

### Results / findings

- Mini sanity: attribution pattern replicates (free 8.7 vs free_res 0.56 ≈ hard 0.56 conserving
  error); hard/projection drift exactly zero; **market-specific amplification**: soft-absolute
  inflates conserving error +68% (worse than physics families) and soft_res inflates 2× (in
  physics families soft_res ≈ free_res) — the stochastic-engine targets interact with the
  penalty; to be adjudicated at full training.
- Families B/C still running on both V100s (no JSONs yet; GPUs 98--100%).

### Open questions / blockers

- Family-M full-run adjudication; B/C completion; whether soft_res's market-specific inflation
  survives full training (if yes, it is a genuine domain-dependence finding).

### Next steps

- Pull and adjudicate B/C and M; write family RESULTS files; cross-family synthesis
  (physics × finance) for the paper draft.

## Session 2 (03:30)

### Goal

- Monitor families; adjudicate whatever completes; clear the D-2 verification debt.

### What was done

- Fixed a device bug that crashed family M on howard-pc (`cons_sq` on CPU vs CUDA tensors);
  relaunched; completed.
- Verified the Beucler-line manifest entries and corrected an overclaim in the D-2 manifest:
  their line *does* evaluate out-of-sample climates; the true gap is the missing
  matched-ID/matched-budget parameterization control and error decomposition. Manifest updated.
- Pulled and adjudicated family M (analyzer + frontier dump); wrote
  `experiments/constraint_attribution_audit/familyM/RESULTS.md`.

### Results / findings

- **Family M: attribution PASS (140× at matched cell; hard ≡ free_res with overlapping seed
  bands — the accounting constraint's entire effect is exact drift zeroing); laundering
  conjunction PASS — the only family where it holds (soft@30 inflates conserving error 4.7×
  with reduced drift); decoupling exact (0.00%).**
- Domain-dependence finding: penalty damage amplifies with stochastic targets (A: +18–33%
  deterministic PDEs; M: +100–370% stochastic CDA). Absolute-output arm degrades with capacity
  on noisy targets.
- Families B/C still training on both V100s (140 trainings/job, 4 jobs/GPU).

### Open questions / blockers

- B/C completion; family-C mini residual-inversion flag resolution.

### Next steps

- Adjudicate B/C on completion; cross-family synthesis; paper-draft decision (venue:
  NeurIPS/ICML with cross-domain physics×finance framing).

## Session 3 (05:10)

### Goal

- Adjudicate family B on completion.

### What was done

- Pulled all six family-B JSONs; the ±5% window again hit the non-overlap branch at U-Net ID
  scales, so adjudication used matched-pair same-cell comparisons (same capacity/training =
  matched by construction) plus the frozen-rule checks across all three OOD probe classes;
  wrote `experiments/constraint_attribution_audit/familyB/RESULTS.md`.

### Results / findings

- **Attribution PASS 18/18** (3 systems × 2 σ_f × 3 probes incl. 2× resolution shift; ratios
  12.7–6710×); `hard` statistically indistinguishable from `free_res` (marginally better in
  4/6 settings). **Decoupling exact 18/18.** Laundering conjunction fails as in family A
  (+22–30% inflation without drift reduction ⇒ dominated). soft_res ≈ free_res with mild λ30
  degradation. ID-cost finding replicates in U-Nets.
- Family C (2D) still training on .57.

### Open questions / blockers

- Family-C completion and its mini residual-inversion flag.

### Next steps

- Adjudicate C; then four-family synthesis and the paper-draft decision.

## Session 4 (07:45)

### Goal

- Continue: synthesize adjudicated families while C trains.

### What was done

- Wrote `experiments/constraint_attribution_audit/SYNTHESIS.md` (A/B/M; C row pending): three
  consistent findings (attribution to parameterization 30/30 adjudicated settings; soft-penalty
  damage scales with target stochasticity; decoupling exact 42/42), practical prescription,
  and explicit non-claims (Cycle-7/16 closures untouched).

### Results / findings

- Cross-family pattern is stable enough for the paper skeleton: the prescription (hard-on-
  residual for exact invariants; no strong soft penalties; report decompositions) is supported
  by three families, two domains, two architectures.

### Open questions / blockers

- Family C (~9 h/job scale on shared V100; still training at 98% GPU); its mini residual-
  inversion flag.

### Next steps

- Adjudicate C on completion, fill the synthesis row; then paper-draft decision.

## Session 5 (09:40)

### Goal

- PI approved 1+3: draft paper skeleton while C trains.

### What was done

- Created `papers/paper_d_constraints/` (main.tex + refs.bib): title with fallback, abstract
  with the confirmatory claim, sections with adjudicated A/B/M results and a reserved family-C
  slot with outcome mapping, prescriptions, limitations/non-claims, reproducibility statement;
  eight verified references (Beucler PRL/JAMES-line, HNN, PDEBench, MeshGraphNets, Suh CoRL,
  Cohen–Reisinger, Gode–Sunder).
- Local LaTeX toolchain absent (no pdflatex/xelatex/tectonic on the Mac); compile verification
  deferred (server texlive or tectonic install later).
- Monitored C: ~14.8 h wall per job, still training at 98% GPU.

### Results / findings

- None new (C pending).

### Open questions / blockers

- C completion; first compile check of the skeleton.

### Next steps

- Keep polling C; adjudicate and fill the C row in SYNTHESIS and the paper; run the first
  compile.

## Session 6 (2026-08-29 07:10; logged under 08-28 for continuity)

### Goal

- C landed overnight; adjudicate, review, and complete the four-family synthesis.

### What was done

- The local watcher auto-pulled family C and ran the frozen analyzer (worked as designed).
- Adjudicated C with matched-pair frontier analysis; wrote familyC/RESULTS.md; **the
  parameterization effect inverts** (free beats free_res 4.6–6.3×; hard tracks its residual
  twin; decoupling exact; laundering dominated) → triggered the D0 freeze-and-review clause.
- Identified the mechanism (map-contraction vs near-identity) and verified it by single-factor
  manipulation on the mini grid (ν=2.0: no flip; ν=10.0: flip). Patched
  `audit_family_analytic.py` with --nu/--dt and launched the full-grid exploratory ν-sweep
  (E family) on both idle V100s.
- Updated SYNTHESIS.md (four findings, E row, revised prescription: choose parameterization by
  identity-distance; constraints are free but creditless in both directions) and the paper
  skeleton (new title: "Constraints Deserve No Credit: Output Parameterization Governs OOD
  Error in Learned Simulators with Exact Invariants"; C and E sections filled).

### Results / findings

- Four-family conclusion is stronger than the uniform one: the constraint never matters in
  either direction; the parameterization is causal with a predictable sign.

### Open questions / blockers

- E-family full-grid results (~30 min/job); paper compile check still owed (no local LaTeX).

### Next steps

- Pull E results; finalize synthesis numbers; first compile of the paper skeleton.

## Session 7 (2026-08-29 08:40)

### Goal

- Adjudicate the E-family mechanism sweep.

### What was done

- Pulled E JSONs; dose-response confirmed at full grid; wrote familyE/RESULTS.md; updated
  SYNTHESIS E row and the paper's family-E section; lint fixes (`float | None`).

### Results / findings

- **Dose-response confirmed**: ν = 0.02 → free_res wins 100×; 2.0 → 1.5×; 10.0 → flip, free
  wins 1.9×; hard ≡ free_res at every dose; decoupling exact. Contraction is a usable
  parameterization-choice predictor.

### Open questions / blockers

- Paper compile (no local LaTeX toolchain).

### Next steps

- First compile (install tectonic or server texlive); full manuscript draft; then release-prep
  (code cleanup, per-run provenance bundle).
