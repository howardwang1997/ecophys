# 2026-08-20

## Session 1 (00:14)

### Goal

Complete the result-blind dYdX T0 state audit, formalize its decision, and freeze a reproducible metadata-only
D0 before any Indexer response row is requested.

### What was done

- Recovered exact historical block headers around all six governance deadlines and binary-searched each pair to
  adjacent heights.
- Read block results at the first post-deadline block. Proposals 220 and 314--318 each emit an
  `active_proposal=proposal_passed` event in `EndBlock`; proposal 314's immediately preceding block has no such
  event.
- Queried the public Cosmos governance list as one counted 391-proposal page and compared every frozen ticker's
  immediately previous passed `MsgUpdatePerpetualParams` payload with its event payload.
- Audited official dYdX source. The perpetual-update message requires module authority, is classified as an
  internal message rejected from external transactions, and the trade API filters fills to `Liquidity.TAKER`
  before exposing `side`.
- Wrote the formal T0 result, machine-readable event manifest, D0 narrative/YAML freeze, no-values response
  reducer, acquisition launcher and unit tests.
- The first post-freeze D0 attempt stopped on a 30-second Indexer read timeout and wrote no result manifest. A
  fixed three-retry idempotent-GET transport policy was added without changing any scientific query or gate;
  the amended runner must be committed before retrying.

### Results / findings

- T0 PASS: ten paired and 28 reversal markets have a clean singleton payload change in
  `default_funding_ppm`; the development waves alone contain 21 markets.
- Exact executing heights are 38694464 (220), 63326127 (314), 63626789 (315), 63626888 (316), 63981005 (317)
  and 63981519 (318). Execution occurs in EndBlock, so the partial hour remains dropped.
- The state proof is the complete passed-governance transition ledger plus source-level authority restriction
  plus exact execution event. It is not mislabeled as a historical RPC snapshot.
- Two unit tests pass; targeted Ruff and strict mypy pass. The sanitizer test proves recognizable fake side,
  price, size and IDs cannot appear in the serialized metadata result.
- No market outcome row, GPU, paid data or EcoMD run was used.
- The committed retry run completed D0 from `ddb729525`: 21/21 markets expose non-empty trade, one-minute candle
  and funding history; every fixed candle window has 60 rows; BEAM/ENA/PAXG page pairs have zero overlap. The
  sanitized manifest SHA is `777d5026…` and contains no retained market value.
- Eight markets (`2Z`, `ATH`, `BEAM`, `BERA`, `DRIFT`, `KAITO`, `PAXG`, `S`) had the same latest trade at the
  execution and +20,000-block cursors. This is a serious activity/power warning, not a D0 schema failure.

### Open questions / blockers

- D0 passed, but full pre-period activity eligibility remains unknown and may shrink the 21-market development
  cohort below a defensible stacked-design size.
- Passing metadata coverage does not establish the behavioral effect or activity eligibility over the full
  56-day windows.

### Next steps

1. Commit and push the D0 manifest/result.
2. Freeze and commit D1A: 28 pre-event daily candles, 21 development markets, unchanged 100-trades-on-21-days
   rule, and stop below 15 eligible markets.
3. Run D1A before bulk trades. Keep proposal 317 and proposal 220 outcome windows sealed.

### D1A freeze addendum (00:31)

- Froze a 21-request daily-candle activity screen before opening any daily trade count.
- It retains only pre-period UTC date and `trades`; recognizable OHLC/volume/OI fixtures are forbidden from the
  serialized result by unit test.
- The inherited rule remains 100 trades on 21 of 28 complete pre-event days. Proceed at 20--21 markets, AMBER
  at 15--19 and stop the dYdX-only route below 15. Proposal 317 cannot rescue the gate.

### D1A result addendum (00:38)

- Ran the frozen 21-request activity screen from clean commit `fba2c9a2b`; canonical result SHA is `536cfcd5…`.
- All 21 markets have all 28 requested daily candles. Official source confirms the candle `trades` field
  increments once per trade.
- Only HYPE qualifies (28/28 days). ENA has 20 qualifying days, SPX 15, ASTER 11 and all others at most five.
- Formal decision: hard STOP dYdX-only before bulk ticks. Proposal 317 cannot rescue the gate; no threshold
  relaxation, one-market anecdote, D1B, GPU, paid data or EcoMD run is allowed.
- The intervention-first identification logic and outcome-blind tooling survive as reusable infrastructure. A
  new candidate must pass activity metadata before hypothesis freeze and supply at least 15 active units or
  repeated independent interventions in liquid units.

## Session 2 (00:59)

### Goal

Screen a higher-liquidity free-data intervention domain without reading market outcomes, then freeze a
reproducible T0 before any Aave borrowing or repayment log.

### What was done

- Audited the official Aave governance cache and proposal repository in temporary clones. The cache contains
  proposal-to-cross-chain-payload mappings and exact `PayloadExecuted` block/transaction events; proposal
  source contains executable payloads and before/after state diffs.
- Identified six executed Ethereum Core proposals (3, 94, 130, 159, 247, 271) whose DAI, USDC and USDT changes
  have only `variableRateSlope1` as a configured field. Derived maximum-rate changes and legacy strategy-address
  replacements are recorded but are not additional configured policy changes.
- Separated 15 primary rate-decrease market-event units from three reverse-sign probe units. Excluded proposal
  69 from the singleton panel because it also changes `UOptimal`, and proposal 216 because it changes slope 1
  and slope 2.
- Read the Aave Interest Rate Curve Risk Oracle specification. It already estimates aggregate isoelastic,
  exponential and linear demand models from 10--15 minute data and optimizes slope 1, ruling out generic demand
  elasticity as our contribution.
- Reframed the candidate as rate-step response spectroscopy: test whether responses collapse under integrated
  incremental carry and whether a censored/feedback-aware inversion can recover adjustment barriers.
- Verified that the Aave dataset preprint's claimed Zenodo DOI currently returns an unregistered-identifier 404;
  an exact-title API search returns zero records. The claimed 50-million-row dataset is not treated as acquired.
- Added the frozen T0 config, audit helpers, executable audit, three unit tests, narrative protocol and long-term
  memory. Targeted pytest and Ruff pass; strict mypy passes.

### Results / findings

- The metadata-stage candidate clears the inherited 15-unit floor on paper (18 singleton units), unlike dYdX,
  but it has not yet passed formal T0 or the pre-period activity gate.
- Free direct chain logs are a viable fallback route; the public Ethereum RPC answered chain-ID and event-topic
  metadata probes without opening a market outcome.
- The potential larger story is real-agent adjustment friction under exact rate steps, not simulator error and
  not an aggregate elasticity re-fit.

### Open questions / blockers

- Formal T0 must run from a clean commit against the pinned official repository SHAs.
- Governance timing is anticipated and policy is endogenous; cross-chain stagger and unaffected assets are only
  candidate controls, not yet a valid design.
- Risk-steward rate changes between governance proposals require a complete intervention ledger.
- Entry after rate decreases needs a defensible at-risk population and cannot be inferred from transaction counts
  alone.

### Next steps

1. Commit the frozen T0 before running it.
2. Run formal T0 and record the exact payload executions, source diffs, Zenodo failure and RPC metadata only.
3. If T0 passes, freeze and commit a 28-day pre-period-only activity screen; do not query any post-event value.
4. Keep all GPUs idle until activity, identification and novelty gates authorize a compute experiment.

### T0 audit correction addendum (01:04)

- The first formal attempt stopped without writing a manifest because proposal 3's rendered diff includes a
  synchronized `baseStableBorrowRate` change in addition to `variableRateSlope1`.
- Pinned payload source sets only `variableRateSlope1`; every other rate parameter, including
  `baseStableRateOffset`, is `KEEP_CURRENT`. The pinned Aave helper factory reconstructs
  `baseStableRateOffset` as `baseStableBorrowRate - variableRateSlope1`, proving the displayed base stable rate
  is algebraically derived.
- Amend the parser to classify this field as derived, add the identity and helper-source pin to the protocol,
  and commit the technical correction before any retry. No market log or outcome was opened.

### T0 result addendum (01:06)

- Reran formal T0 from clean commit `15e01b577` after the source-backed parser correction.
- T0 PASS: six proposals map one-to-one to exact Ethereum payload executions; 18/18 selected asset-event diffs
  pass the singleton configured-change audit. The five rate decreases provide exactly 15 primary units.
- The free RPC metadata probe passes. No market log was queried. Zenodo still returns 404/unregistered and exact
  title search zero; no bulk download was attempted.
- Canonical result SHA is `fd696f66…`. The only authorized next action is a separately frozen, pre-period-only
  activity screen. GPU, paid data, EcoMD and post-event values remain locked.

### T0 manifest correction addendum (01:07)

- A read-back check found that the reported canonical SHA was not JSON-round-trip stable: two YAML exclusion
  maps used integer proposal IDs as keys, which become strings after serialization and sort differently.
- The T0 scientific decision and every execution/diff audit remain unchanged, but `fd696f66…` is not accepted as
  the final canonical artifact identifier.
- Normalize through JSON before canonical hashing, add an integer-key regression test, commit the correction,
  and rerun T0 from the new clean commit before freezing D1A.

### Final T0 rerun addendum (01:09)

- Reran T0 from clean canonicalization-fix commit `479534f16`.
- Final decision remains PASS with 18/18 singleton asset-event units and no market outcome access.
- Canonical SHA is `9db20918…`; independent JSON read-back recomputation matches exactly.
- The corrected artifact now authorizes writing and committing D1A, but still does not authorize a pre-period log
  request until that freeze is in a clean commit.

## Session 3 (01:21)

### Goal

Freeze a reproducible, outcome-blind Aave D1A pre-period activity screen before making the first market-log
request.

### What was done

- Defined four consecutive seven-day bins over the 28 days before each exact payload execution; the boundary is
  the first block at or after each target UTC time and the execution block is excluded.
- Froze per-unit weekly floors of 10 Borrows, 10 Repays and 10 distinct debt users, plus 28-day floors of 200
  combined actions and 50 distinct debt users.
- Kept the hard panel gate at 15/18 total, 12/15 primary decreases, 2/3 reverse-sign probes and at least two
  eligible assets per proposal. There is no post-hoc amber rescue.
- Implemented a direct Ethereum RPC runner with exact T0 digest/execution-header checks, fixed 5,000-block
  chunks and range splitting that cannot alter scientific windows.
- Implemented a pure aggregate reducer. Participant topics are used only in memory; amounts, rates, transaction
  hashes, per-log identifiers, raw logs and response bodies cannot enter the output.
- Added recognizable-secret and duplicate-log regression tests. Six targeted tests, Ruff and strict mypy pass.

### Results / findings

- D1A is ready to commit before data access. No Aave Borrow/Repay log has yet been requested.
- The screen needs only CPU and public RPC access. GPU idleness remains an explicit identification gate, not a
  compute constraint.

### Open questions / blockers

- Actual pre-period activity remains sealed until the freeze commit is clean.
- Even a D1A pass leaves anticipation, policy endogeneity, intervening risk-steward changes, repeated-unit
  dependence, the at-risk population and utilization feedback unresolved.

### Next steps

1. Commit and push the D1A freeze.
2. Run the aggregate-only activity screen from that clean commit and verify the JSON-round-trip digest.
3. Record and commit the pass/stop result. If it passes, freeze D1B before any post-event query.

### D1A transport-failure addendum (01:25)

- The first formal attempt from clean commit `4ed11b638` stopped at the first historical `eth_getLogs` call.
  PublicNode returned HTTP 403 and explicitly required a personal token for archive requests. No Aave log result
  was returned and no result artifact was written.
- The runner incorrectly treated the 403 as a range-size problem and recursively split it. This did not expose
  data but caused avoidable requests; it is now classified as a fatal transport/authorization failure.
- Outcome-blind capability probes used the impossible address `0x000…001` at historical block 19,000,000.
  dRPC and Flashbots both returned zero rows for one-block and 5,000-block spans. dRPC's official documentation
  names `https://eth.drpc.org/` as a public endpoint and lists `eth_getLogs` without the paid-tier marker.
- Amend only the replaceable transport to dRPC and restrict recursive splitting to recognized range/result-size
  errors. Scientific windows, event topics, retained fields and all activity/decision thresholds are unchanged.
- Commit and push this technical amendment before retrying formal D1A.

### D1A public-rate-limit addendum (01:31)

- The retry from clean transport commit `e617f85aa` stopped on dRPC HTTP 429 and wrote no artifact. Authorized
  pre-period logs may have been returned transiently before the failure, but no aggregate value was printed,
  inspected or persisted.
- Official dRPC documentation confirms dynamic rate limits on free public nodes, a two-second provider timeout
  and a 10,000-log cap. Add a conservative fixed 0.5-second minimum request interval and at most six explicit
  429 retries with capped exponential backoff.
- Record actual RPC method counts, 429 retry count and total backoff time in the sanitized output. Do not split
  a 429 or ambiguous RPC limit code as if it were a block-range failure.
- Ten targeted tests, Ruff and strict mypy pass after adding pacing/backoff and the non-splittable rate-limit
  regression cases.
- This technical scheduling change leaves the committed proposals, assets, event topics, block windows,
  retained counts and every scientific threshold unchanged. Commit before retrying again.

### D1A free-provider-timeout addendum (01:38)

- The paced run from clean `1115109ab` survived about four minutes and then stopped on dRPC HTTP 408, explicitly
  its free-plan request timeout. It wrote no final artifact.
- Classify HTTP 408/413 and an exhausted 504 as splittable transport-size/time errors. Bisection changes only
  chunk delivery; the exact block union stays fixed. Keep 403, 429 and ambiguous numeric limit codes fatal or
  explicitly rate-limited.
- Add an outside-repository checkpoint after every complete proposal. It may contain only boundary metadata and
  already allowed aggregate counts, is bound to repository/config/T0 digests, must be a prefix of the frozen
  proposal order, and is removed after success. Raw logs and participant/amount/rate/transaction values remain
  forbidden.
- Eleven targeted tests, Ruff and strict mypy pass after the timeout classifier and checkpoint round-trip test.
- Commit this recoverability amendment before another run; do not change activity or decision thresholds.

### Formal D1A result addendum (02:04)

- Ran the resumable protocol from clean commit `c0ebdf50d`. Formal decision: PASS to a separately frozen
  intervention-ledger/identification gate.
- All 18/18 units qualify: 15/15 primary rate decreases, 3/3 reverse-sign probes and 3/3 assets for every
  proposal. All four frozen decision checks pass.
- The weakest weekly counts are 42 Borrows, 46 Repays and 49 distinct debt users; the weakest 28-day cells have
  564 combined actions and 210 distinct debt users. The pass is not threshold-edge.
- Canonical result SHA is `f3ca5d19…`; an independent JSON read-back recomputation matches exactly. All weekly
  boundaries also land exactly on their target block timestamps.
- The successful invocation made 481 block and 306 log calls, bisected one slow range, retried 107 HTTP-429s
  and recorded 720 seconds of backoff. The sanitized `/tmp` checkpoint was deleted after success.
- A key scan confirms the result contains no raw data, participant, amount, transaction/log identifier, realized
  rate or utilization field. No execution/post-event log, reserve update, position/balance call, paid data, GPU
  or EcoMD was used.
- D1A only clears visible activity/power. It does not resolve anticipation, endogenous policy, contaminating
  reserve changes, repeated assets, at-risk population or utilization feedback, and it is not a causal result.

### Next steps

1. Commit and push the D1A result before further acquisition.
2. Freeze D1B: complete reserve-intervention ledger, announcement-to-execution timing, candidate cross-chain
   stagger/controls and falsification rules.
3. Reproduce extraction against Flashbots or a controlled archive node before the main study.
4. Keep post-event outcomes and all GPUs sealed until D1B passes.

## Session 4 (02:18)

### Goal

Translate the D1A activity pass into a hard identification gate before opening any post-execution behavior.

### What was done

- Audited official Aave governance metadata and found proposal creation already precedes Ethereum execution by
  several days; linked ARFC discussions are earlier. Froze execution-as-surprise claims as forbidden.
- Pinned official historical V3 Core, current V3 Origin and address-book repositories. The union is necessary
  because the study spans legacy `ReserveInterestRateStrategyChanged` and later configuration interfaces.
- Enumerated PoolConfigurator reserve/rate/cap/collateral/status/eMode/token-upgrade events, Oracle source events,
  AddressesProvider upgrades and reward-configuration changes. Historical unindexed asset events prevent a
  topic1-only shortcut.
- Confirmed that all six governance proposals map to many V3 chain payloads and that pinned source directories
  contain per-chain payload implementations. Exact cross-chain execution times remain sealed for formal D1B.
- Froze D1B YAML and narrative: ledger support day −35 to +28, no material confounder in day −14 to +14,
  minimum 14 clean post days, and administrative right censoring for later events.
- Froze the cross-chain gate before aggregating execution times: at least four proposals must each have three
  executed V3 chains, two non-Ethereum comparators, a source-audited slope1-only stablecoin update and at least
  24 hours of execution stagger.

### Results / findings

- The original naive causal story is invalid even before outcome data: payload execution is anticipated.
- The viable high-impact design is common announcement plus staggered implementation, coupled to a new
  anticipation/censoring-aware response-spectroscopy method. It remains only a candidate until D1B/D1C pass.
- D1B is metadata/configuration-only and requires no GPU or paid data.

### Open questions / blockers

- Exact protocol-side contamination counts, forum leads and cross-chain stagger are not yet formally computed.
- Cross-chain payload presence does not imply comparable assets, liquidity or users; D1C must test those without
  post-event data if D1B passes.

### Next steps

1. Add ABI/source parsers and sanitized policy-ledger reducers with tests.
2. Commit and push the D1B freeze before formal forum, configuration-log or execution-stagger aggregation.
3. Run D1B from the clean commit and obey its NCS stop line.

## Session 5 (02:37)

### Goal

Implement the frozen D1B auditor completely before the formal metadata and policy-ledger run.

### What was done

- Added pure Solidity ABI extraction with canonical event signatures and indexed-position checks.
- Added a non-compiling historical proposal parser supporting both legacy `_bpsToRay(...)` and current
  direct-basis-point rate inputs; it reports every configured field and accepts a comparator only when slope 1
  is the sole change.
- Added a sanitized policy-log reducer for indexed underlying assets, the legacy unindexed isolation event,
  selected a/debt reward tokens and global configuration events. Raw topics/data/addresses are discarded;
  only policy provenance, scope and a data digest survive.
- Added exact-T0 target matching, ±14-day contamination failure, day-14-to-28 administrative censoring and
  hard panel arithmetic, plus regression tests for raw-value erasure and no amber fallback.
- Implemented the formal D1B runner: pinned-repository/ABI/address verification, Discourse/governance timing,
  historical per-chain source audit, exact `PayloadExecuted` resolution, merged Ethereum policy-log windows,
  robust public-RPC transport and canonical result hashing.
- Clarified before the formal run that only DAI/DAIe, USDC/USDCe/USDCn and USDT/USDTe aliases count, and that
  stagger uses executed source-audited comparable chains including Ethereum. Missing source is non-comparable.

### Results / findings

- Twelve pure qualification tests and five existing runner tests pass; Ruff and strict core mypy pass.
- All frozen ABI signatures are present in the pinned official sources, and all frozen Ethereum addresses are
  present in the pinned address book.
- A development source/parser check succeeds across all six historical proposal commits. This is not yet the
  formal D1B artifact; the full run still requires a clean implementation commit.
- No Borrow/Repay response, reserve outcome, utilization, realized rate, position, price or volume was queried.

### Open questions / blockers

- The formal forum and Ethereum policy-ledger transport has not yet run from a clean commit.
- Passing D1B would still authorize only D1C pre-period comparability, not response outcomes or GPU work.

### Next steps

1. Run all targeted checks, commit and push the D1B implementation/freeze clarification.
2. Execute the full D1B audit from that clean commit and independently verify the artifact digest.
3. Obey the frozen pass/stop result before scheduling any cross-chain behavioral or GPU experiment.

### D1B topic-hash transport addendum (02:42)

- The first clean D1B invocation from `06b202bd9` resolved local pinned sources and forum/cross-chain metadata,
  then stopped before `eth_getLogs`: Flashbots rejected `web3_sha3` with HTTP 403 because the method is not
  whitelisted. No result artifact or policy event was returned or persisted.
- Replace the denied hash RPC with local OpenSSL Keccak-256. A regression test fixes the canonical ERC-20
  `Transfer(address,address,uint256)` topic, and an independent command reproduces the already frozen Aave
  Borrow topic exactly.
- This is a transport-only correction. The ABI-derived signature set, contract addresses, block windows,
  materiality rules and all pass/stop thresholds remain unchanged. Commit it before retrying formal D1B.

### D1B primary rate-limit addendum (02:47)

- The local-hash retry from clean `b89e5251f` stopped during time-to-block boundary resolution, before the first
  `eth_getLogs`, after Flashbots exhausted all six HTTP-429 backoffs. No result artifact was written.
- Move the formal replaceable transport to dRPC, which completed D1A historical access, and restore its tested
  0.5-second pacing. Keep Flashbots recorded as the failed primary and future independent reproduction source.
- Add a repository-external checkpoint after all boundaries and after each disjoint merged policy interval. It
  is bound to the code/config/T0/D1A/RPC identity, contains only boundary metadata and sanitized policy-event
  summaries, must be a prefix of the merged intervals, and is deleted after success.
- This changes recoverability and transport only; the block union, event topics, materiality, timing windows and
  decision thresholds remain frozen.

### Formal D1B result addendum (03:09)

- Completed D1B from clean commit `3a4fe2196`. Canonical result SHA is `e7a1b74f…`; independent JSON read-back
  recomputation matches, and the external checkpoint was deleted.
- Both nontrivial identification gates fail decisively. Only proposal 159's three assets are clean (3/18 total,
  3/15 primary, 0/3 reverse; required 15/12/2), and only proposals 3 and 271 exceed 24 hours of comparable
  cross-chain execution stagger (2/6; required four).
- All 18 target rate events match the exact T0 transaction exactly once, so the clean-panel failure is not a
  target-extraction error. Official governance metadata links major contaminants to the Pool update and v3.1,
  v3.2 and v3.3 upgrades plus collateral/eMode proposals; direct on-chain logs cover risk-steward changes.
- All executions are anticipated: public announcement leads are 255.82--784.20 hours. The surprise-event-study
  framing is independently ruled out.
- The run retained 669 sanitized policy events across four merged intervals and made 519 block plus 546 log
  calls, with 157 429 retries, 484 seconds of backoff and no range split. No raw log or behavioral outcome was
  retained; no paid data, GPU or EcoMD was used.
- Binding decision: `stop_ncs_causal_route_before_behavioral_outcomes`. Do not run D1C, query response outcomes,
  relax timing/cleanliness rules, or treat one proposal's three assets as independent experiments.

### Next steps

1. Commit and push the formal D1B artifact and stop document.
2. Preserve the activity/source/governance/policy-ledger code as reusable infrastructure, not a paper claim.
3. Return to problem selection and run a zero-cost novelty/identifiability gate before opening another dataset
   or scheduling any V100/RTX 2060 work.
