# 2026-08-20

## Session 1 (00:14)

### Goal

Complete the result-blind dYdX T0 state audit, formalize its decision, and freeze a reproducible metadata-only
D0 before any Indexer response row is requested.

### What was done

- Recovered exact historical block headers around all six governance deadlines and binary-searched each pair to
  adjacent heights.
- Read block results at the first post-deadline block. Proposals 220 and 314--318 each emit an
  `active_proposal=proposal_passed` event in `EndBlock`; proposal 314's immediately preceding block has no such
  event.
- Queried the public Cosmos governance list as one counted 391-proposal page and compared every frozen ticker's
  immediately previous passed `MsgUpdatePerpetualParams` payload with its event payload.
- Audited official dYdX source. The perpetual-update message requires module authority, is classified as an
  internal message rejected from external transactions, and the trade API filters fills to `Liquidity.TAKER`
  before exposing `side`.
- Wrote the formal T0 result, machine-readable event manifest, D0 narrative/YAML freeze, no-values response
  reducer, acquisition launcher and unit tests.
- The first post-freeze D0 attempt stopped on a 30-second Indexer read timeout and wrote no result manifest. A
  fixed three-retry idempotent-GET transport policy was added without changing any scientific query or gate;
  the amended runner must be committed before retrying.

### Results / findings

- T0 PASS: ten paired and 28 reversal markets have a clean singleton payload change in
  `default_funding_ppm`; the development waves alone contain 21 markets.
- Exact executing heights are 38694464 (220), 63326127 (314), 63626789 (315), 63626888 (316), 63981005 (317)
  and 63981519 (318). Execution occurs in EndBlock, so the partial hour remains dropped.
- The state proof is the complete passed-governance transition ledger plus source-level authority restriction
  plus exact execution event. It is not mislabeled as a historical RPC snapshot.
- Two unit tests pass; targeted Ruff and strict mypy pass. The sanitizer test proves recognizable fake side,
  price, size and IDs cannot appear in the serialized metadata result.
- No market outcome row, GPU, paid data or EcoMD run was used.
- The committed retry run completed D0 from `ddb729525`: 21/21 markets expose non-empty trade, one-minute candle
  and funding history; every fixed candle window has 60 rows; BEAM/ENA/PAXG page pairs have zero overlap. The
  sanitized manifest SHA is `777d5026…` and contains no retained market value.
- Eight markets (`2Z`, `ATH`, `BEAM`, `BERA`, `DRIFT`, `KAITO`, `PAXG`, `S`) had the same latest trade at the
  execution and +20,000-block cursors. This is a serious activity/power warning, not a D0 schema failure.

### Open questions / blockers

- D0 passed, but full pre-period activity eligibility remains unknown and may shrink the 21-market development
  cohort below a defensible stacked-design size.
- Passing metadata coverage does not establish the behavioral effect or activity eligibility over the full
  56-day windows.

### Next steps

1. Commit and push the D0 manifest/result.
2. Freeze and commit D1A: 28 pre-event daily candles, 21 development markets, unchanged 100-trades-on-21-days
   rule, and stop below 15 eligible markets.
3. Run D1A before bulk trades. Keep proposal 317 and proposal 220 outcome windows sealed.

### D1A freeze addendum (00:31)

- Froze a 21-request daily-candle activity screen before opening any daily trade count.
- It retains only pre-period UTC date and `trades`; recognizable OHLC/volume/OI fixtures are forbidden from the
  serialized result by unit test.
- The inherited rule remains 100 trades on 21 of 28 complete pre-event days. Proceed at 20--21 markets, AMBER
  at 15--19 and stop the dYdX-only route below 15. Proposal 317 cannot rescue the gate.

### D1A result addendum (00:38)

- Ran the frozen 21-request activity screen from clean commit `fba2c9a2b`; canonical result SHA is `536cfcd5…`.
- All 21 markets have all 28 requested daily candles. Official source confirms the candle `trades` field
  increments once per trade.
- Only HYPE qualifies (28/28 days). ENA has 20 qualifying days, SPX 15, ASTER 11 and all others at most five.
- Formal decision: hard STOP dYdX-only before bulk ticks. Proposal 317 cannot rescue the gate; no threshold
  relaxation, one-market anecdote, D1B, GPU, paid data or EcoMD run is allowed.
- The intervention-first identification logic and outcome-blind tooling survive as reusable infrastructure. A
  new candidate must pass activity metadata before hypothesis freeze and supply at least 15 active units or
  repeated independent interventions in liquid units.

## Session 2 (00:59)

### Goal

Screen a higher-liquidity free-data intervention domain without reading market outcomes, then freeze a
reproducible T0 before any Aave borrowing or repayment log.

### What was done

- Audited the official Aave governance cache and proposal repository in temporary clones. The cache contains
  proposal-to-cross-chain-payload mappings and exact `PayloadExecuted` block/transaction events; proposal
  source contains executable payloads and before/after state diffs.
- Identified six executed Ethereum Core proposals (3, 94, 130, 159, 247, 271) whose DAI, USDC and USDT changes
  have only `variableRateSlope1` as a configured field. Derived maximum-rate changes and legacy strategy-address
  replacements are recorded but are not additional configured policy changes.
- Separated 15 primary rate-decrease market-event units from three reverse-sign probe units. Excluded proposal
  69 from the singleton panel because it also changes `UOptimal`, and proposal 216 because it changes slope 1
  and slope 2.
- Read the Aave Interest Rate Curve Risk Oracle specification. It already estimates aggregate isoelastic,
  exponential and linear demand models from 10--15 minute data and optimizes slope 1, ruling out generic demand
  elasticity as our contribution.
- Reframed the candidate as rate-step response spectroscopy: test whether responses collapse under integrated
  incremental carry and whether a censored/feedback-aware inversion can recover adjustment barriers.
- Verified that the Aave dataset preprint's claimed Zenodo DOI currently returns an unregistered-identifier 404;
  an exact-title API search returns zero records. The claimed 50-million-row dataset is not treated as acquired.
- Added the frozen T0 config, audit helpers, executable audit, three unit tests, narrative protocol and long-term
  memory. Targeted pytest and Ruff pass; strict mypy passes.

### Results / findings

- The metadata-stage candidate clears the inherited 15-unit floor on paper (18 singleton units), unlike dYdX,
  but it has not yet passed formal T0 or the pre-period activity gate.
- Free direct chain logs are a viable fallback route; the public Ethereum RPC answered chain-ID and event-topic
  metadata probes without opening a market outcome.
- The potential larger story is real-agent adjustment friction under exact rate steps, not simulator error and
  not an aggregate elasticity re-fit.

### Open questions / blockers

- Formal T0 must run from a clean commit against the pinned official repository SHAs.
- Governance timing is anticipated and policy is endogenous; cross-chain stagger and unaffected assets are only
  candidate controls, not yet a valid design.
- Risk-steward rate changes between governance proposals require a complete intervention ledger.
- Entry after rate decreases needs a defensible at-risk population and cannot be inferred from transaction counts
  alone.

### Next steps

1. Commit the frozen T0 before running it.
2. Run formal T0 and record the exact payload executions, source diffs, Zenodo failure and RPC metadata only.
3. If T0 passes, freeze and commit a 28-day pre-period-only activity screen; do not query any post-event value.
4. Keep all GPUs idle until activity, identification and novelty gates authorize a compute experiment.

### T0 audit correction addendum (01:04)

- The first formal attempt stopped without writing a manifest because proposal 3's rendered diff includes a
  synchronized `baseStableBorrowRate` change in addition to `variableRateSlope1`.
- Pinned payload source sets only `variableRateSlope1`; every other rate parameter, including
  `baseStableRateOffset`, is `KEEP_CURRENT`. The pinned Aave helper factory reconstructs
  `baseStableRateOffset` as `baseStableBorrowRate - variableRateSlope1`, proving the displayed base stable rate
  is algebraically derived.
- Amend the parser to classify this field as derived, add the identity and helper-source pin to the protocol,
  and commit the technical correction before any retry. No market log or outcome was opened.

### T0 result addendum (01:06)

- Reran formal T0 from clean commit `15e01b577` after the source-backed parser correction.
- T0 PASS: six proposals map one-to-one to exact Ethereum payload executions; 18/18 selected asset-event diffs
  pass the singleton configured-change audit. The five rate decreases provide exactly 15 primary units.
- The free RPC metadata probe passes. No market log was queried. Zenodo still returns 404/unregistered and exact
  title search zero; no bulk download was attempted.
- Canonical result SHA is `fd696f66…`. The only authorized next action is a separately frozen, pre-period-only
  activity screen. GPU, paid data, EcoMD and post-event values remain locked.

### T0 manifest correction addendum (01:07)

- A read-back check found that the reported canonical SHA was not JSON-round-trip stable: two YAML exclusion
  maps used integer proposal IDs as keys, which become strings after serialization and sort differently.
- The T0 scientific decision and every execution/diff audit remain unchanged, but `fd696f66…` is not accepted as
  the final canonical artifact identifier.
- Normalize through JSON before canonical hashing, add an integer-key regression test, commit the correction,
  and rerun T0 from the new clean commit before freezing D1A.

### Final T0 rerun addendum (01:09)

- Reran T0 from clean canonicalization-fix commit `479534f16`.
- Final decision remains PASS with 18/18 singleton asset-event units and no market outcome access.
- Canonical SHA is `9db20918…`; independent JSON read-back recomputation matches exactly.
- The corrected artifact now authorizes writing and committing D1A, but still does not authorize a pre-period log
  request until that freeze is in a clean commit.

## Session 3 (01:21)

### Goal

Freeze a reproducible, outcome-blind Aave D1A pre-period activity screen before making the first market-log
request.

### What was done

- Defined four consecutive seven-day bins over the 28 days before each exact payload execution; the boundary is
  the first block at or after each target UTC time and the execution block is excluded.
- Froze per-unit weekly floors of 10 Borrows, 10 Repays and 10 distinct debt users, plus 28-day floors of 200
  combined actions and 50 distinct debt users.
- Kept the hard panel gate at 15/18 total, 12/15 primary decreases, 2/3 reverse-sign probes and at least two
  eligible assets per proposal. There is no post-hoc amber rescue.
- Implemented a direct Ethereum RPC runner with exact T0 digest/execution-header checks, fixed 5,000-block
  chunks and range splitting that cannot alter scientific windows.
- Implemented a pure aggregate reducer. Participant topics are used only in memory; amounts, rates, transaction
  hashes, per-log identifiers, raw logs and response bodies cannot enter the output.
- Added recognizable-secret and duplicate-log regression tests. Six targeted tests, Ruff and strict mypy pass.

### Results / findings

- D1A is ready to commit before data access. No Aave Borrow/Repay log has yet been requested.
- The screen needs only CPU and public RPC access. GPU idleness remains an explicit identification gate, not a
  compute constraint.

### Open questions / blockers

- Actual pre-period activity remains sealed until the freeze commit is clean.
- Even a D1A pass leaves anticipation, policy endogeneity, intervening risk-steward changes, repeated-unit
  dependence, the at-risk population and utilization feedback unresolved.

### Next steps

1. Commit and push the D1A freeze.
2. Run the aggregate-only activity screen from that clean commit and verify the JSON-round-trip digest.
3. Record and commit the pass/stop result. If it passes, freeze D1B before any post-event query.

### D1A transport-failure addendum (01:25)

- The first formal attempt from clean commit `4ed11b638` stopped at the first historical `eth_getLogs` call.
  PublicNode returned HTTP 403 and explicitly required a personal token for archive requests. No Aave log result
  was returned and no result artifact was written.
- The runner incorrectly treated the 403 as a range-size problem and recursively split it. This did not expose
  data but caused avoidable requests; it is now classified as a fatal transport/authorization failure.
- Outcome-blind capability probes used the impossible address `0x000…001` at historical block 19,000,000.
  dRPC and Flashbots both returned zero rows for one-block and 5,000-block spans. dRPC's official documentation
  names `https://eth.drpc.org/` as a public endpoint and lists `eth_getLogs` without the paid-tier marker.
- Amend only the replaceable transport to dRPC and restrict recursive splitting to recognized range/result-size
  errors. Scientific windows, event topics, retained fields and all activity/decision thresholds are unchanged.
- Commit and push this technical amendment before retrying formal D1A.

### D1A public-rate-limit addendum (01:31)

- The retry from clean transport commit `e617f85aa` stopped on dRPC HTTP 429 and wrote no artifact. Authorized
  pre-period logs may have been returned transiently before the failure, but no aggregate value was printed,
  inspected or persisted.
- Official dRPC documentation confirms dynamic rate limits on free public nodes, a two-second provider timeout
  and a 10,000-log cap. Add a conservative fixed 0.5-second minimum request interval and at most six explicit
  429 retries with capped exponential backoff.
- Record actual RPC method counts, 429 retry count and total backoff time in the sanitized output. Do not split
  a 429 or ambiguous RPC limit code as if it were a block-range failure.
- Ten targeted tests, Ruff and strict mypy pass after adding pacing/backoff and the non-splittable rate-limit
  regression cases.
- This technical scheduling change leaves the committed proposals, assets, event topics, block windows,
  retained counts and every scientific threshold unchanged. Commit before retrying again.

### D1A free-provider-timeout addendum (01:38)

- The paced run from clean `1115109ab` survived about four minutes and then stopped on dRPC HTTP 408, explicitly
  its free-plan request timeout. It wrote no final artifact.
- Classify HTTP 408/413 and an exhausted 504 as splittable transport-size/time errors. Bisection changes only
  chunk delivery; the exact block union stays fixed. Keep 403, 429 and ambiguous numeric limit codes fatal or
  explicitly rate-limited.
- Add an outside-repository checkpoint after every complete proposal. It may contain only boundary metadata and
  already allowed aggregate counts, is bound to repository/config/T0 digests, must be a prefix of the frozen
  proposal order, and is removed after success. Raw logs and participant/amount/rate/transaction values remain
  forbidden.
- Eleven targeted tests, Ruff and strict mypy pass after the timeout classifier and checkpoint round-trip test.
- Commit this recoverability amendment before another run; do not change activity or decision thresholds.

### Formal D1A result addendum (02:04)

- Ran the resumable protocol from clean commit `c0ebdf50d`. Formal decision: PASS to a separately frozen
  intervention-ledger/identification gate.
- All 18/18 units qualify: 15/15 primary rate decreases, 3/3 reverse-sign probes and 3/3 assets for every
  proposal. All four frozen decision checks pass.
- The weakest weekly counts are 42 Borrows, 46 Repays and 49 distinct debt users; the weakest 28-day cells have
  564 combined actions and 210 distinct debt users. The pass is not threshold-edge.
- Canonical result SHA is `f3ca5d19…`; an independent JSON read-back recomputation matches exactly. All weekly
  boundaries also land exactly on their target block timestamps.
- The successful invocation made 481 block and 306 log calls, bisected one slow range, retried 107 HTTP-429s
  and recorded 720 seconds of backoff. The sanitized `/tmp` checkpoint was deleted after success.
- A key scan confirms the result contains no raw data, participant, amount, transaction/log identifier, realized
  rate or utilization field. No execution/post-event log, reserve update, position/balance call, paid data, GPU
  or EcoMD was used.
- D1A only clears visible activity/power. It does not resolve anticipation, endogenous policy, contaminating
  reserve changes, repeated assets, at-risk population or utilization feedback, and it is not a causal result.

### Next steps

1. Commit and push the D1A result before further acquisition.
2. Freeze D1B: complete reserve-intervention ledger, announcement-to-execution timing, candidate cross-chain
   stagger/controls and falsification rules.
3. Reproduce extraction against Flashbots or a controlled archive node before the main study.
4. Keep post-event outcomes and all GPUs sealed until D1B passes.

## Session 4 (02:18)

### Goal

Translate the D1A activity pass into a hard identification gate before opening any post-execution behavior.

### What was done

- Audited official Aave governance metadata and found proposal creation already precedes Ethereum execution by
  several days; linked ARFC discussions are earlier. Froze execution-as-surprise claims as forbidden.
- Pinned official historical V3 Core, current V3 Origin and address-book repositories. The union is necessary
  because the study spans legacy `ReserveInterestRateStrategyChanged` and later configuration interfaces.
- Enumerated PoolConfigurator reserve/rate/cap/collateral/status/eMode/token-upgrade events, Oracle source events,
  AddressesProvider upgrades and reward-configuration changes. Historical unindexed asset events prevent a
  topic1-only shortcut.
- Confirmed that all six governance proposals map to many V3 chain payloads and that pinned source directories
  contain per-chain payload implementations. Exact cross-chain execution times remain sealed for formal D1B.
- Froze D1B YAML and narrative: ledger support day −35 to +28, no material confounder in day −14 to +14,
  minimum 14 clean post days, and administrative right censoring for later events.
- Froze the cross-chain gate before aggregating execution times: at least four proposals must each have three
  executed V3 chains, two non-Ethereum comparators, a source-audited slope1-only stablecoin update and at least
  24 hours of execution stagger.

### Results / findings

- The original naive causal story is invalid even before outcome data: payload execution is anticipated.
- The viable high-impact design is common announcement plus staggered implementation, coupled to a new
  anticipation/censoring-aware response-spectroscopy method. It remains only a candidate until D1B/D1C pass.
- D1B is metadata/configuration-only and requires no GPU or paid data.

### Open questions / blockers

- Exact protocol-side contamination counts, forum leads and cross-chain stagger are not yet formally computed.
- Cross-chain payload presence does not imply comparable assets, liquidity or users; D1C must test those without
  post-event data if D1B passes.

### Next steps

1. Add ABI/source parsers and sanitized policy-ledger reducers with tests.
2. Commit and push the D1B freeze before formal forum, configuration-log or execution-stagger aggregation.
3. Run D1B from the clean commit and obey its NCS stop line.

## Session 5 (02:37)

### Goal

Implement the frozen D1B auditor completely before the formal metadata and policy-ledger run.

### What was done

- Added pure Solidity ABI extraction with canonical event signatures and indexed-position checks.
- Added a non-compiling historical proposal parser supporting both legacy `_bpsToRay(...)` and current
  direct-basis-point rate inputs; it reports every configured field and accepts a comparator only when slope 1
  is the sole change.
- Added a sanitized policy-log reducer for indexed underlying assets, the legacy unindexed isolation event,
  selected a/debt reward tokens and global configuration events. Raw topics/data/addresses are discarded;
  only policy provenance, scope and a data digest survive.
- Added exact-T0 target matching, ±14-day contamination failure, day-14-to-28 administrative censoring and
  hard panel arithmetic, plus regression tests for raw-value erasure and no amber fallback.
- Implemented the formal D1B runner: pinned-repository/ABI/address verification, Discourse/governance timing,
  historical per-chain source audit, exact `PayloadExecuted` resolution, merged Ethereum policy-log windows,
  robust public-RPC transport and canonical result hashing.
- Clarified before the formal run that only DAI/DAIe, USDC/USDCe/USDCn and USDT/USDTe aliases count, and that
  stagger uses executed source-audited comparable chains including Ethereum. Missing source is non-comparable.

### Results / findings

- Twelve pure qualification tests and five existing runner tests pass; Ruff and strict core mypy pass.
- All frozen ABI signatures are present in the pinned official sources, and all frozen Ethereum addresses are
  present in the pinned address book.
- A development source/parser check succeeds across all six historical proposal commits. This is not yet the
  formal D1B artifact; the full run still requires a clean implementation commit.
- No Borrow/Repay response, reserve outcome, utilization, realized rate, position, price or volume was queried.

### Open questions / blockers

- The formal forum and Ethereum policy-ledger transport has not yet run from a clean commit.
- Passing D1B would still authorize only D1C pre-period comparability, not response outcomes or GPU work.

### Next steps

1. Run all targeted checks, commit and push the D1B implementation/freeze clarification.
2. Execute the full D1B audit from that clean commit and independently verify the artifact digest.
3. Obey the frozen pass/stop result before scheduling any cross-chain behavioral or GPU experiment.

### D1B topic-hash transport addendum (02:42)

- The first clean D1B invocation from `06b202bd9` resolved local pinned sources and forum/cross-chain metadata,
  then stopped before `eth_getLogs`: Flashbots rejected `web3_sha3` with HTTP 403 because the method is not
  whitelisted. No result artifact or policy event was returned or persisted.
- Replace the denied hash RPC with local OpenSSL Keccak-256. A regression test fixes the canonical ERC-20
  `Transfer(address,address,uint256)` topic, and an independent command reproduces the already frozen Aave
  Borrow topic exactly.
- This is a transport-only correction. The ABI-derived signature set, contract addresses, block windows,
  materiality rules and all pass/stop thresholds remain unchanged. Commit it before retrying formal D1B.

### D1B primary rate-limit addendum (02:47)

- The local-hash retry from clean `b89e5251f` stopped during time-to-block boundary resolution, before the first
  `eth_getLogs`, after Flashbots exhausted all six HTTP-429 backoffs. No result artifact was written.
- Move the formal replaceable transport to dRPC, which completed D1A historical access, and restore its tested
  0.5-second pacing. Keep Flashbots recorded as the failed primary and future independent reproduction source.
- Add a repository-external checkpoint after all boundaries and after each disjoint merged policy interval. It
  is bound to the code/config/T0/D1A/RPC identity, contains only boundary metadata and sanitized policy-event
  summaries, must be a prefix of the merged intervals, and is deleted after success.
- This changes recoverability and transport only; the block union, event topics, materiality, timing windows and
  decision thresholds remain frozen.

### Formal D1B result addendum (03:09)

- Completed D1B from clean commit `3a4fe2196`. Canonical result SHA is `e7a1b74f…`; independent JSON read-back
  recomputation matches, and the external checkpoint was deleted.
- Both nontrivial identification gates fail decisively. Only proposal 159's three assets are clean (3/18 total,
  3/15 primary, 0/3 reverse; required 15/12/2), and only proposals 3 and 271 exceed 24 hours of comparable
  cross-chain execution stagger (2/6; required four).
- All 18 target rate events match the exact T0 transaction exactly once, so the clean-panel failure is not a
  target-extraction error. Official governance metadata links major contaminants to the Pool update and v3.1,
  v3.2 and v3.3 upgrades plus collateral/eMode proposals; direct on-chain logs cover risk-steward changes.
- All executions are anticipated: public announcement leads are 255.82--784.20 hours. The surprise-event-study
  framing is independently ruled out.
- The run retained 669 sanitized policy events across four merged intervals and made 519 block plus 546 log
  calls, with 157 429 retries, 484 seconds of backoff and no range split. No raw log or behavioral outcome was
  retained; no paid data, GPU or EcoMD was used.
- Binding decision: `stop_ncs_causal_route_before_behavioral_outcomes`. Do not run D1C, query response outcomes,
  relax timing/cleanliness rules, or treat one proposal's three assets as independent experiments.

### Next steps

1. Commit and push the formal D1B artifact and stop document.
2. Preserve the activity/source/governance/policy-ledger code as reusable infrastructure, not a paper claim.
3. Return to problem selection and run a zero-cost novelty/identifiability gate before opening another dataset
   or scheduling any V100/RTX 2060 work.

## Session 6 (03:33)

### Goal

Select the next scientifically defensible free-data feasibility test after the Aave rate-response hard stop,
without opening another market outcome or assigning idle GPUs to an unidentified problem.

### What was done

- Falsified intervention frequency times mixing time as a universal identification law: fast persistently
  exciting inputs may identify a system, while sparse confounded inputs may not.
- Falsified generic learned controller conformance as the main NCS novelty after auditing smart-contract
  specification mining, runtime/formal verification and prior automated DeFi-controller work.
- Separated the standard safety--responsiveness issue of actuator/rate constraints from a narrower empirical
  opportunity: Aave Risk Oracle proposals and AgentHub injections may expose both sides of deterministic
  guardrail thresholds.
- Verified from official source that the architecture records proposed `ParameterUpdated` actions before
  AgentHub checks expiration, minimum delay and agent-specific validation, and records successful
  `UpdateInjected` actions separately. Non-execution must be reconstructed; an invalid proposal is not emitted
  as an explicit rejection.
- Mapped the post-Aave research topology and froze an outcome-blind D0 action-support protocol and YAML before
  retrieving any proposal value or matching any proposal to an execution.

### Results / findings

- The broad controller-design story is occupied by Auto.gov, AgileRate and classical constrained-control
  theory. A single retrospective of the published WETH Slope2 episode is also ineligible because Aave has
  already documented the specification/behavior dispute and delayed response.
- A potentially nontrivial field design remains only if proposal scores have genuine two-sided and near-boundary
  support. An off-chain proposer that clips actions to known limits destroys that design before outcomes.
- D0 therefore has hard action-ledger gates: 30 proposals, 20 exact injections, two update types, five markets,
  three agents, ten resolved non-immediate proposals, and a reconstructable guardrail with two-sided
  near-boundary support. Any failure stops before market outcomes.
- D0 uses only public source, configuration/proposal/injection events and block provenance. It is capped at 20
  CPU core-hours, 2 GB retained derived data and zero GPU hours.

### Open questions / blockers

- Historical proposal count, exact proposal--injection matching and the prevalence of delayed, overwritten,
  expired or disabled actions remain unknown by construction at freeze time.
- Exact range scores may require protocol state that D0 forbids; Risk Oracle `previousValue` cannot be silently
  substituted for the value read by the validation contract.
- If the proposal process clips every action into the admissible range, the causal-threshold route terminates
  even if all injections are operationally successful.

### Next steps

1. Commit and push the D0 freeze before querying any Risk Oracle proposal values.
2. Implement source-pinned event decoders and a sanitized action-ledger reducer with unit tests.
3. Run D0 against the frozen Ethereum interval and independently verify its result digest.
4. Obey the hard stop: do not read market outcomes, launch EcoMD or use V100/RTX 2060 unless D0 and a later
   exact-validation replay both pass.

## Session 7 (03:56)

### Goal

Implement the frozen D0 action-support audit completely before opening Risk Oracle proposal values.

### What was done

- Added a dependency-free strict ABI decoder for the dynamic `ParameterUpdated` and `UpdateInjected` payloads,
  indexed addresses/integers/booleans, and static/dynamic RangeValidation tuples.
- Added a source-ordered action-ledger reducer that maps each proposal to exactly one registered agent, matches
  injected values exactly, reconstructs time-varying expiration, annotates minimum-delay exposure and assigns
  injected, overwritten, expired, disabled, censored or ambiguous terminal classes.
- Added an agent- and delay-epoch-specific support audit. It does not pool heterogeneous controllers and excludes
  exact-zero margins from either side of the local-support count.
- Operationalized exact-boundary bunching before reading values: at least five exact-zero scores comprising at
  least 50% of a boundary's scores. Added those constants to the frozen config and protocol clarification.
- Added the formal runner with fixed-block source/RPC identity checks, chunked event-only queries, canonical block
  verification, source/address-book/submodule audits, sanitized output and a hard no-outcome decision.
- Cloned five official repositories into `/tmp`, checked out their frozen commits, and ran a source-only dry-run.
  All commits, the Risk Agents Chaos submodule pin, AgentHub interfaces and frozen addresses agree.

### Results / findings

- Six targeted tests pass, covering dynamic ABI fields, padding rejection, indexed identity, range tuples,
  injection/overwrite/expiration classification, local two-sided support and bunching rejection.
- Ruff and strict mypy pass for the new core logic; no new dependency is required.
- Pinned source confirms Risk Oracle `previousValue` is the prior oracle proposal, whereas agent range validation
  reads contemporaneous protocol state. D0 therefore reports zero amplitude scores unless allowed exact state is
  independently available; it never substitutes the oracle field.
- No Risk Oracle proposal value, proposal--execution match, pool behavior or market outcome was queried during
  implementation. GPUs and EcoMD remain unused.

### Open questions / blockers

- Formal event counts and whether a minimum-delay boundary has two-sided local support remain result-blind.
- Public RPC rate limiting may require a transport-only resumability amendment; scientific thresholds and the
  fixed block union cannot change.

### Next steps

1. Run the full Aave regression slice, commit and push this implementation.
2. Execute D0 from the clean implementation commit against the frozen endpoint block.
3. Independently recompute the artifact digest and all decision checks.
4. If any gate fails, stop before market outcomes and return to problem selection.

### D0 transport/recoverability addendum (04:03)

- The first clean formal invocation from `c8aed2a12` reached the first AgentHub `eth_getLogs` request and was
  rejected with dRPC code 35 because free-plan ranges are now capped at 10,000 blocks. No log, proposal value or
  result artifact was returned.
- Reduce only the transport shard from 50,000 to 10,000 blocks; preserve the exact 24,000,000--25,790,047 union,
  event allowlist, matching, thresholds and no-outcome boundary.
- Add a repository-external checkpoint after every decoded shard and block header. It is bound to code, config,
  source commits, RPC, chain and endpoint block; contains only decoded allowed policy events and headers; and is
  deleted after success.
- The checkpoint round-trip and identity rejection test passes. The full Aave regression slice now has 28 tests;
  Ruff and strict core mypy remain clean.
- The retry advanced the sanitized Hub checkpoint through block 24,309,999 with 75 decoded events, then the same
  next shard returned JSON-RPC `method handler crashed` twice. Classify only that exact message as a splittable
  range/response failure; preserve 429 as non-splittable. No proposal stage or result artifact was reached.
- The split-enabled run crossed the earlier shard, then the same error cascaded through bisection to a singleton
  block. This falsifies the range-size interpretation and points to a transient backend failure. Revert the split
  rule; retry only exact code `-32000` plus message `method handler crashed` with bounded exponential backoff and
  report its wait/count separately. Other `-32000` errors remain fatal.
- Transport probes then localize the failure to compound topic filters. On the same unfinished 10,000-block Hub
  shard, one allowed topic returns normally; a 7-topic group returns three events; and the other failing 7-topic
  group succeeds when divided into 3 and 4 topics. Freeze at most four allowed topics per request and union by
  canonical log identity. Flashbots is ineligible because it returned zero for a dRPC-verified 68-event shard;
  LlamaRPC returned 521 and 1RPC reported plan exhaustion.
- The implemented four-topic partition re-fetches the known deployment shard in four requests and exactly
  reproduces all 68 canonical dRPC log identities, with zero server/rate retries. The transport partition test
  also fixes the 4/4/2 topic groups across two block shards. The Aave regression slice now passes 30 tests.
- The next formal run again reached about 128 requests within a minute before the public endpoint degraded to
  handler crashes and explicit 429s. Freeze pacing at 0.75 seconds, at most 80 request starts per minute; do not
  increase concurrency or distribute calls across IPs merely to evade a public limit.
- Audited Google BigQuery as a potential complete free fallback. The new Ethereum logs table is current through
  the endpoint, but the fixed query dry-runs at 789,854,078,153 bytes. `rooy-data` shows zero billed query bytes
  this month, but a second project shares the billing account and cannot be audited. Because the 1 TiB free tier
  is account-level, do not execute this query or risk a charge. The legacy table is worse at 851.9 GB.
- The paced run remains correct but slows to 10--20k blocks/minute after some 4-topic combinations enter repeated
  backoff. Stop at a clean checkpoint rather than spend roughly 70 more minutes. Freeze two-dimensional recovery:
  handler/timeout/result-size errors split compound allowed-topic sets first; only singleton-topic range failures
  split blocks. Record topic and block splits independently. D1B keeps its existing block-first default. Two new
  regression tests fix the preference and exact recursive call topology; the Aave slice passes 32 tests.
- The next run shows the same fatal transition after roughly 128 total requests even after reaching singleton
  topics. Accept that dRPC cannot complete the panel rather than changing query geometry again.
- Audited Blockscout's documented no-key Ethereum per-instance ETH RPC. It exactly reproduces the complete dRPC
  canonical identity sets in two pre-specified diagnostic shards: 68/68 deployment events and 5/5 sparse events.
  Freeze Blockscout as the formal replaceable transport, retain 10,000 blocks and 0.75-second pacing, and restore
  the doubly validated 14-topic Hub query. Blockscout's documented 1,000-log cap exceeds the observed maximum of
  68 logs per shard. Its chain ID is 1 and block 25,790,047 hash exactly matches the frozen endpoint.
- The first clean Blockscout formal attempt stops before any log because the instance rejects a one-element
  address array as `invalid address`; the successful identity probes used a scalar. Encode exactly one address as
  a scalar and multiple addresses as arrays. This is JSON-RPC request normalization only.

## Session 8 (05:26)

### Goal

Complete the frozen Aave agent-guardrail D0, independently verify the decision and preserve the stop without
opening any market outcome.

### What was done

- Normalized one-contract `eth_getLogs` requests to the scalar address form, added a transport regression test,
  ran the 32-test Aave slice plus Ruff and strict mypy, and committed/pushed the clean formal-run SHA
  `cce8d234e7eb8d208c4bb2731bb61f63baee5ad7`.
- Ran the entire frozen block interval through Blockscout. The sandboxed attempt failed at DNS before any log;
  the authorized-network retry completed without a transport retry, topic split or range split.
- Persisted the 518 KiB sanitized formal artifact at
  `results/empirical_physics/aave_agent_guardrail_d0_result.json`; its canonical payload SHA-256 is
  `764f08a7a02c550f28f8b7ace275cf4e451ba5430ea5768aff55f374c1226460`. The external checkpoint was deleted.
- Added an independent result test that does not call the runner's gate or ledger helpers. It recomputes the
  artifact digest, all frozen checks, exact injection links, event uniqueness, blinding and the unmatched-row
  diagnosis.
- Added `papers/proposal/aave_agent_guardrail_d0_result_2026-08-20.md` and updated the problem map without changing
  the frozen protocol or reclassifying any row.

### Results / findings

- Formal decision: `stop_threshold_causal_route_before_market_outcomes`.
- Eight of nine gates pass: 155 proposals, 133 unambiguous proposals, 127 exact injections, five update types,
  17 markets, five represented agents and 38 resolved non-immediate proposals.
- One three-day agent-specific delay boundary qualifies with 68 scores, 28 negative, 39 positive, one exact zero,
  and 28/13 observations near the negative/positive sides. It is not exactly bunched.
- The sole failure is terminal classification: `133/155 = 85.81%`, below the frozen 90% minimum. Terminal counts
  are 127 injected, four expired, two overwritten and 22 unmatched/ambiguous.
- Post-hoc diagnosis, not a pass revision: 21 unmatched proposals precede registration of their eventual source
  agent and one uses an update type never registered in this Hub. All 133 post-registration proposals classify.
  This reveals a left-truncated risk-set definition that must be corrected only in a new untouched holdout.
- The run made 540 log calls and 281 block-header calls. It queried no Pool state or market outcome, retained no
  raw RPC, used no paid data, no EcoMD and no GPU.

### Open questions / blockers

- The Ethereum D0 cannot authorize D1 under its frozen conjunctive rule, even though its action surface is large.
- A non-Ethereum holdout may share proposals and proposer logic across chains; it can validate measurement/support
  but does not automatically create independent shocks.
- A future-time Ethereum holdout is cleaner but cannot be accelerated. Any new holdout requires a new freeze
  before event values are read.

### Next steps

1. Finish the independent result regression/static checks, commit and push the immutable stop artifact.
2. Source-audit the complete non-Ethereum deployment panel without reading event values.
3. Freeze one prospective eligibility rule—strictly post-unique-registration and post-initialization—plus
   cross-chain deduplication before any holdout event scan.
4. Run only the CPU/network support gate. Keep D1, market outcomes, EcoMD and GPU work locked unless it passes.

## Session 9 (05:47)

### Goal

Create a genuinely untouched holdout after the Ethereum D0 stop, freezing the full panel and corrected risk set
before reading any non-Ethereum agent event.

### What was done

- Source-audited pinned Risk Agents `Deploy.s.sol` and address-book modules. The complete non-Ethereum panel is
  Arbitrum, Avalanche, Base, BNB, Gnosis, Optimism, Polygon, Plasma and Linea; source fixes 17 expected agent
  deployments across cap, discount-rate, E-Mode and rates controllers.
- Verified only chain IDs, block headers and end-anchor AgentHub bytecode on official public RPCs. All nine chain
  IDs match the pinned address book and all nine AgentHubs have nonempty code. No `eth_getLogs` was issued.
- Fixed a common start time of 2025-11-24 00:00 UTC and located the first block at or after it on every chain by
  header-only binary search, checking each preceding timestamp. Fixed exact start/end block hashes.
- Where archive state was available, code-only binary search placed AgentHub creation on Gnosis, Optimism,
  Polygon, Plasma and Linea within a 26-minute interval on 2025-11-25. Arbitrum and BNB public nodes lacked old
  state; this did not change their common calendar start.
- Added `configs/empirical_physics/aave_agent_guardrail_holdout_d0_v1.yaml`,
  `papers/proposal/aave_agent_guardrail_holdout_d0_freeze_2026-08-20.md` and four contract regression tests.

### Results / findings

- D0H excludes all Ethereum pilot rows and cannot reclassify the failed pilot. The pilot supplies only a
  prospectively frozen activation condition for untouched deployments.
- Eligibility begins only after one unique source registration and prior AgentAddressSet, enabled=true,
  expiration and minimum-delay initialization. Pre-activation/never-registered rows are retained exclusions;
  later ambiguity remains in the denominator.
- Adjacent eligible proposals no more than 120 seconds apart form one cross-chain action batch regardless of
  chain or type. This conservatively addresses synchronized off-chain decisions and pseudoreplication.
- A pass needs three represented chains and ten batches globally, plus two qualifying chain-specific delay
  boundaries on two chains. Each boundary must pass both row-level and batch-median two-sided/near support and
  non-bunching checks.
- D0H remains public-data, CPU/network-only: at most 50 core-hours, 5 GB, zero paid data and zero GPU.

### Open questions / blockers

- Each chain still needs post-freeze log-transport qualification with exact identities from two sources. BNB's
  official public RPC explicitly disables `eth_getLogs`, so it is an anchor only and requires listed free
  replacements.
- The panel may have little activity or synchronized actions may collapse to too few independent batches. That is
  a valid hard stop, not a reason to drop chains or relax batching.
- Cross-chain measurement transfer does not itself create independent economic shocks.

### Next steps

1. Commit and push the D0H freeze before the first non-Ethereum event query.
2. Implement source/address/anchor validation, activation-conditioned ledgers and deterministic batch clustering.
3. Qualify transports one chain at a time with nonempty exact-identity crosschecks.
4. Run D0H from a clean commit and obey its stop before D1/outcomes/GPU.

### Pre-event eligibility/transport clarification (05:54)

- No non-Ethereum event query has occurred. Clarify that at least one prior initialized registration activates a
  proposal's risk set; exactly one makes it source-unambiguous. Multiple initialized matches remain eligible and
  count as ambiguity failures rather than disappearing as exclusions.
- Keep 10,000 blocks for two-source transport qualification. For formal extraction, predeclare a chain-specific
  initial span approximating seven UTC days from frozen header rates, then recursively split only on explicit
  range/timeout/result-size failures. This preserves every block while avoiding about 27,000 knowingly redundant
  requests on Arbitrum under a fixed 10,000-block grid.
- All scientific support, batching, boundary and hard-stop thresholds remain unchanged. The four contract tests
  pass after the clarification.

## Session 10 (06:20)

### Goal

Finish and lock the reproducible nine-chain D0H runner before the first non-Ethereum event query.

### What was done

- Added `scripts/audit_aave_agent_guardrail_holdout_d0.py` with separate `chain` and `merge` modes. A chain run
  revalidates the frozen pilot, pinned source repositories/files, chain anchors, deployed Hub code and an exact
  two-transport log-identity shard before the formal scan; merge requires all nine fixed chain artifacts.
- Implemented repository-external, atomic checkpoints bound to code, config, source SHAs, chain, anchors and RPC.
  They retain only decoded allowed policy events and canonical headers and are removed after successful output.
- Tightened activation so required initialization events must occur after their matching registration and before
  the proposal. Added explicit 120-second batch-gap data to the executable contract without changing its frozen
  scientific rule.
- Added runner regressions for canonical identity sorting/duplicate rejection, checkpoint digest and identity,
  pilot-artifact binding, zero-code rejection, topic partitioning and artifact digest verification. Also repaired
  a strict-mypy cast in the already-used Ethereum runner.
- Re-ran the pinned source audit: all nine chains and 26 source/address files bind successfully. The Ethereum
  pilot artifact recomputes to `764f08a7a02c550f28f8b7ace275cf4e451ba5430ea5768aff55f374c1226460`.

### Results / findings

- The 28-test focused Aave suite passes; Ruff, Ruff format and strict mypy pass for the core and new runner.
- The CLI smoke test exposes only the intended `chain` and `merge` modes.
- No non-Ethereum `eth_getLogs`, proposal value or market outcome has been requested yet. No paid data, EcoMD or
  GPU was used.

### Open questions / blockers

- Public endpoints may fail transport qualification or require resumption, especially BNB where the official
  endpoint disables logs. Such failures permit only transport replacement from the frozen candidates, never
  chain removal or scientific threshold changes.
- Actual eligible support, independent batch count and two-chain delay-boundary support remain blinded.

### Next steps

1. Commit and push the complete runner from a clean, tested snapshot.
2. Launch all nine chain shards from that exact SHA, storing shards/checkpoints outside the repository.
3. Merge only after every chain succeeds; independently recompute the digest and gate before any outcome query.

### Transport-only amendment after first qualification attempt (06:31)

- Committed/pushed the first runner snapshot as `4f826b2a35fc2f86a81ef766d56ad9a8ccf358fb`, verified local and remote
  equality, then launched all nine shards concurrently from a clean worktree.
- Avalanche stopped because its official endpoint explicitly caps log ranges at 2,048 blocks; Polygon stopped on
  dRPC's explicit free-plan range cap. BNB stopped because PublicNode requires a token for historical state and
  SubQuery returned TLS EOF. No chain output or checkpoint was written. The seven remaining old-SHA processes
  were terminated so their outputs could not be mixed with a repaired implementation.
- Preserve the fixed 10,000-block qualification union but recognize the two explicit provider-cap messages as
  recursively splittable. Separate fixed anchor validation (chain, hashes, nonzero Hub code) from log-transport
  validation (chain, hashes, exact log identity), since a log source need not expose historical state.
- Added documented public Avalanche dRPC and archive-capable BNB OnFinality transports. Source-blind anchor/code
  probes passed on both. 1RPC was plan-exhausted; the second OnFinality URL and LlamaRPC returned TLS EOF and were
  not added.
- Added regressions for both provider error messages and for log-only transport validation. The expanded focused
  suite passes 37 tests; both runners pass Ruff, formatting and strict mypy. Scientific gates and data union are
  unchanged.

The next formal attempt must use a new clean, pushed SHA and restart every chain; no old-SHA shard is reusable.

### Independent frozen-state witness amendment (06:37)

- Committed/pushed the first transport repair as `700d39f92cd0f80036c143dfe88bb01ab19a4b5b` and restarted all nine
  chains from empty external directories.
- Arbitrum and BNB immediately showed that their fixed official anchors still serve the exact frozen block
  headers but have already pruned `eth_getCode` state at the endpoint. Again, no chain artifact or checkpoint was
  written; the other seven old-SHA processes were terminated.
- Keep the fixed anchor RPC for chain ID and both exact block hashes. Select the first archive-capable endpoint in
  the already fixed anchor/formal-candidate order solely as a state witness for nonzero AgentHub code at the exact
  `to_block`. Log transports remain independently qualified by chain/hashes and exact event identities.
- A header/code-only preflight passed on all nine chains. Witnesses were Arbitrum Blockscout, BNB OnFinality, and
  the first fixed anchor/candidate on each of the other seven chains; no event value informed selection.
- Added a deterministic fallback regression. The focused suite now passes 38 tests, with Ruff and strict mypy
  clean. No scientific or sample rule changed.

Every chain must restart once more from a new clean, pushed SHA; the two failed attempts remain unusable.

## Session 11 (07:26)

### Goal

Recover BNB and Linea transport deterministically after the third clean D0H attempt, without changing any
scientific rule, then prepare a new clean formal run.

### What was done

- Audited attempt `e9e4b3dc9`. All nine state witnesses passed, but BNB qualification exhausted the listed log
  transports through rate limiting/TLS and Linea's formal scan stopped on `range 151199 exceeds limit of 10000`.
  No chain artifact was written. The only surviving checkpoint was an old-SHA Gnosis checkpoint and is not
  reusable under the runner's identity lock.
- Tested current public BNB transports without API keys. Chainpulse, Nodies, OMNIA, Blast, Routescan and Etherscan
  were unusable for this historical scan; generic `limit exceeded` was reproduced on a one-block request and is
  therefore not safe to classify as a range-size error.
- Used pinned Aave proposal history and archive `getAgentCount()` calls to locate the first registration-state
  transition: block 75,187,733 has zero agents and consecutive block 75,187,734 has two. This fixes the aligned
  qualification shard 75,184,723--75,194,722 independently of market outcomes.
- Queried the full fixed shard through SQD Portal, Nodeflare and Pocket. Each returned 30 allowed AgentHub events;
  Nodeflare and Pocket canonical identities exactly match SQD with SHA-256
  `2d50fe7fd5bcb03ca93f6783a94047a9664c28daf21acfc490e168f5591985a4`.
- Nodeflare also returned a valid empty result for the earliest frozen 10,000-block BNB interval
  69,254,723--69,264,722, rather than a pruned-history error. The primary therefore covers the start of the
  formal union as well as the later nonempty qualification shard.
- Added Nodeflare/Pocket as the first two BNB log transports while retaining OnFinality as the independent state
  witness. Fixed the BNB formal span at the documented 10,000-block free cap and froze the state transition,
  qualification shard and identity digest in the executable contract.
- Taught the shared log splitter only the observed Linea phrase `exceeds limit of`; retained a regression that
  generic `limit exceeded` is fatal. Added aligned-shard, canonical-digest and state-transition regressions.

### Results / findings

- BNB now has two independent no-key JSON-RPC transports plus an independent SQD identity audit; all 30 canonical
  records agree exactly on the full qualification shard.
- The qualification start is state-derived rather than activity-picked: no registered agent exists before the
  consecutive zero-to-two transition, so earlier grid shards cannot contain a registered-agent event.
- The complete 58-test Aave suite passes, as do Ruff and strict mypy for the core and both runners.
- This work used only source history, contract state, allowed Hub logs and CPU/network. It read no market outcome,
  used no paid data, EcoMD or GPU, and changed no chain, block union, event family, sample rule, gate or stop rule.

### Open questions / blockers

- Nodeflare's free cap implies about 14,300 paced log requests for BNB's three formal stages, approximately three
  hours at the frozen 0.75-second interval before retries and header attachment.
- The other eight chains must restart from the same new clean SHA; no result or checkpoint from the three failed
  code versions is mergeable.
- D0H may still hard-stop on eligible support, independent batches, classification or two-chain boundary support.

### Next steps

1. Run the complete Aave regression suite and inspect the final diff.
2. Commit and push the transport-only repair.
3. Launch BNB on one V100 host's CPU and split the other eight CPU/network shards across the remaining machines,
   keeping CUDA hidden and all artifacts/checkpoints outside the repositories.
4. Merge only all nine artifacts from the identical SHA; do not open market outcomes unless every frozen D0H
   gate passes.

## Session 12 (07:57)

### Goal

Launch the clean nine-chain holdout on the three authorized nodes, audit the first live failure, and preserve the
single-SHA/identity invariant rather than forcing a partial merge.

### What was done

- Pushed and independently verified clean commit `e7bf9a0157c01c1b4a1188ad8d873060b1d3e65f`. Provisioned fresh sparse
  EcoPhys clones at that exact SHA and five pinned source repositories on both V100 hosts and the RTX 2060 host.
  All three environments passed the real Keccak vector; source audits bind nine chains and 26 files, and the
  frozen pilot digest is `764f08a7...c1226460`.
- The first detached invocation used a script path and failed at Python import before any RPC request. Preserved
  those logs and relaunched with `conda run ... python -m`; CUDA was hidden and all three GPUs remained at 0%.
- Distributed three chains per node. Gnosis completed at old SHA with artifact digest `806a8e4f...12498fff`, one
  eligible proposal and no excluded proposal; its independent payload digest, clean SHA, config hash, checkpoint
  removal and outcome blinding all verify. Other old-SHA outputs/checkpoints were retained outside the repos.
- BNB failed before artifact/checkpoint: Nodeflare passed the frozen shard, but Pocket no longer returned start
  anchor 69,254,723 on the V100 route. The same missing anchor was reproduced from RTX and Mac, so it is not a
  single-host anomaly.
- Probed 19 current no-user-key ChainList candidates using only chain ID and the two fixed headers. Seven passed.
  On the fixed 30-event shard, FastNode returned zero and was rejected; official BNB seeds returned the generic
  one-block-capable `limit exceeded`; ZAN required registration. Both bloXroute and Sentio returned all 30 exact
  identities on Mac.
- Verified the documented bloXroute public BSC RPC on the RTX host: both anchors and all 30 identities match
  digest `2d50fe7f...5a4`. On the same single egress, Nodeflare matches the digest and OnFinality verifies nonzero
  Hub code plus the exact zero-to-two `getAgentCount()` transition.
- Terminated the remaining old-SHA parent and Python child processes by exact PID, preserving every log,
  checkpoint and completed artifact. Added bloXroute as the second BNB candidate; no scientific rule changed.

### Results / findings

- Public RPC service names do not guarantee stable archive routing. Pocket's qualification-log support coexisted
  with loss of the earliest header; FastNode's successful header responses coexisted with silent log omission.
  The two-anchor plus exact-identity gate caught both failure modes.
- BNB has a complete single-egress path on the RTX host: Nodeflare primary, bloXroute identity reference,
  OnFinality state witness. bloXroute officially documents the endpoint and a 3 requests/s public limit; frozen
  pacing is 1.33 requests/s.
- Any old-SHA shard is unmergeable after a candidate-list/config change, even if individually valid. The cost is
  a full nine-chain restart; retaining this rule is preferable to mixing acquisition protocols.
- No market outcome, paid data, EcoMD or GPU was used.

### Open questions / blockers

- All nine chains must restart from the next clean SHA. BNB should take roughly three hours; other chains depend
  on public endpoint availability and may finish much earlier.
- The V100-B SSH server intermittently closes sessions when ssh-agent offers multiple keys; `IdentitiesOnly=yes`
  with the configured Ed25519 key is the required control path.

### Next steps

1. Run the complete Aave regressions, commit and push the fourth transport-only amendment.
2. Provision a new run root per node at that SHA and restart all nine chains; place BNB on the preflighted RTX
   host.
3. Pull each completed digest-verified shard to one external merge directory, then merge from the exact run SHA.

## Session 13 (09:49)

### Goal

Replace unreliable or pathologically slow free transports without touching the frozen science, finish a single
version-2 acquisition contract, and prepare a clean all-chain restart.

### What was done

- Audited the clean `ac99559212dd0a0943c669b5d0981908e44d81e8` attempt. Gnosis, Arbitrum and Linea completed
  internally valid version-1 artifacts; their ledgers contain respectively 1/0, 29/5 and 14/40 eligible/excluded
  proposals. Base failed qualification from Mac/RTX/V100 egresses. Other partial checkpoints remain external.
- Tested only chain IDs, frozen block hashes, historical Hub code/state and allowed AgentHub logs. No price,
  utilization, rate, balance, position, liquidation, user transaction or response-window data was opened.
- Fixed one nonempty 10,000-block qualification shard for each chain and reproduced its complete canonical
  identity set with at least two independent public sources. Event counts are Arbitrum 48, Avalanche 45, Base 38,
  BNB 30, Gnosis 30, Linea 12, Optimism 32, Plasma 30 and Polygon 38.
- Verified fast no-key transports: Tenderly reproduced the reference identities and frozen anchors/code for
  Arbitrum, Avalanche, Base, Gnosis, Linea, Optimism and Polygon; Sentio did so for BNB and Plasma. Polygon also
  matches dRPC, Plasma matches official/thirdweb, and BNB matches bloXroute plus its exact consecutive state
  witness. BlockPI was rejected for silently returning zero Base logs; dRPC Base timed out recursively; pruned
  or unavailable public alternatives were not promoted.
- Created version-2 config digest `93be07281ff57e853d32f5caadc2f08d9a38e9e91728c35c8126a22aab25246c`.
  It binds the parent v1 digest/SHA, records that no outcomes were queried, fixes all nine qualification shards,
  marks every v1 chain artifact diagnostic-only and requires a same-digest nine-chain rerun.
- Hardened the runner so version-2 provenance is validated and embedded in chain/merged artifacts. It now refuses
  an amendment unless block/scientific invariance, outcome blinding, all-chain restart and pre-outcome independent
  full-union transport replication are explicit. Expanded contract/runner tests cover all nine fixed shards and
  amendment tampering.
- Updated the freeze and memory documents. The fast primary scan is explicitly not sufficient to unlock outcomes:
  complete decoded unions must be reproduced by non-primary providers and compared by identity and payload.

### Results / findings

- The focused contract/runner suite passes 17 tests and the complete Aave suite passes 60; Ruff, strict mypy and
  `git diff --check` pass. The original v1
  digest remains exactly `1c2eaa505b1993df153ff8cdd90ff698745fb4a7ec398aee8fa9f301e5a47eb9`.
- Tenderly handles the tested dense formal ranges in roughly one second. Sentio handles Plasma similarly and six
  consecutive BNB 10,000-block shards in 0.35--0.93 seconds each. Projected BNB wall time falls from roughly two
  days on bloXroute to roughly three hours on Sentio; the other eight chains should be minute-scale.
- Fast acquisition centralizes seven primary scans at Tenderly. Qualification agreement controls local omission,
  but not a correlated omission over the full union; the independent full-union replication gate is therefore
  necessary before market-outcome access.
- Version-1 completed artifacts are useful diagnostics but scientifically unmergeable with version 2.

### Open questions / blockers

- The fast endpoints must pass the same anchor, code/state and exact-identity checks from each designated compute
  egress at formal launch. A failure permits only a recorded transport replacement, never chain removal.
- The full-union independent replication path still needs execution after the primary nine-chain result and
  before any outcome access.
- D0H itself may still fail support, batch, classification or two-chain boundary gates. That is a scientific stop.

### Next steps

1. Run the complete Aave suite, inspect the entire diff, commit and push version 2.
2. Provision fresh exact-SHA roots and launch three chains per authorized node with CUDA hidden.
3. Stop obsolete version-1 processes only after version-2 checkpoints prove the new SHA/config/endpoint binding.
4. Pull and verify all nine artifacts, merge once, then run the required independent-provider full-union audit.
