# 2026-06-23 — workshop page-count verification + figure-imperfection fixes

**Page limit (verified, not assumed).** "≤4pp" came from our own headers, not the venue registry.
Checked the CFPs: **ML4PS** = "short paper" (NOT "extended abstract" — I had mislabeled it) up to **4
pages**, NeurIPS template, **references unlimited / excluded** from the limit, non-archival. **GenAI in
Finance** (NeurIPS 2025) = **≤4 pages main body, refs excluded**, double-blind non-archival.

**True page count (real NeurIPS format).** My weasyprint preview over-counted (it included refs + loose
single-column). Installed TinyTeX on `howardwang@100.105.21.7` (bare box, net OK), fetched the official
`neurips_2024.sty`, generated `.tex` from each `.md` via pandoc (refs excluded, figures `\linewidth`),
compiled with xelatex → **both workshops ≈ 6 pages of main body** (~5.5 after discounting a
double-caption artifact). So both are **~2 pages over** → compression is real (figure cuts + text trim).
Engine note: TinyTeX/xelatex ≈ Overleaf within ~±0.3p; pdfLaTeX on Overleaf is the ground truth.

**Source of truth = `.md`** (user decision). `.tex` is regenerated from `.md` at submission (pandoc);
do not hand-maintain. Updated both workshop header notes to say so; the existing `main.tex` are stale.

**Overleaf sync.** No public read/write API. Premium-only **Git bridge** (push to git.overleaf.com) or
**GitHub sync** (manual, can't link existing-to-existing). Cleanest for us: generate `.tex` here, sync
via whichever the account supports; or compile on Overleaf for the exact count.

**Figure-imperfection audit + fixes** (`shared/make_figures.py`, regenerated + redistributed to all 3):
- fig_transient(a): "burn-in dip (same depth, fast)" was crossing the t=3000 shock line → moved left.
- fig_mechanism(a): in-figure annotation still said "**sharp** relax" (intensifier I'd cut elsewhere)
  and hugged the spike → changed to bare "τ_OFI≈20", moved right of the peak.
- fig_boundary: china "−0.91" label crowded the lower-left legend → legend to upper-left, ylim raised.
- fig_hero(b): "exact internal observables" caption hugged the readout boxes + the shock dotted line
  crossed them → boxes lowered, axvline clipped to y≥axis.
- Deferred: fig_transient(b) asset-diamond overlap at the floor — auto-resolves to 5 curves when the
  dose-sweep lands.

**Dose-sweep (from 2026-06-22 16:30).** Running overnight on the 8-card box (ndx done, chain → gold→
btc→eurusd→eval). Status not re-checked this session.
