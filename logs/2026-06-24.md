# 2026-06-24 — F-a → causal |ρ|, and the eurusd |ρ| investigation (with a self-correction)

Continues 2026-06-23 (Session PM-2 ran C-a/F-a/B3 on the real exp-123 trajectories via the 8-card H20
`100.91.194.14`, repo+trajectories on GPFS `/AI4S/Users/howardwang/h204/ecophys/`). Today: replaced the
F-a observable, then chased — and corrected — a apparent eurusd anomaly.

## F-a: windowed OFI memory → per-step |ρ_t| (causal, spikes AT the shock)

The centered-window OFI "memory" (lag-1 autocorr) peaks ≈+250 after the shock (window-fill, inherent to
W=500) — honest but not "jumps at the shock". User asked for the per-step |ρ_t| (the instantaneous
order-flow imbalance magnitude) instead. Added `rho_abs_arm` to `analyze_transient_extras.py`; re-ran on
the GPFS trajectories. **|ρ| spikes AT the shock (t≈+5–6), no window lag:** spx 0.006→**0.70**,
ndx 0.66, gold 0.68, btc 0.68; eurusd only **0.07**. fig_mechanism panel a now plots the real causal |ρ|
series (control + price-gap flat); memory persistence keeps panels b (dose-response) / c (τ_OFI). Commit
`d6bcec726`. Lightened `rho_abs` smoothing 9→3 (barely moved the peaks).

## eurusd |ρ| investigation — appeared muted; resolved as genuinely weak-but-STABLE

User: "check eurusd |ρ| at kick12; if it's a model problem fix it, else don't write it." Long chase, with
a **self-correction** worth recording so it isn't repeated:

1. eurusd |ρ| (model `ofi`, production) = 0.069 (kick6) / 0.115 (kick12) vs spx 0.70 — robust across 58
   trajectories. So genuinely weak. NOT a smoothing artifact (raw 0.069 ≈ smoothed).
2. I then wrote a kick-geometry diagnostic computing ρ from **outer-step state-diffs** `Σ(s_t−s_{t-1})/Σ|·|`
   → got eurusd |ρ|≈**0.61**, and across checkpoint seeds the equilibrated gross flow varied ~50×. I
   (wrongly) concluded "eurusd model is seed-UNSTABLE" and committed that (`b4fa169e6`, memory
   `project_eurusd_model_instability.md`).
3. **That was a diagnostic bug.** The state-diff includes the **imposed kick jump** (kick applied to `s`
   at the start of the step, `ecomd.py:790`, before forces). The model's `ofi`
   (`price_formation.py:463`, `dpos=pos_next−pos_prev`, `pos_prev`=the kicked `s`) measures the
   **post-kick coordinated-flow RESPONSE**, excluding the kick. So state-diff (0.61) ≠ model ofi (0.07).
4. **Decisive re-test with the model's `ofi`** (production quantity), 4 rollout seeds/asset (mag6):

   | asset | mean |ρ| | spread |
   |---|---|---|
   | spx | 0.697 | 0.002 |
   | ndx | 0.659 | 0.005 |
   | gold | 0.685 | 0.008 |
   | btcusdt | 0.686 | 0.004 |
   | eurusd | 0.071 | 0.017 |

   **All five seed-STABLE.** ndx/gold/btc rock-solid (spread <0.01) — NOT unstable. eurusd genuinely weak
   (~0.07) but stable (= production). **No model instability → nothing to fix, nothing to drop; keep the
   |ρ| signature.**

**Mechanism (the honest "why eurusd is weak"):** equities/NASDAQ/gold/crypto translate the coordinated
kick into a coordinated order-flow response (|ρ|~0.7); the low-vol FX model does **not** (|ρ|~0.07) — a
stable, asset-dependent property of the trained dynamics. eurusd's *tail* still revives at kick12 (real).

**Correction commit `61af815b4`:** 3 drafts reworded "anomalous/seed-unstable/exclude" →
"robust seed-stable four-asset signature (0.66–0.70); eurusd a marginal but equally stable response
(0.07)"; renamed memory → `project_eurusd_weak_ofi.md` with the state-diff-vs-`ofi` lesson; fixed the
analyze-script comment.

## Lessons / debugging gotchas (so I don't repeat)
- Measure |ρ| via the model's `ofi` (post-kick flow response), **never** outer-step `Σ state-diff` (that
  includes the imposed kick).
- SSH batch over `bash -lc "…"` with associative arrays / `eval` silently no-op'd; and `grep rho_peak`
  missed the output `|rho|_peak=…` (the `|` breaks the literal). Use plain scripts + match `peak`.
- GPU diagnostics on `100.91.194.14`: load via `/root/miniconda3/envs/ecophys/bin/python`, `bash -lc`
  for conda; ckpts `experiments/114_concave_confirm/results_<asset>_concave_d050_seed0/checkpoint.pt`
  (spx is `experiments/113_gabaix_solve/results_concave_d050_seed0/`).

## State of the three drafts (NCS / ML4PS / GenAI)
Mechanism/Fig-4 now fully real-data-backed: F-a causal |ρ| spike (4-asset 0.66–0.70, eurusd 0.07 stable),
C-a DHVG irreversibility flat 5/5 (Δ≈0.01) confirming coherence-not-irreversibility, B3 censoring-free dip
kurtosis 120–150. Real-return boundary (Stage 3) stationary/no-heavy-up. **NCS Results §6 (LOB) still
blank pending Tardis L2.** Branch `feature/paper-a-figures-voice` @ `61af815b4`.
