# H20 三机批次 — 2026-06-09(~20h,涌现临界性深挖 + δ* 普适图收尾)

每台机器先 `git fetch --all && git checkout feature/exp113-gabaix-solve && git pull`,
确保数据在位 `bash scripts/h20_pull_from_r2.sh`(116 锚点会自动从 R2 恢复)。

## H20-1(8 卡)— 临界性核心:train-at-N 控制 + 推理 FSS
```bash
# 1) train-at-N 控制(30 cfg ≈ 4h):证明 hill(N) 是学到的动力学性质,非外推假象
conda run -n ecophys python experiments/120_fss_train/generate_configs.py --asset spx --seeds 0-5
DAEMON=0 PARALLEL=8 bash scripts/h20_run_phase.sh experiments/120_fss_train/spx
# 2) 推理 FSS(480 rollouts ≈ 10h):hill(N) 主曲线,N{1k..14k}×κ{0,.4,.8,1.2}×3anchor×5seed
conda run -n ecophys python experiments/116_criticality/generate_configs.py
bash scripts/h20_116_criticality.sh
# 评分
conda run -n ecophys python scripts/score_criticality.py experiments/116_criticality
conda run -n ecophys python scripts/score_fss_train.py    # 叠加 train-at-N vs inference
```

## H20-2(2 卡)— δ* 普适图收尾:btcusdt δ-grid(36 cfg ≈ 18.6h)
```bash
conda run -n ecophys python experiments/118_delta_grid/generate_configs.py --assets btcusdt --seed-start 0 --seed-end 17
DAEMON=0 PARALLEL=2 bash scripts/h20_run_phase.sh experiments/118_delta_grid/btcusdt
conda run -n ecophys python scripts/score_delta_grid.py experiments   # btc 从 2 点 → 4 点
```

## H20-3(2 卡)— 临界性第 2 资产普适:btc train-at-N(20 cfg ≈ 10h)
```bash
conda run -n ecophys python experiments/120_fss_train/generate_configs.py --asset btcusdt --seeds 0-3
DAEMON=0 PARALLEL=2 bash scripts/h20_run_phase.sh experiments/120_fss_train/btcusdt
```

## 回收后的判定
- `score_criticality.py`:hill(N) 是否在临界 N_c 穿 α=2 + κ 依赖性 + 标度坍缩就绪
- `score_fss_train.py`:train-at-N 与 inference-swept 曲线是否一致(✓=临界性非假象)→ 这是 plan v3.1 主 claim 分叉的裁决者
- `score_delta_grid.py`:5 资产 δ*≈0.51 普适图(btc 补全)

数据回 Mac:各机 `git add experiments/{116,118,120}* && git commit && git push`(沿用 114 模式)。
注:三台都用 `h20_run_phase.sh`,自带 SKIP_DONE 断点续跑;116 锚点缺失自动 R2 恢复。
```
```
