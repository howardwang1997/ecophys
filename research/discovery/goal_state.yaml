schema_version: 1
goal_id: ecomd_publishable_topic_goal_v1
terminal_objective:
  machine_card_status: active
  gates: research/discovery/protocol.yaml#activation_gate
  executable_plan_within_compute_envelope: true
  venue_consistent_evidence_contract: true
mode: execute_authorized_decision
mode_basis:
  derived_from:
    - research/discovery/current_machine_decision.yaml (truth_asset_a2_platform_qualification_20260905, status authorized)
  note: >-
    Re-derived 2026-09-05 after PI directives. Mode A: execute exactly the authorized A-2 scope
    (extend scripts/lab_asset to the A-3 Part 4 tape contract; deterministic replay + conformance
    fixtures; robot pilots; Mac CPU only; two-iteration stop rule). No humans, outreach, ethics,
    treatment adjudication, candidate harvest, EcoMD, GPU, or market outcome access. Discovery
    topic cycles remain closed (99 trigger audits, zero qualified); the route graph's single
    active node (verification_liquidity) stays sealed until 2026-10-17 UTC.
last_derived_at: "2026-09-05"
next_action_queue:
  - action: >-
      A-2 iteration 1: audit scripts/lab_asset against A-3 Part 4 field-by-field; implement the
      missing tape fields (three clocks, reason-coded rejections, pre/post quotes, per-action
      full-book state hash, replay prestate); extend tests; run conformance fixtures and a robot
      pilot on both arms; write the append-only iteration record.
    rationale: Authorized machine decision; platform qualification is the bounded unit.
    cost_estimate: 1-2 local CPU sessions; zero GPU.
  - action: >-
      PI: ICLR 2027 submission execution (OpenReview account/reciprocal-reviewer eligibility,
      abstract by 2026-09-18, artifact upload after anonymity confirmed).
    rationale: Repo is now private and the branch is pushed; remaining steps need the PI account.
    cost_estimate: PI session.
  - action: >-
      After A-2 exit criteria or stop rule: write the formal A-2 result and re-evaluate the A-3
      ladder (treatment-selection audit is the other legal next rung).
    rationale: Plan Part 8 stop rules and exit criteria bind.
    cost_estimate: one session.
  - action: After 2026-10-17 UTC, re-derive the verification_liquidity sealed-holdout state.
    rationale: Separate sealed protocol; holdout opening is forbidden before that date.
    cost_estimate: one orientation check.
family_cycle_counters:
  simulated_market_physics_and_validity:
    saturation_status: saturated
    note: >-
      Cycles 10-16 plus the 2026-09-04/05 trigger-audit rounds produced zero machine cards;
      re-entry requires a qualified ledger entry. Exact counts derive from
      search_cycle_ledger.yaml and reentry_trigger_ledger.yaml and are not copied here.
session_history:
  - date: "2026-09-05"
    mode: A_resolved_terminal_reconciliation
    ledger_ref: logs/2026-09-05.md Session 53
    note: >-
      Machine-decision chain reconciled to terminal (complete_gate_not_passed); stale Paper D
      memory corrected; goal_state initialized from canonical sources; both validators pass.
  - date: "2026-09-05"
    mode: PI_directives_paperd_git_a2
    ledger_ref: logs/2026-09-05.md Session 54
    note: >-
      Repo made private (double-blind fix); 202 files committed and pushed to
      paper-d-iclr-2027-completion; A-2 truth-asset platform qualification authorized and
      registered as the current machine decision.
derivation_rule: >-
  Every field in this file except goal_id, terminal_objective, and derivation_rule must be
  re-derived from the canonical sources listed in the research-goal skill at the start of each
  session. Never copy route statuses, counts, or failure history into this file.
