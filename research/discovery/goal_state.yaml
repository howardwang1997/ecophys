schema_version: 1
goal_id: ecomd_publishable_topic_goal_v1
terminal_objective:
  machine_card_status: active
  gates: research/discovery/protocol.yaml#activation_gate
  executable_plan_within_compute_envelope: true
  venue_consistent_evidence_contract: true
mode: gamma_activated_pre_freeze_builds_d0_20261009
mode_basis:
  derived_from:
    - research/discovery/current_machine_decision.yaml (pi_gamma_activation_d0_amendment_20260919, active)
    - .claude/memory/research_route_knowledge_graph.yaml (reexploration_merged_gamma_led_paper active;
      verification-liquidity also active as a sealed field protocol)
    - papers/proposal/ecomd_reexploration_d0_calendar_proposal_2026-09-08.md (freeze-date
      decision item and the named amendment mechanism)
  note: >-
    Re-derived 2026-09-19 after the PI activation ("激活，但是不要在 MacBook 上面做任何实验，
    都在远程的服务器上面做"). The merged GAMMA-led program is ACTIVE: the 2026-09-12 withhold is
    lifted; the D0 outcome-blind freeze is amended pre-hash from 2026-09-19 to 2026-10-09
    (prereg v2 Annex B(j) + Section 1.3 + consistency sites; errata entry C-12; no freeze hash
    existed at amendment); Annex B convention constants ratified as pinned. Compute-location rule
    in force: no experiment of any size on the MacBook (supersedes the ops plan's Mac-legal
    dry-run/smoke classifications for pre-freeze items 9-10); all execution remote via ssh.
    Live tracks: (1) pre-freeze builds 2026-09-19 to 2026-10-09 — analyzer contract (readiness
    item 8, the long pole) DONE 2026-09-19: v0.3 authored from 233 pins, adversarial audit
    (22 agents, 12 findings) and verifier pass complete, all 16 PI flags ratified at their
    defaults (decision pi_analyzer_contract_flags_ratification_20260919, "整体批，授权"),
    committed; item 9 (cleanup/archive tooling) completed and committed 002b91c53 the same
    day (both-node dry-run evidence under research/discovery/ops/d0_cleanup_dryrun_20260919/);
    item 10 (reflexive deployment cell) completed the same day: module + 21-test suite,
    remote smoke matrix 16/16 all-filled/replay_ok on v100ts (evidence under
    research/discovery/ops/d0_reflexive_smoke_20260919/); remaining pre-freeze work is the
    regression-battery status re-verification; D0 freeze session
    2026-10-09 per prereg Section 1.3; post-freeze Stage 1 (~650 V100-h) and conditional
    Stage 2 (~410 V100-h) per the frozen ops plan, hard ceiling 2026-11-06; the merged paper
    targets ICML 2027 main track per the PI 2026-09-18 venue directive. (2) Paper D ICLR 2027
    reported submitted by the PI 2026-09-18 (ahead of the 09-25 plan) — no further repo action
    unless the PI requests changes. (3) g42 post-terminal assess deferred (lowest priority;
    capped at tainted D-3 child-card motivation). (4) verification-liquidity embargo lifts
    2026-10-17 UTC. (5) the d1_merged_gammas_led_paper_gate forecast (0.18/0.34/0.55)
    resolves at the outcome-blind D-1 exit gates; its resolve_by anchor and the five other
    campaign-anchored d1 entries are re-based +20 days by the same decision (recorded in the
    decision record and prereg C-12; ledger file untouched, prefix preserved), the merged
    gate to 2026-11-20. V100 disk state: cold-data migration completed 2026-09-19 (v100bts
    / 100%->87%; v100ts /data 99%->86%); prereg Section 8.3 makes >=40%-free nodes a
    STOP-class LAUNCH prerequisite and both training volumes are below it (v100bts /data
    ~32% free, v100ts /data ~14% free). PI directive 2026-09-19
    (pi_bulk_icloud_no_action_20260919, "bulk和icloud不用管"): neither item is
    agent-managed or re-raised further; the frozen 8.3 gate itself is NOT amended and
    still fires mechanically at launch if below floor (STOP, PI decision at that point);
    the freeze session proceeds on the one-shot iCloud-safe git flags. PI disk
    directive 2026-09-20 (pi_disk_directive_20260920, "有额外数据盘，不要让一个磁盘卡住你"):
    both nodes' second 200G data disks enumerated (vdc → /data2); campaign
    storage spreads across /data + /data2 with R2-canonical final checkpoints;
    the directive is the standing PI answer the Section 8.3 STOP reads at
    D0-S1; frozen text unamended; physics bulk untouched.
last_derived_at: "2026-09-19"
next_action_queue:
  - action: >-
      Campaign launch on all three PI-named machines. D0 freeze EXECUTED
      2026-09-21 (UTC 2026-09-20T15:12Z, C-13 + pi_campaign_advance_20260921):
      freeze sha256 fd4a40b0306d415cef9a8a41b8e94e5afb21126e4e3d7ff99097e066f9981f57,
      freeze commit f4e61bafed21f12a6ec73dbe82b9ed4ea87bdd37, R2 record
      alpha_cube_d0_20260921/freeze_record.json etag 52b5b8edfdffb962b1c86c12cfde70c1.
      Order: node staging (repo bundle from the freeze commit + CUDA env on
      v100ts/v100bts, CPU env on the 3080 node) -> D0-S1 preflight per host
      (focus tests >=25, seed-999 CUDA preflight, launch receipt) -> D0-S2
      corpus generation -> D0-S3 training. Machine assignment: v100ts = L1
      channel (B1 + A10 audit + Stage-2 B2), v100bts = L2 channel (B3 +
      Stage-2 B4), 3080 node = CPU + exploratory channel (corpus/DGP/F_exec
      streams core-capped, S3 FIM grid, analyzers, G9 engine replay).
      Heterogeneous-pool rule binding: no 3080 cell inside a V100-benchmarked
      block without a qualification benchmark + PI arbitration; B1 never on
      the 3080s. Section 8.3 >=40%-free STOP gate reads the 2026-09-20
      disk-spread directive at D0-S1. Re-verify the v100ts discovery validator
      on the full-history bundle clone (node-only git-history red disposition
      from the freeze session).
    rationale: Freeze complete; campaign order is frozen in the ops plan; the PI ordered launch without waiting (pi_campaign_advance_20260921).
    cost_estimate: Stage 1 ~650 V100-h over ~19-20 wall days; conditional Stage 2 ~410 V100-h.
  - action: >-
      After 2026-10-17 UTC, re-derive the verification_liquidity sealed-holdout state.
    rationale: Separate sealed protocol; holdout opening is forbidden before that date.
    cost_estimate: one orientation check.
family_cycle_counters:
  simulated_market_physics_and_validity:
    saturation_status: saturated
    note: >-
      Cycles 10-16 plus the 2026-09-04/05 trigger-audit rounds produced zero machine cards;
      re-entry requires a qualified ledger entry. Exact counts derive from
      search_cycle_ledger.yaml and reentry_trigger_ledger.yaml and are not copied here.
session_history:
  - date: "2026-09-05"
    mode: A_resolved_terminal_reconciliation
    ledger_ref: logs/2026-09-05.md Session 53
    note: >-
      Machine-decision chain reconciled to terminal (complete_gate_not_passed); stale Paper D
      memory corrected; goal_state initialized from canonical sources; both validators pass.
  - date: "2026-09-05"
    mode: PI_directives_paperd_git_a2
    ledger_ref: logs/2026-09-05.md Session 54
    note: >-
      Repo made private (double-blind fix); 202 files committed and pushed to
      paper-d-iclr-2027-completion; A-2 truth-asset platform qualification authorized and
      registered as the current machine decision.
  - date: "2026-09-05"
    mode: A2_iteration1_part4_gaps
    ledger_ref: logs/2026-09-05.md Session 55
    note: >-
      Iteration 1 closed the four Part 4 gaps (roles, replace lineage, rejection ids, quotes);
      31 tests and both pilot arms replay-validated.
  - date: "2026-09-05"
    mode: A2_exit_bundle_and_result
    ledger_ref: logs/2026-09-05.md Session 56
    note: >-
      Iteration 2 froze the lab-asset-v3 schema artifact with serialized fixtures and an
      independent verifier; formal A-2 result written; exit criteria satisfied, decision
      terminal-complete.
  - date: "2026-09-05"
    mode: A3_treatment_selection
    ledger_ref: logs/2026-09-05.md Session 57
    note: >-
      PI authorized the treatment-selection audit; primary selected (FIFO vs random-unit queue
      priority), backup minimum resting time, resting-depth visibility disqualified by direct
      human prior. Decision terminal-complete.
  - date: "2026-09-06"
    mode: PI_directive_reexploration_reopening
    ledger_ref: logs/2026-09-06.md Session 2
    note: >-
      PI paused A-1 (parked, not failed) and reopened market-simulation/EcoMD topic
      exploration under a changed search basis (methodology-first, constructive ground truth).
      Decision pi_topic_reexploration_directive_20260906 active; screening only; plan frozen
      with two priority families (ALPHA audit-first, GAMMA theory-first) and pre-killed list.
  - date: "2026-09-06"
    mode: reexploration_d3_framing
    ledger_ref: logs/2026-09-06.md Session 3
    note: >-
      D-3 framing complete for GAMMA (matching-fiber gauge; target theorems T1 fiber taxonomy
      with kernel-randomness x granularity interaction, T2 consumer instability, T3
      partial-mechanism exposure, T4 labeled conjecture) and ALPHA (eight-cell mechanism-layer
      attribution audit; Paper D statistical contract inherited verbatim; two DGPs, two
      lineages, market-native OOD axes, reflexive secondary). Recommendation: target one paper
      (GAMMA theory core + ALPHA cube), final merge gate at end of D-2. D-2 hostile
      neighborhoods assigned; MARL co-training lane flagged as ALPHA's highest collision risk.
      Paper-only; no outcome access.
  - date: "2026-09-06"
    mode: reexploration_d2_complete_and_merge_gate
    ledger_ref: logs/2026-09-06.md Session 4
    note: >-
      D-2 complete for both families via 55-agent citation-verified audit plus a dedicated
      3-stance panel: GAMMA 68 verified works, ALPHA 61, zero confirmed and zero unresolved
      collisions (GAMMA Perturb-Argmax cleared 3-0; ALPHA Duruisseaux 2024 genre-parent ruling
      3-0 collision_refuted). Both verdicts survive_with_conditions (GAMMA 6 conditions, ALPHA 7
      with the V1 panel condition satisfied). Hostile-T0 pre-calibration: GAMMA 0.55 [0.38,
      0.70], ALPHA 0.22 [0.12, 0.33] (lower bound below the 0.15 floor). MERGE GATE: one paper,
      GAMMA-led (activation asymmetry; mutually neutralizing strongest attacks; shared
      lab-asset-v3 instrument and Paper-D-scale compute). Evidence map frozen at
      papers/proposal/ecomd_reexploration_d2_evidence_map_2026-09-06.md; next stage D-1.
      Paper/theorem work only; no outcome access.
  - date: "2026-09-06"
    mode: pi_blanket_d1_authorization
    ledger_ref: logs/2026-09-06.md Session 5
    note: >-
      PI issued "全部授权" covering all ten experiment-plan section-11 decisions: F_exec corpus
      freeze, fiber resampler + conformance tests, fixture enrichment (schema-EXTENSION, frozen
      bundle untouched), two-stage amendment, Stage-2 synthetic members, DGP-only zero-GPU
      K-preflight (K in {4,8,16}, default 8), estimator menu, reference calendar (theory
      ~09-12, D0 ~09-19) + 3-reviewer prereg panel, node cleanup at D0, seed namespaces
      11000-11029/12000-12029. Decision recorded at
      research/discovery/decisions/pi_reexploration_d1_authorization_20260906.yaml with explicit
      bounds: GPU/confirmatory-execution/market-data stay D0-gated; CPU-only engine runs
      limited to fixture generation, resampler conformance and the K preflight. D-1 execution
      begins (theorem lemmas + instrument builds).
  - date: "2026-09-06"
    mode: d1_execution_theory_and_instruments_complete
    ledger_ref: logs/2026-09-06.md Session 5 (D-1 record)
    note: >-
      D-1 session 1 (workflow wf_65cd14e7-d12, 10 agents, 0 errors; independent re-verification
      93 pass + 1 by-design xfail; frozen bundle untouched). ALL seven pre-D0 theorem items
      discharged at theorem level, zero REFRAME triggers: KT-G1 R2 non-lumpability PROVEN
      (TV = 1 - max p_x exact constants, uniform over responsive policies, triple-interaction
      scoping, sharp M0 boundary "no active identity channel"); KT-G2 separation proven
      (kernel-blind uniformly valid bounds forced to zero; 3x-to-infinity separation); KT-G3
      Lemma A/B + soft quotients; KT-G4 prongs (a)-(c) proven, prong (d) honestly refuted
      (DPIOT owns the theta-chart residue); KT-G5 exhaustive exposed-set characterization;
      KT-A2 three-layer TV separation with per-fixture constants; KT-M2 table delivered.
      Instruments: enrichment (10 fixtures, lab-asset-v3.1), dual-hash fiber resampler (40+1
      tests, MVHG conformance, frozen prereg statement), K preflight (rule output K=16 with
      none_in_set; PI to choose 16 vs 8 at D0). Contracts doc: L1 RNG gap (L1-4 build before
      D0), L2 ~90% D0 build, pro_rata not an engine kernel (C3 tightening). Estimator menu
      built (ecomd/mechanisms, 20/20 tests). New PI decision items: K choice, resampler
      criterion-(ii) ratification, L2 scope, C3 tightening. Next: prereg draft + 3-reviewer
      panel; D0 ~2026-09-19.
  - date: "2026-09-06"
    mode: pi_addendum_four_rulings_and_d0_prep
    ledger_ref: logs/2026-09-06.md Session 6
    note: >-
      PI answered all four session-1 decision items ("用需要算力多的那个，批准，确认，确认"):
      K = 16 FROZEN (contract C2/C16 amended, G8/G10 to 16 draw-records, window closed,
      alloc_proprata_dev_mean draw-noise caveat recorded); resampler criterion-(ii)
      native-RNG-state-preservation RATIFIED (frozen prereg statement stands, no code change);
      L2 build scope CONFIRMED (code-only pre-D0 build, CPU smoke validation, training stays
      D0-gated); C3 kernel sentence TIGHTENED (engine kernels = fifo + random_unit_within_price;
      pro_rata = S3 exact-arithmetic only). Recorded as addendum D1_ADD_session1_followup in
      the active decision. D0-prep workflow launched: prereg draft (five wording changes +
      amendments) + adversarial 3-reviewer panel + L1-4 RNG fix + L2 build.
  - date: "2026-09-16"
    mode: paper_d_abstract_submitted_g42_preflight_staged
    ledger_ref: logs/2026-09-16.md Session 7
    note: >-
      Paper D ICLR 2027 abstract submitted (P0 cleared). Launcher hardening completed earlier
      in the day; this session: g42 engine 1.1.0 (graceful certification_ambiguous terminal
      after a probe-found ambiguity crash at the periodic corner recertification, tail
      autocorrelation statistics, pivot ambiguity tightening; 38 tests, mypy --strict, ruff
      green), branch-aggregate runner, image rebuilt and requalified
      (sha256:208d3fed...e9fa0c355), item-4 beacon/pulse/derivation freeze, full draft
      authorization payload staged non-operatively with verified hash chain (members ->
      partition -> provenance -> snapshot -> manifest -> genesis -> decision), preflight
      appendix written, goal_state structural bug fixed (derivation_rule had folded eight
      session entries), current machine decision re-registered and re-pointed. Both offline
      validators green. No sandbox authorized; authorization-only merge reserved to the PI.
  - date: "2026-09-16"
    mode: paper_d_abstract_submitted_g42_preflight_staged
    ledger_ref: logs/2026-09-16.md Session 8
    note: >-
      Adversarial audit of all g42 deliverables (5-dimension workflow, 27 agents,
      per-finding refutation): 22 findings confirmed (2 blockers, 6 majors, 14
      minors), all fixed pre-merge. Engine hardened to 1.1.1 (low-temperature
      acceptance overflow clamp, zero-bucket context-policy rejection at
      construction, rg2_var_tail degenerate-tail marker plus frozen assess-rule
      exclusion, runner unit-envelope key/body binding; 44 tests, mypy --strict,
      ruff green). Staged payload restructured to the dx-precedent shape
      (source-only snapshot; asset fingerprint recomputed per the sandbox
      validator formula; chain acyclic), per-unit CPU caps redesigned to
      (branch-128)//32 for wall-deadline headroom, evidence_ref locator
      g42_fixed_knot_moves_preflight registered on the g42 route node (route
      graph now 1996 locators), image rebuilt and requalified
      (sha256:3428e3025c64a17173365290951b780fcf9a0b2e998ffaa8e3445057cc31deda),
      payload re-staged 2026-09-16T06:43:08Z with the chain independently
      re-verified, pre_execution_verification.json staged, appendix corrected
      in place (832nd corner flip ordinal, frozen threshold constants, 7-stage
      chain enumeration, audit record section 7), memo section-6 item-2
      launcher-pin claim corrected to canonical scripts/ pins. Both offline
      validators green. No sandbox authorized; authorization-only merge
      reserved to the PI.
  - date: "2026-09-16"
    mode: g42_campaign_terminal_quarantined_host_oom
    ledger_ref: logs/2026-09-16.md Session 9
    note: >-
      PI blanket authorization "全部授权" executed: preparation commit 91c08c767a,
      operative re-freeze 2026-09-16T08:48:13Z, authorization-only merge 56522d954
      pushed; validators green before and after. Campaign ran outcome-blind:
      b01 325 s, b02 97 s, b03 838 s, b04 142 s all completed inside their
      execution contracts; mid-b05 the host killed the launcher on memory
      pressure and the frozen incident contract fired — quarantine handler
      terminalized the sandbox (host_interruption, container removed,
      runtime_incident.json, state_transition authorized→quarantined, anchor
      terminal, taint registry entry), charging 3,482 of 14,400 reserved
      CPU-seconds; retry forbidden. Operational counts only were inspected
      pre-terminal (b01 25/32 and b02 32/32 certification_ambiguous); the
      frozen assess rule (>50% excluded → cell void) trends both n128 cells
      void; assess application deferred to a dedicated post-terminal session.
      Both offline validators green at terminal (2 quarantined sandboxes, 2
      tainted results registered). Anchor backups at 085526Z and terminal
      092936Z (05ade80e…); off-machine copy pending.
  - date: "2026-09-19"
    mode: gamma_activated_pre_freeze_builds_d0_20261009
    ledger_ref: logs/2026-09-19.md Session 1
    note: >-
      PI activated the merged GAMMA-led program with a strengthened compute-location rule
      (no experiment of any size on the MacBook; all execution remote via ssh). Recorded as
      decision pi_gamma_activation_d0_amendment_20260919: D0 amended pre-hash 2026-09-19 to
      2026-10-09 (prereg v2 eight pin sites + errata C-12, C-11-lawful, no freeze hash
      existed), Annex B constants ratified, campaign-anchored forecast resolve_by dates
      re-based +20 days (recorded in the decision and C-12; ledger untouched), route node
      un-parked to active, current_machine_decision re-pointed, venue target ICML 2027 main
      track, Paper D reported submitted. A three-lens adversarial verification workflow (10
      findings) ran pre-commit and its corrections are incorporated. V100 cold-data migration
      completed the same day (both nodes verified, symlinks intact); the >=40%-free
      STOP-class launch gate (prereg 8.3) still requires the physics-bulk PI decision before
      campaign launch. Pre-freeze build window 2026-09-19 to 2026-10-09 begins.
  - date: "2026-09-19"
    mode: analyzer_contract_authored_audited_ratified_item_8_done
    ledger_ref: logs/2026-09-19.md Session 2
    note: >-
      Analyzer contract (D0 readiness item 8, the pre-freeze long pole) authored at
      papers/proposal/ecomd_reexploration_analyzer_contract_2026-09-19.md from 233 frozen
      pins: one-shot G1-G12 execution order, four block analyzers A_B1/A_B2/A_B3/A_B4
      plus the A10 audit as a separately published fifth analysis unit (five-hash
      publication story), record schema, O-tests with the verbatim PR Section 4.4 count
      column, Annex B(a) bootstrap with the FLAG-1 byte rule, Annex B(g) macros, dummies.
      Adversarial audit workflow (22 agents, 2.29M tokens): 12 confirmed findings fixed
      (publication mechanics, Stage-2 bootstrap-seed coverage on all five blocks,
      estimands family, h=64 descriptive stream, 15-seed halt translation), 4 allegations
      refuted on verified arithmetic. Independent verifier: PASS-with-notes, 3 residuals
      fixed. PI ratified all 16 flags at their defaults and authorized the commit
      ("整体批，授权", decision pi_analyzer_contract_flags_ratification_20260919);
      contract at v0.3, no flag open. No code executed anywhere.
  - date: "2026-09-19"
    mode: pi_directive_bulk_icloud_no_action
    ledger_ref: logs/2026-09-19.md Session 4
    note: >-
      PI directive "bulk和icloud不用管" recorded as decision
      pi_bulk_icloud_no_action_20260919: the physics-bulk disk decision and the
      iCloud durable fix are removed from agent-tracked open items and are not
      re-raised. Not an amendment of frozen text: prereg Section 8.3's
      >=40%-free STOP-class launch gate stands unchanged and fires mechanically
      at D0-S1 if either training volume is below floor (STOP, PI decision at
      that point); the 2026-10-09 freeze session proceeds on the proven
      one-shot core.checkStat=minimal / core.trustctime=false git flags with
      pre-cleaned index.lock and no bare git status / git add -A. Queue head is
      now pre-freeze items 9-10.
  - date: "2026-09-19"
    mode: pre_freeze_item9_cleanup_scripts_complete
    ledger_ref: logs/2026-09-19.md Session 5
    note: >-
      Item 9 complete pending commit authorization: scripts/d0_cleanup_archive.py
      (schemas d0-cleanup-*-v2) + d0_cleanup_dryrun_driver.sh + 30-test suite.
      77-agent adversarial audit returned 35 CONFIRMED (2 blocker, 8 major), all
      fixed: eligible-row dedup + two-pass plan-bound delete with remote
      re-verify, triple-tarball content verification against .sha256s + manifest
      upload to <prefix>/manifests/, strict-literal default policy (inclusive
      singular .sha256 now opt-in), bare-name dual-candidate resolution, upload
      safe_join, check-free floor 0.40. Remote dry-run re-executed on both nodes
      per the PI compute rule: 12 plans, 289 eligible, 2,232,714 bytes, 172
      duplicate pins merged; only ~2.2MB is manifest-covered, so the Section 8.3
      >=40%-free STOP gate is expected to fire mechanically at D0-S1.
  - date: "2026-09-19"
    mode: pre_freeze_item10_reflexive_cell_complete
    ledger_ref: logs/2026-09-19.md Session 6
    note: >-
      Item 10 complete (PI-authorized): ecomd/reexploration/reflexive.py (R00/R11
      campaign-grid cells, C12 five firewalls in code, deterministic no-RNG
      participation policy with EWMA volume as the single sim-in-loop channel,
      E-2 sha256 seed derivation with training-substream disjointness asserted)
      + 21-test suite. Two fill-rate defects from the first smoke fixed
      (floating empty-side anchoring; same-price FIFO hold). Remote smoke on
      v100ts under scripts/rfx_smoke_driver_20260919.sh: full matrix
      {l1,l2} x {R00,R11} x {dgp,sim-untrained} x 2 seeds, 16/16 filled 100/100,
      replay_ok, firewalls stamped; l2 sim metrics match dgp per seed (benign
      chunk_max saturation), l1 sim paths diverge as expected. tape_sha256 is
      not a cross-run_id comparator (session_id embedded in tape records).
      ReferenceEngine per-event state seal = O(tape^2), ~10 CPU-min per
      225-round session; lab_asset untouched (frozen); scheduled cell ~13 CPU-h
      remote. Queue head is now the regression-battery re-verification.
  - date: "2026-09-19"
    mode: regression_battery_v3_rerun_classified_adversarially_verified
    ledger_ref: logs/2026-09-19.md Session 7
    note: >-
      Battery v3 (v2-equivalent per-file semantics, all remote on v100ts):
      154 files / 1562 tests; +8 new files since the 09-08 baseline all green,
      0 dropped; 151 rc=0, 3 rc=1 fully dispositioned — dynamic_graph (same
      stale pinned constant as baseline), research_discovery (launcher sha256
      pin red only on the partial-git mirror, canonical/CI green; plus the f1
      stale count pin (36,173,0) vs live (38,191,0), canonical), route_graph
      (the one green-to-red: stale 1995-locator pin vs live 1997 AND a stale
      route-status pin 'parked' vs 'active' from 21359e930; plus the governance
      finding that 2 route-graph locators reference gitignored logs/private
      files, worktree-only green, merge-blocking not freeze-blocking).
      Both offline validators green on canonical. 15-agent adversarial
      verification absorbed 4 material corrections (route-status pin kind;
      node-only launcher red; merge-vs-freeze timing; mid-sync epoch of the
      151 greens) — the last resolved by a full post-sync battery re-run the
      same night. Seven-item PI decision list surfaced (4 pin fixes, 2
      gitignored-locator disposition, node-red acceptance, durable-record +
      script disposal, queue acceptance semantics, commit authorization).
  - date: "2026-09-20"
    mode: pi_battery_v3_dispositions_approved_and_executed
    ledger_ref: logs/2026-09-20.md Session 1
    note: >-
      PI approved all seven battery-v3 decision items ("都批准", decision
      pi_battery_v3_dispositions_20260920). Executed: four test pins
      re-pinned (each traced to its documented commit); remote verification
      test_dynamic_graph rc=0, test_research_route_graph rc=0,
      test_research_discovery rc=1 with exactly the accepted node-only
      launcher failure; two gitignored locators dispositioned keep+document
      (merge-blocking consequence scheduled to the post-freeze PR);
      node-red accepted per the 09-08 section 5.3.5 class; durable battery-v3
      record + driver committed; queue acceptance criterion amended to "no
      unexplained regressions; every red dispositioned". Post-disposition
      node state 153/154 green. Queue head is now the D0 freeze session
      2026-10-09.
  - date: "2026-09-20"
    mode: d0_freeze_runbook_rehearsed
    ledger_ref: logs/2026-09-20.md Session 4
    note: >-
      15-agent readiness audit + full mechanical rehearsal of the 2026-10-09
      freeze session: prereg Section 1.3(2) resolved to 26 concrete paths
      with an expected-hash table; full hash recipe rehearsed (all paths
      locally present, pinned hashes match, placeholder-state commit-level
      56e368eb...); R2 real prefix alpha_cube_d0_20261009/ verified empty and
      the upload+etag route proven end-to-end via Mac boto3 r2_sync (rclone
      absent on every host; item-9 archive tooling needs rclone installed on
      the D0-S1 node before campaign launch - separate task). Runbook:
      research/discovery/ops/d0_freeze_session_runbook_20260920.md
      (verbatim command sequence, four pin-field fill drafts with the
      self-embedding resolution, failure paths, five PI one-liners for
      session open). Freeze not executed early; frozen text untouched.
  - date: "2026-09-21"
    mode: rclone_installed_three_nodes_third_node_enumerated
    ledger_ref: logs/2026-09-21.md Session 1
    note: >-
      PI directive "做，不要等" with three usable machines (100.70.122.100 new). Third
      node enumerated: Ubuntu 24.04, 2x RTX 3080 10GB both idle, one 50G /data (3% used),
      shared with the atomistic fleet (worker claims both GPUs, slots 4, server on
      v100ts:18117); heterogeneous pool per project rules - not enrolled in the V100
      campaign without a separate qualification benchmark and PI arbitration. rclone
      v1.75.1 + r2: remote installed and bucket-verified on all three nodes (conf mode
      600, credentials never printed, remote name matches R2_BASE "r2:ecophys") - the
      item-9 D0-S1 dependency is closed. No GAMMA/campaign experiment may run before
      the date-pinned freeze (D1 authorization bounds); all four GPUs verified idle.
      Disk map unchanged from 2026-09-20 (no new empty volumes materialized despite
      the PI's description; ts 70+19G, bts 153+82G free, 3080 box 46G free on one 50G
      /data - the 2026-09-20 spread directive already covers campaign storage). An
      earlier-freeze option (pre-hash C-13 amendment on the C-11/C-12 precedent, then
      runbook execution, then launch) was surfaced to the PI as the lawful "don't
      wait" path; not executed unilaterally.
  - date: "2026-09-21"
    mode: d0_freeze_executed_campaign_ordered_launched
    ledger_ref: logs/2026-09-21.md Session 2
    note: >-
      PI ordered the campaign advanced now ("不要有任何冻结，不要等...直接开始跑"); on the
      structured follow-up the PI chose freeze-in-parallel over waiver (decision
      pi_campaign_advance_20260921). C-13 pre-hash amendment executed (freeze date
      2026-10-09 -> 2026-09-21, R2 path alpha_cube_d0_20260921/, commit df7b60cdc; agent
      pushback recorded once: the prereg is NOT without force). D0 freeze session then
      EXECUTED per runbook §5, all seven steps: validators dispositioned (local green,
      node route-graph green, one documented node-only git-history red), pin-fill at
      2026-09-20T15:11Z, 26-path hash -> commit-level freeze sha256
      fd4a40b0306d415cef9a8a41b8e94e5afb21126e4e3d7ff99097e066f9981f57, freeze commit
      f4e61bafed21f12a6ec73dbe82b9ed4ea87bdd37 (dual id + object sha256 720c4111...fe205b),
      freeze_record.json at r2://ecophys/alpha_cube_d0_20260921/ etag 52b5b8ed...70c1
      (local sha256 bdf92b2c...a4ca1, 11261 bytes), session log + second commit d519824fd,
      FLAG-12 line recorded (L1-4 landed pre-freeze, 15e139928). 24/26 per-file rows match
      the runbook baseline; the 2 deltas are exactly the C-13-edited files. Zero outcome
      access pre-hash; zero Mac experiments. Derived anchors at D0=2026-09-21: ceiling
      2026-10-19, t2 2026-10-17, D0-S4 2026-10-07/08. Queue head = node staging +
      campaign launch on all three machines.
derivation_rule: >-
  Every field in this file except goal_id, terminal_objective, and derivation_rule must be
  re-derived from the canonical sources listed in the research-goal skill at the start of each
  session. Never copy route statuses, counts, or failure history into this file.
