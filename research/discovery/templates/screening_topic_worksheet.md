# Screening topic worksheet

This is an outcome-blind search worksheet, not a topic card or execution authorization. Complete
only the section for the program's current funnel stage. Do not backfill an expensive contract for
an idea already killed cheaply.

## F0 — raw question program

- Program ID and working name:
- Source lane: `unresolved_model_disagreement` / `new_truth_or_control_capability` /
  `market_native_action_or_constraint` / `cross_domain_theorem_with_market_specific_obstruction`
- Topic archetype: `theory_mechanism` / `measurement_method` /
  `empirical_intervention` / `simulator_method`
- Market-native object:
- Rival explanation `H1`:
- Rival explanation `H0`:
- One result that would distinguish them:
- Scientific value if `H1` wins:
- Scientific value if the result is null or favors `H0`:
- Cheapest next check:

Stop here unless the program is selected for one of at most six F1 quick screens.

## F1 — quick screen

- Parent route IDs and closest failure families:
- Anchor primary work 1:
- Anchor primary work 2:
- Optional anchor primary work 3:
- Exact killer toy or reduction:
- Does the proposed observation actually separate `H1` and `H0`?
- For a cross-domain effect, what is the market-native control parameter, and which changes of
  representation, unit, clock, initial family, or distance metric must leave the claim invariant?
- Plausible truth source, without claiming it is qualified:
- Seven Pareto dimensions: discriminative power / identifiability / irreducible residual /
  positive-null value / transfer / feasibility / next-update cost:
- Dominated, deduplicated, selected for F2, or held out by portfolio limit:
- Reason:

Stop here unless selected as one of at most three F2 programs.

## F2 — collision and archetype contract

### Identity

- Scientific domain and target venue:
- Literature cutoff, frozen queries, and inclusion rules:
- Outcome access: none
- Why this archetype's evidence ladder fits:

### Five-part contract

- Native state `X`:
- Admissible intervention `do(A)`:
- Primary response `Y`, horizon, units, and clock:
- Main mechanism `H1` and distinct prediction:
- Strongest alternative `H0` and distinct prediction:
- Same-estimand truth contract `T` appropriate to the archetype:
- Minimum scientifically meaningful effect or theorem boundary:

### Exact claim and nonclaims

- One-sentence question:
- Directional falsifiable statement:
- Candidate theorem or phenomenon:
- Explicit nonclaim 1:
- Explicit nonclaim 2:
- One result that closes the formulation:
- Reusable residue under a null result:

### Invariances and killer twins

- Required representation invariances:
- Twin 1, summary fixed and response changed:
- Twin 2, proposed cause changed and response law fixed:
- Unit/clock/order-refinement test:
- Negative or sham control:

### Six-work collision screen

- Exact market-object works:
- Parent-mathematics works:
- Identification or truth-contract works:
- Hardness, impossibility, or counterexample works:
- Closest direct work and exact residual:
- Unresolved collision:

### Archetype-specific contract

- Theory/mechanism: proof route, parent-class escape, boundary/lower bound, executable falsifier:
- Measurement method: truth generator, calibration/coverage, failure regimes, observation bridge:
- Empirical intervention: assignment, state, outcome, interference, timing, rights, replication:
- Simulator method: truth oracle, equal-budget baseline, lineages, cost frontier, external bridge:
- Fill only the selected archetype; mark the other three `not_applicable`.

- Advance to F3, defer unresolved, or close:
- Independent hard reason if closed:

Stop here unless selected as one of at most two F3 audits.

## F3 — full hostile audit and decision

- At least 15 primary works recorded in the novelty manifest:
- At least two killer tests survived or killed:
- Failure-family distinctions:
- Exact simulator/source/data contracts required by the claim:
- Contamination and confirmation split:
- Strongest equal-budget baseline:
- Explicit hostile-T0 failure events:
- Prospective hostile T0 lower / point / upper:
- Conditional NMI and NCS completion probability:
- Cheapest bounded information action and cost unit:
- Success value `B`, failure salvage `S`, action cost `C`, and break-even probability:
- Does robust value of information remain positive without a failed hard gate?
- Machine card, defer, or failed-closed:
- Reason independent of probability if terminal:
