# Today's H20 Run List — 2026-05-09 (Branch E, full Option B)

**Branch**: `feature/paper-a-v4-mechanisms`
**Launcher**: `scripts/h20_branch_e_combo.sh`
**Total**: 828 effective configs, ~12-15h on 8-card H20
**Plan**: `papers/proposal/branch_e_plan.md` + `/Users/howardwang/.claude/plans/curried-cuddling-cloud.md`

This batch combines:
- **A-round** (082 + 075 + 081): the three experiments that decide
  Paper A's direction
- **Completion** (083 + 073): ablations strengthening §4.2 / §4.3
- **B-round** (084-087): four new mechanisms implemented tonight rather
  than waiting for 082 results, per user directive

---

## 0. Pre-flight on H20

```bash
ssh h20
cd ecophys
git fetch --all
git checkout feature/paper-a-v4-mechanisms
git pull
unset NPROC
bash scripts/h20_refresh_deps.sh
bash scripts/h20_branch_e_combo.sh --dry-run
```

Dry-run prints per-phase config count, preflight checks, and wallclock
estimate. **If preflight fails, do not launch.** Common failure modes:
stale torchrun processes, disk near-full, schema regressions in 081.

The launcher has 9 hardcoded preflight checks:

1-2. 081 v4combo schema (noise_dist=levy, target_dataset=btcusdt)
3. 082 has 180 configs
4. combo_full's 4-mechanism wiring
5-9. 083-087 each have 90 configs
10-12. B1/B3/B4 combo cells stack correctly on combo_full
13. Disk free ≥ 50 GB
14. No orphan torchrun
15. GPU presence

---

## 1. Phase 082 — V4 four-mechanism combo (180 cfg, ~2.6h)

**Path**: `experiments/082_v4_combo_30seed/`
**Decisive experiment**: do v4 mechanisms compose additively? Result
determines whether Paper A goes ICML 2027 main (case 1), workshop+B1
(case 2), or full B-round (case 3).

| cell | levy_α | asym_α | memk (λ, scale) | inner | answers |
|---|---:|---:|---|---:|---|
| `combo_full` | 1.9 | 0.4 | (0.95, 1.0) | 3 | flagship: full additive? |
| `combo_no_levy` | — | 0.4 | (0.95, 1.0) | 3 | does Lévy add? |
| `combo_no_inner` | 1.9 | 0.4 | (0.95, 1.0) | 1 | does adiabatic add? |
| `combo_no_memk` | 1.9 | 0.4 | — | 3 | does memk add? |
| `combo_asym05` | 1.9 | 0.5 | (0.95, 1.0) | 3 | asym dose-response |
| `combo_levy17` | 1.7 | 0.4 | (0.95, 1.0) | 3 | Lévy dose-response |

**Success (all required)**: combo_full mean ≥ 5.5, ≥4/30 seeds at ≥8/11,
max ≥ 10/11. Stretch: mean ≥ 6.0 AND ≥1 seed at 11/11.

---

## 2. Phase 075 — Adiabatic dose-response leftover (90 cfg, ~1.3h)

**Path**: `experiments/075_adiabatic_30seed/` (SKIP_DONE auto-filters
inner_1, inner_5 which are already done)

**Cells effective tonight**: inner_3 / inner_10 / inner_20 × 30 seeds.

**Hypothesis**: adiabatic is the only lever shown to move autocorr.
Branch D's inner_5 has ac_pass=43% vs inner_1's 20%. If inner_10 or
inner_20 pushes ac_pass > 60%, we have proof that adiabatic decouples
the AR(1) artifact.

---

## 3. Phase 081 — BTC cross-asset regen (60 cfg, ~0.9h)

**Path**: `experiments/081_btc_30seed/` (regenerated with bug fixes)

**Critical fix**: prior 081 configs had Lévy never enabled (wrong field
name) AND target_dataset key fell back to SPX. Earlier "BTC results"
were SPX masquerading. Both issues fixed; 60 configs (30 baseline +
30 v4combo) regenerated. The launcher's preflight grep verifies the fix.

| cell | what it tests |
|---|---|
| `btc_baseline` × 30 | v3 architecture on BTCUSDT 1-min Q1 2024 |
| `btc_v4combo` × 30 | levy=1.7 + asym=0.6 + memk(0.95, 1.0) on BTC |

**Success**: btc_v4combo mean > btc_baseline mean by ≥ 0.3 → physics is
universal, not SPX-specific.

---

## 4. Phase 083 — Asym-drag fine grid (90 cfg, ~1.3h)

**Path**: `experiments/083_asym_drag_finegrid_30seed/`

| cell | asym_α |
|---|---:|
| `asymdrag_a04` | 0.4 |
| `asymdrag_a05` | 0.5 |
| `asymdrag_a07` | 0.7 |

**Goal**: dose-response curve for §4.3 ablation. Find sweet spot
between Branch D's a03 (mean 4.57, low) and a06 (mean 5.10, high
variance). a07 tests just below the unstable a09.

---

## 5. Phase 073 — Hawkes/jump ablation (48 cfg, ~0.7h)

**Path**: `experiments/073_hawkes_jump_ablation/` (already configured)

8 cells × 6 seeds = 48 cfg covering Hawkes excitation κ ∈ {0, 0.15, 0.6}
× jump intensity λ ∈ {0, 0.25, 1} grid.

**Goal**: §4.2 mechanism attribution table. For each of the 11 facts,
which combination of Hawkes / jumps contributes to its v3 baseline pass?

---

## 6. Phase 084 — B1 microstructure noise (90 cfg, ~1.3h)

**Path**: `experiments/084_b1_microstructure_30seed/`
**Code**: `ecomd/physics/integrator.py` (new `microstructure_rho` field)

Mechanism: ε_t_eff = ε_t − ρ · ε_{t-1} (MA(1) bid-ask bounce).

| cell | rho | base | answers |
|---|---:|---|---|
| `b1_rho03_pure` | 0.3 | v3 baseline | does B1 fix autocorr in isolation? |
| `b1_rho05_pure` | 0.5 | v3 baseline | over-correction: does ac go negative? |
| `b1_rho03_combo` | 0.3 | combo_full | does B1 fix the one fact combo_full fails? |

**Success**: rho03_combo autocorr_pass ≥ 60% (currently <30% across all v4 cells).

---

## 7. Phase 085 — B2 Wasserstein loss (90 cfg, ~1.3h)

**Path**: `experiments/085_b2_wasserstein_30seed/`
**Code**: NO CHANGES — `ecomd/training/losses.py` already supports
loss_family ∈ {moments, wasserstein, mmd, sinkhorn, hybrid}.

| cell | loss_family | base | answers |
|---|---|---|---|
| `b2_wasserstein_pure` | wasserstein | v3 | does loss alone lift v3? |
| `b2_wasserstein_combo` | wasserstein | combo_full | pure W2 + v4 mechanisms |
| `b2_hybrid_combo` | hybrid (moments+W2) | combo_full | reviewer-defensible |

**Success**: hybrid_combo mean ≥ combo_full mean + 0.3 → loss change is
worth the architectural complexity.

---

## 8. Phase 086 — B3 discrete regime (90 cfg, ~1.3h)

**Path**: `experiments/086_b3_discrete_regime_30seed/`
**Code**: `ecomd/models/regime_latent.py` (new `DiscreteRegimeGRU`),
`ecomd/models/ecomd.py` (regime_discrete_enabled flag + branch)

| cell | K | base | answers |
|---|---:|---|---|
| `b3_k3_pure` | 3 | v3 + regime_enabled | discrete switching alone |
| `b3_k3_combo` | 3 | combo_full | does B3 fix aggregational_gaussianity in combo? |
| `b3_k5_combo` | 5 | combo_full | finer regime granularity |

**Success**: k3_combo aggregational_gaussianity_pass ≥ 70% (currently <30%).

---

## 9. Phase 087 — B4 power-law potential (90 cfg, ~1.3h)

**Path**: `experiments/087_b4_power_law_30seed/`
**Code**: `ecomd/models/potentials.py` (new `PowerLawExternalPotential`)

V_ext(s) = w_mlp · MLP + w_pow · |s|^α / α; sub-linear restoring force at
α<2 produces fat-tailed dynamics.

| cell | α | base | answers |
|---|---:|---|---|
| `b4_alpha13_pure` | 1.3 | v3 | very heavy tails alone |
| `b4_alpha15_combo` | 1.5 | combo_full | headline: does B4 + combo fix hill? |
| `b4_alpha18_combo` | 1.8 | combo_full | gentler dose-response |

**Success**: alpha15_combo hill_pass ≥ 50% (currently 1-23%).

---

## Phase summary

| # | dir | configs | done | effective | hours |
|---|---|---:|---:|---:|---:|
| 1 | 082 v4 combo | 180 | 0 | 180 | 2.6 |
| 2 | 075 leftover | 150 | 60 | 90 | 1.3 |
| 3 | 081 BTC regen | 60 | 0 | 60 | 0.9 |
| 4 | 083 asym fine | 90 | 0 | 90 | 1.3 |
| 5 | 073 Hawkes | 48 | 0 | 48 | 0.7 |
| 6 | 084 B1 microstructure | 90 | 0 | 90 | 1.3 |
| 7 | 085 B2 Wasserstein | 90 | 0 | 90 | 1.3 |
| 8 | 086 B3 discrete regime | 90 | 0 | 90 | 1.3 |
| 9 | 087 B4 power-law | 90 | 0 | 90 | 1.3 |
| **total** | | **888** | **60** | **828** | **~12h** |

15h is the conservative dry-run estimate (assumes 8.8 min/cfg sequential).
With 8-way parallel + I/O overhead the realistic wall is ~12h.

---

## Launch sequence

```bash
# 1. Verify on H20
bash scripts/h20_branch_e_combo.sh --dry-run

# 2. Daemon launch
DAEMON=1 bash scripts/h20_branch_e_combo.sh

# 3. Monitor
tail -f experiments/_branch_e_*.log

# 4. Pull results to Mac when done
git pull
for d in 082 075 081 083 073 084 085 086 087; do
    conda run -n ecophys python scripts/score_phase.py "experiments/${d}"_*
done
```

---

## Decision tree (run after results pull)

```
  combo_full result
        │
        ├── case 1 (~30%): mean ≥ 6.0 / max ≥ 10
        │   → A-round wins. Start Paper A writing immediately.
        │   → Use B-round results as §4.5 ablation supplement.
        │   → Optional: scale to ETH + QQQ for stronger §4.4.
        │
        ├── case 2 (~45%): mean 5.3–5.9 / max 9–10
        │   → Look at B1/B4 results to plug remaining holes.
        │   → If B1+B4 lift autocorr+hill into bands → Paper A.
        │   → If not → try B2 (Wasserstein) loss for universal lift.
        │
        └── case 3 (~25%): combo_full mean ≤ 5.1 (saturation)
            → Combo + B-round results combined; pick best cell.
            → Likely path: combo_full + B1 + B4 stacked together,
              re-run as 088 with 50 seeds for tight CI.
```

---

## Phase ordering rationale

1. **082 first** because it's the decisive experiment; if H20 dies at +12h,
   we still have the most important answer.
2. **075 second** because adiabatic interacts with combo cells; understanding
   the inner_steps dose-response strengthens 082's interpretation.
3. **081 third** for cross-asset before B-round noise.
4. **083 + 073** as ablation completion.
5. **B-round 084-087 last** because they're contingency; if H20 stops mid-B,
   we lose less.

---

## Next morning analysis steps

1. `git pull` — get all H20 results
2. Run continuous scoring across all 9 dirs:
   ```bash
   conda run -n ecophys python scripts/score_continuous.py \
       experiments/{082,075,081,083,073,084,085,086,087}_* \
       --csv results_branch_e_continuous.csv
   ```
3. Per-phase scoreboard inspection (auto-generated by launcher)
4. Identify decision tree case (1/2/3) from combo_full
5. Update `papers/proposal/branch_e_plan.md` decision log
6. If case 1: start Paper A §3 / §4.1 drafts
7. If case 2/3: identify which B-round cells beat combo_full

---

## References

- **Plan (high-level)**: `papers/proposal/branch_e_plan.md`
- **Plan (corrections + B-round)**: `/Users/howardwang/.claude/plans/curried-cuddling-cloud.md`
- **Today's review log**: `logs/2026-05-09.md`
- **Branch D results summary**: `logs/2026-05-07.md`
- **Generators**: `experiments/{082,083,084,085,086,087}_*/generate_configs.py`
- **B-round code**:
  - B1: `ecomd/physics/integrator.py:OverdampedLangevin` (microstructure_rho)
  - B2: `ecomd/training/losses.py:compute_loss` (loss_family already exists)
  - B3: `ecomd/models/regime_latent.py:DiscreteRegimeGRU`
  - B4: `ecomd/models/potentials.py:PowerLawExternalPotential`
- **Stability filter**: `scripts/score_phase.py:instability_reason`
- **Launcher**: `scripts/h20_branch_e_combo.sh` (9 phases)
