# PDEBench Burgers transport-only amendment

Frozen 2026-09-01 NZST after the data-admission wrapper logged an HTTP/2 stream cancellation and
the observed `.part` size regressed from 2,856,763,392 to 502,501,376 bytes, but before any HDF5
value, invariant statistic, or model outcome was inspected.

The released file identity and every scientific gate remain unchanged: DaRUS v8 file ID 281363,
8,232,968,312 bytes, MD5 `e6d9a4f62baf9a29121a816b919e2770`, the subsequently computed global
SHA-256, the frozen coordinate schema, the full finiteness scan, the mass-drift thresholds, and
the conservative-restriction identities.

The active curl 7.81.0 HTTP/2 transfer may be terminated by exact PID after its log is preserved.
The existing unadmitted partial bytes may be resumed once with GNU Wget 1.21.2, which uses an
HTTP/1.1 transfer path and re-evaluates continuation state across reconnects. The command must
write only the same `.part` path. It may rename that path to the frozen final filename only after
the exact byte count and MD5 pass. The unchanged preparation script then performs its full
SHA-256, schema, finiteness, invariant-drift, and restriction scan without another download.

This is a transport repair, not a second dataset attempt. A final byte-count, MD5, schema, or
scientific-gate failure remains terminal. No alternate file, mirror, viscosity, selected subset,
threshold change, or model launch is authorized by this amendment.

