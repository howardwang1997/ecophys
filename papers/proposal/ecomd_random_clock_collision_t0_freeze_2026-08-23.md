# Random-clock free-flight / hard-collision dynamics — T0 freeze

**Frozen:** 2026-08-23

**Status:** AMBER; T0 only

**Primary target:** *Nature Computational Science*

**Config:** `configs/empirical_physics/ecomd_random_clock_collision_t0_v1.yaml`

## Frozen question

Can externally randomized market matching times identify the observable free-flight semigroup through a hard
collision operator and support a calibrated in-support prediction under a frozen change of the clock law, while
certifying the directions that the collision observation destroys?

## Non-negotiable interpretation

- The market event sequence is observed; this is not the closed partial-order/linearizability problem.
- The random variable is the externally generated waiting time between hard matching events.
- The primary object is the observable operator family $HCP_t$, not a claim that a random auction improves a
  scalar market-quality metric.
- A separation of $C$ and $P_t$ may be claimed only if it is identified; otherwise the equivalence class must be
  the result.
- EcoMD has no evidential role in T0. It becomes relevant only after a theorem, a qualifying protocol and a
  non-financial transfer benchmark all survive.

## Gate order

1. **D-1 protocol/schema:** two independently governed clocks; observable realized draw, empty events, declared
   pre/post state, sufficient collision semantics, independent-unit clustering and compatible licences.
2. **G0 mathematical object:** filtration, exogeneity, support, quotient and finite toys.
3. **G1 primary prior art:** at least 40 unique works over all buckets frozen in the YAML.
4. **G2 reductions:** attempt exact reductions to random-time Markov GMM, observability, semigroup inverse
   problems, irregular Koopman, continuous-dose inference, auction design and PDMP/kinetic theory.
5. **G3 residual:** at least one of E1--E3 plus a reviewer-safe one-sentence contribution.

The first failed gate closes the route. In particular, AlphaX public TRF executions do not qualify unless a
participant/exchange contract exposes the actual Match Events, including empty events and sufficient matching
state. Securities sharing one closing clock are a single randomized unit.

## Frozen resource and information boundary

Ten calendar days; Mac CPU only; zero GPU, paid data, benchmark runs and market-outcome queries. Official rules,
regulatory filings, schema/licence documents, primary literature, repository source and hand-worked toys are
allowed. No venue substitution, hidden-clock proxy, outcome peeking, EcoMD fit, threshold relaxation or
synthetic rescue is permitted after a failure.

## Pass statement template

T0 may pass only if the following sentence can be completed without hype:

> We establish [exact new identification/statistical result] for stochastic semigroups observed through hard
> collision maps at randomized times, prove [matching boundary], and validate the same obstruction in [market
> protocol] and [independent non-financial process].

If the honest completion is instead “we fit market outcomes against realized auction duration,” the route is
RED.
