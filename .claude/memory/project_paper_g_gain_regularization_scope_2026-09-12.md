# Paper G gain regularization scope — partial capability, no F2

PRIVATE / INTERNAL. Direct PI continuation prompted an ordinary capability/re-entry audit of the existing F1 question, not a new cycle or an extension of the consumed generation exception.

For the fixed physical shear map M, noiseless linear population training with fixed Frobenius penalty has exact minimizer J=M Sigma(Sigma+lambda I)^(-1). Isotropic excitation adds uniform damping and preserves shear coupling; gain reduction alone cannot identify loss of the shear mechanism. With the same physical M, penalty and covariance eigenvalues, two covariance orientations yield respectively unstable and stable fitted operators. These are exact estimator-conditioning controls, not new fluid physics or observed neural-network findings.

A population linear prediction-risk bound plus a lower covariance eigenvalue bounds gain error; empirical training error and nonlinear derivative calibration require further hypotheses. JAWS jointly adapts spatial weights, so the fixed-penalty control is not its solved training law. Bishop's noise/derivative regularization, Wang–Mao's gain diagnostic and spectrum-clipping stable identification are mandatory parents, not defeated baselines.

Decision: partial_capability; zero recorded blockers removed, no qualified trigger, no F2 or scientific execution. The cycle31 question remains candidate at F1; its historical disposition and original artifact hashes are preserved. Stop generic ridge/covariance variants as topic generators. Require a distinct residual and actual proof/truth capability before further escalation.

Counts: Paper G55formulations/15cycles/0cards; detailed22cycles/139questions. Evidence1064, triggers173/qualified0. Graph299nodes/279edges/1649locators. Three new primary works, two internal audit documents, four paper-only controls; no scientific runs, outcomes, data/model payloads, GPU, paid work, delegation or outreach.

Formal: papers/proposal/ecomd_paper_g_gain_regularization_scope_2026-09-12.md.
Contract: research/paper_g/gain_regularization_scope_20260912.yaml.

The fully excited noiseless population reference has an exact unregularized least-squares solution with correct stability and gain. Positive regularization is a deliberately biased control here, not a demonstrated practical stability–accuracy trade-off. Governance, graph, protected history and four targeted tests pass.
