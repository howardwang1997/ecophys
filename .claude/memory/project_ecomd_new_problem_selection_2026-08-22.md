---
name: ecomd-new-problem-selection-2026-08-22
description: "Historical post-Q/C selection: its sole AMBER partial-order card failed T0 on 2026-08-23; no reserve card was promoted."
metadata:
  node_type: memory
  type: project
---

# EcoMD new-problem selection — lasting decision

**Superseded on 2026-08-23:** the sole activated card is now FAIL/RED. Correct state-enabled semantics reduce to
linearizability/reachability, learned response sets to PAC/robust stochastic games, and abstaining certificates
to observational determinism/model checking. See `project_ecomd_partial_order_event_t0_2026-08-23.md`. No card
from the reserve ranking is automatically active.

After the observation-quotient and intervention-preserving coarse-graining T0 closure, three independent screens
covered computational physics, NMI/multi-agent learning and reviewer-2 falsification. No candidate is GREEN.

Exactly one new card was activated at **AMBER / T0-only** for the now-completed audit:

> When a market feed specifies only a partial order of hard, state-dependent events, compute or learn a
> calibrated, non-vacuous set of outcomes or intervention responses over all legal event orders without
> inventing a total order.

This card is selected because the problem is externally anchored, occurs in the current repository's timestamp-
tie semantics, transfers to event-driven MD/concurrent systems, and makes stateful event simulation central.
It is not a novelty claim. Trace monoids, probabilistic concurrency, partial-order reduction, uncertain event-log
resolution, interval-censored point processes, linear-extension complexity and operator-splitting commutators are
strong direct collisions.

The elementary facts that linear extensions are connected by adjacent incomparable swaps, pairwise commuting
updates preserve a declared property, and the two-event discrepancy is an update-operator commutator are frozen
as non-novel baselines. T0 passes only if a new semantic-conflict algorithm and matching boundary, calibrated
learned response set with a lower bound, or property-specific abstaining certificate survives a 40-plus-primary-
work reduction and a dual-resolution source-contract audit.

The nearest fallback is a certificate for gradients through hard stochastic matching events, but stochastic AD,
queueing weak derivatives, differentiable DES, saltation and hard-forward/surrogate-backward CTMC work make it a
comparator only. A randomized call-auction stopping-time experiment is the best empirical wildcard. A code--
trajectory-anchored market theory-of-mind problem is the best NMI reserve, but public agent-by-mechanism support
is likely too sparse and the MD link is weaker.

Full selection memo: `papers/proposal/ecomd_new_problem_selection_2026-08-22.md`. T0 freeze:
`papers/proposal/ecomd_partial_order_event_t0_freeze_2026-08-22.md`. Config:
`configs/empirical_physics/ecomd_partial_order_event_t0_v1.yaml`.

T0 did not pass. Do not inspect market outcomes, purchase/bulk-download data, implement the benchmark, train
EcoMD, open the exposed exp138 test as confirmation or use GPUs for this card. Do not rescue it by combining
partial-order semantics with hard-event gradients or the failed Q/C cards.
