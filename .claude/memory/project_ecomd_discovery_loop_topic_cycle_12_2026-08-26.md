---
name: EcoMD Discovery Loop topic cycle 12
description: Price-time commitment screen complete with zero cards. The first prospectively forecast full-T0 route resolved false after exact-parent, parameter-completion, lifecycle and same-estimand failures.
node_type: memory
type: project
---

# EcoMD Discovery Loop topic cycle 12

Cycle 12 stayed inside simulated markets, market microstructure and financial physics. Twelve raw
programs became six quick screens, three collision screens, one full hostile audit and zero cards.
The terminal report is
`papers/proposal/ecomd_discovery_loop_topic_cycle_12_price_time_commitment_result_2026-08-26.md`.

## Terminal result

The sole F3 program was `price_time_commitment_cpi_scaling`, motivated by official EBS
Conditional Price Increments: eligible sub-pip orders can have an alternate minimum quote life.
Before the fifteen-work audit, the subject and controls
`chi = sigma*sqrt(tau)/delta`, `rho = lambda*tau` were frozen with a 5--25% full-T0 forecast and a
12% point estimate.

The route is failed-closed:

- the clean Brownian result is exactly reflection-principle barrier crossing;
- with chi and rho fixed, informed-flow composition can reverse expected liquidity value;
- a matched-variance compound-jump price has a different crossing and overshoot law;
- CPI use is state-gated and selected rather than assigned;
- official EBS MBP uses aggregate price-level entries and explicitly does not use the OrderID
  repeating group for EBS, so quote age, priority and locked cancel exposure are unavailable;
- ABIDES can host only a custom implementation, while PAMS and Bourse lack the same native
  physical-time CPI/MQL semantics; and
- no independent real fine-price/slow-cancel mechanism with lawful complete lifecycle data was
  qualified.

The forecast resolved false at 2026-08-26T04:08:16Z. Its Brier score is 0.0144. This is only one
comparable full-T0 resolution, far below the predeclared twenty needed to review the 15%
active-status brake. Probability did not close the route; the named hard gates did.

## Durable process changes

- Opposite signs count as a model fork only for the same native state, legal intervention,
  response and conditioning set.
- A proposed dimensionless market law must pass a parameter-completion twin: hold every proposed
  group fixed and vary one omitted legal event kernel or strategic state. A sign reversal closes
  the universal claim; a repair that encodes the full kernel is a measurement reformulation.
- Reusable assets are the Brownian barrier reduction, marked-flow twin, matched-variance jump
  twin, and EBS lifecycle-schema failure.

No simulation, outcome access, dataset purchase, sandbox, outreach, EcoMD edit, model
implementation, or compute is authorized.
