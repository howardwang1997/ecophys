---
name: EcoMD Discovery Loop topic cycle 14
description: Residual-state and loss-transfer screen complete with zero cards. Observable order-age echo reduced to a Volterra cross-kernel without native phase; ranked ADL was directly occupied; GMX pending impact reduced to documented complete-state closeability.
node_type: memory
type: project
---

# EcoMD Discovery Loop topic cycle 14

Cycle 14 stayed inside simulated markets, market microstructure, and financial physics. Twelve raw
programs became six quick screens, two six-work collision screens, zero F3 audits, and zero cards.
The terminal report is
`papers/proposal/ecomd_discovery_loop_topic_cycle_14_residual_state_result_2026-08-26.md`.

## Terminal result

- `observable_order_age_liquidity_echo` is failed-closed. For weak inputs, paired response minus
  the two singles is the off-diagonal second-order Volterra kernel. Native order age follows one
  unit-velocity characteristic, so it supplies renewal delay but not the heterogeneous phase
  velocities required for mixing and rephasing. An age scramble either changes FIFO, expiry,
  cancellation or agent state, or preserves the future law and cannot change the response.
- `ranked_adl_loss_transfer_front` is failed-closed. Current ADL impossibility, online-learning,
  risk-minimization and production-queue counterfactual work directly occupies the ranking,
  overshoot, fairness, solvency and cross-margin objects. Calling the consumed queue a front adds
  no theorem.
- `gmx_pending_impact_closeability_gap` is failed-closed. GMX documents the stored-impact corner;
  complete position, pool, cap, acceptable-price, oracle and fee state makes closeability a
  deterministic feasibility predicate. A cascade additionally needs future flow and policy state,
  while v2.2 bundles several rule changes and no independent same-semantics implementation passed.
- Vega parked-peg release and Eurex synthetic-path allocation were deduplicated against existing
  hosted-conditional-order and implied-liquidity closures.

No program reached F3, so no prospective full-T0 forecast was created. The provisional 15% brake
did not close any route and receives no new calibration evidence.

## Durable process changes

- Expand paired-pulse claims through second order before interpreting a residual as an echo.
- Require an independently observable phase coordinate, frozen timing law, and legal
  state-preserving phase operation for a rephasing claim.
- Treat an officially documented protocol corner as a verification target unless a scientific
  residual survives complete-state replay and the nearest reachability/control parent.
- Map loss-transfer questions against current ADL and default-waterfall work before constructing a
  simulator.

No simulation, outcome access, dataset action or purchase, sandbox, outreach, EcoMD edit,
implementation, or compute is authorized.
