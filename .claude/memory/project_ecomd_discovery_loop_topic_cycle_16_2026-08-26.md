---
name: EcoMD Discovery Loop topic cycle 16
description: Prospective market-simulator validity screen complete with zero cards. The frozen F3 route failed off-support transport, independent-intervention replication, common-action, and field-asset contracts.
node_type: memory
type: project
---

# EcoMD Discovery Loop topic cycle 16

Cycle 16 returned to the core simulated-market question. Twelve raw programs became six quick
screens, three six-work collision screens, one prospectively frozen F3 hostile audit, and zero
cards. The terminal report is
`papers/proposal/ecomd_discovery_loop_topic_cycle_16_prospective_simulator_validity_result_2026-08-26.md`;
the pre-audit freeze is
`papers/proposal/ecomd_cycle16_prospective_simulator_validity_f3_freeze_2026-08-26.md`.

## Terminal result

- `prospective_counterfactual_market_simulator_validity` is failed-closed. No exact paper was found
  that uses a predeclared simulator-interventional score to predict several unseen real market-rule
  responses, but the formulation failed more basic gates.
- Simulator-native interventions do not identify participant adaptation outside the observed real
  regime. Two real kernels can match every pre-change observation and finite synthetic probe while
  responding oppositely in a newly reachable post-rule state.
- Two future rule changes provide only two independent policy environments. Models, securities,
  seeds and horizons nested within them cannot be treated as independent replications.
- Learned LOB generators and structural market ABMs do not currently expose one frozen, externally
  validated rule-action language. A researcher-built adapter can encode the intended response.
- TSE 2027, CME SR3, the US tick reform and volatility pauses each fail at least one joint
  assignment, complete-state, licence, independent-replication or same-action gate.
- `task_conditioned_market_simulator_adequacy` and
  `circuit_breaker_stored_order_release_response` are also failed-closed under direct parent,
  assignment and counterfactual-state reductions.

The frozen full-T0 forecast was 4--22%, point 10%, and resolved false with Brier 0.0100. Together
with Cycle 12, mean Brier is 0.0122 over only two all-negative resolutions. This is not enough to
recalibrate the provisional 15% active-status brake; the hard gates, not the threshold, closed the
route.

## Durable process changes

- Treat entire independently governed intervention families, not model--security rows, as the
  replication unit for field-validity claims.
- Require a frozen common native action language and deterministic adapter conformance before a
  simulator comparison.
- State the structural transport domain; do not infer unrestricted real validity from consistency
  with the source simulator.
- After two zero-card cycles in one parent/failure family, do not spend a third cycle on a renamed
  venue or method without a new model fork, truth/control asset, or theorem removing a blocker.

No simulation, outcome access, dataset action or purchase, sandbox, outreach, EcoMD edit,
implementation, or compute is authorized.
