---
name: Merge analysis + C4 preflight + trigger scan 2026-08-27
description: PI elected continued execution with optional paper merge; merge-compatible design recommended (prediction freeze before outcomes); C4 go/no-go now hinges on sigma_s <= 0.12; Zhang-Yu AAAI 2026 recorded not_trigger.
type: project
---
# Merge + C4 preflight + trigger scan (2026-08-27)

PI direction: continue the live route, consider merging Paper 1 (priority-rent) with Paper 2
(simulator validation), and keep exploring market-simulation/physics topics.

## Merge analysis (`papers/proposal/ecomd_paper_merge_analysis_2026-08-27.md`)

- Governing constraint: the simulator-validation claim is prospective only if the prediction
  freeze predates outcome access. One experiment can feed both papers iff predictions are frozen
  before the human experiment runs.
- Options: A one-experiment/two-papers; B single merged NMI paper; C old sequence (Paper 1 first,
  Paper 2 needs a fresh rule family). A and B allow deferring the merge choice to manuscript
  stage because both use the same prospective freeze.
- **Recommended: Option-A infrastructure** — build/freeze both simulator lineages and a frozen
  prediction battery (speed primary + depth/spread/efficiency/surplus/learning slopes, both arms)
  before any outcome access; decide one-paper-vs-two after results. NMI sufficiency risk:
  "predict one treatment effect" reads as application — mitigate with the multi-outcome frozen
  battery across two rule families. Contamination: lineages calibrate on public/prior data only.
- EcoMD/LLM-agent implementation, platform, participants: all still unauthorized.

## C4 preflight (`papers/proposal/ecomd_random_unit_priority_c4_preflight_2026-08-27.md`)

- Freeze-candidate payoff scale: 8 units/session, k=4 demanded, 100 tokens @ $0.10, arrival rank
  by spend, filled-unit surplus $2. δ=0.10 = $1.00 = the *entire* marginal FIFO speed rent of a
  median participant; KZ 2021 magnitude anchor (~20% investment reduction from asymmetric bumps)
  makes a full-rent effect theory-coherent.
- Power/cost (per arm, 80% superiority, $368/session incl. 15% attrition): σ_s 0.10→17/arm
  ($12.5k/site); 0.12→24 ($17.7k); 0.13→28 ($20.6k > ceiling). TOST at σ_s 0.12→18/arm.
- **Verdict: two-site feasibility at the $20k/site ceiling holds iff σ_s ≤ 0.12.** No published
  source identifies σ_s (KZ is within-group; not transportable). Lawful anchor = human internal
  pilot n₀=6 sessions/arm, variance-only, requires ethics/site authority (not yet requested).
  Stop rule frozen: σ̂_s > 0.12 or welfare-weak δ ⇒ C4 terminal as not-executable; never enlarge
  δ to fit budget. C4 remains open.

## Trigger scan (`papers/proposal/ecomd_reentry_trigger_scan_2026-08-27.md`)

- Zhang & Yu (AAAI/IAAI 40(47):41493, 2026): ensemble-imitation interactive market sim for RL
  training; regime conditioning ≠ legal intervention; no checkpoint/counterfactual/second
  lineage ⇒ `not_trigger` (8th ledger entry, 0 qualified). Market-physics families not
  rescanned: closures rest on exact reductions; re-entry needs an explicit named removal.
- Admissible continued exploration forms: trigger monitoring + the U3 route itself. A
  measurement-method-weighted fresh cycle would need its own bounded session under F0 sampling
  rules and genuinely new questions.

Governance validators pass (236 evidence records; 8 re-entry audits, 0 qualified).
