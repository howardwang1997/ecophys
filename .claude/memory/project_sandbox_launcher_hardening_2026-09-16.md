---
name: sandbox-launcher-hardening-2026-09-16
description: Sandbox launcher hardening + full g42 preflight through the 2026-09-16 adversarial audit (engine 1.1.1, payload re-staged non-operatively); only the PI authorization-only merge remains
metadata: 
  node_type: memory
  type: project
  originSessionId: 95ff19ae-8e7e-4d46-930e-42fb13badfa0
  modified: 2026-09-16T04:10:59.842Z
---

2026-09-16 Session 2 (sandbox preflight, PI-selected option after the strategy review):

- Adversarial launcher review: 14 findings → 13 distinct, all confirmed; fixes in commit 4173f9e6f (out-of-tree hash-chained anchor at `~/.ecomd/discovery_sandbox_anchors/<id>.jsonl`, triple config re-hash, manifest genesis re-check, receipt digest checks, expired-sandbox quarantine unlock via `as_of = min(now, expires_at - 1s)`, launcher provenance fields, lock hardening, capture ordering, resumable quarantine). Residuals: anchor erasable by same-uid; ms-scale config-swap window; first-launch `--base-ref` operator trust.
- Launcher-pin defect fixed: manifest execution-contract pins used to hash the working tree, freezing any shared script a terminal manifest pinned. Terminal sandboxes now also accept a committed version (git history, ≤64 commits); live sandboxes stay working-tree-strict. Prefer per-sandbox launcher copies (`sandbox_inputs/<id>/`) for future manifests over shared `scripts/` pins.
- The 2026-09-12 response-DX sandbox state was untracked; its freeze commits (9d9071196, ac49b0e21) are dangling on no branch. Landed as authorization-only commit 6727e7dad + terminal commit 52a328c01.
- Platform gate resolved 2026-09-16 (Session 3): PI chose the public-repository option and re-confirmed it after the 2026-09-05 private setting was traced to Paper D ICLR-2027 double-blind protection (abstract deadline 2026-09-18). Credential scan clean (tree + full history; only placeholder-empty example env files ever added). howardwang1997/ecophys is public; `main` carries the required `research-governance` check with force-push/deletion disabled and admin bypass off. Bourse preflight item 1 satisfied; its stale "now satisfies item 1" claim corrected in place.
- PI sandbox-target selection: g42 fixed-knot learned moves (g38 not selected).
- Validator green `--base-ref HEAD`; canonical counts now evidence 1390 / 191 raw questions / 38 cycles. No sandbox authorized; Paper G 107/31/0 unchanged. Conformance rerun on the hardened launcher completed 2026-09-16 (Session 4): make_plan had missed the new RuntimePlan.base_commit field (mypy flags it on the committed tree; CI never ran pre-protection, so it surfaced only at the rerun) — fixed with a conformance-only sentinel; fresh report `research/discovery/conformance/local_colima_arm64_report_2026-09-16.json` (14 isolation + 3 failure-mode checks green, outcome-free), `--verify-recorded-report` and the conformance pytest green. Durability sweep 55af5d0aa landed the 2026-09-08→09-15 uncommitted canonical records (4173f9e6f's canonical test needs those ledger counts); .gitignore now guards logs/private/, experiments/constraint_attribution_iclr/private/, research/paper_g/private_internal/ and .response_dx_sealed_20260912/. Before any execution authorization: Bourse 0.4.0 authorization-only merge, g42 manifest/decision.
- g42 item 3 (engine snapshot + isolation) completed 2026-09-16 Session 6: frozen pure-stdlib engine `research/paper_g/g42_fixed_knot_moves/model.py` (`g42_fixed_knot_moves_engine_1.0.0`; integer PCG64-XSL-RR, cubic-lattice closed SAW rings, corner-flip/pivot move library with per-pivot exact Alexander |Δ(-1)| certification over the frozen classes {0_1, 3_1, 4_1}, exact-Fraction MH ratios, fail-closed EngineError discipline) + runner/oci_probe/Dockerfile (base pinned sha256:2e32f7d…) + outcome-free `tests/test_paper_g_g42_engine.py` 32/32, ruff + `mypy --strict` clean. Image built locally (colima, light); `qualify_image.py` green after one fix (resolve the tag to the full image ID before make_plan — inspect_local_image pins IDs, not tags); 4 conformance cases + 14 isolation checks passed, image files byte-equal to repo sources, report at `research/paper_g/g42_fixed_knot_moves/preparation/image_conformance.json`. No MC production runs anywhere; no scientific outcomes. Session-6 files not yet committed (pending PI review).
- g42 preflight COMPLETED to staged-authorization 2026-09-16 Session 7 (all but the PI's authorization-only merge):
  - Engine 1.1.0: a rate probe found `certify()` raising all-directions-ambiguity at the PERIODIC corner recertification (trajectory tail event) — campaign-fatal under the branch=container stop rule. Fix: `certify_current()` seam (ambiguity→None), graceful `completed_reason="certification_ambiguous"` terminal at periodic + final checks (wrong determinant stays fatal; pivot path no longer swallows non-ambiguity EngineErrors), new `rg2_tau_int_tail`/`rg2_ess_tail` second-half statistics (ζ_ED burn-in lesson: full-trajectory means made T=1.0/T=3.0 indistinguishable). Replay proof: the same crashed unit terminates gracefully at the identical trajectory point. 38 tests, ruff + mypy --strict clean.
  - Runner is BRANCH-AGGREGATE (g_response_dx pattern), not one-unit-per-container: the launcher runs ONE container per branch (single config mount, single docker name, cpu ulimit = branch CPU), so 224 units must batch 32-per-branch; the runner envelope is {branch_id, epistemic_class, unit_ids (frozen execution order), units:{unit_id: 8-key engine config}} emitting one deterministic `exploratory_response.json` member.
  - Launcher mechanics learned: branch CPU accounting is CUMULATIVE against the reservation; manifest launcher/incident_handler refs MUST resolve to the canonical `scripts/` paths (launcher self-verifies), so per-sandbox launcher copies are impossible by construction (memo §6 residual resolved); partition member files must be bytewise sorted.
  - Final image sha256:208d3fed482f21f3f9cafc7836592fa58902da832cbdf88aae08e1ee9fa0c355 (rebuilt after runner change), requalified green. Build context is `research/paper_g/g42_fixed_knot_moves/` — repo root as context stalls on a 13GB transfer.
  - Frozen design: 7 cells (0_1@128 T1/T3, 3_1@160 T1/T3, 4_1@256 T1/T3, 0_1@96 T1) × 4 policies (P0 uniform, P1 corner-heavy, P2 pivot-heavy, P3 context buckets with frozen per-N threshold constants N=96 [20.6,107.2] / N=128 [25.4,150.5] / N=160 [29.5,195.4] / N=256 [40.3,339.7] — literals consumed by the engine, any scaling-law origin motivational only — × multipliers corner[4,2,1]/pivot[1,2,4]/self[1,1,1]) × 8 seeds = 224 units in 7 branches (12,960 of 14,400 CPU-s; 1,440 + 8th slot contingency); hypothesis_id context_move_selection_matched_budget_decorrelation; primary test tail-tau ratio P0/P3; >50%-excluded cell void. Confirmation = 112 templates (P0/P3×7×8), NIST Beacon v2 clone with `g42_confirm_<16hex>_<template>` unit-id construction (no engine change needed).
  - Staged payload `research/paper_g/g42_fixed_knot_moves/preparation/authorization/` (manifest/decision/genesis/events.jsonl + inputs: derivation, partition, members, unit_contract, 7 branch configs, provenance, snapshot, README) — DRAFT NON-OPERATIVE; regenerated by `stage_authorization.py --stamp <UTC>` (re-runnable, whole hash chain members→partition→provenance→snapshot→manifest→genesis→decision); chain independently reverified; all 224 unit configs pass engine validate_config. Promotion procedure in the README.
  - Appendix `papers/proposal/ecomd_g42_fixed_knot_moves_sandbox_preflight_2026-09-16.md`; governance repaired (goal_state.yaml structural bug fixed + re-derived, decision pi_paper_d_abstract_and_g42_preflight_continuation_20260916 registered, current_machine_decision re-pointed, CLAUDE.md/MEMORY.md staleness fixed); both validators green. PI decision points: beacon reuse, D0-anchor wording, commit batching, anchor backup.

- Adversarial audit of all g42 deliverables 2026-09-16 Session 8 (5-dimension workflow, 27 agents, per-finding refutation): 22 confirmed findings (2 blockers both in my own staged payload, 6 majors, 14 minors), all fixed before re-staging. Blocker 1: asset fingerprint was sha256(model.py) instead of the validator formula sha256(canonical_json({asset_key, kind, snapshot_sha256, source, version})) AND the snapshot hashed partition/provenance (unresolvable cycle) — fixed by the dx-precedent restructure (snapshot covers source+preparation files only, 23 entries; fingerprint derived from the staged snapshot's sha256; chain members→unit contract/configs/derivation→provenance→snapshot→fingerprint→partition→manifest→genesis→decision). Blocker 2: evidence_ref named an unregistered id — fixed by registering the `g42_fixed_knot_moves_preflight` locator (kind private_asset_preflight) on the g42 route node (route graph now 1996 locators). Engine 1.1.1 (low-T acceptance OverflowError clamp — 1.1.0 would have wall-crashed a cold trajectory; zero-bucket context policy rejected at construction; rg2_var_tail degenerate-tail marker + frozen assess-rule exclusion; runner key/body unit_id binding; 44 tests, mypy --strict + ruff clean). Per-unit CPU caps redesigned (branch−128)//32 = 56/56/51/51/61/61/41 s in branch order — the old 60/60/55/55/65/65/45 left Σ caps above the wall deadline (branch−42 s), so a fully cpu-capped branch would have been wall-killed and the sandbox quarantined with no retry. New image sha256:3428e3025c64a17173365290951b780fcf9a0b2e998ffaa8e3445057cc31deda requalified green. Appendix corrected in place (832nd not 64th accepted corner flip at the ambiguity — engine recertifies every 64th accepted flip so the 13th check fired at 832; frozen threshold wording; §7 audit record; promotion flags --preparation-commit/--operative; pre_execution_verification.json staged with validators 0/0). Memo §6 item-2 corrected: canonical scripts/ pins are MANDATORY (launcher self-verifies its own path); the earlier "prefer per-sandbox copies" preference above is wrong and was corrected in place. Payload re-stamped 2026-09-16T06:43:08Z, chain independently re-verified. Both validators green. Session-8 files still uncommitted pending PI review; authorization-only merge remains the PI's act.

- PI "全部授权" executed 2026-09-16 Session 9: preparation commit 91c08c767a + authorization-only merge 56522d954 pushed (decision pi_paper_g_g42_authorization_merge_20260916); campaign ran outcome-blind. b01 325 s / b02 97 s / b03 838 s / b04 142 s completed; mid-b05 the HOST (not the sandbox) killed the launcher on memory pressure — container kept running without its capture path, bundle.tar.partial — and the frozen incident contract fired: quarantine handler (reason_code host_interruption) removed the container, wrote runtime_incident.json, appended branch_quarantined + state_transition authorized→quarantined, anchored terminal=True, registered the taint entry; 3,482 of 14,400 CPU-s charged; retry forbidden; sandbox TERMINAL. Both validators green at terminal. Anchor backups ~/.ecomd_discovery_anchor_backups/g42_fixed_knot_moves_20260916/ (085526Z pre-campaign, 092936Z terminal 05ade80e…); off-machine copy still pending. Operational observation (counts only): certification_ambiguous dominated b01 (25/32) and b02 (32/32) — under the frozen >50%-exclusion rule both n128 cells trend VOID; the design's ambiguity rate at 400k proposals was never measurable pre-authorization (probes were ≤10k; full-length dry runs would have violated the local-compute rule). Post-terminal assess of the four completed cells is the next session's job; outcome capped at tainted D-3 child-card motivation. Lesson: background long campaigns via the launcher are exposed to host OOM — the quarantine path absorbs it cleanly (dx validated the mechanics; g42 validated it at campaign scale), but the cost is real branches; consider closing memory-hungry neighbors before campaign runs.

Related: [[paper-g-strategy-review-2026-09-16]]
