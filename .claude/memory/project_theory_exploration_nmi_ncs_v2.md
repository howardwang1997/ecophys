---
name: Dual-track NMI/NCS theory exploration v2
description: Frozen continuation from v1. NCS audits relaxation exceedance and metadata-only rule loops; NMI searches target-conditioned identifiability primitives. Data values and GPUs remain locked.
type: project
---

# Dual-track NMI/NCS theory exploration v2

Started on `theory-exploration-nmi-ncs-v2` from integrated `main@648fd96da` on 2026-08-12. Plan of record:
`papers/proposal/plan_theory_exploration_nmi_ncs_v2.md`.

Inherited decisions are binding: v1 NMI `NO_SURVIVOR`, v1 NCS `CONJECTURE_ONLY`, and exp144
`NEGATIVE_CONTROLS_CONFIRMED`. V2 may inspect primary literature and public rule/date/schema/licence metadata, but
not treatment outcomes or sealed values. It uses <=50 CPU core-hours and zero GPU-hours; no worker is contacted.

NCS C0 asks whether a real response outside every prevalidated frozen relaxation envelope yields a distinct
prospective prediction and whether one development plus an untouched replication design exists. NMI N0 tests
target completeness, horizon thresholds, complementary targets and Blackwell-like target ordering, each presumed
occupied until equation-level non-equivalence is shown.
