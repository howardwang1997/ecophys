# EcoMD laboratory truth-asset and coercive-stationarity trigger audit — 2026-09-05

- Decision: three `not_trigger`; zero removed blockers, no candidate harvesting, machine card,
  implementation, outcome asset, outreach, SSH or GPU work.
- Friedman--Gu--Zheng expose a randomized human bond-market experiment varying default rate,
  information cost and market format, but INFORMS restricts the replication files to reproducing
  the same paper with the same data/models. Any other use needs permission from all authors or data
  originators. We did not accept the terms or download the files. Published results also prevent
  this source from serving as untouched confirmation.
- M0 uses unconstrained SiLU pair/external potentials and disables its optional power-law external
  term. The admitted class contains negative linear tails and therefore does not guarantee
  `int exp(-V/T) < infinity`; a stable finite window is not an invariant-law certificate.
- This is not residual novelty. ICLR 2024 Stable Neural SDEs covers dissipativity/stability, and
  2026 UIDD uses a neural squared residual plus a fixed positive quadratic term specifically to
  guarantee integrability while proving a universal identifiable dissipative-SDE form.
- Exact common-shift invariance makes the Gibbs normalizer infinite and leaves the noisy population
  mean as Brownian motion. Only centered/quotient dynamics can be stationary. Translation-invariant
  mean-field theory already works seen from the centre of mass, and ICLR 2026 Quotient-Space
  Diffusion Models supplies a general quotient SDE and sampler.
- The zero-mode lemma is not a live contradiction for current EcoMD M0: v2 gauge enforcement is
  false by default, M0 uses `stochastic_mlp`, and the enclosing absolute-state external potential
  generally breaks the symmetry even when the v2 interaction term is gauged.
- Formal result:
  `papers/proposal/ecomd_lab_truth_asset_coercive_stationarity_trigger_audit_2026-09-05.md`.
- Re-enter only with explicit new-analysis rights plus independent untouched laboratory
  confirmation, a theorem beyond fixed-confiner/stable-SDE parents on two gold systems, or a
  genuinely new noncompact-symmetry quotient result beyond centre-of-mass and general quotient
  diffusion. A confinement patch, gauge fix or restricted-file download is not a trigger.
