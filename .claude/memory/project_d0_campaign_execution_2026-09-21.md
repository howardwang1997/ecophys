---
name: project-d0-campaign-execution-2026-09-21
description: "GAMMA D0 campaign (alpha_cube_d0_20260921) live execution state — nodes, decisions, driver, structural through-M finding, next milestones"
metadata: 
  node_type: memory
  type: project
  originSessionId: c288fd15-32cc-4d46-b3af-8cf7ca9c5f5e
  modified: 2026-09-21T15:26:38.121Z
---

Private/internal campaign record. Exclude implementation history and invalid-test provenance from public research artifacts.

D0 campaign **alpha_cube_d0_20260921** is executing (freeze sha256 fd4a40b0…, freeze commit f4e61bafe, branch paper-d-iclr-2027-completion). Original machine assignment: **v100ts (100.80.236.112) = B1/L1** seeds 11000:11030 n_train 2048; **v100bts (100.123.220.57) = B3/L2** seeds 12000:12030 n_train 4096; **3080 node (100.70.122.100) = CPU channel** (corpus + validate). The ratified B1 cross-host reassignment below supersedes the original assignment. D0-S2 corpus complete: 23,040 sessions, 12/12 manifests, 3.6G; post_corpus.sh (R2 staging → 12-batch validate) ran from 2026-09-21.

**Milestone verified 2026-09-23:** all 120 B1 training records exist across the two V100s (60/60 through-M); both V100 corpus validators have ALL-PASS markers. Full B1 evaluation and the one-shot analysis are still outstanding. See `logs/2026-09-23.md` for the operational snapshot and `logs/private/gamma_status_20260923.md` for the private evidence/disposition audit. Completion counts do not certify convergence or scientific effects.

**Handoff accepted 2026-09-23 02:45 NZST:** the 3080 CPU worker now holds all 120 B1 checkpoint locks and all 480 job files; deep lock verification passed for every job, with 120 jobs / zero missing in the merged manifest. The PI approved `pi_d0s4_run_id_horizon_20260923` for a separate complete B1 evaluation record version at `/data/ecophys-campaign/eval_d0s4_run_id_horizon_20260923/out/`, preserving the prior output as private/internal history. The runtime now requires that ratified decision. See its private package at `logs/private/gamma_run_id_amendment_20260923.md`; no scientific setting changed and no formal analyzer was run.

**Execution update 2026-09-23 03:30 NZST:** R2 handoff accepted, 481 matching files / zero differences at `alpha_cube_d0_20260921/handoff/B1_20260923/train_stage1/B1`. B1 producer commit `29bee8139d0441ba097ff0ec93018178e32f6672` runs from the isolated 3080 worktree `/data/ecophys-campaign/eval_code_29bee8139`. Four CPU workers process disjoint seed shards; six of thirty seed manifests were complete at 03:29. The controller merges records only after all shards finish. Track `seed_shards/` and `parallel.log`, not the incomplete canonical `out/` count. Full coverage, R2 result acceptance and the one-shot analyzer remain outstanding.

**A10 runtime preparation:** `train_a10_d0s4.py` reuses the Stage-1 loop with the frozen PAM estimator and verifies paired parent settings, data hashes and initialization. All thirty parent inputs passed check-only on v100ts (61,440 training episode hashes). Three new runtime checks and twenty existing estimator checks passed on v100bts. `eval_a10_d0s4.py` reuses B1 fixtures, scales and record bindings for 7,680 RC3 records. Private half-hour training snapshots are inspection artifacts; they do not implement resumable training and are excluded from final checkpoint inputs. These checks establish runtime readiness, not production A10 completion. Ordered deployment status belongs in the private execution plan.

**Ordered tasks verified 2026-09-23 03:42 NZST:** B1 has 12/30 completed seed manifests and continues on the 3080 CPU worker. A10 code commit `a93bade05fec47b3de554cb18a58fdad47d4f74f` is deployed in isolated `a10_code_a93bade05` worktrees on v100ts and 3080; both deployments pass 23 checks, and the committed version passes all thirty parent-input checks. The B1 publisher waits for `parallel.exit_status=0` after coverage and R2 checks. v100ts `/data/ecophys-campaign/a10_d0s4_20260923/run_a10_after_b1.sh` waits for a nonempty hash-bound B1 completion marker, then runs four independent CUDA seed tasks through all thirty seeds and archives accepted jobs. The 3080 `/data/ecophys-campaign/a10_eval_d0s4_20260923/run_a10_eval_after_training.sh` waits for that training archive, then produces 7,680 CPU evaluation records. Both waiters are alive and have launched no A10 production job at this snapshot. Their status files and private operational scripts are documented in `logs/private/gamma_iclr_submission_plan_20260923.md`. This chain does not launch formal analysis, B3, Stage 2 or reflexive work.

**Latest execution 2026-09-23 14:15 NZST:** B1 has 27/30 complete seed manifests. Remaining seeds are 11001, 11014 and 11015; all primary processes continue computing. Slow record intervals are roughly 42/54/16 minutes respectively at the inspected positions, so seed-count completion is not a duration forecast. Three extra CPU tasks now precompute later independent cells through the unchanged approved producer and its existing CLI flags. Verified completed records are linked atomically into the primary seed directories; conflicts fail closed, and the B1 publisher additionally waits for the private prefetch receipt. Primary production code and every scientific setting remain unchanged. Private scheduling source and paths: `logs/private/gamma_execution_20260923/prefetch_b1_cells.py` and the updated submission plan.

**A10 actually started 2026-09-23 14:13 NZST:** training depends on accepted B1 checkpoints, not B1 evaluation outcomes. The previous agent-added wait was removed from the training start and retained before publishing the joint completion marker. v100ts runs `run_a10_after_checkpoints.sh` under the same A10 producer commit, four CUDA seed tasks (11000–11003 initially), with all thirty seeds queued. Input checks passed and the four Python workers were active at 14:15; no accepted A10 training receipt yet. A10 evaluation still waits for both complete B1 evaluation and accepted A10 training. No B3, Stage 2, formal analyzer or scientific endpoint inspection was started.

**Status check 2026-09-23 16:23 NZST:** B1 has 86,850/92,280 generated records and 27/30 complete seeds; the remaining seeds continue computing. Three auxiliary cells have merged 48 records; the 11014 tick_2x/h31 cell remains active. A10's first four private checkpoints show 68/59/67/68 of 100 iterations at 16:15–16:17, with zero accepted final training receipts. These iteration counters establish ongoing progress, not convergence or a full-campaign completion forecast. Both downstream waiters remain alive and no failure exit status was found. No task setting changed in this read-only check.

Key decisions: `pi_d0s2_corpus_seed_pins_20260921` (draw tag, 64 episodes/seed); `pi_d0s3_train_constants_20260921` (through-M **differentiated** coordinate mode; n_train 2048/4096 at window [64, 64+n); n_iters 100; **device cuda** — PI overrode the CPU recommendation); `pi_d0s4_eval_constants_20260922` (B1 eval: horizon **chained**; D2-D12 defaults blanket-ratified; **RC2 extend-now** with W grid trunc_cap=40/trunc_lag=20 + mechanical package; production eval authorized on the 3080 CPU node at B1 60/60 with `--production-decision pi_d0s4_eval_constants_20260922`). Drivers `scripts/reexploration/train_stage1_d0s3.py` (commits 3b692ec57 → a2090dc53, 31-agent adversarial check consumed) and `scripts/reexploration/eval_draws_d0s4.py` (commits 9beea5a9d → 93b2da32c, 69-agent adversarial check consumed; evidence `research/discovery/ops/d0s4_eval_driver_audit_20260922/`).

Durable facts:
- **L2 invalid-test (2026-09-21, decision pi_d0s3_l2_invalid_test_20260921)**: ALL L2/B3 cells sit at the trivial-predictor floor (raw 0.0085%/0.043%, through-M 0.00% flat). Root cause: RecurrentFactSurrogate's bounded GRUCell hidden (measured on tanh rails, |h|≈0.96) fed only O(1) features cannot reach the flow lattice O(10-100) the O(1e4)-scale channels require (needs ≥3e4-3e5 iters at lr 3e-4; loss rescaling ineffective — Adam scale-invariant, mechanism semantics fix output units). L1 escapes architecturally: its unbounded SiLU encoder consumes the teacher-forced observed base O(1e4), so its representation carries the magnitude (trained flow reaches O(75-913) with flow_head |W| still 0.016). B3 terminal = 30+30 raw + 1 through-M; the 59 remaining through-M jobs PI-halted; no L2 cell may be reported as a trained comparison.
- **Host reassignment (same decision)**: bts deviates from frozen ts=L1/bts=L2 within the V100 pool — B1 completion split across hosts (ts 3 drivers absolute_through_m, bts 4 drivers increment_through_m, 55-job partition, done seeds never retrained). Merged B1 block manifest must be assembled from BOTH hosts' per-job artifacts.
- **Structural**: frozen `_predicted_channels` is coordinate-blind on the through-M path — blind training makes (absolute,through_m) ≡ (increment,through_m) byte-identical per seed, D_01 = D_11 ≡ 0, J degenerates to T_0. Differentiated mode (ratified) preserves all 8 cells; through-M cost anchor ~2.6 h (L1) / 5 h (L2) per (arm,seed) on V100.
- **G9 exemption**: training records are G4-hash-covered and explicitly NOT a G9 re-execution class (analyzer contract Part 4.9 FLAG-7) — so CUDA training breaks no frozen pin; residual (non-bitwise voluntary retraining) accepted by PI.
- G4 hook per job: `training_log.json` with `checkpoint_sha256` (analyzer contract §4.4 pin).
- rclone IS installed on all three nodes at /usr/local/bin/rclone (memory saying "rclone on no host" was stale).
- **RC2 mechanics (2026-09-22)**: corpus prestates do NOT serialize arrival clocks (`prestate_from_json` has no field; KeyError on node) — clocks live only in the generator's raw flow. Recovery = deterministic re-derivation via frozen `generate_episode` under the manifest's `episode_canonical_json_sha256` guard; ID re-execution (build_prestate → ReferenceEngine → project_fexec_corpus) reproduces the disk corpus byte-exactly. D_trunc^W = remove initial resting orders with arrival_clock > W from the deployed book, replay the recorded request stream (lemma writeup kt_g4_g5 §4). DGP clocks: first-in-LEVEL ∈ [0,40], followers ∈ [41,80], requests at tick ≥100 (clock-independent); W=40 keeps exactly the 5 per-level firsts (deterministic), W=20 mean 2.5 (levels can empty). Smoke exposure structure confirmed the frozen prediction: truncation invisible at h=1/4, ≠ ID at h=16, lag≠cap at h=31.
- **s_ch sealed (2026-09-22)**: namespace 20260977, 512 episodes, 6,368 rounds pooled, s_ch = [5.4546989294724275, 5458.5636934342465], sha256 debb15a7… (file 3080:/data/ecophys-campaign/eval_d0s4_production/s_ch_20260977.json; producer `scripts/reexploration/materialize_s_ch_d0s4.py`). Driver refuses the [1.0, 1.0] placeholder.
- B1 eval production layout on 3080: /data/ecophys-campaign/eval_d0s4_production/{train_stage1/ (checkpoints relayed from ts/bts via Mac), s_ch_20260977.json, out/}; seed 11000's 4 checkpoints relayed 2026-09-22 for the full-grid dry-run. Remaining seeds must be relayed before the 60/60 production invocation.
- ssh lessons re-confirmed: pkill -f self-match fires whenever the launcher's own command line contains the target pattern (bracket trick fails if the relaunch text is in the same remote command) — kill and relaunch in SEPARATE connections; `echo ===X===` triggers zsh =cmd expansion.

Next milestones: accept the complete B1 evaluation and R2 result archive; finish the queued A10 audit; review and run S3/S1a/S2 theory checks; resolve complete-records G1-G12 handling before one-shot analysis. Checkpoint relay, merged B1 manifest and handoff R2 acceptance are complete. Stage 2 requires its mechanical gates, including an explicit disposition of the incomplete L2 block; B2 needs its own synthetic-DGP corpus and training. Reflexive execution remains behind the four-analyzer-hash firewall. Verification-liquidity embargo remains 2026-10-17 UTC. D0 is 2026-09-21 and the campaign ceiling is 2026-10-19, per C-13. See [[project-gamma-activation-2026-09-19]].
