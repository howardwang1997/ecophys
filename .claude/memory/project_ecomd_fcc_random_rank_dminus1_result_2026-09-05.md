# EcoMD FCC randomized-rank D−1 result (2026-09-05)

## Canonical decision

The `fcc_clock1_random_rank_cascade` candidate is **failed-closed**. The formal result is
`papers/proposal/fcc_clock1_random_rank_cascade_dminus1_result_2026-09-05.md`; the separate
outcome-access record is
`papers/proposal/fcc_clock1_historical_family_outcome_access_incident_2026-09-05.md`.

Auction 108 and 113 pass only the frozen structural-support prefilter, at upper-bound counts 244 and
53 respectively; Auction 110 has 362 comparator blocks. These are neither confirmed
order-sensitive blocks nor independent experimental units. Auction 108's observed-row contract
also closes: 665,637 bid-product keys, 657,524 result-product keys, 8,113 system-created Round-1
quantity-zero missing-bid placeholders, 12,202 matched quantity-one bids, and no impossible matched
transition. This is accounting consistency, not whole-processor or behavioral-response truth.

## Terminal scientific result

Let a complete state have two admissible orderings and potential paths `Y(0), Y(1)`. Two latent
worlds can induce the same randomized observed law while having opposite paired dispersion: with
`U ~ Bernoulli(1/2)`, `(Y(0),Y(1))=(U,U)` gives expected distance zero, while
`(Y(0),Y(1))=(U,1-U)` gives distance one. Thus randomized marginal paths do not identify the
same-state damage-spreading coupling. This binary subcase embeds in any larger permutation space.

The surviving interpretations are not a new ICLR object:

- immediate application under a complete deterministic rule is exact algorithmic replay;
- later bidder response is standard sequential randomization inference under global interference
  and carryover;
- paired same-state path divergence is nonidentified without structural assumptions or branchable
  repeated state;
- a neural executor reduces to established order-of-addition, OPE and neural algorithmic reasoning;
  U.S. application publication 20260141450 already describes the precise missing-bid, random-sort,
  feasibility and rejection-queue executor.

The 26-primary-work hostile audit additionally found direct parents in FCC/spectrum simulators,
market-design causal inference, general interference, order-of-addition design, large-action OPE,
treatment-effect-distribution partial identification and multiple-solution neural execution. The
public randomization probability contract, a common action/state map across the clock-1 and generic
multi-block mechanisms, and two independent faithful simulator lineages are absent. Diagnostic
ICLR main-track survival is 2–3%; the hard-gate failures, not this percentage, close the route.

## Contamination and authority

Four attempted schema-header page requests exposed embedded outcomes. Auctions 102 and 105 are
conservatively tainted for untouched development/confirmation in this project. No effect was
estimated and no archive was intentionally saved. Outcome-page access stopped; later work used
only rules, schemas, primary literature and the patent record.

No experiment plan exists. No simulator or EcoMD code was run, no SSH session was opened, and the
A800 plus two V100 workers received zero jobs. Canonical totals after closure are 377 evidence
records, 37 trigger audits with zero qualified, 244 route nodes, 252 edges and 901 locators; open
routes are only parked DCRDEX and the separate active verification-liquidity protocol.

## Re-entry condition

Reopen only for prospectively repeated independent randomizations and resets of the same complete
state or a branchable human/laboratory system; a theorem giving defensible identification or sharp
bounds for paired path divergence under queues, switching, eligibility and adaptation; or two
independently governed exact-mechanism simulators plus sealed same-estimand field confirmation. A
new auction number, more tie blocks, a learned executor or a renamed damage metric is not a trigger.
