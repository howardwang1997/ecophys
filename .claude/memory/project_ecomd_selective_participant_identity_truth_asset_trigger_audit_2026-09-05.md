---
name: EcoMD selective participant identity truth-asset trigger audit
description: Mendeley identity claims fail provenance; Nasdaq MPIDs are real but participant-selected, split/merge-noninvariant partial labels, so no EcoMD or ICLR route reopens.
type: project
---

# EcoMD selective participant identity truth-asset trigger audit — 2026-09-05

The trigger decision is `not_trigger`; `candidate_harvest_authorized=false`. No outcome rows,
implementation, simulator, SSH or GPU work is authorized.

The Mendeley dataset `10.17632/fsn6fn7ht8.1` does not name its exchange, instruments, dates,
acquisition path, market rules or ID construction. Its only 24,062,365-byte ZIP has SHA-256
`406f34f004a68328a6048c38ae0cab37e2283cc6f6943fa98658aab9dd3775da` and contains only
`Trades .csv` and `Orders .csv`; no CSV row was opened. Treating MEMBER_CODE, CLIENT_ID and
TRADER_ID as distinct entities is a depositor convention, not verified actor truth.

Nasdaq TotalView-ITCH is an authoritative displayed-order lifecycle source and its public sample
directory contains full-day logs. However, message `A` is unattributed and message `F` is attributed;
Nasdaq Rule 4703 makes attribution a participant-selected order attribute. One member may use
supplemental MPIDs, while a broker/member can aggregate customers. Non-displayed orders have no add
message and current non-cross trade messages null the order reference. Rejections, inventory,
beneficial ownership and cross-venue state are absent.

For observed `O=(X,R,RM)`, any hidden-actor kernel `q(M|X,R=0)` induces the same public law. The
anonymous mass can therefore be allocated among actors with incompatible population and response
laws. This is standard MNAR nonidentification. Behavioral clustering also cannot create identity:
for `C=g(X)`, `I(M;C|X)=0`.

The 2015 BX/PSX attribution rollout enabled voluntary disclosure rather than assigning it; the
located public samples do not give an open 2015 panel and the venues share one operator. ClusterLOB,
participant-network work, Hyperliquid public-identity prediction, Cartea--Chang--Graumans identity
signaling/collusion and generic informative-label/MNAR methods directly occupy the broad claims.

Formal result:
`papers/proposal/ecomd_selective_participant_identity_truth_asset_trigger_audit_2026-09-05.md`.
The validated registries should contain 605 evidence records, 79 trigger audits, 249 route nodes,
267 typed edges and 951 route evidence/artifact locators.
