# EcoMD minimal-semantic-flip scientific-debugging trigger audit — 2026-09-05

- Decision: `not_trigger`; zero removed blockers, no candidate harvesting, no machine card, and no
  CPU/GPU experiment authorization.
- Proposed object: find the minimum-cost simulator parameter, assumption, or rule edit that reverses
  a declared intervention-contrast sign.
- Exact coordinate witness: with `q(theta)=(theta+1/2)(theta-1)`, baseline `theta=0` and flips at
  `-1` and `2`, Euclidean distance ranks `-1` nearer. The smooth increasing coordinate
  `phi=-exp(-k theta)`, `k>log 2`, ranks `2` nearer without changing any simulator kernel or result.
- Exact grammar witness: replacing one conclusion-flipping macro edit by `m` mandatory micro-edits
  preserves simulator denotation but changes Hamming/graph-edit cost from one to `m`. Minimal causes
  require a behavior-equivalence quotient and exogenous cost before optimization.
- A path-law divergence is conditional on observation, policy/intervention distribution, horizon and
  state weighting. Observable divergence misses off-support/hidden semantic changes; full-latent
  divergence is not invariant to state refinement. It is a declared robustness metric, not a
  canonical semantic distance.
- Continuous search collides with Dawson--Fan differentiable failure prediction/repair, VERIFAI,
  MACE and global differentiable-simulation optimization. Discrete search collides with classical
  delta debugging and GReduce's validity-preserving generator-trace reduction.
- EcoMD supplies gradients inside one aggregate dynamics specification, not a formal legal-edit
  language, market-native edit cost, exact hard-matching semantics, gold minimal causes, or external
  assigned response truth. The fragmented-market replication is a hard negative, not that benchmark.
- Formal result:
  `papers/proposal/ecomd_minimal_semantic_flip_scientific_debugging_trigger_audit_2026-09-05.md`.
- Re-review only with a frozen complete stochastic-market edit language, behavior quotient,
  representation/atomization-invariant market cost, an irreducible theorem plus lower bound, two
  independent gold systems, and assigned external truth for any real-market claim.
- Another norm, mutation library, gradient optimizer, EcoMD-only planted benchmark, seed sweep, SSH
  session, or A800/V100 run is not a trigger.
