# Experiment 149 — fee-controller protocol oracle

This experiment independently replays selected official Ethereum execution-spec fixtures. Read
`PREREGISTRATION.md` and `config.yaml` first. The fixture archive is external, transient and intentionally not
committed.

No chain outcome, demand inference, training, remote worker or GPU is involved. The only valid scientific
interpretation of a pass is that the local controller oracle meets V6's implementation-conformance gate.

