# Experiment 150 — closed-loop fee-controller identifiability witness

This generated-only experiment verifies a pathwise observational alias between distinct behavioral response
matrices under one exact feedback controller, tests whether the near-scale-equivalent BPO schedules break that
alias, and checks whether a deliberately non-proportional second gain can separate the frozen witness class under
an explicit latent-dynamics-invariance assumption.

Read `PREREGISTRATION.md` and `config.yaml` first. No implementation, formal freeze or result exists at the
preregistration commit. No chain outcome, remote worker or GPU is authorized.
