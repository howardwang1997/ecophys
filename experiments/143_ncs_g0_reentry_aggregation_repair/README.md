# Experiment 143 runbook

Experiment 143 is the completed one-defect repair of exp142's aggregate boolean semantics. The formal decision is
`PASS_AGGREGATION_REPAIR`; all ten positive predicates passed. It reuses the frozen exp142 fixture and candidate
contract and contains no real method candidate. See `RESULTS.md` and `artifacts/RAW_MANIFEST.yaml`.

The preregistration, implementation and freeze were committed separately before the formal run. No market data,
network service or GPU was used. The command is retained for provenance and must not be used to regenerate the
immutable formal result.
