# Experiment 143 runbook

Experiment 143 is a one-defect repair of exp142's aggregate boolean semantics. It reuses the frozen exp142 fixture
and candidate contract and contains no real method candidate.

The preregistration must be committed before the repaired runner is written. A later freeze manifest records the
runner/test hashes and exact implementation commit. No market data, network service or GPU is used.
