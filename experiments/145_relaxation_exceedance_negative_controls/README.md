# Experiment 145

This directory contains the preregistered CPU-only negative controls for attributing relaxation-envelope
exceedance to adaptation.

- `PREREGISTRATION.md` freezes the fixtures and decision rule.
- `config.yaml` contains generated constants only.
- `run_negative_controls.py` verifies a clean checkout and the implementation hashes in `FREEZE.yaml`.
- `artifacts/raw/negative_controls.json` is the immutable formal result.
- `RESULTS.md` states the permitted scientific interpretation.

The experiment cannot produce a novelty pass or adaptation claim. Its formal result is
`ATTRIBUTION_NOT_IDENTIFIED`.
