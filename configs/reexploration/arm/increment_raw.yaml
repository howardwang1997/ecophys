# Trained arm (increment, raw): predict the conserving-channel INCREMENT.
# The (increment, raw-train, raw-infer) cell is R00 — the SESOI reference
# coordinate pinned by C16 item (3).
arm_id: increment_raw
coordinate: increment
enforcement: raw
estimator: null
training_kernel: fifo
