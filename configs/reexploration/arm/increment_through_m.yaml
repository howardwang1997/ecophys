# Trained arm (increment, through-M): predict the conserving-channel
# INCREMENT, train through the exact clearing layer. The A10 audit retrains
# exactly this arm's (increment, through-M-train, raw-infer) cell under PAM.
arm_id: increment_through_m
coordinate: increment
enforcement: through_m
estimator: straight_through
training_kernel: fifo
