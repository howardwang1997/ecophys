# Trained arm (absolute, through-M): predict the next conserving-channel
# STATE, train through the exact clearing layer via the frozen estimator menu.
arm_id: absolute_through_m
coordinate: absolute
enforcement: through_m
estimator: straight_through  # KT-A4 primary (pinned scale 1.0); PAM is the A10 audit only
training_kernel: fifo  # C4(i): the truth kernel on the FIFO-generated F_exec corpus
