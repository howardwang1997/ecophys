# Population axis (C4): prestates regenerated at 2N actors with induced
# capacities rescaled per the lab-asset-v3 schema. Realized by E-2
# config_for_axis(base, "pop_2x") — budgets, schedule lengths and replenish
# depth x2, per-actor slack ratios preserved (the frozen resolution recorded
# in the E-2 receipt).
axis_id: pop_2x
dgp_axis: pop_2x
inference_kernel_through_m: fifo
raw_infer_duplicate_of: null
