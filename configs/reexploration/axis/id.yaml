# ID axis: in-distribution deployment (the stratum every J_axis contrast is
# taken against). DGP base config unchanged.
axis_id: id
dgp_axis: id  # E-2 config_for_axis key
inference_kernel_through_m: fifo  # through-M inference cells deploy the training kernel
raw_infer_duplicate_of: null  # raw-inference cells have no deployment kernel here
