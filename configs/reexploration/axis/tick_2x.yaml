# Tick axis (C4): tick 2-delta coarser grid under the frozen band-scaling
# rule — homogeneous price dilation by tick_factor 2 (reference, half-band and
# every offset x2; band width in 2-delta ticks invariant), realized by E-2
# config_for_axis(base, "tick_2x").
axis_id: tick_2x
dgp_axis: tick_2x
inference_kernel_through_m: fifo
raw_infer_duplicate_of: null
