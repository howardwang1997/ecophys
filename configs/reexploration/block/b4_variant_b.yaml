# B4-variantB = L2 x multiscale_logvol / D2 variant B (Stage 2; variant B is
# L2-ONLY per PI decision D1_05 — B4 carries BOTH variant sub-blocks).
defaults:
  - /lineage: l2
  - /dgp: multiscale_logvol
  - _self_

block_id: B4-variantB
stage: 2
lineage_id: l2
dgp_id: multiscale_logvol
seed_namespace: l2_training
seed_start: 12000
seed_stop_inclusive: 12029
n_seeds: 15
stage2_subset:
  seed_start: 12000
  seed_stop_inclusive: 12014
truncation_conditions: [trunc_lag]
