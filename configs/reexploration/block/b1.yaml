# B1 = L1 x lab-asset-v3 (Stage 1, CONFIRMATORY; the primary block — EP1
# primary cell is B1 kernel-swap horizon 16).
defaults:
  - /lineage: l1
  - /dgp: lab_asset
  - _self_

block_id: B1
stage: 1
lineage_id: l1
dgp_id: lab_asset
seed_namespace: l1_training  # 11000-11029 (PI decision D1_10)
seed_start: 11000
seed_stop_inclusive: 11029
n_seeds: 30
stage2_subset: null  # Stage-1 block: full namespace
truncation_conditions: [trunc_lag, trunc_cap]  # C16 item (14): two-test family on L1 blocks
