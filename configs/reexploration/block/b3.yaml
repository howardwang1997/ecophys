# B3 = L2 x lab-asset-v3 (Stage 1, CONFIRMATORY; the architecture-transfer
# block — its own primary-analogue cell is kernel-swap h16 per C16 item (16)).
defaults:
  - /lineage: l2
  - /dgp: lab_asset
  - _self_

block_id: B3
stage: 1
lineage_id: l2
dgp_id: lab_asset
seed_namespace: l2_training  # 12000-12029 (PI decision D1_10)
seed_start: 12000
seed_stop_inclusive: 12029
n_seeds: 30
stage2_subset: null
truncation_conditions: [trunc_lag]  # C16 item (14): one-test family on L2 blocks (trunc_cap de-scope ladder)
